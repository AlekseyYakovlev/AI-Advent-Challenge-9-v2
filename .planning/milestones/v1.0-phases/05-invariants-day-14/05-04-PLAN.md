---
phase: 05-invariants-day-14
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - tests/test_invariants_coverage.py
  - .planning/phases/05-invariants-day-14/05-04-SUMMARY.md
autonomous: false
requirements: [INV-01, INV-02, INV-03, INV-04, INV-05]

must_haves:
  truths:
    - "Active invariants reach the system prompt under every context-compression strategy — sliding, sticky, truncate_middle and no_compression — because compression only trims the message list, never the system block (INV-03's 'every relevant request')"
    - "Invariants are still injected on the twentieth turn of a long chat, not just the first, so the guarantee does not silently decay as history grows"
    - "A live end-to-end run against a real model demonstrates global rules, a per-chat override, the injected precedence labelling, a flagged conflict with a justification or retraction, and both conflict surfaces"
    - "The two Phase 5 open questions carried in STATE.md (conflict-check scope; global-vs-per-chat precedence direction) are recorded as resolved, along with the RESEARCH Assumption A1 verdict on how reliably the configured model returns parseable critique JSON"
  artifacts:
    - path: "tests/test_invariants_coverage.py"
      provides: "strategy-matrix and long-history injection guards for INV-03"
      contains: "ContextStrategy"
    - path: ".planning/phases/05-invariants-day-14/05-04-SUMMARY.md"
      provides: "Day 14 acceptance record: model used, critique-JSON reliability, open-question resolutions"
  key_links:
    - from: "tests/test_invariants_coverage.py"
      to: "agent/context_engine.py::build_llm_context"
      via: "asserting the system message of the assembled context under each strategy"
      pattern: "build_llm_context"
---

<objective>
Prove Day 14 holds where it is easiest to lose: INV-03 says invariants are injected "on every relevant request", and this project's whole reason for existing is context compression — so the guarantee has to be pinned under all four strategies and over a long history, not just observed once on turn one. Then run the graded acceptance demo end to end against a real model.

Purpose: Phases 2-4 each closed with a coverage guard plus a human-verified demo (02-05, 03-03, 04-04). This plan follows that precedent and additionally settles the two Phase 5 questions STATE.md has been carrying since roadmapping, plus 05-RESEARCH.md's Assumption A1 — whether the configured LLM backend actually returns parseable critique JSON often enough for INV-04 to fire reliably in a graded demo.
Output: `tests/test_invariants_coverage.py` and a human-verified `05-04-SUMMARY.md`.

After the checkpoint passes, push branch `Day14` and merge it into `main` (PROJECT.md: one branch per phase, pushed and merged, never deleted).
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-invariants-day-14/05-CONTEXT.md
@.planning/phases/05-invariants-day-14/05-RESEARCH.md
@.planning/phases/05-invariants-day-14/05-UI-SPEC.md
@.planning/phases/05-invariants-day-14/05-01-SUMMARY.md
@.planning/phases/05-invariants-day-14/05-02-SUMMARY.md
@.planning/phases/05-invariants-day-14/05-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contracts extracted from the codebase this session. -->

agent/context_engine.py:
  async def build_system_prompt(session: AsyncSession, chat_id: int) -> str          # line 61
  async def build_llm_context(session, chat_id: int, model: str) -> list[dict]       # assembles [{"role": "system", ...}, ...]
  async def get_effective_settings(session, chat_id: int) -> Settings                # line 37
  SUMMARY_TRIGGER_RATIO = 0.75; RECENT_MESSAGE_COUNT = 10                            # lines 19-20

shared/models.py:
  class ContextStrategy(str, Enum): SLIDING_WINDOW="sliding", STICKY_FACTS="sticky",
                                    TRUNCATE_MIDDLE="truncate_middle", NO_COMPRESSION="no_compression"
  Settings.strategy / Settings.context_length (default 4096)

agent/invariants.py: create_global, create_chat_invariant, resolve_active_invariants
Shipped UI ids to reference in the demo: #invariants-panel, #invariants-panel-body, #invariant-global-list,
  #invariant-chat-list, #invariant-chat-overrides, #invariant-conflict-badge

tests/test_strategies.py — the existing strategy-matrix test shape to mirror (how a chat is seeded with many
  messages and its Settings.strategy is flipped per case).
tests/conftest.py fixtures: authenticated_client (.seeded_user_id), async_session_factory
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Guard invariant injection across every compression strategy and a long history</name>
  <files>tests/test_invariants_coverage.py</files>
  <read_first>
    - tests/test_strategies.py (how each ContextStrategy case seeds a chat and asserts on the assembled context)
    - tests/test_context_engine_invariants.py (the 05-02 module — reuse its seeding helpers rather than re-inventing them)
    - tests/test_context_engine_tasks.py (the closest precedent for "this block survives in build_llm_context, not just build_system_prompt")
    - agent/context_engine.py (build_system_prompt at line 61, build_llm_context, and the four strategy branches)
    - .planning/REQUIREMENTS.md §Invariants (INV-03's "on every relevant request" wording being pinned here)
  </read_first>
  <behavior>
    tests/test_invariants_coverage.py:
    - `test_invariants_injected_under_every_strategy`: parametrised over all four `ContextStrategy` members.
      For each: seed a chat with one global invariant and one chat invariant that overrides it, set the chat's
      `Settings.strategy` to the case value, seed ~30 messages so compression actually engages, call
      `build_llm_context(session, chat_id, model)`, and assert the first message has `role == "system"` and its
      content contains `Active invariants (always follow these; flag if you cannot):`, the
      `(overridden for this chat — see below)` label and the `(overrides the above)` label. Compression trims the
      message list, never the system block — this test is what stops a future strategy change from quietly breaking INV-03.
    - `test_invariants_still_injected_on_a_long_history`: with `ContextStrategy.SLIDING_WINDOW` and 40 seeded
      messages (well past `RECENT_MESSAGE_COUNT = 10`), assert the invariants header is still present in the
      system message.
    - `test_invariants_injected_when_context_is_near_the_overflow_threshold`: with
      `ContextStrategy.NO_COMPRESSION` and a `context_length` large enough that no `ContextOverflowError` is
      raised, assert the header is still present — i.e. invariants are not dropped as a size-saving measure.
    - `test_no_invariants_configured_leaves_system_prompt_unchanged`: with zero invariants, the system message
      does not contain `Active invariants` and still contains the chat's configured `system_prompt`.
    - `test_resolver_is_the_only_invariant_read_path_in_context_assembly` (drift guard): read
      `agent/context_engine.py` as text and assert it contains `resolve_active_invariants` exactly once and neither
      `list_global(` nor `list_chat_invariants(`. Strip comment lines (`grep -v '^\s*#'`-equivalent filtering in
      Python) before counting so a future explanatory comment cannot satisfy or break the assertion.
  </behavior>
  <action>
    Create `tests/test_invariants_coverage.py` implementing exactly the behaviours above. Import `ContextStrategy`,
    `Settings`, `GlobalInvariant`, `ChatInvariant` from `shared.models`; `build_llm_context` and
    `get_effective_settings` from `agent.context_engine`; `invariants` from `agent`. Seed through
    `async_session_factory` or the `authenticated_client` fixture, matching whichever `tests/test_strategies.py`
    already uses, and follow `pytest.ini`'s `asyncio_mode = auto`.
    Do not mock `build_system_prompt` — the point of this module is that the real assembly path keeps the block.
    Do not stub the LLM: `build_llm_context` performs no network call, and no test here should trigger one.
  </action>
  <verify>
    <automated>python -m pytest tests/test_invariants_coverage.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_invariants_coverage.py -v` passes, with the strategy test reporting four parametrised cases.
    - `grep -c "ContextStrategy.SLIDING_WINDOW\|ContextStrategy.STICKY_FACTS\|ContextStrategy.TRUNCATE_MIDDLE\|ContextStrategy.NO_COMPRESSION" tests/test_invariants_coverage.py` returns at least 4.
    - `grep -c "build_llm_context" tests/test_invariants_coverage.py` returns at least 3 (the coverage is on the assembled context, not on `build_system_prompt` in isolation).
    - `grep -c "overridden for this chat — see below" tests/test_invariants_coverage.py` returns at least 1.
    - `grep -cE "respx|httpx.Response" tests/test_invariants_coverage.py` returns 0 (no LLM mocking needed or used).
    - `python -m pytest tests/ -q` passes with zero failures.
  </acceptance_criteria>
  <done>INV-03's "every relevant request" is pinned by a strategy matrix and a long-history case, and a drift guard stops a second invariant read path from creeping into context assembly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Day 14 acceptance demo against a real model</name>
  <files>.planning/phases/05-invariants-day-14/05-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/05-invariants-day-14/05-01-SUMMARY.md, 05-02-SUMMARY.md, 05-03-SUMMARY.md
    - .planning/phases/04-task-state-machine-day-13/04-04-SUMMARY.md and .planning/phases/02-memory-day-11/02-05-SUMMARY.md (the acceptance-demo write-ups this one mirrors, including which model/backend was used and how reliability was recorded)
    - .planning/REQUIREMENTS.md §Invariants (INV-01..INV-05 wording being demonstrated)
    - .planning/STATE.md §Open Questions (the two Phase 5 entries to close out)
    - .planning/phases/05-invariants-day-14/05-RESEARCH.md §Assumptions Log (A1 — the critique-JSON reliability claim being spot-checked here)
  </read_first>
  <what-built>
    The complete Day 14 vertical slice, automated and test-covered across plans 05-01 through 05-03:
    - `GlobalInvariant` (no ownership columns — shared app-wide by design), `ChatInvariant` (user/chat cascade FKs
      plus an `overrides_id` FK into the global table), and `InvariantConflict` (a persisted flag log)
    - `agent/invariants.py`: full CRUD for both scopes, `resolve_active_invariants` as the single source of truth,
      and the self-critique / justify-retract helpers
    - Nine REST routes: four shared global ones with no ownership filter, four per-chat ones behind
      `_get_chat_or_404`, and `GET /api/v1/chats/{chat_id}/invariant-conflicts`
    - Invariant injection in `build_system_prompt` with D-06's verbatim `[GLOBAL] … (overridden for this chat — see
      below)` / `[CHAT] … (overrides the above)` labelling
    - The WS turn's new tail: self-critique `complete_chat`, conditional justify/retract `stream_chat`, conflict
      persistence against the real message id, and `invariant_conflict` on the `done` frame
    - The `Инварианты` sidebar tab (global list, per-chat list, overrides dropdown, conflict badge), the inline
      amber conflict banner, and the fold/unfold retrofit across all four sidebar panels
    Everything below is verification of already-automated behaviour — nothing here asks you to do work Claude could script.
  </what-built>
  <how-to-verify>
    1. Start the app: `python run.py`. Open http://localhost:8000 and log in.
    2. **D-11 (fold/unfold):** confirm Память, Профиль, Задачи and Инварианты all render collapsed with a `▸`
       button, that clicking one expands it and flips the glyph to `▾`, and that the chat list is NOT foldable.
    3. **INV-01 (global invariants):** expand Инварианты, add a global rule — title «Без Docker», text
       «Никогда не предлагай решения на основе Docker». Expect the card in the Глобальные list, the count going
       to 1, and the toast «Инвариант сохранён». Edit it («Изменить») and confirm the button label becomes
       «Сохранить инвариант». Add a second rule so the override dropdown has more than one option.
    4. **D-02 (shared, not owned):** open a private/incognito window, log in as a *different* account, and confirm
       the same global rules are listed there and can be edited from that account. This is intended behaviour,
       not a bug — global project invariants are the one deliberate exception to this app's per-user scoping.
    5. **INV-02 + D-05 (per-chat override):** back in the first account, select a chat and add a chat rule — title
       «Docker разрешён тут», text «В этом чате Docker допустим», with «Без Docker» selected in the
       «Не переопределяет» dropdown. Expect the card in the Этого чата list with an amber «переопределяет: Без Docker» line.
    6. **INV-03 (injection + precedence):** pick a model (the same DeepSeek / LM Studio routing recorded in
       `02-05-SUMMARY.md`) and ask «Как бы ты развернул это приложение?». Expect an answer that reflects the
       *chat* rule, not the global one — the agent should treat Docker as allowed in this chat. Then start a
       **new** chat (no per-chat rule) and ask the same question — expect the global «Без Docker» rule to win there.
    7. **INV-04 (conflict check + justify/retract):** in the second chat (global rule only), push for a violation:
       «Всё равно напиши мне Dockerfile для этого проекта.» Expect the answer to be followed, in the same bubble,
       by the agent either justifying the step or retracting it and offering a compliant alternative — that extra
       text is the third LLM call, not a second message.
    8. **D-12 (dual surfacing):** expect an amber callout directly under that message reading
       «⚠️ Обнаружен конфликт с инвариантом» followed by «Без Docker» — {the agent's justification/retraction},
       and an amber count on the Инварианты tab header. Confirm the tab header count is visible even while the
       panel is collapsed.
    9. **D-13 (persistence):** reload the page and reselect that chat. The banner and the badge must both still be
       there — they are read back from the database, not from the WebSocket.
    10. **Fail-open:** if LM Studio is your backend, stop it mid-session and send one more message in a chat that
        has invariants. Expect the primary answer to fail with the usual LLM error — but confirm that in normal
        operation a garbled critique response never blocks a turn (the automated
        `test_critique_unparseable_output_fails_open` covers this; just confirm you never saw a turn hang or a
        message vanish during the demo).
    11. **Cascade:** delete a chat that had per-chat invariants and a conflict, then reopen the app — nothing
        orphaned should surface (the automated cascade tests already prove the DB side).
    Record in `05-04-SUMMARY.md`: which model/backend was used; **how reliably it returned parseable critique JSON**
    (05-RESEARCH.md Assumption A1 — note any turn where the check silently under-fired); confirmation that
    STATE.md's two Phase 5 open questions are closed (conflict-check scope resolved as full-response prose+tool-calls
    per D-08; precedence direction resolved as per-chat-overrides-global via an explicit `overrides_id` link per D-05);
    and your read on the per-turn latency/cost of the always-on critique call (05-RESEARCH.md §State of the Art
    flagged this as a real tradeoff).
  </how-to-verify>
  <action>
    Before pausing for the developer: run `python -m pytest tests/ -q` and confirm it is green, then start the app
    with `python run.py` and confirm both processes come up (UI on port 8000, Agent `/health` on port 8001).
    Present the eleven verification steps above to the developer, then stop and wait — do not drive the browser and
    do not simulate the model's critique output. After the developer responds, write their observations, the
    model/backend used, the Assumption A1 reliability verdict, the two STATE.md open-question resolutions and the
    latency/cost note into `.planning/phases/05-invariants-day-14/05-04-SUMMARY.md`.
  </action>
  <verify>
    <automated>python -m pytest tests/ -q</automated>
    <human-check>Developer confirms all eleven demo steps, or reports the specific deviations.</human-check>
  </verify>
  <acceptance_criteria>
    - All eleven numbered checks above are confirmed by the developer, or the failing ones are captured verbatim.
    - `05-04-SUMMARY.md` records: the model/backend used, the critique-JSON reliability verdict for Assumption A1, the two STATE.md open-question resolutions, and the latency/cost observation.
    - `python -m pytest tests/ -q` passes at demo time.
    - Nothing observed contradicts D-02 (a second account can edit global rules), D-05 (the chat rule wins in its own chat), or the phase boundary (a conflict is flagged and justified/retracted, never hard-blocked — hard rejection is Phase 6).
  </acceptance_criteria>
  <done>The developer has typed "approved" (or their reported deviations have been captured in 05-04-SUMMARY.md), and the summary records the model used, the critique-JSON reliability verdict and the two open-question resolutions.</done>
  <resume-signal>Type "approved" to accept the Day 14 slice, or describe what did not behave as written above.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer browser → running app | The demo exercises the live REST/WS surface with real session cookies; no new boundary is introduced by this plan |
| Real LLM backend → Agent | The demo is the first exercise of the critique path against a non-mocked model, which is where 05-RESEARCH.md Assumption A1 is actually tested |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-20 | Tampering | invariant injection silently dropped by a future compression change | mitigate | `test_invariants_injected_under_every_strategy` asserts the block survives in the assembled `build_llm_context` output for all four `ContextStrategy` values plus a 40-message history, so a regression fails CI rather than degrading INV-03 invisibly. |
| T-05-21 | Tampering | a second, divergent invariant read path added later | mitigate | `test_resolver_is_the_only_invariant_read_path_in_context_assembly` asserts `agent/context_engine.py` references `resolve_active_invariants` exactly once and never calls the raw list functions, with comment lines stripped before counting so prose cannot satisfy the gate. |
| T-05-22 | Information Disclosure | demo credentials / API keys in the write-up | mitigate | `05-04-SUMMARY.md` records the model/backend *name* and reliability observations only — never the `DEEPSEEK_API_KEY` value or any session cookie, per this project's "never log secrets" convention. |
| T-05-23 | Denial of Service (cost) | the always-on critique call under a real backend | accept | Measured and recorded in the demo rather than mitigated: D-07 locks "the check is a second LLM call", and the only safe reduction (skip when no invariants are active) already shipped in 05-03. The developer's latency/cost note is the artifact that lets a future phase revisit it. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | Zero new packages this phase (05-RESEARCH.md §Package Legitimacy Audit: not applicable). No `requirements.txt` change. |
</threat_model>

<verification>
- `python -m pytest tests/ -q` → zero failures.
- `python -m pytest tests/test_invariants_coverage.py -v` → the strategy matrix reports four parametrised cases, all green.
- `python run.py` → UI on 8000, Agent `/health` on 8001, both healthy.
- Developer confirmation of the eleven demo steps.
- `05-04-SUMMARY.md` exists and records the model, the Assumption A1 verdict, the two STATE.md open-question resolutions and the cost/latency note.
</verification>

<success_criteria>
- Invariant injection is proven under all four compression strategies and on a long history (INV-03).
- A drift guard prevents a second invariant read path from entering context assembly.
- The full Day 14 slice — global rules, per-chat override, injected precedence, flagged conflict with justification/retraction, dual conflict surfacing, persistence across reload — is demonstrated against a real model (INV-01..INV-05).
- STATE.md's two Phase 5 open questions and 05-RESEARCH.md's Assumption A1 are all recorded as resolved in the summary.
- Branch `Day14` is pushed and merged into `main` after approval.
</success_criteria>

<output>
Create `.planning/phases/05-invariants-day-14/05-04-SUMMARY.md` when done
</output>
