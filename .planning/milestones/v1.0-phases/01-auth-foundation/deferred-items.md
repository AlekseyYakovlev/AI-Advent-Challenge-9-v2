# Deferred Items — Phase 01 Auth Foundation

Out-of-scope discoveries logged during plan execution, not fixed per the executor's SCOPE BOUNDARY rule (only auto-fix issues directly caused by the current task's changes).

## 01-04: `test_five_parallel_ws_messages_no_integrity_error` fails when run in the specific 5-file combination `test_ws_auth.py tests/test_ws_origin_validation.py tests/test_ws_security.py tests/test_websocket_cors.py tests/test_concurrent_ws.py`

- **Found during:** Plan 01-04, Task 2 verification.
- **Symptom:** When exactly these 5 WS test files are selected together on the pytest command line (matching the plan's own `<verify>` command for Task 2), `test_concurrent_ws.py::test_five_parallel_ws_messages_no_integrity_error` reliably hangs until the WS 300s idle timeout and fails with `WebSocketDisconnect(code=1000, reason="Idle timeout")` — reproduced 3/3 times with this exact file selection.
- **Confirmed pre-existing, not caused by Plan 01-04:** The identical failure reproduces with `agent/ws.py` reverted to its pre-Plan-01-04 state (no session/ownership gate at all) run with the same narrower file combination (`test_ws_origin_validation.py test_ws_security.py test_websocket_cors.py test_concurrent_ws.py`) — confirmed via `git checkout -- agent/ws.py` + rerun before reapplying the Task 2 diff.
- **Confirmed NOT reproducible in isolation or in the full suite:** `test_concurrent_ws.py` alone passes cleanly and quickly every time (6/6 consecutive isolated runs, ~1.1-1.2s each). `python -m pytest tests/ -q` (full 114-test suite, both with and without the Task 2 fix present) passes cleanly with zero failures in ~25s. The flake is specific to this narrow multi-file subset selection, likely an inter-file WS-connection/event-loop resource accumulation artifact of running many `TestClient(app)` WS sessions back-to-back within a small pytest invocation — not related to session-cookie auth or chat-ownership logic.
- **Not fixed:** Root cause (likely a pytest/anyio-portal or Windows-specific WS test-isolation artifact between exactly this file set) is outside `agent/ws.py`'s auth gate and `tests/test_ws_auth.py`, i.e., outside Plan 01-04's file list (`agent/ws.py`, `tests/test_ws_auth.py`). Same category as the already-documented pre-existing flake `tests/test_context_engine.py::test_extract_and_update_facts_debounce_and_merge` (Plans 01-01/01-02/01-03 summaries).
- **Evidence:** `python -m pytest tests/ -q` — 114 passed, 0 failures (authoritative verification per the plan's own `<verification>` block, item 2). The narrower 5-file command is a convenience verification aid in the plan's Task 2 `<verify>` block, not restated in the plan's `<acceptance_criteria>`, which only requires `tests/test_ws_auth.py` and `tests/ -q` to be green — both confirmed green.
