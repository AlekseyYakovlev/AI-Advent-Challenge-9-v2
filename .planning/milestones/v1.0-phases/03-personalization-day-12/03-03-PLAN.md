---
phase: 03-personalization-day-12
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - tests/test_context_engine_profile.py
autonomous: false
requirements: [PERS-02, PERS-04]

must_haves:
  truths:
    - "The profile reaches the outbound system message under every one of the four context strategies, not just the default"
    - "The profile is present on the first turn of a brand-new chat and still present after many turns have been compressed away"
    - "The same prompt, asked under two visibly different profiles, produces two observably different answers from a real LLM"
    - "The full automated test suite passes"
  artifacts:
    - path: "tests/test_context_engine_profile.py"
      provides: "Strategy-coverage and turn-count-coverage guards for PERS-02's 'every request, not just some'"
      contains: "ContextStrategy"
    - path: ".planning/phases/03-personalization-day-12/03-03-SUMMARY.md"
      provides: "Day 12 acceptance evidence: the two profiles used, the shared prompt, and both responses"
  key_links:
    - from: "saved profile"
      to: "outbound LLM system message"
      via: "build_llm_context -> build_system_prompt -> profile.get_profile"
      pattern: "User's stated preferences"
---

<objective>
Prove the two personalization claims that unit tests of a single function cannot prove on their own: that the profile is attached to **every** request regardless of context strategy or conversation length (PERS-02), and that two different profiles produce **observably different** answers from a real model (PERS-04).

Plans 03-01 and 03-02 are verified by mocked, offline tests. This plan closes the remaining gap with one automated guard and one human-judged demo, and records the Day 12 acceptance evidence for the graded assignment.

Backend routing note inherited from `02-05-SUMMARY.md`: `agent/` constructs exactly one `LLMClient`, pointed at `settings.LM_STUDIO_BASE_URL`; changing the selected model name changes only the JSON `model` field, never the HTTP target. DeepSeek is therefore **not** reachable by picking a model name, and wiring per-backend routing is out of scope for this phase. Run the demo against LM Studio with a locally loaded model, exactly as Day 11 did. Do not spend budget trying to make DeepSeek work.

Purpose: PERS-02 and PERS-04 acceptance evidence.
Output: two new strategy/longevity tests plus `03-03-SUMMARY.md` containing the A/B transcript.
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-personalization-day-12/03-CONTEXT.md
@.planning/phases/03-personalization-day-12/03-RESEARCH.md
@.planning/phases/03-personalization-day-12/03-01-SUMMARY.md
@.planning/phases/03-personalization-day-12/03-02-SUMMARY.md
@.planning/phases/02-memory-day-11/02-05-SUMMARY.md

<interfaces>
<!-- Contracts needed for the guards in Task 1. Extracted from agent/context_engine.py. -->

async def build_llm_context(session, chat_id, model=None) -> list[dict[str, Any]]
    Returns [{"role": "system", "content": <build_system_prompt output>}, *compressed_history].
    The system message is always element 0, for every strategy.

shared.models.ContextStrategy:
    SLIDING_WINDOW = "sliding"        # last 10 messages
    STICKY_FACTS = "sticky"           # summarize old, keep recent
    TRUNCATE_MIDDLE = "truncate_middle"
    NO_COMPRESSION = "no_compression" # raises ContextOverflowError past Settings.context_length

agent.context_engine.RECENT_MESSAGE_COUNT = 10
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Lock "every request" with strategy and conversation-length coverage</name>
  <files>tests/test_context_engine_profile.py</files>
  <read_first>
    - tests/test_context_engine_profile.py (written in plan 03-01 — reuse its existing `_create_chat` helper and assertion style rather than duplicating them)
    - agent/context_engine.py lines 61-100 and 308-322 (`build_system_prompt`, `build_llm_context` — the system message is always element 0)
    - tests/test_strategies.py (how existing tests seed many messages and set `Settings.strategy` per strategy; copy that seeding helper's shape)
    - .planning/phases/03-personalization-day-12/03-RESEARCH.md (Pitfall 2 — the failure mode these guards exist to catch)
  </read_first>
  <action>
Append two tests to `tests/test_context_engine_profile.py`. They target `build_llm_context` (not `build_system_prompt` directly), because PERS-02's claim is about what actually goes out on the wire.

**`test_profile_present_under_every_strategy`** — parametrize over all four `ContextStrategy` members with `@pytest.mark.parametrize("strategy", list(ContextStrategy))`. For each: create a chat owned by `authenticated_client.seeded_user_id`, seed its `Settings` row with that strategy and a `context_length` large enough that `no_compression` cannot overflow (use `context_length=131072`, the schema maximum), save a profile whose `style` is a distinctive marker string, seed roughly 25 alternating user/assistant messages so the sliding/truncate/sticky paths actually engage their compression branches, then call `build_llm_context(session, chat_id, "test-model")` and assert `result[0]["role"] == "system"` and that `result[0]["content"]` contains both `User's stated preferences` and the marker string. Summarization calls the LLM — reuse whatever mocking `tests/test_strategies.py` already uses for `sticky`/`truncate_middle` so this test stays offline and fast; if that file patches `llm_client`, patch it the same way here.

**`test_profile_present_on_first_and_late_turns`** — one chat, `sliding` strategy. Assert the marker appears in `build_llm_context(...)[0]["content"]` when the chat has zero messages, and again after seeding 30 messages (well past `RECENT_MESSAGE_COUNT = 10`, so every original message has been compressed out of the window). This is the direct regression guard for RESEARCH Pitfall 2 — profile injection must never be gated on message count, compression state, or whether the chat predates this phase.

Add no production-code changes in this task. If either test fails, the bug is in `agent/context_engine.py` and must be fixed there rather than by weakening the assertion.
  </action>
  <verify>
    <automated>pytest tests/test_context_engine_profile.py -q && pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_context_engine_profile.py -q` passes, reporting at least `12` tests (7 from plan 03-01, plus 4 parametrized strategy cases, plus 1 turn-count case).
    - `grep -c "build_llm_context" tests/test_context_engine_profile.py` outputs at least `3`.
    - `grep -c "ContextStrategy" tests/test_context_engine_profile.py` outputs at least `2`.
    - `pytest tests/ -q` is fully green.
    - `git diff --name-only` lists only `tests/test_context_engine_profile.py` for this task.
  </acceptance_criteria>
  <done>Automated proof that the outbound system message carries the user's preferences under all four compression strategies, on an empty chat, and on a chat long enough that the original messages have been compressed away.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Day 12 acceptance demo — same prompt, two profiles, different answers</name>
  <files>(no source changes — live demo and evidence capture)</files>
  <read_first>
    - .planning/phases/02-memory-day-11/02-05-SUMMARY.md (which LM Studio model was confirmed working, and the recorded DeepSeek-routing limitation)
    - .planning/phases/03-personalization-day-12/03-02-SUMMARY.md (the shipped panel's field ids and save flow)
    - .planning/REQUIREMENTS.md §Personalization (the exact PERS-04 wording being demonstrated)
  </read_first>
  <action>
Run the pre-flight yourself first, make no source changes, then pause for the human A/B judgement described below.

Pre-flight, recording each result for the SUMMARY: `pytest tests/ -v` must be green — if anything fails, stop and report rather than starting the demo; `python run.py` starts and `curl -s http://localhost:8001/health` returns `{"status":"ok"}`; the `profile` table is present in `app.db`; and a tool-capable model is loaded from the app's model selector over the same LM Studio path recorded in `02-05-SUMMARY.md`.

Then present the `<how-to-verify>` steps to the user and wait for their verdict. Do not self-approve, do not simulate or paraphrase the model's responses, and if the two answers come back similar, record that negative result and the models tried instead of declaring PERS-04 satisfied.
  </action>
  <what-built>
    A per-user `Profile` (style / format / constraints) editable from a permanently-visible "Профиль" sidebar panel, persisted via `GET`/`PUT /api/v1/profile`, and injected as a leading directive into the system prompt of every LLM request — under every context strategy, on every turn.

    Before pausing for you, the executor has already run these itself and recorded the results for the SUMMARY:
    - `pytest tests/ -v` fully green (record the pass count).
    - `python run.py` starts, and `curl -s http://localhost:8001/health` returns `{"status":"ok"}`.
    - The `profile` table exists in `app.db`: `python -c "import sqlite3; print([r[0] for r in sqlite3.connect('app.db').execute(\"select name from sqlite_master where type='table'\")])"` includes `profile`.
    - A model is loaded in LM Studio from the app's model selector (same model path as Day 11; DeepSeek is not reachable by model name — see 02-05-SUMMARY).
  </what-built>
  <how-to-verify>
    1. Open http://localhost:8000 and log in. Confirm the "Профиль" panel is visible in the sidebar between "Память" and the agent-status footer, with three empty textareas and a "Сохранить профиль" button.

    2. **Profile A** — enter and save:
       - Стиль общения: `отвечай очень коротко, одним предложением, сухо и по делу`
       - Формат ответа: `обычный текст, без списков и заголовков`
       - Ограничения: `никаких примеров кода`
       Confirm the toast "Профиль сохранён" appears.

    3. Start a **new chat** and send exactly: `Как работает кэш в браузере?`
       Save the reply verbatim — this is Response A.

    4. **Profile B** — return to the panel, replace all three fields, and save:
       - Стиль общения: `отвечай подробно и дружелюбно, с развёрнутыми объяснениями`
       - Формат ответа: `маркированный список с заголовками`
       - Ограничения: `обязательно добавь пример кода`
       Confirm the toast again.

    5. Start **another new chat** (so no conversation history can explain the difference) and send the identical prompt: `Как работает кэш в браузере?`
       Save the reply verbatim — this is Response B.

    6. Judge PERS-04: Response A should be short, prose, code-free; Response B should be long, bulleted, with a code example. If the two responses are not obviously different in style and format, say so — the likely cause is a weak local model ignoring system-prompt directives, and the right fix is trying a stronger loaded model before touching the injection wording.

    7. Reload the browser page and confirm the Профиль panel repopulates with Profile B's values (persistence, PERS-03).

    8. Optional but useful evidence: with Profile B saved, run
       `curl -s -b "<your session cookie>" http://localhost:8001/api/v1/profile`
       and confirm it returns the same three values the panel shows.
  </how-to-verify>
  <verify>
    <human-check>Response A and Response B are observably different in style and format for the identical prompt, and both chats were fresh.</human-check>
    <automated>pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/ -q` is green and the pass count is recorded in the SUMMARY.
    - `03-03-SUMMARY.md` contains both profiles verbatim, the shared prompt, and both full responses, so the difference is auditable after the fact.
    - The SUMMARY records the exact LM Studio model id used.
    - The SUMMARY restates the DeepSeek-routing limitation inherited from 02-05 so Phase 4 does not re-derive it.
    - If the responses did NOT differ, the SUMMARY records that outcome plus which models were tried, rather than declaring PERS-04 satisfied.
  </acceptance_criteria>
  <resume-signal>Type "approved" if Response A and Response B are observably different, or paste both responses and describe what went wrong.</resume-signal>
  <done>Day 12 acceptance evidence captured: the same question answered two visibly different ways because of two different saved profiles, with the transcript recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| profile text → LLM system prompt → response | User-authored directives influence model behavior on every turn |
| local LM Studio HTTP API | The only outbound LLM target; no cloud call is reachable by model-name selection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Tampering | system-prompt override via the `constraints` field | accept | A user can already set `Settings.system_prompt` freely from the Settings modal; the profile grants no new capability, and every account is equal-"admin" by design. No cross-user surface exists. |
| T-03-11 | Repudiation | acceptance evidence | mitigate | Both profiles, the shared prompt, and both responses are recorded verbatim in `03-03-SUMMARY.md`, so the PERS-04 claim is auditable rather than asserted. |
| T-03-12 | Denial of service | `no_compression` + a large profile | mitigate | Task 1's strategy test exercises `no_compression` explicitly; the 2000-char-per-field cap from plan 03-01 bounds the profile's contribution to the context budget. |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No packages installed; `requirements.txt` unchanged. Nothing to audit. |
</threat_model>

<verification>
1. `pytest tests/ -v` fully green, pass count recorded.
2. Profile injected into `build_llm_context(...)[0]["content"]` under all four `ContextStrategy` values.
3. Profile injected on an empty chat and on a 30-message chat.
4. Live A/B: identical prompt, two fresh chats, two saved profiles, two observably different responses.
5. Profile values survive a browser reload and match `GET /api/v1/profile`.
</verification>

<success_criteria>
- PERS-02 proven structurally (all four strategies, first and late turns) rather than assumed.
- PERS-04 demonstrated against a real model with a recorded transcript.
- `03-03-SUMMARY.md` holds the Day 12 acceptance evidence, the model id used, and the inherited DeepSeek-routing limitation.
- The full suite is green and the phase is ready for `Day12` to be pushed and merged to `main`.
</success_criteria>

<output>
Create `.planning/phases/03-personalization-day-12/03-03-SUMMARY.md` when done, containing the full A/B transcript and the evidence listed in the acceptance criteria.
</output>
