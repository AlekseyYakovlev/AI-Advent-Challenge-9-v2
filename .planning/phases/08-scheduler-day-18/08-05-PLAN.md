---
phase: 08-scheduler-day-18
plan: 05
type: execute
wave: 3
depends_on: ["08-04"]
files_modified:
  - agent/scheduler_ops.py
  - agent/scheduler_api.py
  - agent/main.py
  - tests/test_scheduler_api.py
  - tests/test_scoping.py
autonomous: true
requirements: [SCHED-01, SCHED-08, SCHED-09, SCHED-10, SCHED-14]

must_haves:
  truths:
    - "An authenticated user can create a once / interval / cron job over REST and gets 201 with next_run_at as a UTC-aware ISO string; invalid input gets 422 with a Russian string detail (e.g. 'Некорректное cron-выражение. Нужно 5 полей: минута час день месяц день_недели.')"
    - "The owner can list, get, pause, resume, run now, cancel and delete jobs and read run history and a run's full result; illegal state changes return 409 (pause of non-active, resume of non-paused, run now while running, cancel of completed/cancelled)"
    - "Cancel is soft (status cancelled, runs kept); Delete removes the job and all its runs and aborts an in-flight run first"
    - "Another user's job or run id always answers 404 (never 403) and is left untouched; every scheduler route answers 401 without a session"
    - "Each mutation publishes task_updated / task_deleted to the owner's /ws/events sockets"
    - "A user cannot hold more than SCHEDULER_MAX_ACTIVE_TASKS_PER_USER active+paused jobs"
  artifacts:
    - path: "agent/scheduler_ops.py"
      provides: "user-scoped job operations shared by REST (08-05) and LLM tools (08-06)"
      exports: ["SchedulerNotFoundError", "SchedulerConflictError", "create_scheduled_task", "list_scheduled_tasks", "get_owned_task", "pause_task", "resume_task", "cancel_task", "delete_task", "run_task_now", "list_task_runs", "get_owned_run"]
    - path: "agent/scheduler_api.py"
      provides: "APIRouter(prefix='/api/v1/scheduler')"
      contains: "APIRouter"
    - path: "tests/test_scheduler_api.py"
      provides: "REST status codes, validation copy, lifecycle, IDOR, UTC ISO tests"
      min_lines: 200
  key_links:
    - from: "agent/main.py"
      to: "agent/scheduler_api.py::router"
      via: "app.include_router(scheduler_router)"
      pattern: "include_router\\(scheduler_router\\)"
    - from: "agent/scheduler_ops.py::delete_task"
      to: "agent/scheduler.py::scheduler.abort_task_runs"
      via: "abort before session.delete(task)"
      pattern: "abort_task_runs"
    - from: "agent/scheduler_ops.py::run_task_now"
      to: "agent/scheduler.py::scheduler.start_manual_run"
      via: "RunAlreadyActiveError -> SchedulerConflictError"
      pattern: "start_manual_run"
---

<objective>
Expose the scheduler to the owner: a shared, user-scoped operations module (`agent/scheduler_ops.py`, also used by the LLM tools in 08-06) and the REST router `/api/v1/scheduler/*` with create / list / get / pause / resume / run now / cancel / delete / run history / run detail (D-16), wired into `agent/main.py`.

Purpose: the UI (08-07) talks only to these endpoints; they enforce ownership (404 not 403), Russian validation copy, state-machine conflicts (409) and publish live events.
Output: `agent/scheduler_ops.py`, `agent/scheduler_api.py`, `include_router` in `agent/main.py`, `tests/test_scheduler_api.py`, `UNAUTH_ROUTES` additions.
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-scheduler-day-18/08-CONTEXT.md
@.planning/phases/08-scheduler-day-18/08-RESEARCH.md
@.planning/phases/08-scheduler-day-18/08-PATTERNS.md
@.planning/phases/08-scheduler-day-18/08-UI-SPEC.md
@.planning/phases/08-scheduler-day-18/08-04-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- From 08-01: agent/schedule.py build_schedule_spec(...), initial_next_run(spec, now), next_run_on_resume(...), ScheduleValidationError(.message),
     MSG_TITLE_PROMPT_REQUIRED, TITLE_MAX_LENGTH=200, PROMPT_MAX_LENGTH=4000; models/enums -->
<!-- From 08-02: agent/events.py hub.publish(user_id, frame) -->
<!-- From 08-04: agent/scheduler_schemas.py ScheduledTaskOut, RunSummary, RunDetail, ScheduledTaskCreate, run_to_summary, run_to_detail,
     task_updated_frame, task_deleted_frame; agent/scheduler.py scheduler (start_manual_run, abort_task_runs), build_task_out, RunAlreadyActiveError -->

<!-- Contracts this plan CREATES -->
agent/scheduler_ops.py (every function takes session + user_id; foreign == missing):
- class SchedulerNotFoundError(Exception); class SchedulerConflictError(Exception) with .message (Russian)
- MSG_TOO_MANY_TASKS = "Слишком много активных заданий (максимум {n})"
- async create_scheduled_task(session, user_id, *, title, prompt, model, schedule_type, delay_seconds=None, run_at=None,
      interval_seconds=None, cron=None, max_runs=None, origin_chat_id=None, now=None) -> ScheduledTask     # raises ScheduleValidationError / SchedulerConflictError
- async list_scheduled_tasks(session, user_id, *, statuses: set[ScheduledTaskStatus] | None = None) -> list[ScheduledTask]
- async get_owned_task(session, user_id, task_id) -> ScheduledTask                    # raises SchedulerNotFoundError
- async pause_task / resume_task / cancel_task(session, user_id, task_id, now=None) -> ScheduledTask   # raise NotFound / Conflict
- async delete_task(session, user_id, task_id) -> None
- async run_task_now(session, user_id, task_id) -> TaskRun                             # Conflict "Задание уже выполняется"
- async list_task_runs(session, user_id, task_id, limit=20) -> list[TaskRun]            # newest first
- async get_owned_run(session, user_id, run_id) -> tuple[TaskRun, ScheduledTask]
- async publish_task_updated(session, task) -> ScheduledTaskOut                         # builds out + hub.publish

REST (router prefix /api/v1/scheduler; all Depends(get_current_user); 401 without session):
- GET    /tasks                          -> 200 list[ScheduledTaskOut]  (active/paused first, then by next_run_at, then newest)
- POST   /tasks          body ScheduledTaskCreate -> 201 ScheduledTaskOut | 422 {"detail": "<Russian string>"} | 409 too many
- GET    /tasks/{task_id}                -> 200 ScheduledTaskOut | 404
- POST   /tasks/{task_id}/pause|resume|cancel -> 200 ScheduledTaskOut | 404 | 409
- POST   /tasks/{task_id}/run            -> 202 RunSummary | 404 | 409
- DELETE /tasks/{task_id}                -> 204 | 404
- GET    /tasks/{task_id}/runs?limit=20  -> 200 list[RunSummary] (limit 1..100)
- GET    /runs/{run_id}                  -> 200 RunDetail | 404
Mutating routes: dependencies=[Depends(require_allowed_origin)] (+ Depends(require_json_content_type) on POST /tasks).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: agent/scheduler_ops.py — user-scoped job operations</name>
  <files>agent/scheduler_ops.py, tests/test_scheduler_api.py</files>
  <read_first>
    - agent/scheduler.py, agent/scheduler_schemas.py (08-04 outputs)
    - agent/schedule.py (08-01 output)
    - agent/mcp_config.py (get_server ownership pattern ~76-85; create/update/delete commit+rollback ~118-160)
    - agent/tasks.py (FSM service error classes and conflict style)
    - .planning/phases/08-scheduler-day-18/08-RESEARCH.md Pattern 3 (pause/resume semantics) and Pitfall 9
  </read_first>
  <behavior>
    - create once (delay_seconds=60) -> status active, next_run_at = now+60 s, run_at set, run_count 0, origin_chat_id as given
    - create with blank title or prompt (after strip) -> ScheduleValidationError MSG_TITLE_PROMPT_REQUIRED; title > 200 or prompt > 4000 chars -> same class with a Russian length message; blank model -> "Выберите модель"
    - 51st active job (SCHEDULER_MAX_ACTIVE_TASKS_PER_USER=50, monkeypatch to 2 in tests) -> SchedulerConflictError MSG_TOO_MANY_TASKS
    - pause active -> paused (next_run_at kept); pause paused -> Conflict "Задание не активно"; resume paused interval -> active with next_run_at = now+interval (no catch-up for paused time); resume active -> Conflict "Задание не на паузе"
    - cancel active/paused -> cancelled with next_run_at NULL, runs kept; cancel completed/cancelled -> Conflict "Задание уже завершено"
    - run_task_now on active/paused -> RUNNING manual run (scheduler.spawn_run monkeypatched to a no-op); second call -> Conflict "Задание уже выполняется"; on completed/cancelled -> Conflict "Задание уже завершено"
    - delete -> job and its runs gone; abort_task_runs was awaited before the delete
    - any operation with another user's task_id/run_id -> SchedulerNotFoundError
    - each mutation publishes exactly one task_updated (or task_deleted) frame to the owner
  </behavior>
  <action>
    Create agent/scheduler_ops.py (module docstring "User-scoped scheduler operations shared by REST routes and LLM tools."; `logger = get_logger(__name__)`) implementing <interfaces>:
    - create_scheduled_task: strip title/prompt/model; empty title or prompt -> ScheduleValidationError(MSG_TITLE_PROMPT_REQUIRED); length caps (TITLE_MAX_LENGTH, PROMPT_MAX_LENGTH, model <= 200) -> ScheduleValidationError("Название не длиннее 200 символов" / "Промпт не длиннее 4000 символов"); empty model -> ScheduleValidationError("Выберите модель"); count active+paused jobs of user_id and reject when >= settings.SCHEDULER_MAX_ACTIVE_TASKS_PER_USER with SchedulerConflictError(MSG_TOO_MANY_TASKS.format(n=...)); `spec = build_schedule_spec(...)`, `next_run_at = initial_next_run(spec, now)`; insert ScheduledTask (run_at/interval_seconds/cron_expr/max_runs from spec; origin_chat_id as given, D-13); commit/refresh with rollback on error; `await publish_task_updated(session, row)`; log info `scheduled_task_created user_id= task_id= schedule_type=` (never prompt).
    - Ownership helpers: `session.get(ScheduledTask, task_id)`; None or `row.user_id != user_id` -> log warning `scheduled_task_access_denied user_id= task_id=` and raise SchedulerNotFoundError. get_owned_run uses TaskRun.user_id (denormalised) the same way, then loads its job.
    - pause_task: only ACTIVE -> PAUSED (keep next_run_at so the finalize rule never completes a paused job). resume_task: only PAUSED -> ACTIVE with `next_run_on_resume(...)` (interval/cron from now; once keeps run_at, may fire late on next tick). cancel_task: ACTIVE or PAUSED -> CANCELLED, next_run_at None, in-flight run keeps running and records its result (Assumption A5: soft cancel does not abort; D-14/D-16 history kept). Each sets updated_at, commits (rollback on error) and publishes task_updated. Russian conflict messages exactly as in <behavior>.
    - run_task_now: owned + status in (ACTIVE, PAUSED) else Conflict "Задание уже завершено"; `await scheduler.start_manual_run(session, task)`; RunAlreadyActiveError -> SchedulerConflictError("Задание уже выполняется"); publish task_updated after (run_started is published by the engine).
    - delete_task: owned -> `await scheduler.abort_task_runs(task.id)` -> `await session.delete(task)` -> commit (runs cascade via FK) -> `hub.publish(user_id, task_deleted_frame(task_id))`; log info `scheduled_task_deleted`.
    - list_scheduled_tasks: `select(ScheduledTask).where(ScheduledTask.user_id == user_id)` (+ optional status filter); sort in Python: active/paused first, then next_run_at ascending (None last), then created_at descending.
    - list_task_runs: owned job; `select(TaskRun).where(TaskRun.scheduled_task_id == task_id, TaskRun.user_id == user_id).order_by(TaskRun.started_at.desc(), TaskRun.id.desc()).limit(limit)`.

    Start tests/test_scheduler_api.py with ops-level tests for every <behavior> bullet (fixtures `authenticated_client` / `second_authenticated_client` for user ids; direct `async_session_factory()` sessions; monkeypatch `agent.scheduler.scheduler.spawn_run` to a no-op so no LLM call happens).
  </action>
  <verify>
    <automated>pytest tests/test_scheduler_api.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "user_id != user_id\|\.user_id == user_id" agent/scheduler_ops.py` >= 3
    - `grep -c "abort_task_runs" agent/scheduler_ops.py` == 1 and its line precedes `session.delete(` in delete_task
    - `grep -c "await session.rollback()" agent/scheduler_ops.py` >= 4
    - `pytest tests/test_scheduler_api.py -q` passes with >= 14 ops tests
  </acceptance_criteria>
  <done>All owner operations exist with scoping, Russian conflicts and live events.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: REST router /api/v1/scheduler/* + main.py wiring + scoping tests</name>
  <files>agent/scheduler_api.py, agent/main.py, tests/test_scheduler_api.py, tests/test_scoping.py</files>
  <read_first>
    - agent/scheduler_ops.py (Task 1)
    - agent/main.py (MCP routes ~988-1170: decorator args, 404 helper ~178-191, 409 mapping ~777-794, literal 422 comment ~167-175; CORSMiddleware block ~398-404)
    - agent/dependencies.py (get_current_user, require_allowed_origin, require_json_content_type)
    - tests/test_scoping.py (UNAUTH_ROUTES ~10-20 and IDOR tests ~83-114)
    - tests/test_mcp_api.py (REST test style with authenticated_client)
  </read_first>
  <behavior>
    - POST /tasks {title, prompt, model, schedule_type:"interval", interval_seconds:60, max_runs:3} -> 201; JSON next_run_at/created_at end with "Z" or "+00:00"; schedule_type "interval"; max_runs 3
    - POST /tasks cron "*/5 * * * * *" -> 422 detail == "Некорректное cron-выражение. Нужно 5 полей: минута час день месяц день_недели."; once run_at in the past -> 422 "Время запуска уже прошло"; blank title -> 422 "Заполните название и промпт"; interval 5 -> 422 "Минимальный интервал — 10 секунд"
    - POST /tasks without JSON content-type -> 415 (existing require_json_content_type behaviour); foreign Origin -> 403 (existing require_allowed_origin behaviour)
    - GET /tasks returns only the caller's jobs; GET /tasks/{id} of another user -> 404
    - pause -> 200 status paused; pause again -> 409; resume -> 200 active; cancel -> 200 cancelled; cancel again -> 409
    - POST /tasks/{id}/run -> 202 RunSummary status running trigger manual (spawn_run monkeypatched no-op); again -> 409 detail "Задание уже выполняется"
    - GET /tasks/{id}/runs -> newest first, no result_text key; GET /runs/{run_id} -> RunDetail with result_text, error, tool_trace list, task_title
    - DELETE /tasks/{id} -> 204, then GET -> 404 and no TaskRun rows remain for it
    - second_authenticated_client on every /tasks/{a_id}... and /runs/{a_run} route (GET, POST pause/resume/cancel/run, DELETE, runs) -> 404 and user A's job is unchanged in the DB
    - every scheduler route without a session -> 401 (UNAUTH_ROUTES parametrised test)
  </behavior>
  <action>
    Create agent/scheduler_api.py (module docstring "REST routes for the scheduler (/api/v1/scheduler)."): `router = APIRouter(prefix="/api/v1/scheduler", tags=["scheduler"])`; routes exactly as in <interfaces>, each with `session: AsyncSession = Depends(get_session)` and `current_user: User = Depends(get_current_user)`; mutating routes carry `dependencies=[Depends(require_allowed_origin)]`, POST /tasks additionally `Depends(require_json_content_type)` (same decorator args as the MCP routes in main.py). Exception mapping in one small helper: SchedulerNotFoundError -> HTTPException(404, detail="Задание не найдено") (or "Запуск не найден" for runs; never 403); ScheduleValidationError -> HTTPException(status_code=422, detail=exc.message) (literal 422 with the same comment as main.py about the renamed Starlette constant); SchedulerConflictError -> HTTPException(409, detail=exc.message). Responses via `build_task_out` / `run_to_summary` / `run_to_detail`. `limit: int = Query(20, ge=1, le=100)` on the runs route. Keep route functions short (< 25 lines) and fully typed.

    agent/main.py: `from agent.scheduler_api import router as scheduler_router` and `app.include_router(scheduler_router)` directly after the CORSMiddleware block. No other main.py change in this plan.

    tests/test_scoping.py: append to UNAUTH_ROUTES: ("GET", "/api/v1/scheduler/tasks", None), ("POST", "/api/v1/scheduler/tasks", {"title": "x"}), ("GET", "/api/v1/scheduler/tasks/1", None), ("POST", "/api/v1/scheduler/tasks/1/pause", None), ("POST", "/api/v1/scheduler/tasks/1/resume", None), ("POST", "/api/v1/scheduler/tasks/1/cancel", None), ("POST", "/api/v1/scheduler/tasks/1/run", None), ("DELETE", "/api/v1/scheduler/tasks/1", None), ("GET", "/api/v1/scheduler/tasks/1/runs", None), ("GET", "/api/v1/scheduler/runs/1", None). If the existing parametrised test sends requests that the origin/content-type dependencies reject before auth, mirror whatever headers the existing entries already rely on so each route answers 401.

    Extend tests/test_scheduler_api.py with every <behavior> bullet (httpx AsyncClient fixtures, which do not run the lifespan; send `Origin: http://localhost:8000` on mutating requests the same way tests/test_mcp_api.py does).
  </action>
  <verify>
    <automated>pytest tests/test_scheduler_api.py tests/test_scoping.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'APIRouter(prefix="/api/v1/scheduler"' agent/scheduler_api.py` == 1
    - `grep -c "include_router(scheduler_router)" agent/main.py` == 1
    - `grep -c "status_code=403\|HTTP_403" agent/scheduler_api.py` == 0
    - `grep -c '"/api/v1/scheduler' tests/test_scoping.py` >= 10
    - `pytest tests/test_scheduler_api.py tests/test_scoping.py -q` passes (>= 30 tests in test_scheduler_api.py)
  </acceptance_criteria>
  <done>The full REST surface works, is user-scoped (404) and authenticated (401), with Russian 422/409 copy the UI shows verbatim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> REST /api/v1/scheduler/* | Untrusted JSON bodies and path ids |
| REST -> DB / engine | Mutations of jobs and triggering of runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-21 | Information disclosure / Tampering (IDOR) | all /tasks/{id} and /runs/{id} routes | mitigate | scheduler_ops ownership helpers compare row.user_id to the session user; foreign == missing -> 404; two-user tests on every route |
| T-08-22 | Spoofing | all routes | mitigate | Depends(get_current_user) (HTTP-only session cookie); UNAUTH_ROUTES 401 tests |
| T-08-23 | Tampering (CSRF) | mutating routes | mitigate | require_allowed_origin on every mutating route + require_json_content_type on POST /tasks (existing pattern) |
| T-08-24 | Denial of service | job creation | mitigate | Per-user cap SCHEDULER_MAX_ACTIVE_TASKS_PER_USER, min interval, prompt <= 4000, title <= 200, runs limit <= 100 |
| T-08-25 | Input validation | ScheduledTaskCreate | mitigate | Server-side validation in build_schedule_spec + ops (Russian 422); Pydantic types reject non-numeric values |
| T-08-26 | Information disclosure | logs | mitigate | Log ids and status only; never prompt/title text |
</threat_model>

<verification>
`pytest tests/test_scheduler_api.py tests/test_scoping.py tests/test_scheduler_service.py -q` passes.
</verification>

<success_criteria>
- REST surface complete per D-16 with correct status codes and Russian validation copy
- Ownership enforced everywhere with 404; unauthenticated requests 401
- Mutations publish live events to the owner
</success_criteria>

<output>
Create `.planning/phases/08-scheduler-day-18/08-05-SUMMARY.md` when done
</output>
