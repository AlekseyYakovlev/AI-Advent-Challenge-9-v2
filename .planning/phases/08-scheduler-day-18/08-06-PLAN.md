---
phase: 08-scheduler-day-18
plan: 06
type: execute
wave: 4
depends_on: ["08-05"]
files_modified:
  - agent/state.py
  - agent/schemas.py
  - agent/tool_guard.py
  - agent/scheduler_tools.py
  - agent/ws.py
  - agent/main.py
  - tests/test_scheduler_tools.py
  - tests/test_mcp_tools.py
autonomous: true
requirements: [SCHED-02, SCHED-11, SCHED-14]

must_haves:
  truths:
    - "In a chat turn the LLM can call schedule_task (once via delay_seconds or run_at, interval via interval_seconds, cron via 5-field cron) and a job is created for the chatting user with the chat's current model and origin_chat_id = that chat"
    - "list_scheduled_tasks returns only the calling user's active and paused jobs with id, title, schedule, next_run_at (UTC and local) and last run status"
    - "cancel_scheduled_task cancels (soft) only when the user's latest chat message expresses cancel intent and user_requested_cancellation is true; otherwise it returns error code cancel_not_requested and the job stays active"
    - "Another user's job id -> not_found from list/cancel; deleting the origin chat keeps the job with origin_chat_id NULL"
    - "Scheduler tool results never appear in the done frame's memory_writes"
    - "The tool schema list has 9 tools and no tool property enum contains 'cancelled'"
  artifacts:
    - path: "agent/scheduler_tools.py"
      provides: "@register_tool schedule_task / list_scheduled_tasks / cancel_scheduled_task"
      contains: "cancel_scheduled_task"
    - path: "agent/schemas.py"
      provides: "ScheduleTaskArgs, ListScheduledTasksArgs, CancelScheduledTaskArgs"
      contains: "class ScheduleTaskArgs"
    - path: "agent/tool_guard.py"
      provides: "user_asked_to_cancel(text) heuristic (reusable by backlog 999.1)"
      contains: "def user_asked_to_cancel"
    - path: "agent/state.py"
      provides: "current_chat_model ContextVar"
      contains: "current_chat_model"
    - path: "tests/test_scheduler_tools.py"
      provides: "tool dispatch, validation, scoping, cancel gate, SET NULL and memory_writes tests"
      min_lines: 180
  key_links:
    - from: "agent/ws.py::_handle_chat_message"
      to: "agent/state.py::current_chat_model"
      via: "current_chat_model.set(payload.model)"
      pattern: "current_chat_model\\.set\\(payload\\.model\\)"
    - from: "agent/scheduler_tools.py"
      to: "agent/scheduler_ops.py"
      via: "create_scheduled_task / list_scheduled_tasks / cancel_task"
      pattern: "scheduler_ops\\."
    - from: "agent/main.py"
      to: "agent/scheduler_tools.py"
      via: "import registers tools in TOOL_REGISTRY"
      pattern: "scheduler_tools"
---

<objective>
Let the LLM manage jobs from chat (D-12, D-13, D-14): three tools registered in the existing dispatcher, the model-in-context plumbing (`current_chat_model` ContextVar), the three-layer cancel gate, and the known collateral fixes (`_collect_memory_writes` exclusion, tool-count test 6 -> 9, no `cancelled` enum).

Purpose: the demo path "через минуту прочитай файл X через MCP и перескажи" starts here; the cancel gate stops the model from cancelling jobs on its own initiative.
Output: `agent/scheduler_tools.py`, arg models in `agent/schemas.py`, `user_asked_to_cancel` in `agent/tool_guard.py`, surgical edits to `agent/state.py`, `agent/ws.py`, `agent/main.py`, tests.
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-scheduler-day-18/08-CONTEXT.md
@.planning/phases/08-scheduler-day-18/08-RESEARCH.md
@.planning/phases/08-scheduler-day-18/08-PATTERNS.md
@.planning/phases/08-scheduler-day-18/08-05-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Existing -->
agent/tools.py: register_tool(name, args_model, description) decorator; handler signature
  async (session: AsyncSession, user_id: int, chat_id: int, args: dict[str, Any]) -> dict[str, Any];
  a result with "status" == "error" is treated as ok=False; build_tool_schemas(); TOOL_REGISTRY
agent/ws.py: TASK_TOOL_NAMES = ("create_task", "transition_task", "pause_task", "resume_task") (~line 65);
  _collect_memory_writes(results) (~547-553); _handle_chat_message (~664) — payload.model is the chat's model;
  _persist_user_message commits the user message and sets chat.current_leaf_message_id BEFORE the LLM call
agent/tool_guard.py: _NEGATION_OR_FUTURE_RE / _is_negated_or_future (~91-103) regex heuristic style
<!-- From 08-05 --> agent/scheduler_ops.py: create_scheduled_task(...), list_scheduled_tasks(session, user_id, statuses=...),
  cancel_task(session, user_id, task_id), SchedulerNotFoundError, SchedulerConflictError(.message); agent/schedule.py ScheduleValidationError(.message)
<!-- From 08-04 --> agent/scheduler.py build_task_out(session, task) (last_run)

<!-- Contracts this plan CREATES -->
agent/state.py: current_chat_model: ContextVar[str | None] = ContextVar("current_chat_model", default=None)
agent/ws.py: SCHEDULER_TOOL_NAMES = ("schedule_task", "list_scheduled_tasks", "cancel_scheduled_task");
  NON_MEMORY_TOOL_NAMES = TASK_TOOL_NAMES + SCHEDULER_TOOL_NAMES
agent/tool_guard.py: user_asked_to_cancel(text: str) -> bool
agent/schemas.py:
- ScheduleTaskArgs: schedule_type: Literal["once","interval","cron"]; title: str (1..200); prompt: str (1..4000);
  delay_seconds: int|None (ge=1); run_at: str|None; interval_seconds: int|None (ge=1); cron: str|None; max_runs: int|None (ge=1)
  + model_validator(mode="after"): once -> exactly one of delay_seconds/run_at; interval -> interval_seconds; cron -> cron
- ListScheduledTasksArgs: no fields
- CancelScheduledTaskArgs: task_id: int (gt=0); user_requested_cancellation: bool
Tool results:
- schedule_task ok: {"status":"scheduled","id","title","schedule_type","next_run_at": UTC ISO,"next_run_at_local": local ISO seconds,"max_runs"}
- list_scheduled_tasks ok: {"status":"ok","tasks":[{"id","title","schedule_type","schedule","next_run_at","next_run_at_local","status","last_run_status"}]}
- cancel_scheduled_task ok: {"status":"cancelled","id","title"}
- errors: {"status":"error","code": "invalid_schedule"|"model_unknown"|"too_many"|"not_found"|"cancel_not_requested"|"conflict","error": str}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Arg models, cancel-intent heuristic, ContextVar and ws.py collateral edits</name>
  <files>agent/schemas.py, agent/tool_guard.py, agent/state.py, agent/ws.py, tests/test_scheduler_tools.py</files>
  <read_first>
    - agent/schemas.py (imports ~1-10: Literal, field_validator; SaveLongTermMemoryArgs ~370-382; TransitionTaskArgs ~436-450; module constants near TASK_TITLE_MAX_LENGTH)
    - agent/tool_guard.py (~85-170: regex heuristics, negation window)
    - agent/state.py (whole file)
    - agent/ws.py (imports ~27-32; TASK_TOOL_NAMES ~65; _collect_memory_writes ~547-553; _handle_chat_message head ~664-672)
    - tests/test_tasks.py::test_cancel_is_not_an_llm_tool (~618-623)
  </read_first>
  <behavior>
    - user_asked_to_cancel returns True for: "отмени задание 3", "Останови периодическую задачу", "удали это задание пожалуйста", "убери напоминание", "выключи расписание", "прекрати проверку", "cancel job 5", "please stop the scheduled task", "delete it", "remove the job", "disable that schedule"
    - user_asked_to_cancel returns False for: "не отменяй задание", "не надо удалять", "don't cancel it", "do not stop the job", "покажи мои задания", "через минуту прочитай файл", "" 
    - ScheduleTaskArgs: once with both delay_seconds and run_at -> ValidationError; once with neither -> ValidationError; interval without interval_seconds -> ValidationError; cron without cron -> ValidationError; valid once/interval/cron pass
    - CancelScheduledTaskArgs requires user_requested_cancellation (missing -> ValidationError)
    - No property enum in any of the three schemas contains "cancelled"
  </behavior>
  <action>
    agent/schemas.py: add the three arg models from <interfaces> after the task-tool arg models, with `Field(description=...)` on every field (the model reads them): schedule_type "once = single run; interval = every N seconds; cron = 5-field cron in the agent machine's LOCAL time"; delay_seconds "For once: run after this many seconds"; run_at "For once: absolute local time, ISO 8601 like 2026-09-26T14:05:00 (compute it from the current local time in the system message)"; interval_seconds "For interval: period in seconds (minimum 10)"; cron "For cron: exactly 5 fields: minute hour day month weekday"; prompt "What the agent must do at fire time, as a self-contained instruction (no user will be present)"; title "Short human-readable job title"; max_runs "Optional: stop after this many runs (periodic jobs only)"; task_id "The numeric id of an existing scheduled job (call list_scheduled_tasks first if unknown)"; user_requested_cancellation "Must be true, and only when the user explicitly asked in their latest message to cancel/stop/delete this job". Import `model_validator` from pydantic. The validator raises ValueError with an English message (the dispatcher returns it to the model). No enum containing "cancelled" anywhere.

    agent/tool_guard.py: add `_CANCEL_INTENT_RE` (case-insensitive) matching the stems `отмен`, `останов`, `прекрат`, `удал`, `убер`, `убра`, `выключ`, `cancel`, `stop`, `delete`, `remove`, `disable` and `_CANCEL_NEGATION_RE` matching a negation (`не`, `нельзя`, `don't`, `dont`, `do not`, `never`, `not`) within the 2 words immediately before the match; `user_asked_to_cancel(text) -> bool` returns True if any intent match is not negated (Assumption A4 resolved: accept this stem heuristic with a negation window; false negatives just make the model relay "ask the user"; it is intentionally small and reusable by backlog 999.1). Single-line docstring.

    agent/state.py: `from contextvars import ContextVar` and `current_chat_model: ContextVar[str | None] = ContextVar("current_chat_model", default=None)` with a comment: set per chat turn in ws._handle_chat_message so tool handlers know the chat's model (D-03); no cleanup needed.

    agent/ws.py (surgical, D-02 chat behaviour unchanged): define `SCHEDULER_TOOL_NAMES` and `NON_MEMORY_TOOL_NAMES` next to TASK_TOOL_NAMES; change `_collect_memory_writes` to exclude `NON_MEMORY_TOOL_NAMES` (keep `_collect_task_writes` on TASK_TOOL_NAMES only; RESEARCH Pitfall 10); import `current_chat_model` from agent.state in the existing import block and add `current_chat_model.set(payload.model)` as the first statement of `_handle_chat_message` body (after the docstring).

    Create tests/test_scheduler_tools.py with the <behavior> tests (pure, no DB).
  </action>
  <verify>
    <automated>pytest tests/test_scheduler_tools.py tests/test_tasks.py tests/test_tool_guard.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "current_chat_model.set(payload.model)" agent/ws.py` == 1
    - `grep -c "NON_MEMORY_TOOL_NAMES" agent/ws.py` >= 2
    - `grep -c "def user_asked_to_cancel" agent/tool_guard.py` == 1
    - `pytest tests/test_tasks.py::test_cancel_is_not_an_llm_tool -q` passes (no tool property enum contains "cancelled")
    - `pytest tests/test_scheduler_tools.py tests/test_tasks.py tests/test_tool_guard.py -q` passes (>= 20 new heuristic/validation tests)
  </acceptance_criteria>
  <done>Arg models, heuristic, ContextVar and ws.py exclusions in place; existing tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: schedule_task / list_scheduled_tasks / cancel_scheduled_task handlers + registration + integration tests</name>
  <files>agent/scheduler_tools.py, agent/main.py, tests/test_scheduler_tools.py, tests/test_mcp_tools.py</files>
  <read_first>
    - agent/tools.py (register_tool ~25-47; _transition_task ~323-354 error-dict style; _save_long_term_memory ~278-292)
    - agent/scheduler_ops.py, agent/scheduler.py (build_task_out), agent/schedule.py (as_aware_utc)
    - agent/main.py (import line ~56 `from agent import invariants, mcp_client, mcp_config, memory, profile, tasks`)
    - shared/models.py (Chat.current_leaf_message_id, Message.role/content)
    - tests/test_mcp_tools.py (~333 tool count assertion)
    - tests/test_memory_ws.py (full WS chat turn with respx, done-frame memory_writes assertions)
    - tests/test_cascade_delete.py (chat delete via REST)
  </read_first>
  <behavior>
    - dispatch_tool_calls schedule_task {"schedule_type":"once","delay_seconds":60,"title":"t","prompt":"p"} with current_chat_model set to "m1" -> ok True, a ScheduledTask for that user with model "m1", origin_chat_id = chat id, next_run_at ~ now+60 s; content JSON has next_run_at ending in Z/+00:00 and next_run_at_local
    - schedule_task with current_chat_model unset -> ok False code model_unknown
    - schedule_task cron "* * * * * *" -> ok False code invalid_schedule with the Russian cron message; interval 5 -> invalid_schedule
    - list_scheduled_tasks returns only the caller's active/paused jobs (a cancelled job and another user's job are absent) with last_run_status null when no runs
    - cancel gate: leaf user message "покажи задания" + user_requested_cancellation true -> code cancel_not_requested, job still active; leaf "отмени задание" + flag false -> cancel_not_requested; leaf "отмени задание" + flag true -> {"status":"cancelled"} and job status cancelled with runs kept; foreign/missing task_id -> not_found; already cancelled -> conflict
    - chat_id 0 (headless) -> cancel_not_requested (no chat to read)
    - Chat delete via DELETE /api/v1/chats/{id} keeps the job and sets origin_chat_id NULL
    - Full WS chat turn (respx) where the model calls schedule_task then answers: the done frame's memory_writes contains no entry for schedule_task, a ScheduledTask row exists with model == the payload model
    - len(build_tool_schemas()) == 9 and set(SCHEDULER_TOOL_NAMES) <= set(TOOL_REGISTRY)
  </behavior>
  <action>
    Create agent/scheduler_tools.py (module docstring "LLM tools for creating, listing and cancelling scheduled jobs."; `logger = get_logger(__name__)`), registering three handlers with `@register_tool(name, ArgsModel, description)`:
    - schedule_task description: "Schedule a background job that the agent will run later WITHOUT the user present: once (after delay_seconds or at run_at local time), every interval_seconds, or on a 5-field cron in the machine's local time. prompt is the full instruction to execute at fire time. Use only when the user asks to do something later or periodically." Handler: `model = current_chat_model.get()`; None -> error model_unknown "The chat model is unknown; ask the user to create the job from the scheduler panel."; call `scheduler_ops.create_scheduled_task(session, user_id, title=..., prompt=..., model=model, schedule_type=..., delay_seconds=..., run_at=..., interval_seconds=..., cron=..., max_runs=..., origin_chat_id=chat_id)` (D-12, D-13); ScheduleValidationError -> code invalid_schedule with exc.message; SchedulerConflictError -> code too_many; success dict per <interfaces> with next_run_at via as_aware_utc(...).isoformat() and next_run_at_local via `.astimezone().isoformat(timespec="seconds")`.
    - list_scheduled_tasks description: "List the user's active and paused scheduled jobs (id, title, schedule, next run, last run status)." Handler: statuses {ACTIVE, PAUSED} (D-14); `schedule` is a short English summary ("once at <local iso>", "every N s", "cron <expr>"); last_run_status from build_task_out(...).last_run.
    - cancel_scheduled_task description (gate layer 1, D-14): "Cancel a scheduled job ONLY when the user explicitly asked to cancel/stop/delete it in their LATEST message; never cancel on your own initiative. Requires the explicit numeric task_id (call list_scheduled_tasks first if unknown) - there is no implicit 'current job'. Set user_requested_cancellation=true only in that case." Handler order: (layer 2) `args["user_requested_cancellation"]` must be True; (layer 3, code guard) load `Chat` by chat_id with `chat.user_id == user_id`, then the leaf `Message` by `chat.current_leaf_message_id`; require `message.role == "user"` and `user_asked_to_cancel(message.content)`; any failure (including chat_id 0 or missing chat) -> {"status":"error","code":"cancel_not_requested","error":"The user did not ask to cancel a job in their latest message. Do not cancel; ask the user to confirm explicitly."}; log warning `scheduler_cancel_gate_blocked user_id= task_id=`. Then `scheduler_ops.cancel_task(...)` (soft cancel, history kept); SchedulerNotFoundError -> not_found "scheduled job <id> not found"; SchedulerConflictError -> conflict with exc.message. UI/REST cancel bypasses this gate (user action).
    - Handlers never log prompts or titles.

    agent/main.py: add `from agent import scheduler_tools  # noqa: F401  (registers the scheduler LLM tools)` next to the existing `from agent import ...` line.

    tests/test_mcp_tools.py line ~333: change `== 6` to `== 9` (three new tools) and adjust the assertion message/comment if any.

    Extend tests/test_scheduler_tools.py with every <behavior> bullet: direct `dispatch_tool_calls` calls with a seeded user/chat and seeded user Message as the chat's current leaf; set the ContextVar with `token = current_chat_model.set("m1")` / `current_chat_model.reset(token)`; the WS turn test follows tests/test_memory_ws.py (respx queue: tool_calls schedule_task, then plain answer) and asserts on the done frame.
  </action>
  <verify>
    <automated>pytest tests/test_scheduler_tools.py tests/test_mcp_tools.py tests/test_tasks.py tests/test_mcp_chat_ws.py tests/test_memory_ws.py tests/test_cascade_delete.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@register_tool" agent/scheduler_tools.py` == 3
    - `grep -c "user_asked_to_cancel" agent/scheduler_tools.py` >= 1 and `grep -c "cancel_not_requested" agent/scheduler_tools.py` >= 1
    - `grep -c "origin_chat_id=chat_id" agent/scheduler_tools.py` == 1
    - `grep -c "build_tool_schemas()) == 9" tests/test_mcp_tools.py` == 1
    - `grep -c "scheduler_tools" agent/main.py` == 1
    - Listed test files pass
  </acceptance_criteria>
  <done>The LLM can schedule, list and (only when asked) cancel jobs from chat; collateral tests updated and green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool calls -> scheduler_ops | Model-chosen arguments create and cancel persistent background work |
| user chat text -> cancel gate | The latest user message is the authority for cancellation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-27 | Tampering | cancel_scheduled_task | mitigate | Three layers: description rule, required user_requested_cancellation flag, code guard user_asked_to_cancel on the chat's current leaf user message (with negation window); unit + integration tests |
| T-08-28 | Information disclosure / Tampering (IDOR) | list/cancel handlers | mitigate | user_id from the dispatcher (never from args; dispatcher already ignores scope keys); scheduler_ops ownership -> not_found |
| T-08-29 | Denial of service | schedule_task spam by the model | mitigate | Same server-side validation and per-user cap as REST (shared scheduler_ops), min interval 10 s |
| T-08-30 | Elevation of privilege | jobs spawning jobs | mitigate | Scheduler tools are excluded from the headless allowlist (08-03) |
| T-08-31 | Repudiation | model-created jobs | mitigate | origin_chat_id records provenance; log ids only |
</threat_model>

<verification>
`pytest tests/test_scheduler_tools.py tests/test_mcp_tools.py tests/test_tasks.py tests/test_memory_ws.py tests/test_mcp_chat_ws.py -q` passes.
</verification>

<success_criteria>
- Three tools registered and usable from a chat turn with the chat's model and provenance
- Cancel gate enforced in code, description and arguments
- memory_writes contract and existing schema tests preserved
</success_criteria>

<output>
Create `.planning/phases/08-scheduler-day-18/08-06-SUMMARY.md` when done
</output>
