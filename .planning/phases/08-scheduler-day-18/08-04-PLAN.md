---
phase: 08-scheduler-day-18
plan: 04
type: execute
wave: 2
depends_on: ["08-01", "08-02", "08-03"]
files_modified:
  - agent/scheduler_schemas.py
  - agent/scheduler.py
  - agent/main.py
  - tests/test_scheduler_service.py
  - tests/test_scheduler_lifespan.py
autonomous: true
requirements: [SCHED-03, SCHED-04, SCHED-05, SCHED-06, SCHED-07, SCHED-08, SCHED-09, SCHED-14]

must_haves:
  truths:
    - "A due active job is claimed exactly once per slot: UPDATE scheduledtask ... WHERE id=? AND status='active' AND next_run_at=<old> with rowcount==1, and the TaskRun insert happens in the same commit; a concurrent second claim of the same slot gets no run"
    - "After downtime an overdue job produces exactly ONE run flagged is_late and next_run_at moves to the next FUTURE slot (D-08)"
    - "A slot arriving while the job's previous run is still running is recorded as a skipped run with a Russian reason and does not count toward max_runs (D-09)"
    - "A once job completes after its run; a periodic job with max_runs completes after N counted runs (D-11); a failed run is recorded and the next slot fires normally (D-10)"
    - "On startup every TaskRun left 'running' is marked failed 'Прервано: Agent был перезапущен' and exhausted jobs are finalized (completed)"
    - "Each run executes run_headless_turn under a concurrency semaphore and an overall asyncio.timeout(SCHEDULER_RUN_TIMEOUT); a timeout ends the run failed with 'Превышено время выполнения (120 с)' (D-05)"
    - "run_started / run_finished events are published through the hub only for the job owner; all timestamps in frames are UTC-aware ISO"
    - "The poll loop starts in the agent lifespan only when SCHEDULER_ENABLED is true and is stopped (loop + in-flight runs) before MCP cleanup and engine.dispose()"
  artifacts:
    - path: "agent/scheduler_schemas.py"
      provides: "RunSummary, RunDetail, ScheduledTaskOut, ScheduledTaskCreate, mappers and WS frame builders"
      exports: ["RunSummary", "RunDetail", "ScheduledTaskOut", "ScheduledTaskCreate", "run_to_summary", "run_to_detail", "task_to_out", "run_started_frame", "run_finished_frame", "task_updated_frame", "task_deleted_frame"]
    - path: "agent/scheduler.py"
      provides: "SchedulerService (tick, claim_slot, recover_orphaned_runs, finalize, execute_run, start_manual_run, abort_task_runs, start, stop) + module singleton scheduler + build_task_out"
      contains: "class SchedulerService"
    - path: "agent/main.py"
      provides: "lifespan wiring"
      contains: "recover_orphaned_runs"
    - path: "tests/test_scheduler_service.py"
      provides: "claim / catch-up / overlap / max_runs / recovery / execute tests"
      min_lines: 200
    - path: "tests/test_scheduler_lifespan.py"
      provides: "loop start gating + stop ordering tests"
      min_lines: 30
  key_links:
    - from: "agent/scheduler.py::claim_slot"
      to: "scheduledtask optimistic guard"
      via: "session.exec(update(ScheduledTask).where(... ScheduledTask.next_run_at == old_next)) and result.rowcount"
      pattern: "rowcount"
    - from: "agent/scheduler.py::execute_run"
      to: "agent/headless.py::run_headless_turn"
      via: "async with asyncio.timeout(settings.SCHEDULER_RUN_TIMEOUT)"
      pattern: "asyncio\\.timeout"
    - from: "agent/scheduler.py"
      to: "agent/events.py::hub.publish"
      via: "run_started / run_finished frames keyed by task.user_id"
      pattern: "hub\\.publish\\("
    - from: "agent/main.py::lifespan"
      to: "agent/scheduler.py::scheduler"
      via: "recover_orphaned_runs() always; start() if app_config.SCHEDULER_ENABLED; stop() before cleanup_all_sessions/engine.dispose"
      pattern: "scheduler\\.(start|stop|recover_orphaned_runs)"
---

<objective>
Build the scheduler engine inside the Agent process: response/frame schemas, the atomic slot claim, the poll loop, startup recovery (orphaned runs + finalize), the run executor (semaphore + overall timeout around the headless runner), manual "run now", abort-for-delete, and lifespan wiring.

Purpose: this is the heart of SCHED-03/04/07/08 — jobs fire reliably exactly once per slot, survive restarts, never overlap, and every outcome lands on a TaskRun row and in a live event.
Output: `agent/scheduler_schemas.py`, `agent/scheduler.py`, lifespan edit in `agent/main.py`, two test files.
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-scheduler-day-18/08-CONTEXT.md
@.planning/phases/08-scheduler-day-18/08-RESEARCH.md
@.planning/phases/08-scheduler-day-18/08-PATTERNS.md
@.planning/phases/08-scheduler-day-18/08-01-SUMMARY.md
@.planning/phases/08-scheduler-day-18/08-02-SUMMARY.md
@.planning/phases/08-scheduler-day-18/08-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- From 08-01: shared/models.py ScheduledTask/TaskRun + enums (ScheduleType, ScheduledTaskStatus, RunStatus, RunTrigger);
     shared/config.py SCHEDULER_*; agent/schedule.py as_aware_utc, next_run_after_claim, ScheduleSpec ... -->
<!-- From 08-02: agent/events.py hub.publish(user_id, frame) (sync, non-blocking) -->
<!-- From 08-03: agent/headless.py run_headless_turn(session, user_id, prompt, model) -> HeadlessResult(text, results, tool_trace, mcp_tool_count); HeadlessRunError(.message) -->

<!-- Contracts this plan CREATES (08-05 ops/REST, 08-06 tools, 08-07 UI consume) -->
agent/scheduler_schemas.py (Pydantic v2; every datetime passed through as_aware_utc so JSON ends in "Z"/"+00:00"):
- RunSummary: id: int; task_id: int; status: str; trigger: str; scheduled_for: datetime|None; started_at: datetime;
  finished_at: datetime|None; duration_ms: int|None (None while running/skipped-without-finish); is_late: bool; model: str
- RunDetail(RunSummary): task_title: str; result_text: str|None; error: str|None; tool_trace: list[dict[str, Any]] (parsed JSON, [] when None/invalid)
- ScheduledTaskOut: id; title; prompt; model; schedule_type: str; run_at: datetime|None; interval_seconds: int|None; cron: str|None;
  max_runs: int|None; run_count: int; next_run_at: datetime|None; status: str; origin_chat_id: int|None;
  created_at; updated_at; last_run: RunSummary|None; is_running: bool
- ScheduledTaskCreate (REST body; types only, semantic validation is Russian and server-side in 08-05):
  title: str = ""; prompt: str = ""; model: str = ""; schedule_type: str = "once"; delay_seconds: int|None = None;
  run_at: str|None = None; interval_seconds: int|None = None; cron: str|None = None; max_runs: int|None = None
- run_to_summary(run: TaskRun) -> RunSummary; run_to_detail(run: TaskRun, task_title: str) -> RunDetail
- task_to_out(task: ScheduledTask, last_run: TaskRun | None, is_running: bool) -> ScheduledTaskOut
- run_started_frame(run) / run_finished_frame(run, task_out) / task_updated_frame(task_out) / task_deleted_frame(task_id) -> dict (model_dump(mode="json"))

agent/scheduler.py:
- MSG_SKIPPED_OVERLAP = "Предыдущий запуск ещё выполнялся"; MSG_INTERRUPTED_RESTART = "Прервано: Agent был перезапущен";
  MSG_INTERRUPTED_STOP = "Прервано: Agent остановлен"; MSG_ABORTED_DELETE = "Прервано: задание удалено";
  msg_timeout(seconds) -> f"Превышено время выполнения ({int(seconds)} с)"; msg_unexpected(exc) -> f"Непредвиденная ошибка ({type(exc).__name__})"
- class RunAlreadyActiveError(Exception)
- async build_task_out(session, task: ScheduledTask) -> ScheduledTaskOut      # loads newest run + running flag
- class SchedulerService:
    async tick(now: datetime | None = None, *, spawn: bool = True) -> list[int]          # returns ids of RUNNING runs created
    async claim_slot(session, task: ScheduledTask, now: datetime) -> TaskRun | None
    async recover_orphaned_runs(now: datetime | None = None) -> int                    # number of runs marked failed
    async finalize_task(session, task_id: int) -> bool                                 # True if it became completed
    async execute_run(run_id: int) -> None
    async start_manual_run(session, task: ScheduledTask, now: datetime | None = None) -> TaskRun   # raises RunAlreadyActiveError
    spawn_run(run_id: int) -> None                                                      # create_task(execute_run) with strong ref; tests monkeypatch it
    async abort_task_runs(task_id: int) -> None                                         # cancel + await in-flight run tasks of that job
    async start() -> None; async stop() -> None; is_running_loop: bool (property)
- scheduler: SchedulerService (module singleton)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schemas/mappers + atomic claim, tick, finalize and startup recovery</name>
  <files>agent/scheduler_schemas.py, agent/scheduler.py, tests/test_scheduler_service.py</files>
  <read_first>
    - shared/models.py (ScheduledTask, TaskRun, enums from 08-01)
    - agent/schedule.py (as_aware_utc, next_run_after_claim from 08-01)
    - agent/events.py (hub from 08-02)
    - agent/mcp_config.py (DB service shape and commit/rollback pattern ~118-125; select style ~66-85)
    - agent/schemas.py (TaskResponse ~482-494 and McpServerResponse for response-model style)
    - shared/database.py (async_session_factory ~27-31 expire_on_commit=False; retry_on_locked_db ~44-65)
    - .planning/phases/08-scheduler-day-18/08-RESEARCH.md Patterns 1-3 (claim code, finalize invariant, recovery)
    - tests/test_cascade_delete.py (direct DB seeding style)
  </read_first>
  <behavior>
    - once job due (next_run_at <= now): tick(now, spawn=False) creates one RUNNING run with trigger schedule, scheduled_for = old next_run_at, run_count 1, next_run_at NULL; a second tick creates nothing
    - interval 60 s job with next_run_at = now-10 s: one run, next_run_at = old+60 s, is_late False
    - Catch-up: interval 60 s job with next_run_at = now-2 h -> exactly one run with is_late True and next_run_at in (now, now+60 s]; cron "*/5 * * * *" job overdue by a day -> one late run, next_run_at = next 5-minute boundary after now
    - Concurrent claim: two sessions load the same task object, both call claim_slot(now) -> exactly one TaskRun exists and exactly one call returned a run
    - Overlap: job with an existing RUNNING run becomes due -> a SKIPPED run with error MSG_SKIPPED_OVERLAP, finished_at set, run_count unchanged, next_run_at advanced, and a run_finished frame with status "skipped" is published to the owner
    - Status guard: a paused or cancelled job with past next_run_at is never claimed
    - max_runs=2 interval job: after two counted claims next_run_at is NULL; once the running run is finished (set SUCCESS) finalize_task marks the job completed; skipped runs do not count
    - finalize_task never completes a job that still has a RUNNING run, and never touches a paused job with non-null next_run_at
    - recover_orphaned_runs marks every RUNNING run failed with MSG_INTERRUPTED_RESTART and finished_at, then finalizes exhausted jobs (a once job claimed before the "crash" becomes completed); returns the count
    - Every TaskOut/RunSummary JSON datetime ends with "Z" or "+00:00" (model_dump_json check)
    - tick publishes run_started to the owner only (subscribe two users' queues on a fresh hub via monkeypatch of agent.scheduler.hub)
  </behavior>
  <action>
    agent/scheduler_schemas.py (new; module docstring "Pydantic models, mappers and WS frames for the scheduler."): implement everything listed under scheduler_schemas in <interfaces>. Mappers convert every datetime with `as_aware_utc` (from agent/schedule.py) so the browser never gets naive strings (RESEARCH Pitfall 6); `duration_ms = int((finished_at - started_at).total_seconds() * 1000)` when finished_at is set; `ScheduledTaskOut.cron` maps from `task.cron_expr`; enum values are emitted as `.value` strings; `RunDetail.tool_trace` parses `run.tool_trace` with json.loads inside try/except json.JSONDecodeError -> []. Frame builders return `{"type": ..., ...}` dicts built with `.model_dump(mode="json")` (the RunSummary inside frames never carries result_text).

    agent/scheduler.py (new; module docstring "Background job scheduler: poll loop, atomic claim, recovery and run execution."; `logger = get_logger(__name__)`): implement `build_task_out`, the message constants, `RunAlreadyActiveError` and the Task-1 half of SchedulerService:
    - `__init__`: `self._semaphore = asyncio.Semaphore(settings.SCHEDULER_MAX_CONCURRENT_RUNS)` (Assumption A8 resolved: cap 2, single-GPU LM Studio), `self._runs: dict[int, asyncio.Task[None]] = {}` (run_id -> task, strong refs), `self._run_task_ids: dict[int, int] = {}` (run_id -> scheduled_task_id), `self._cancel_reason: dict[int, str] = {}` (run_id -> Russian reason used when a run task is cancelled), `self._loop_task: asyncio.Task[None] | None = None`.
    - `claim_slot(session, task, now)`: exactly RESEARCH Pattern 1. overlap = a RUNNING TaskRun exists for task.id; counts = not overlap; new_next = `next_run_after_claim(task.schedule_type, old_next=task.next_run_at, now=now, interval_seconds=..., cron_expr=...)`, then if counts and task.max_runs is not None and task.run_count + 1 >= task.max_runs -> new_next = None; `result = await session.exec(update(ScheduledTask).where(ScheduledTask.id == task.id, ScheduledTask.status == ScheduledTaskStatus.ACTIVE, ScheduledTask.next_run_at == task.next_run_at).values(next_run_at=new_next, run_count=ScheduledTask.run_count + (1 if counts else 0), updated_at=now))`; `if result.rowcount != 1: await session.rollback(); return None`. Insert TaskRun(scheduled_task_id, user_id=task.user_id, trigger=SCHEDULE, status=SKIPPED if overlap else RUNNING, scheduled_for=old_next, started_at=now, finished_at=now if overlap else None, is_late=(now - as_aware_utc(old_next)).total_seconds() > settings.SCHEDULER_LATE_THRESHOLD_SECONDS, error=MSG_SKIPPED_OVERLAP if overlap else None, model=task.model); commit; on `IntegrityError` (partial unique index vs a racing manual run) rollback and return None — the caller (tick) then retries the task once in a fresh session, where the overlap check now yields a SKIPPED run. On any other Exception rollback and raise.
    - `tick(now=None, *, spawn=True)`: now defaults to datetime.now(timezone.utc); in a short session select up to 20 ids of `ScheduledTask.status == ACTIVE, ScheduledTask.next_run_at.is_not(None), ScheduledTask.next_run_at <= now` ordered by next_run_at; for each id open a fresh session, load the task, claim (retry once on None-due-to-IntegrityError as described; distinguish via a small private flag or by re-checking whether next_run_at still equals old), then: RUNNING run -> publish run_started_frame to task.user_id and `if spawn: self.spawn_run(run.id)`; SKIPPED run -> finalize_task, publish run_finished_frame(run, await build_task_out(...)). Log info `scheduler_run_claimed task_id= run_id= user_id= status= is_late=` (no prompt). Return RUNNING run ids.
    - `finalize_task(session, task_id)`: `UPDATE scheduledtask SET status='completed', updated_at=now WHERE id=:id AND status IN ('active','paused') AND next_run_at IS NULL AND NOT EXISTS (SELECT 1 FROM taskrun WHERE scheduled_task_id=:id AND status='running')` via SQLAlchemy `update(...).where(..., ScheduledTask.next_run_at.is_(None), ~exists().where(...))`; commit; return rowcount == 1 (single rule covers once completion, max_runs completion and crash-between-claim-and-finish). Paused jobs keep a non-null next_run_at (08-05 pause does not clear it), so they are never completed by this rule.
    - `recover_orphaned_runs(now=None)`: one session: UPDATE all RUNNING runs -> FAILED, finished_at=now, error=MSG_INTERRUPTED_RESTART; commit; then finalize every job that is active/paused with next_run_at NULL; log info `scheduler_recovered_orphans count=`; return count. Catch-up itself needs no special code: overdue next_run_at rows are handled by the first tick (D-08).
    - Use `.is_(None)`/`.is_not(None)` in where clauses, `datetime.now(timezone.utc)`, commit/rollback on every write.

    tests/test_scheduler_service.py (new): implement all <behavior> bullets with injected `now` (never sleep); seed users and ScheduledTask rows directly; call `scheduler.tick(now, spawn=False)` on a fresh `SchedulerService()` instance per test.
  </action>
  <verify>
    <automated>pytest tests/test_scheduler_service.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "rowcount" agent/scheduler.py` >= 1 and `grep -c "ScheduledTask.next_run_at == " agent/scheduler.py` >= 1
    - `grep -c "== None" agent/scheduler.py` == 0 and `grep -c "utcnow" agent/scheduler.py` == 0
    - `grep -c "except IntegrityError" agent/scheduler.py` >= 1
    - `pytest tests/test_scheduler_service.py -q` passes with >= 12 tests covering the concurrent-claim, catch-up, overlap, max_runs and recovery cases
  </acceptance_criteria>
  <done>Claim, tick, finalize and recovery behave per D-08/D-09/D-10/D-11 and are proven by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run executor (semaphore + timeout), run-now, abort, loop start/stop and lifespan wiring</name>
  <files>agent/scheduler.py, agent/main.py, tests/test_scheduler_service.py, tests/test_scheduler_lifespan.py</files>
  <read_first>
    - agent/scheduler.py (Task 1 output)
    - agent/headless.py (run_headless_turn, HeadlessRunError from 08-03)
    - agent/main.py (imports ~1-70 incl. `settings as app_config`; lifespan ~385-393)
    - ui/supervisor.py (~97-124: single worker, terminate() = hard kill on Windows -> why recovery runs at startup)
    - tests/test_memory_ws.py (a `with TestClient(app)` lifespan test shape)
  </read_first>
  <behavior>
    - execute_run with run_headless_turn monkeypatched to return HeadlessResult(text="ok", results=[], tool_trace=None, mcp_tool_count=0): run becomes SUCCESS with result_text "ok", finished_at set; run_finished frame published to owner with status success and task snapshot
    - HeadlessRunError("Модель недоступна: LM Studio не запущен") -> run FAILED with exactly that error; job stays active and its next slot still fires on the next tick (no retry, D-10)
    - monkeypatch settings.SCHEDULER_RUN_TIMEOUT = 0.05 and a fake runner that awaits asyncio.sleep(1) -> run FAILED with msg_timeout(0.05) text; for the default setting the message reads "Превышено время выполнения (120 с)"
    - Unexpected RuntimeError in the runner -> FAILED "Непредвиденная ошибка (RuntimeError)"
    - execute_run for a run id whose row was deleted returns quietly (no exception)
    - once job: after execute_run finishes, the job is completed and a task snapshot with status completed is in the run_finished frame
    - start_manual_run on an active interval job inserts a MANUAL RUNNING run, increments run_count, leaves next_run_at unchanged; on a once job it sets next_run_at NULL (job completes after the run); a second start_manual_run while running raises RunAlreadyActiveError (partial index)
    - abort_task_runs(task_id) cancels an in-flight execute_run task and awaits it; the run is not left RUNNING if its row still exists (marked FAILED MSG_ABORTED_DELETE)
    - Lifespan: with SCHEDULER_ENABLED false (conftest default) `with TestClient(app)` leaves scheduler.is_running_loop False; with monkeypatch settings.SCHEDULER_ENABLED True the loop runs inside the context and is stopped after exit; a RUNNING run inserted before startup is FAILED after startup in both cases
  </behavior>
  <action>
    agent/scheduler.py — complete SchedulerService:
    - `spawn_run(run_id)`: `task = asyncio.create_task(self.execute_run(run_id), name=f"scheduler-run-{run_id}")`; store in `self._runs[run_id]`; `add_done_callback` pops it (strong references, RESEARCH Pattern 2).
    - `execute_run(run_id)`: load run + task in a short session (missing -> return); `async with self._semaphore:` (acquired BEFORE the timeout so queueing does not eat the 120 s budget) then `async with asyncio.timeout(settings.SCHEDULER_RUN_TIMEOUT):` open a dedicated `async_session_factory()` session and `await run_headless_turn(session, task.user_id, task.prompt, task.model)` (model stored on the job, D-03). Outcome mapping: HeadlessResult -> SUCCESS(result_text=text, tool_trace=tool_trace); HeadlessRunError -> FAILED(error=exc.message); TimeoutError -> FAILED(msg_timeout(settings.SCHEDULER_RUN_TIMEOUT)); asyncio.CancelledError -> best-effort `_finish_run(... FAILED, error=self._cancel_reason.get(run_id, MSG_INTERRUPTED_STOP))` then re-raise; other Exception -> FAILED(msg_unexpected(exc)) and log error `scheduler_run_failed run_id= error=type(exc).__name__`. When the result has mcp_tool_count == 0 log info `scheduler_run_no_mcp_tools run_id=` (RESEARCH Pitfall 8 / Open Question 1 resolved: MCP retry behaviour is unchanged; the count is logged for diagnosis).
    - `_finish_run(run_id, status, *, result_text=None, error=None, tool_trace=None)`: short session; `session.get(TaskRun, run_id)` None -> return (job deleted, Pitfall 9); set fields + finished_at; commit; `finalize_task`; publish `run_finished_frame(run, await build_task_out(session, task))` to task.user_id; log info `scheduler_run_finished run_id= task_id= status= duration_ms=`.
    - `start_manual_run(session, task, now=None)` (Assumption A5 resolved: "Run now" counts toward run_count/max_runs, does not move a periodic job's next_run_at, and exhausts a once job; allowed for active and paused jobs — ownership/status checks live in 08-05): insert TaskRun(trigger=MANUAL, status=RUNNING, scheduled_for=None, started_at=now, is_late=False, model=task.model) and in the same commit set run_count += 1, and next_run_at = None when schedule_type is ONCE or (max_runs is set and run_count+1 >= max_runs); on IntegrityError rollback and raise RunAlreadyActiveError. After commit publish run_started_frame and `self.spawn_run(run.id)`; return the run.
    - `abort_task_runs(task_id)`: for run ids mapped to task_id set `self._cancel_reason[run_id] = MSG_ABORTED_DELETE`, cancel their asyncio tasks and `await asyncio.gather(*tasks, return_exceptions=True)`.
    - `start()`: if already running return; `self._loop_task = asyncio.create_task(self._run_loop(), name="scheduler-loop")`; `_run_loop`: `while True: try: await self.tick() except asyncio.CancelledError: raise except Exception as exc: logger.error("scheduler_tick_failed", error=type(exc).__name__)` then `await asyncio.sleep(settings.SCHEDULER_POLL_INTERVAL)` (1.0 s default: one indexed SELECT per second). `stop()`: cancel + gather the loop task, then cancel + gather every in-flight run task (their CancelledError handler records MSG_INTERRUPTED_STOP best-effort; startup recovery covers hard kills). `is_running_loop` property = loop task exists and not done.
    - Module singleton `scheduler = SchedulerService()`. NOTE: the Semaphore is created in __init__ at import time; on Python 3.10+ asyncio primitives bind to the running loop lazily, which is fine for the per-test TestClient loops.

    agent/main.py lifespan (keep docstring accurate): after `await init_db()` call `await scheduler.recover_orphaned_runs()` (always — the supervisor hard-kills the Agent so shutdown hooks may never run, RESEARCH Pitfall 2) and `if app_config.SCHEDULER_ENABLED: await scheduler.start()`; on shutdown call `await scheduler.stop()` BEFORE `mcp_client.cleanup_all_sessions()` and `engine.dispose()`. Import `from agent.scheduler import scheduler`.

    Extend tests/test_scheduler_service.py with the executor / manual-run / abort bullets (monkeypatch `agent.scheduler.run_headless_turn`; subscribe a queue on a fresh EventHub monkeypatched into `agent.scheduler.hub`). Create tests/test_scheduler_lifespan.py for the lifespan bullets (`with TestClient(app):`; insert the orphan RUNNING run via `client.portal.call` before entering, or via a direct async insert before constructing the client; monkeypatch `shared.config.settings.SCHEDULER_ENABLED` / the `app_config` object used in agent.main).
  </action>
  <verify>
    <automated>pytest tests/test_scheduler_service.py tests/test_scheduler_lifespan.py tests/test_memory_ws.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "asyncio.timeout(settings.SCHEDULER_RUN_TIMEOUT)" agent/scheduler.py` == 1
    - `grep -n "async with self._semaphore" agent/scheduler.py` line number is smaller than the `asyncio.timeout(` line number
    - agent/main.py lifespan: `grep -n "scheduler.stop()\|cleanup_all_sessions()\|engine.dispose()" agent/main.py` shows scheduler.stop() first; `grep -c "recover_orphaned_runs" agent/main.py` == 1; `grep -c "SCHEDULER_ENABLED" agent/main.py` == 1
    - `grep -c "except asyncio.CancelledError" agent/scheduler.py` >= 2
    - `pytest tests/test_scheduler_service.py tests/test_scheduler_lifespan.py -q` passes (>= 22 tests total) and `pytest tests/test_memory_ws.py -q` still passes
  </acceptance_criteria>
  <done>Runs execute with timeout and semaphore, results persist on TaskRun with live events; the loop is lifespan-managed and gated; restart recovery is automatic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DB rows -> background executor | Jobs authored earlier (by user or LLM) are executed later with no user present |
| engine -> hub | Run data is pushed to browsers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-15 | Tampering / Repudiation | claim_slot | mitigate | Optimistic UPDATE ... WHERE next_run_at = old with rowcount check + insert in one commit; partial unique index backstop with IntegrityError handling |
| T-08-16 | Denial of service | executor | mitigate | Semaphore (SCHEDULER_MAX_CONCURRENT_RUNS=2), asyncio.timeout(SCHEDULER_RUN_TIMEOUT), overlap skip, tick batch limit 20, loop survives exceptions |
| T-08-17 | Information disclosure | run_started/run_finished frames | mitigate | Published with hub.publish(task.user_id, ...) only; frames exclude result_text; test with two subscribed users |
| T-08-18 | Integrity | crash leaves run RUNNING forever (would block the job via the partial index) | mitigate | recover_orphaned_runs at every startup before the loop; finalize invariant |
| T-08-19 | Information disclosure | logs | mitigate | Log ids, status, durations, exception type names only; never prompt/result/tool args |
| T-08-20 | Integrity | delete during in-flight run | mitigate | abort_task_runs cancels + awaits; _finish_run tolerates a missing row |
</threat_model>

<verification>
`pytest tests/test_scheduler_service.py tests/test_scheduler_lifespan.py tests/test_scheduler_runner.py tests/test_scheduler_events.py -q` passes.
</verification>

<success_criteria>
- Exactly-once claim per slot, one compensating late run after downtime, overlap -> skipped, max_runs/once completion, no retries
- Orphaned runs recovered at startup; loop gated by SCHEDULER_ENABLED and stopped before engine.dispose()
- Every run outcome persisted on TaskRun and published to the owner only
</success_criteria>

<output>
Create `.planning/phases/08-scheduler-day-18/08-04-SUMMARY.md` when done
</output>
