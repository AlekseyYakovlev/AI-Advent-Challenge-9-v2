---
phase: 08-scheduler-day-18
plan: 09
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - agent/scheduler_ops.py
  - agent/scheduler.py
  - agent/schedule.py
  - agent/schemas.py
  - agent/scheduler_schemas.py
  - agent/tools.py
  - tests/test_scheduler_service.py
  - tests/test_scheduler_api.py
  - tests/test_scheduler_schedule.py
  - tests/test_scheduler_tools.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Pause then resume of a job whose final run is in flight (next_run_at is NULL) never re-arms it: a once job runs exactly once and a max_runs=N job never runs N+1 times (CR-01)"
    - "start_manual_run refuses a cancelled/completed job inside the same guarded UPDATE (rowcount != 1 -> SchedulerConflictError) and inserts the run only after that update succeeds (WR-08)"
    - "Right after the claim commit the run task is spawned before any further await; a failure while announcing a claimed run never leaves a RUNNING row without a task, and one failing job never aborts the rest of the tick (WR-01)"
    - "delete_task takes the job out of scheduling (status CANCELLED, next_run_at NULL, committed) before aborting its runs, so the poll loop cannot claim a new slot of a job that is being deleted (WR-02)"
    - "Out-of-range delay_seconds / interval_seconds / max_runs / run_at are rejected with a validation error (REST 422, tool ok=False with invalid_schedule), never an unhandled 500 and never an exception that kills a chat WebSocket turn (WR-03)"
---

<objective>
Close the gaps the phase 08 code review (08-REVIEW.md) found and the user chose to fix now: CR-01 plus WR-01, WR-02, WR-03 and WR-08. All other review findings (WR-04..WR-07, IN-01..IN-07) are parked to backlog and must NOT be touched here.
</objective>

<context>
@.planning/phases/08-scheduler-day-18/08-REVIEW.md (exact locations, failure scenarios and suggested fixes for the five findings)
@.planning/phases/08-scheduler-day-18/08-VERIFICATION.md
@.planning/phases/08-scheduler-day-18/08-04-SUMMARY.md, 08-05-SUMMARY.md, 08-06-SUMMARY.md (semantics of claim_slot, start_manual_run, ops, tools)
@./CLAUDE.md (conventions: async, structlog, commit/rollback pattern, no bare except, type hints, no print)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CR-01 pause/resume re-arm and WR-08 unguarded manual run</name>
  <files>agent/scheduler_ops.py, agent/scheduler.py, tests/test_scheduler_service.py, tests/test_scheduler_api.py</files>
  <action>
    CR-01: a job with next_run_at IS NULL while status is still ACTIVE/PAUSED has its final run in flight; finalize_task completes it when the run ends. Make resume_task raise SchedulerConflictError (reuse an existing "already finished" style Russian message constant, or add one next to the others) when task.next_run_at is None, and make pause_task reject the same state so the UI cannot pause a job whose final run is in flight (the API returns 409 with the Russian detail; check how the UI surfaces 409 toasts and keep it consistent). Do not change finalize_task.
    WR-08: in start_manual_run add ScheduledTask.status.in_((ACTIVE, PAUSED)) to the guarded UPDATE's WHERE, treat rowcount != 1 as a conflict (rollback, raise SchedulerConflictError with the existing already-finished message), and insert the TaskRun only after the guarded update succeeded. Compute the max_runs exhaustion inside/against the updated row, not from the stale ORM value where practical.
    Tests (add to existing files, do not weaken existing tests): (a) once job: claim a slot, pause -> 409/conflict, resume -> 409/conflict, tick -> still exactly one run and the job ends COMPLETED after the run finishes; (b) interval job with max_runs=1: same, no run number 2 ever; (c) start_manual_run on a job cancelled between the caller's status check and the update raises SchedulerConflictError and creates no run; (d) REST: pause/resume of such a job returns 409.
  </action>
  <verify><automated>python -m pytest tests/test_scheduler_service.py tests/test_scheduler_api.py -q -p no:warnings</automated></verify>
  <done>CR-01 and WR-08 cannot be reproduced; new tests fail on the old code and pass on the new.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-01 spawn-before-await and per-job tick isolation; WR-02 delete race</name>
  <files>agent/scheduler.py, agent/scheduler_ops.py, tests/test_scheduler_service.py, tests/test_scheduler_api.py</files>
  <action>
    WR-01: in _claim_and_announce and start_manual_run, call spawn_run(run.id) (and record _run_task_ids) immediately after the claim/insert commit, before refresh/build_task_out/publish; the post-commit announce section must not be able to strand a RUNNING row. In tick, isolate per-job failures: wrap each _claim_and_announce in try/except Exception (log scheduler_claim_failed with task_id and error=type(exc).__name__, never a traceback), let asyncio.CancelledError propagate, and continue with the next due job.
    WR-02: in delete_task, first take the job out of scheduling with one guarded UPDATE (status=CANCELLED, next_run_at=NULL, updated_at) and commit, then await scheduler.abort_task_runs(task_id), then delete the row (cascade). Optionally call abort_task_runs once more after the delete for a run spawned in between. Keep the 404-for-foreign-id behaviour and the task_deleted event.
    Tests: (a) monkeypatch the announce step (build_task_out or publish) to raise after the claim commit: the run is still executed/finished (not stuck RUNNING) and a later tick can claim the next slot; (b) tick with two due jobs where the first job's announce raises: the second job is still claimed; (c) delete a job while a run is in flight and while the poll loop is ticking: no run is executing for the deleted job afterwards and no run_started frame follows task_deleted; use tick(now) directly like the other tests, no real loop timing.
  </action>
  <verify><automated>python -m pytest tests/test_scheduler_service.py tests/test_scheduler_api.py tests/test_scheduler_lifespan.py -q -p no:warnings</automated></verify>
  <done>No RUNNING row can be stranded after a failed announce; one failing job does not block the tick; delete cannot race the poll loop.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: WR-03 unbounded schedule inputs</name>
  <files>agent/schedule.py, agent/schemas.py, agent/scheduler_schemas.py, agent/tools.py, tests/test_scheduler_schedule.py, tests/test_scheduler_api.py, tests/test_scheduler_tools.py</files>
  <action>
    Add upper bounds (le=) to ScheduleTaskArgs and ScheduledTaskCreate numeric fields (delay_seconds and interval_seconds <= 10 years in seconds, max_runs <= 1_000_000) as named constants; keep existing lower bounds and messages. In agent/schedule.py convert OverflowError/OSError/ValueError from timedelta addition, astimezone and datetime construction into ScheduleValidationError(MSG_BAD_SCHEDULE) (parse_run_at, _to_local_naive/_from_local_naive, next_* helpers as applicable), so a far-out run_at like 0001-01-01 or 9999-12-31T23:59:59 is a validation error. In agent/tools.py dispatch_tool_calls wrap the handler call so an unexpected handler exception becomes an ok=False tool result (log tool_handler_failed with tool name and error=type(exc).__name__) instead of propagating out of the chat turn; asyncio.CancelledError must still propagate. Do not change behaviour for valid input.
    Tests: schedule unit tests for delay/interval/max_runs/run_at extremes (ScheduleValidationError); REST create with those values -> 422 with a Russian string detail (never 500); schedule_task tool called with out-of-range values returns ok=False code invalid_schedule and a full WebSocket chat turn with such a tool call still completes and replies (respx-mocked stream like the existing tool tests); a dispatch_tool_calls test where a registered handler raises returns ok=False without raising.
  </action>
  <verify><automated>python -m pytest tests/test_scheduler_schedule.py tests/test_scheduler_api.py tests/test_scheduler_tools.py tests/test_mcp_tools.py -q -p no:warnings</automated></verify>
  <done>No out-of-range input produces a 500 or kills a chat turn.</done>
</task>

</tasks>

<verification>
Run the full suite `python -m pytest tests/ -q` (baseline 881 passed) — it must stay green with the new tests added. Loop the scheduler test files a few times to make sure none of the new tests is flaky (the repo has had Windows aiosqlite teardown flakes: never stop the scheduler service while a run is still finishing; wait for `svc._runs` to drain first).
</verification>

<success_criteria>
- CR-01, WR-01, WR-02, WR-03, WR-08 fixed with tests that fail on the old code
- Full suite green, no new flaky test
- No changes for WR-04..WR-07 and INFO items
</success_criteria>
