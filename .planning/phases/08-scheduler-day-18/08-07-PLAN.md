---
phase: 08-scheduler-day-18
plan: 07
type: execute
wave: 4
depends_on: ["08-02", "08-05"]
files_modified:
  - ui/static/index.html
  - ui/static/app.js
  - tests/test_static_js_syntax.py
autonomous: true
requirements: [SCHED-12, SCHED-13]

must_haves:
  truths:
    - "The sidebar shows a foldable 'Расписание' panel (collapsed by default) with the active+paused job count, a running badge, a '+ Новое задание' button and job cards with Russian status chips (D-15)"
    - "Clicking a job card expands its run history (newest first, max 20); clicking a run opens a modal with the Markdown result rendered through renderMarkdown (Marked + DOMPurify), the error box for failed runs, a collapsible tool trace and the skip reason for skipped runs (D-15)"
    - "The create modal submits title, prompt, schedule type (once: delay or local datetime; interval: number + unit; cron), model and optional max_runs to POST /api/v1/scheduler/tasks and shows server Russian validation errors inline (D-16)"
    - "Pause / Продолжить / Запустить сейчас / Отменить / Удалить call the REST endpoints, confirm destructive actions with native confirm(), disable while in flight, re-fetch the list and toast (D-16)"
    - "A /ws/events socket opened after page load applies run_started / run_finished / task_updated / task_deleted to the panel live, toasts on finished success/failed, re-syncs over REST on every (re)connect, reconnects with backoff (not after close code 1008), and polls every 10 s only while disconnected and the panel is expanded, with at most one poll timer alive at any time (D-17)"
    - "While a run is in progress the card shows the fresh next_run_at / run_count from the run_started frame's task snapshot"
    - "ui/static/app.js passes a Node-free bracket/string/template balance check (pytest tests/test_static_js_syntax.py), so a structural JS syntax error is caught before the manual checkpoint"
    - "No server-provided text is inserted with innerHTML except the result body through renderMarkdown()"
  artifacts:
    - path: "ui/static/index.html"
      provides: "#scheduler-panel, #scheduler-create-modal, #scheduler-run-modal markup"
      contains: "scheduler-panel"
    - path: "ui/static/app.js"
      provides: "scheduler state, rendering, actions, modals, /ws/events client"
      contains: "renderSchedulerPanel"
    - path: "tests/test_static_js_syntax.py"
      provides: "Python (no Node) balance check of ui/static/*.js: brackets, strings, template literals with ${} nesting, comments, regex literals"
      contains: "def check_js_balance"
  key_links:
    - from: "ui/static/app.js"
      to: "/api/v1/scheduler/tasks"
      via: "apiFetch in loadSchedulerTasks and actions"
      pattern: "/api/v1/scheduler/"
    - from: "ui/static/app.js"
      to: "/ws/events"
      via: "new WebSocket(`${WS_BASE}/ws/events`)"
      pattern: "/ws/events"
    - from: "ui/static/app.js run modal"
      to: "renderMarkdown"
      via: "result body innerHTML = renderMarkdown(result_text)"
      pattern: "renderMarkdown\\("
---

<objective>
Implement the scheduler UI exactly per `08-UI-SPEC.md` (D-15, D-16, D-17): the `#scheduler-panel` sidebar panel, the create-job modal, the run-result modal, all actions, and the live `/ws/events` client with REST re-sync and polling fallback. Vanilla JS + existing CDN libraries only; all labels in Russian.

Purpose: this is what the demo video shows — a job created from chat or the form appears in the sidebar, goes `выполняется` -> `успешно` live, and its result opens in a modal.
Output: edits to `ui/static/index.html` and `ui/static/app.js`.
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-scheduler-day-18/08-UI-SPEC.md
@.planning/phases/08-scheduler-day-18/08-CONTEXT.md
@.planning/phases/08-scheduler-day-18/08-PATTERNS.md
@.planning/phases/08-scheduler-day-18/08-05-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- REST (08-05); all JSON datetimes are UTC ISO with Z/+00:00 -->
GET    /api/v1/scheduler/tasks -> ScheduledTaskOut[]
POST   /api/v1/scheduler/tasks {title, prompt, model, schedule_type: "once"|"interval"|"cron", delay_seconds?, run_at? (local "YYYY-MM-DDTHH:MM" from datetime-local), interval_seconds?, cron?, max_runs?} -> 201 | 422 {detail: "<Russian>"} | 409
POST   /api/v1/scheduler/tasks/{id}/pause | /resume | /cancel -> ScheduledTaskOut | 409 {detail}
POST   /api/v1/scheduler/tasks/{id}/run -> 202 RunSummary | 409 {detail: "Задание уже выполняется"}
DELETE /api/v1/scheduler/tasks/{id} -> 204
GET    /api/v1/scheduler/tasks/{id}/runs?limit=20 -> RunSummary[] (newest first)
GET    /api/v1/scheduler/runs/{run_id} -> RunDetail
ScheduledTaskOut: {id, title, prompt, model, schedule_type, run_at, interval_seconds, cron, max_runs, run_count, next_run_at, status, origin_chat_id, created_at, updated_at, last_run: RunSummary|null, is_running}
RunSummary: {id, task_id, status: "running"|"success"|"failed"|"skipped", trigger, scheduled_for, started_at, finished_at, duration_ms, is_late, model}
RunDetail: RunSummary + {task_title, result_text, error, tool_trace: [{name, arguments?, result?/content?, ok?...}]}  (inspect agent/context_engine.py::serialize_tool_trace for exact entry keys)

<!-- WS /ws/events (08-02 / 08-04 frames) -->
{"type":"run_started","task_id","run": RunSummary,"task": ScheduledTaskOut}   (task = post-claim snapshot: advanced next_run_at, run_count, is_running true)
{"type":"run_finished","task_id","run": RunSummary,"task": ScheduledTaskOut}
{"type":"task_updated","task": ScheduledTaskOut}
{"type":"task_deleted","task_id"}
Client sends the text "ping" every 25 s; server closes with 1008 when unauthenticated/bad origin.

<!-- Existing app.js helpers to reuse (read their current line numbers) -->
WS_BASE (~5), STOP_RECONNECT_CODES (~6), MAX_RECONNECT_DELAY (~7), apiFetch (~74-91, throws Error(detail), returns null on 204),
renderMarkdown (~98-101), showToast(message, type), formatTaskTimestamp (~353-361), renderTaskPanel (~398-497),
cancelTask/pauseTask (~499-528), setupFoldablePanels (~564-575), connectWs/scheduleReconnect (~1107-1151),
MCP block: state.mcp* (~1482-1486), mcpEl(tag, className, text) (~1488-1493), bindMcpFold (~1509-1525), saveMcpServer in-flight pattern (~1751-1804),
modal open/close (~1876-1886), bindEvents (~1997-2029), init (~2032-2049), state.models / state.selectedModel (loadModels ~2036)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Panel + modal markup, panel rendering/actions in app.js, and a Node-free JS balance test</name>
  <files>ui/static/index.html, ui/static/app.js, tests/test_static_js_syntax.py</files>
  <read_first>
    - .planning/phases/08-scheduler-day-18/08-UI-SPEC.md (sections 1-3, 7, Color, Copywriting — exact classes and Russian copy)
    - ui/static/index.html (#task-panel ~105-114, #invariants-panel ~115-154, #agent-status after it, #settings-modal shell ~235-240, MCP form inputs ~305-349, #add-user-modal ~354-384)
    - ui/static/app.js (lines 1-110; 350-600; 1460-1540; 1990-2051)
  </read_first>
  <action>
    ui/static/index.html:
    - Insert `#scheduler-panel` between the closing tag of `#invariants-panel` and `#agent-status`, copying the `#task-panel` skeleton: container `border-t border-slate-800 p-3 text-xs text-slate-400 overflow-y-auto max-h-72`; h3 `text-slate-300 font-semibold mb-2 flex items-center justify-between` with `<span>Расписание <span id="scheduler-count" class="text-white font-semibold">0</span> <span id="scheduler-running-badge" class="text-sky-400 font-semibold hidden"></span></span>` and the fold button `data-fold-toggle="scheduler-panel-body" aria-expanded="false" title="Свернуть/развернуть"` with `▸`; body `<div id="scheduler-panel-body" class="hidden">` containing `<button type="button" id="btn-scheduler-new" class="w-full rounded-lg bg-indigo-600 hover:bg-indigo-500 px-4 py-2 text-sm font-semibold transition">+ Новое задание</button>` and `<div id="scheduler-list" class="space-y-2 mt-2"></div>`.
    - Add `#scheduler-create-modal` (shell `hidden fixed inset-0 z-50 flex items-center justify-center bg-black/60`, inner `bg-slate-900 border border-slate-700 rounded-xl shadow-xl w-full max-w-lg mx-4 max-h-[90vh] overflow-y-auto`, `role="dialog" aria-modal="true" aria-labelledby="scheduler-create-title"`), header `<h2 id="scheduler-create-title" class="text-lg font-semibold">Новое задание</h2>` + `&times;` close `id="btn-close-scheduler-create" aria-label="Закрыть"`, `<form id="scheduler-create-form" class="p-5 space-y-4">` with labelled fields per UI-SPEC section 4: `#scheduler-title` (maxlength 200, placeholder "Например: Сводка по репозиторию"), `#scheduler-prompt` textarea rows 4 maxlength 4000 + helper text, `#scheduler-type` select (once "Однократно" / interval "Интервал" / cron "Cron"), group `#scheduler-once-fields` (radios `name="scheduler-once-mode"` values delay/at with `#scheduler-delay` number min 1 "Через N секунд" and `#scheduler-run-at` datetime-local "В указанное время" + helper "Локальное время"), group `#scheduler-interval-fields` (`#scheduler-interval-value` number min 1 + `#scheduler-interval-unit` select seconds/minutes/hours "секунд/минут/часов" + helper "Минимальный интервал — 10 секунд"), group `#scheduler-cron-fields` (`#scheduler-cron` font-mono placeholder "*/5 * * * *" + helper "5 полей: минута час день месяц день_недели. Время — локальное время машины агента."), `#scheduler-model` select + helper "Если модель будет недоступна, запуск завершится ошибкой", `#scheduler-max-runs` number min 1 placeholder "Без ограничения" label "Максимум запусков (необязательно)" inside `#scheduler-max-runs-field`; `<p id="scheduler-create-error" class="text-xs text-red-400"></p>`; footer buttons `Отмена` (`id="btn-cancel-scheduler-create"`, text-only) and submit `Создать задание` (`id="btn-submit-scheduler-create"`, accent, font-semibold). Inputs use `w-full rounded-lg bg-slate-800 border border-slate-700 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500`; labels `block text-sm text-slate-400 mb-1`. No `py-1.5`/`px-1.5`/`gap-1.5`, no `font-medium` in new markup.
    - Add `#scheduler-run-modal` (same shell, `max-w-2xl`, `role="dialog" aria-modal="true" aria-labelledby="scheduler-run-title"`): header `<h2 id="scheduler-run-title" class="text-lg font-semibold truncate">` + close `id="btn-close-scheduler-run" aria-label="Закрыть"`, sub-header `<div id="scheduler-run-meta" class="px-5 pt-3 text-xs text-slate-500 flex flex-wrap gap-2"></div>`, body `<div id="scheduler-run-body" class="p-5 space-y-4"></div>`. No new script tags.

    ui/static/app.js (new block after the MCP block; state attached like the MCP block, not by editing the big state literal):
    - State: `state.lastSchedulerTasks = null; state.schedulerExpanded = new Set(); state.schedulerRuns = new Map(); state.schedulerBusy = new Set();`.
    - Constants: `SCHEDULER_JOB_STATUS_LABELS` {active:"активно", paused:"на паузе", completed:"завершено", cancelled:"отменено"}, `SCHEDULER_JOB_STATUS_CLASSES` {active:"text-sky-400", paused:"text-amber-400", completed:"text-emerald-400", cancelled:"text-slate-500"}, `SCHEDULER_RUN_STATUS_LABELS` {running:"выполняется", success:"успешно", failed:"ошибка", skipped:"пропущено"}, `SCHEDULER_RUN_STATUS_CLASSES` {running:"text-sky-400", success:"text-emerald-400", failed:"text-red-400", skipped:"text-slate-400"}, button class constants neutral/accent/destructive exactly as UI-SPEC section 2.
    - Helpers: `formatSchedulerTimestamp(iso, withSeconds)` using `toLocaleString('ru-RU', {day:'2-digit', month:'2-digit', hour:'2-digit', minute:'2-digit'[, second:'2-digit']})` ("—" for null); `formatSchedulerDuration(ms)` -> "12 с" / "1 мин 5 с"; `formatSchedulerSchedule(task)` -> "Однократно · dd.mm hh:mm" / "Каждые N сек|мин|ч" (largest whole unit) / "Cron: expr" + " · запусков: x/y" or " · запусков: x"; `schedulerStatusChip(labels, classes, status)` (rounded px-1 text-xs, running gets a leading `animate-pulse motion-reduce:animate-none` dot), all built with `mcpEl` (textContent only).
    - `loadSchedulerTasks()`: `apiFetch('/api/v1/scheduler/tasks')` -> `state.lastSchedulerTasks` -> `renderSchedulerPanel()`; on error `showToast('Не удалось загрузить расписание. Проверьте соединение и попробуйте снова.', 'error')`.
    - `renderSchedulerPanel()`: early return when null; update `#scheduler-count` (active+paused) and `#scheduler-running-badge` ("N выполняется", hidden when 0); `listEl.replaceChildren()`; empty state heading "Заданий пока нет" + body copy from UI-SPEC; per job a card `rounded-lg bg-slate-800 px-2 py-1` with Row 1 header `<button type="button" aria-expanded>` (caret ▸/▾, title `text-sm font-semibold text-slate-300 truncate` with `title` attr, status chip), Row 2 schedule summary (cron in `font-mono`), Row 3 "Следующий: …" ("—" unless active) + "Последний:" run chip (omitted without runs), Row 4 actions by status (active: Запустить сейчас / Пауза / Отменить / Удалить-link; paused: Запустить сейчас / Продолжить / Отменить / Удалить-link; completed/cancelled: Удалить-link only); "Запустить сейчас" disabled with `title="Задание уже выполняется"` when `is_running`. Expanded state kept in `state.schedulerExpanded` (copy `bindMcpFold` as `bindSchedulerFold`); on expand fetch `/api/v1/scheduler/tasks/${id}/runs?limit=20` into `state.schedulerRuns` and render run rows (`button` `w-full text-left rounded bg-slate-900 hover:bg-slate-700 px-2 py-1 flex items-center justify-between gap-2 focus:outline-none focus:ring-2 focus:ring-indigo-500`: start time with seconds, status chip + "с опозданием" amber chip when is_late, duration unless running/skipped); empty "Запусков пока не было"; row click -> `openSchedulerRunModal(runId, rowButton)` (function declared in Task 2; function declarations are hoisted in the classic script, so Task 1 only wires the call).
    - Actions `schedulerPause/Resume/RunNow/Cancel/Delete(task, button)`: disable the button + `state.schedulerBusy` guard, call the REST route, `finally` re-enable; after success `await loadSchedulerTasks()` and toast exactly: "Задание поставлено на паузу" / "Задание возобновлено" / "Запуск начат" (info) / "Задание отменено" / "Задание удалено"; Cancel confirm `Отменить задание «${title}»? Будущие запуски не состоятся, история запусков сохранится.`; Delete confirm `Удалить задание «${title}» и всю историю его запусков? Это действие нельзя отменить.`; errors -> `showToast(err.message || 'Не удалось выполнить действие. Проверьте соединение и попробуйте снова.', 'error')` (server Russian detail surfaces via apiFetch).
    - In `init()` after `checkAgentHealth()`/`loadInvariants()` call `loadSchedulerTasks()`; the fold toggle already works through `setupFoldablePanels()` — additionally re-sync with `loadSchedulerTasks()` when `#scheduler-panel-body` is first expanded (hook the existing toggle click via a separate listener on the button).

    tests/test_static_js_syntax.py (new; module docstring "Node-free structural sanity check for the vanilla JS frontend."; CLAUDE.md forbids Node, so no eslint / node --check): implement `check_js_balance(src: str) -> list[str]` as a single-pass scanner that tracks a stack of `(`, `[`, `{`, template-literal and `${` frames with line numbers; skips `//` and `/* */` comments, single/double-quoted strings (backslash escapes; a raw newline inside is an "unterminated string" error), template literals (backslash escapes; `${` pushes an expression frame closed by `}`), and regex literals (a `/` starts a regex when the previous significant token is empty, one of `( , = : [ ! & | ? { } ; + - * % < > ~ ^` or one of the keywords return/typeof/case/in/of/void/delete/throw/new; scan to the unescaped `/` outside a `[...]` class). Returns error strings like "mismatch ( @12 closed by } @40", "unclosed { from line 7", "unterminated string line 3". Tests: `test_static_js_is_balanced` parametrized over `sorted(Path('ui/static').glob('*.js'))` asserting `check_js_balance(text) == []`; `test_checker_detects_errors` with inline samples (an unclosed `{`, a `)` closing `{`, an unterminated `'` string, an unclosed template `${`) each yielding a non-empty list; `test_checker_accepts_tricky_valid_code` with a sample containing a regex literal with `/` and `]` inside a class, a nested template literal inside `${...}`, a `//` comment containing `{` and a string containing `)` -> []. The checker was prototyped against the current app.js during planning and reports no errors, so any failure after this plan points at the new code.
  </action>
  <verify>
    <automated>python -c "import re,sys; h=open('ui/static/index.html',encoding='utf-8').read(); j=open('ui/static/app.js',encoding='utf-8').read(); ids=['scheduler-panel','scheduler-panel-body','scheduler-count','scheduler-running-badge','scheduler-list','btn-scheduler-new','scheduler-create-modal','scheduler-run-modal','scheduler-create-error']; missing=[i for i in ids if 'id=\"'+i+'\"' not in h]; assert not missing, missing; assert 'renderSchedulerPanel' in j and 'loadSchedulerTasks' in j; print('ok')" && pytest tests/test_static_js_syntax.py -q</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains ids scheduler-panel, scheduler-panel-body, scheduler-count, scheduler-running-badge, scheduler-list, btn-scheduler-new, scheduler-create-modal, scheduler-run-modal, scheduler-create-error and `data-fold-toggle="scheduler-panel-body"`
    - `grep -c 'role="dialog"' ui/static/index.html` >= 2 (the two new modals)
    - `grep -c "<script" ui/static/index.html` unchanged versus `git show HEAD:ui/static/index.html | grep -c "<script"`
    - In the new scheduler block of app.js there is no `innerHTML` assignment (`grep -n "innerHTML" ui/static/app.js` lists no line inside the scheduler functions added in this task)
    - app.js contains the exact strings "Заданий пока нет", "Запусков пока не было", "Задание поставлено на паузу", "Задание возобновлено", "Запуск начат", "Задание отменено", "Задание удалено", "Задание уже выполняется", "с опозданием"
    - The inserted `#scheduler-panel` markup and the two new modals contain no `py-1.5`, `px-1.5`, `gap-1.5` or `font-medium` classes
    - `pytest tests/test_static_js_syntax.py -q` passes (including the negative samples, proving the checker actually detects breakage)
  </acceptance_criteria>
  <done>Panel, modals markup and list/actions work against the REST API with Russian copy and safe DOM building.</done>
</task>

<task type="auto">
  <name>Task 2: Create-job modal and run-result modal</name>
  <files>ui/static/app.js</files>
  <read_first>
    - .planning/phases/08-scheduler-day-18/08-UI-SPEC.md (sections 4-6)
    - ui/static/app.js (Task 1 scheduler block; modal open/close ~1876-1886; backdrop + Escape handlers ~1997-2005; saveMcpServer in-flight pattern ~1751-1804; loadModels / state.models; bindEvents ~1997-2029)
    - agent/context_engine.py (serialize_tool_trace ~91-107: tool-trace entry keys for rendering)
  </read_first>
  <action>
    ui/static/app.js:
    - Create modal: `openSchedulerCreateModal(opener)` stores `opener` (focus return), resets the form, fills `#scheduler-model` from `state.models` with `state.selectedModel` preselected (disabled option "Нет моделей" when empty), shows only the field group of the selected type (`#scheduler-type` change handler; `#scheduler-max-runs-field` hidden for once), focuses `#scheduler-title`. `submitSchedulerCreate(e)`: preventDefault; client checks (empty title/prompt -> "Заполните название и промпт"); build body: once -> `delay_seconds` (int) or `run_at` (datetime-local value as-is, machine-local per D-07); interval -> `interval_seconds = value * {seconds:1, minutes:60, hours:3600}[unit]`; cron -> `cron`; `max_runs` only when filled and type != once; `model`; POST with `headers: {'Content-Type': 'application/json'}` via apiFetch; disable `#btn-submit-scheduler-create` during the request (`finally` re-enable); on error put `err.message` into `#scheduler-create-error` (server Russian 422 detail shown verbatim); on success close, `await loadSchedulerTasks()`, `showToast('Задание создано', 'success')`. Enter in single-line inputs submits (native form behaviour); Enter in the textarea inserts a newline.
    - Run modal: `openSchedulerRunModal(runId, opener)`: fetch `/api/v1/scheduler/runs/${runId}`; title = `task_title` (textContent); meta = status chip + "начало " + start time with seconds + duration + "с опозданием" chip; body: running -> "Задание выполняется…" with pulsing dot; skipped -> single line "Запуск пропущен: предыдущий запуск ещё выполнялся"; otherwise section "Результат" whose content div (`message-content text-sm`) gets `innerHTML = renderMarkdown(result_text || '')` — the ONLY innerHTML use with server data; failed -> error box `bg-red-900/30 border border-red-700 rounded-lg p-3 text-sm text-red-400` with textContent "Ошибка выполнения: " + error; tool trace (when non-empty) behind a toggle "▸ Показать ход выполнения" / "▾ Скрыть ход выполнения", each entry: name `font-mono text-xs text-slate-300` + args/result in `<pre class="text-xs text-slate-400 bg-black/30 rounded-md p-2 overflow-x-auto whitespace-pre-wrap">` via textContent (JSON.stringify objects with 2-space indent). Track `state.schedulerOpenRunId` (set on open, null on close).
    - Modal behaviour (both): close on ×, backdrop click (`e.target === modalRoot`), Escape (extend the existing Escape handler), focus returns to the stored opener; only one scheduler modal open at a time (opening one closes the other).
    - Wire in `bindEvents`: `#btn-scheduler-new` -> openSchedulerCreateModal, `#scheduler-create-form` submit, close/cancel buttons of both modals, `#scheduler-type` change, once-mode radios toggle which input is enabled.
  </action>
  <verify>
    <automated>python -c "j=open('ui/static/app.js',encoding='utf-8').read(); need=['openSchedulerRunModal','openSchedulerCreateModal','submitSchedulerCreate','renderMarkdown(','Задание создано','Запуск пропущен: предыдущий запуск ещё выполнялся','Показать ход выполнения','schedulerOpenRunId']; miss=[n for n in need if n not in j]; assert not miss, miss; print('ok')" && pytest tests/test_static_js_syntax.py -q</automated>
  </verify>
  <acceptance_criteria>
    - app.js contains `function openSchedulerRunModal`, `function openSchedulerCreateModal`, `function submitSchedulerCreate`
    - Exactly one scheduler innerHTML assignment and it uses renderMarkdown: in the scheduler block `grep -n "innerHTML" ui/static/app.js` shows only a line containing `renderMarkdown(`
    - `grep -c "Content-Type': 'application/json'" ui/static/app.js` increases by at least 1 (create POST)
    - `pytest tests/test_static_js_syntax.py -q` passes
  </acceptance_criteria>
  <done>Create and result modals work with safe rendering, focus return and Russian copy; app.js stays structurally valid.</done>
</task>

<task type="auto">
  <name>Task 3: /ws/events live client with REST re-sync and single-timer polling fallback</name>
  <files>ui/static/app.js</files>
  <read_first>
    - .planning/phases/08-scheduler-day-18/08-UI-SPEC.md (section 7: live updates, no offline banner for this socket)
    - ui/static/app.js (connectWs/scheduleReconnect ~1107-1151 for the backoff shape; Task 1-2 scheduler block; init ~2032-2049)
  </read_first>
  <action>
    ui/static/app.js:
    - State: `eventsWs = null, eventsReconnectAttempt = 0, eventsShouldReconnect = true, schedulerPollTimer = null, eventsPingTimer = null` (attached like the MCP state).
    - Poll helpers (prevents duplicate timers across repeated failed reconnects): `startSchedulerPolling()` returns immediately `if (state.schedulerPollTimer)` (a timer is already alive), else `state.schedulerPollTimer = setInterval(() => { if (!$('scheduler-panel-body').classList.contains('hidden')) loadSchedulerTasks(); }, 10000)`. `stopSchedulerPolling()`: `if (state.schedulerPollTimer) clearInterval(state.schedulerPollTimer);` then `state.schedulerPollTimer = null;`. These two functions are the only places that touch `schedulerPollTimer`.
    - `connectEventsWs()`: `new WebSocket(`${WS_BASE}/ws/events`)`; onopen -> reset attempt, `stopSchedulerPolling()`, `loadSchedulerTasks()` (re-sync on every (re)connect), clear any old ping timer and start a 25 s `setInterval` sending "ping"; onmessage -> JSON.parse in try/catch -> `applySchedulerEvent(frame)`; onclose -> clear the ping timer and null it; if `STOP_RECONNECT_CODES.has(event.code)` (1008: unauthenticated / bad origin) set `eventsShouldReconnect = false`, call `stopSchedulerPolling()` and return WITHOUT a toast and WITHOUT starting polling (REST would fail with 401 as well); otherwise `startSchedulerPolling()` (idempotent) and, if `state.eventsShouldReconnect`, schedule a reconnect after `Math.min(1000 * 2 ** state.eventsReconnectAttempt, MAX_RECONNECT_DELAY)` incrementing the attempt; onerror -> no toast (UI-SPEC: no offline banner for this socket). Call `connectEventsWs()` in `init()` after `loadSchedulerTasks()`.
    - `applySchedulerEvent(frame)`: if `state.lastSchedulerTasks` is null, call `loadSchedulerTasks()` and return. run_started -> upsert `frame.task` (the post-claim snapshot with fresh next_run_at, run_count and is_running true; do not merely flip is_running on the stale object), prepend/replace `frame.run` in `state.schedulerRuns.get(frame.task_id)` if loaded; run_finished -> upsert `frame.task`, replace the run in the loaded history; toast success `Задание «${frame.task.title}» выполнено` / failed `Задание «${frame.task.title}» завершилось с ошибкой` (skipped: no toast); if `state.schedulerOpenRunId === frame.run.id` refresh the run modal in place; task_updated -> upsert `frame.task`; task_deleted -> remove the job, its history and expanded flag. Always finish with `renderSchedulerPanel()` (full re-render preserving `state.schedulerExpanded`).
  </action>
  <verify>
    <automated>python -c "import re; j=open('ui/static/app.js',encoding='utf-8').read(); need=['/ws/events','connectEventsWs','applySchedulerEvent','startSchedulerPolling','stopSchedulerPolling','STOP_RECONNECT_CODES','завершилось с ошибкой','frame.task']; miss=[n for n in need if n not in j]; assert not miss, miss; assert len(re.findall(r'schedulerPollTimer\s*=\s*setInterval', j)) == 1; assert 'if (state.schedulerPollTimer)' in j; print('ok')" && pytest tests/test_static_js_syntax.py -q</automated>
  </verify>
  <acceptance_criteria>
    - app.js contains `${WS_BASE}/ws/events`, `function connectEventsWs`, `function applySchedulerEvent`, `function startSchedulerPolling`, `function stopSchedulerPolling`
    - Exactly one `schedulerPollTimer = setInterval` assignment, guarded by `if (state.schedulerPollTimer)`; stopSchedulerPolling sets `state.schedulerPollTimer = null` after `clearInterval`
    - In connectEventsWs the STOP_RECONNECT_CODES branch calls stopSchedulerPolling and returns before any startSchedulerPolling call
    - The events socket onerror/onclose handlers contain no `showToast(` call (inspect the connectEventsWs function body)
    - The run_started branch of applySchedulerEvent upserts `frame.task`
    - `pytest tests/test_static_js_syntax.py -q` passes
  </acceptance_criteria>
  <done>The panel updates live over /ws/events (including fresh schedule data during a run) with REST re-sync, backoff reconnect, no reconnect or polling after 1008, and never more than one poll timer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent JSON / WS frames -> DOM | Titles, prompts, results, errors, tool traces originate from users, the LLM and MCP servers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-32 | Tampering (stored XSS) | run-result modal | mitigate | Result body only through renderMarkdown (Marked + DOMPurify.sanitize); every other string via textContent (mcpEl) |
| T-08-33 | Tampering (CSRF) | create/actions | mitigate | Same-origin apiFetch with credentials; server enforces Origin + JSON content type |
| T-08-34 | Information disclosure | /ws/events client | mitigate | Socket is authenticated by the session cookie; server fans out per user; client never sends user ids |
| T-08-35 | Denial of service | reconnect/poll storm | mitigate | Exponential backoff capped at MAX_RECONNECT_DELAY; 1008 stops reconnect and polling; single guarded poll timer (startSchedulerPolling is idempotent) only while disconnected and panel expanded |
</threat_model>

<verification>
All three task checks pass and `pytest tests/test_static_js_syntax.py -q` is green after every task (Node-free structural JS check); manual/visual verification happens in 08-08.
</verification>

<success_criteria>
- UI matches 08-UI-SPEC.md structure, classes and Russian copy
- Live updates over /ws/events with REST re-sync and polling fallback
- No unsanitized HTML insertion
</success_criteria>

<output>
Create `.planning/phases/08-scheduler-day-18/08-07-SUMMARY.md` when done
</output>
