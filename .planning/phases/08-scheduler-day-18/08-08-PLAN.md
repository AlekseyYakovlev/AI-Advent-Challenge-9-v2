---
phase: 08-scheduler-day-18
plan: 08
type: execute
wave: 5
depends_on: ["08-06", "08-07"]
files_modified:
  - docs/API_SPEC.md
  - docs/ARCHITECTURE.md
  - docs/TESTING_GUIDE.md
autonomous: false
requirements: [SCHED-01, SCHED-02, SCHED-03, SCHED-04, SCHED-05, SCHED-06, SCHED-07, SCHED-08, SCHED-09, SCHED-10, SCHED-11, SCHED-12, SCHED-13, SCHED-14]

must_haves:
  truths:
    - "docs/API_SPEC.md documents every /api/v1/scheduler/* route, the WS /ws/events handshake and its four frame types, and the three LLM tools"
    - "docs/ARCHITECTURE.md describes the poll loop, atomic claim, catch-up/overlap/max_runs rules, startup recovery, headless runner + allowlist and the per-user EventHub"
    - "docs/TESTING_GUIDE.md lists the required scheduler scenarios per test file"
    - "The full pytest suite passes"
    - "The demo flow works end to end with real LM Studio in the browser (human-verified)"
  artifacts:
    - path: "docs/API_SPEC.md"
      provides: "Scheduler REST + WS + tools spec"
      contains: "/api/v1/scheduler/tasks"
    - path: "docs/ARCHITECTURE.md"
      provides: "Scheduler architecture section"
      contains: "Scheduler"
    - path: "docs/TESTING_GUIDE.md"
      provides: "Scheduler test scenarios"
      contains: "test_scheduler_service.py"
  key_links:
    - from: "docs/TESTING_GUIDE.md"
      to: "tests/test_scheduler_*.py"
      via: "scenario list per file"
      pattern: "test_scheduler_"
---

<objective>
Close the phase: bring the project docs in sync with the scheduler (API, architecture, testing guide), run the full test suite, and have the user verify the Day 18 demo flow in the real app with LM Studio.

Purpose: CLAUDE.md requires `docs/API_SPEC.md` / `docs/ARCHITECTURE.md` / `docs/TESTING_GUIDE.md` to reflect new endpoints and required test scenarios; the browser + real-model demo cannot be automated.
Output: updated docs, green suite, human approval.
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-scheduler-day-18/08-CONTEXT.md
@.planning/phases/08-scheduler-day-18/08-RESEARCH.md
@.planning/phases/08-scheduler-day-18/08-04-SUMMARY.md
@.planning/phases/08-scheduler-day-18/08-05-SUMMARY.md
@.planning/phases/08-scheduler-day-18/08-06-SUMMARY.md
@.planning/phases/08-scheduler-day-18/08-07-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update API_SPEC, ARCHITECTURE and TESTING_GUIDE; run the full suite</name>
  <files>docs/API_SPEC.md, docs/ARCHITECTURE.md, docs/TESTING_GUIDE.md</files>
  <read_first>
    - docs/API_SPEC.md (existing section structure, MCP section format)
    - docs/ARCHITECTURE.md (process/component sections)
    - docs/TESTING_GUIDE.md (per-file scenario block format)
    - agent/scheduler_api.py, agent/scheduler_schemas.py, agent/events.py, agent/scheduler_tools.py, agent/scheduler.py, agent/headless.py (the implemented contracts — document what exists, not the plan)
    - tests/test_scheduler_*.py (actual test files and names)
  </read_first>
  <action>
    docs/API_SPEC.md: add a "Scheduler (Day 18)" section in the existing format: each REST route (method, path, auth, origin/content-type guards, request body, response model fields, status codes 201/202/204/401/404/409/415/422 with the Russian 422/409 detail strings), the ScheduledTaskOut / RunSummary / RunDetail shapes (all datetimes UTC ISO with Z/+00:00), `WS /ws/events` (cookie + Origin checks, close 1008, client "ping" every 25 s, frames run_started / run_finished / task_updated / task_deleted with examples; run_started and run_finished both carry the `task` ScheduledTaskOut snapshot), and the three LLM tools with arguments, result/error codes and the cancel gate rule.
    docs/ARCHITECTURE.md: add a "Scheduler" subsection: tables `scheduledtask` / `taskrun` (FKs: user CASCADE, origin_chat_id SET NULL, partial unique index one running run per job), poll loop in the Agent lifespan (SCHEDULER_ENABLED, 1 s tick, batch 20), atomic claim `UPDATE ... WHERE next_run_at = old` + run insert in one commit, catch-up-once / overlap-skip / no-retry / max_runs / finalize rule, startup recovery (supervisor hard-kill rationale), executor (semaphore 2 + asyncio.timeout SCHEDULER_RUN_TIMEOUT) around the headless runner (RecordingSink reuse of ws tool loop, allowlist save_long_term_memory + MCP tools, sampling from global Settings, model stored on the job), cron in machine-local time with UTC storage, EventHub per user (never the ownerless broadcast), and the SCHEDULER_* settings table.
    docs/TESTING_GUIDE.md: add a "Scheduler (Day 18)" block listing, per file (test_scheduler_schedule.py, test_scheduler_models.py, test_scheduler_runner.py, test_scheduler_service.py, test_scheduler_lifespan.py, test_scheduler_api.py, test_scheduler_tools.py, test_scheduler_events.py), the required scenarios as implemented (claim atomicity, catch-up, overlap skip, max_runs, recovery, headless success/tool round/timeout/model down/disallowed tool, IDOR 404, 401 routes, event isolation, cancel gate, chat delete SET NULL, tool count 9), plus the note that conftest sets SCHEDULER_ENABLED=false and tests drive `tick(now)` directly.
    Then run the full suite `pytest tests/ -q` and fix any regression in the scheduler files touched by this phase (do not weaken existing tests). Record the passing count in the SUMMARY.
  </action>
  <verify>
    <automated>pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "/api/v1/scheduler/tasks" docs/API_SPEC.md` >= 5 and `grep -c "/ws/events" docs/API_SPEC.md` >= 1 and `grep -c "cancel_scheduled_task" docs/API_SPEC.md` >= 1
    - `grep -c "uq_taskrun_one_running\|next_run_at = old" docs/ARCHITECTURE.md` >= 1 and `grep -c "SCHEDULER_RUN_TIMEOUT" docs/ARCHITECTURE.md` >= 1
    - `grep -c "test_scheduler_" docs/TESTING_GUIDE.md` >= 8
    - `pytest tests/ -q` exits 0
  </acceptance_criteria>
  <done>Docs describe the shipped scheduler; the full suite is green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Day 18 demo walkthrough in the browser with real LM Studio</name>
  <files>none (verification only)</files>
  <read_first>
    - .planning/phases/08-scheduler-day-18/08-UI-SPEC.md (expected labels and behaviour)
    - .planning/phases/08-scheduler-day-18/08-CONTEXT.md (specifics: demo path)
  </read_first>
  <action>Start the app with `python run.py` (check ports 8000/8001 are free first), confirm GET http://localhost:8001/health returns 200 and GET http://localhost:1234/v1/models answers (LM Studio up), then present the steps below to the user and wait for their verdict. Record the verdict and any issues in the SUMMARY.</action>
  <verify>
    <automated>curl -s -o /dev/null -w "%{http_code}" http://localhost:8001/health</automated>
  </verify>
  <acceptance_criteria>
    - Agent /health returns 200 before handing over to the user
    - The user replied "approved", or every reported issue is recorded in the SUMMARY with its step number
  </acceptance_criteria>
  <done>User approved the Day 18 demo walkthrough.</done>
  <what-built>Scheduler end to end: DB tables, poll loop with atomic claim and restart recovery, headless LLM+MCP runner, REST API, LLM chat tools with cancel gate, per-user /ws/events, and the "Расписание" sidebar panel with create/result modals.</what-built>
  <how-to-verify>
    Prerequisites: LM Studio running on http://localhost:1234 with a tool-capable model loaded; the filesystem MCP server configured and enabled in Settings (from Day 16).
    1. Start the app: `python run.py`; open http://localhost:8000 and log in.
    2. Expand "Расписание" in the sidebar: empty state "Заданий пока нет" is shown.
    3. In a chat write: "через минуту прочитай файл <path to a small file in the allowed MCP directory> через MCP и перескажи его". Expected: the model calls schedule_task; within ~2 s the job appears in the panel as "активно", "Однократно · <time>".
    4. Wait for the fire time: the card shows "выполняется" with a pulsing dot (live, no page reload), then "успешно" and a toast "Задание «…» выполнено".
    5. Expand the card and click the run: the modal shows the Markdown summary of the file; "Показать ход выполнения" lists the MCP read tool call.
    6. Click "+ Новое задание": create an Интервал job every 20 секунд with "Максимум запусков" 2 and a short prompt. Watch two runs arrive live; the job becomes "завершено" with "запусков: 2/2".
    7. Create another interval job, press "Запустить сейчас" (toast "Запуск начат"), then "Пауза" (status "на паузе"), "Продолжить", then "Отменить" (confirm; status "отменено", history kept), then "Удалить" (confirm; card disappears).
    8. In chat write "покажи мои задания" (model calls list_scheduled_tasks); then "отмени задание <id>" for an active job — it becomes "отменено". Write a message without cancel intent that mentions the job — the model must NOT cancel it.
    9. Restart resilience: create a once job 1 minute ahead, stop the app (Ctrl+C) for ~2 minutes, start it again: the job runs once right after startup and its run shows the "с опозданием" chip.
    10. Optional isolation check: log in as a second user in another browser profile — none of the first user's jobs or live events appear.
  </how-to-verify>
  <resume-signal>Type "approved" or describe the issues found (step number + what happened)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> future implementers | Documentation must not describe weaker controls than implemented |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-36 | Information disclosure | docs examples | mitigate | Examples use placeholder ids/titles; no real prompts, paths with secrets, or API keys |
| T-08-37 | Repudiation | demo verification | accept | Manual UAT result recorded in the SUMMARY by the executor from the user's response |
</threat_model>

<verification>
`pytest tests/ -q` passes and the user approves the walkthrough.
</verification>

<success_criteria>
- Docs updated for REST, WS, tools, architecture and test scenarios
- Full suite green
- Demo flow approved by the user
</success_criteria>

<output>
Create `.planning/phases/08-scheduler-day-18/08-08-SUMMARY.md` when done
</output>
