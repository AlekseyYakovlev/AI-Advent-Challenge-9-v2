---
phase: 07-mcp-connection-day-16
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/models.py
  - agent/mcp_config.py
  - tests/test_mcp_config.py
autonomous: true
requirements: [MCP-01]

must_haves:
  truths:
    - "MCP server configs (name, command, args, env, cwd, enabled) persist in SQLite scoped by user_id and survive an engine restart"
    - "One user's configs are never returned for another user's id"
    - "Args round-trip as a JSON list of strings (Windows paths with spaces intact, no shell parsing)"
    - "Editing env leaves values of masked (•••) keys untouched, replaces retyped keys, and drops removed keys"
    - "Deleting a user cascades to their MCP server configs"
  artifacts:
    - path: "shared/models.py"
      provides: "McpServerConfig table"
      contains: "class McpServerConfig"
    - path: "agent/mcp_config.py"
      provides: "User-scoped CRUD + env merge + JSON (de)serialization"
      exports: ["ENV_MASK", "list_servers", "get_server", "create_server", "update_server", "delete_server", "load_args", "load_env", "merge_env"]
    - path: "tests/test_mcp_config.py"
      provides: "Persistence, scoping, merge and cascade tests"
      min_lines: 80
  key_links:
    - from: "shared/models.py::McpServerConfig.user_id"
      to: "user.id"
      via: "sa_column=Column(Integer, ForeignKey('user.id', ondelete='CASCADE'))"
      pattern: "ForeignKey\\(\"user.id\", ondelete=\"CASCADE\"\\)"
    - from: "agent/mcp_config.py"
      to: "McpServerConfig"
      via: "select(...).where(McpServerConfig.user_id == user_id)"
      pattern: "McpServerConfig.user_id == user_id"
---

<objective>
Add the user-scoped `McpServerConfig` SQLModel table (D-10) and a thin CRUD service `agent/mcp_config.py` (mirroring `agent/invariants.py`) including the D-11 args JSON list and the D-12 env masking/merge semantics.

Purpose: MCP-01 persistence layer consumed by the REST endpoints in 07-03. Independent of the MCP client (runs in parallel with 07-01).
Output: `McpServerConfig` model, `agent/mcp_config.py`, `tests/test_mcp_config.py`.
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-mcp-connection-day-16/07-CONTEXT.md
@.planning/phases/07-mcp-connection-day-16/07-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contracts this plan CREATES. 07-03 codes against these exactly. -->

shared/models.py:
- class McpServerConfig(SQLModel, table=True):
    id: Optional[int] (primary key)
    user_id: int  (sa_column=Column(Integer, ForeignKey("user.id", ondelete="CASCADE"), nullable=False, index=True))  -- NOT unique (many per user, D-02)
    name: str (max_length=100)
    command: str (max_length=1000)
    args_json: str = "[]"      -- JSON list[str] (D-11)
    env_json: str = "{}"       -- JSON dict[str, str], plaintext (D-12)
    cwd: Optional[str] = None (max_length=1000)   -- D-14
    enabled: bool = True       -- D-13
    created_at / updated_at: datetime (default_factory=lambda: datetime.now(timezone.utc))

agent/mcp_config.py:
- ENV_MASK: str = "•••"
- def load_args(row: McpServerConfig) -> list[str]
- def load_env(row: McpServerConfig) -> dict[str, str]
- def merge_env(stored: dict[str, str], incoming: dict[str, str]) -> dict[str, str]
- async def list_servers(session: AsyncSession, user_id: int) -> list[McpServerConfig]            (ordered by id)
- async def get_server(session: AsyncSession, user_id: int, server_id: int) -> McpServerConfig | None   (None when missing OR owned by another user)
- async def create_server(session: AsyncSession, user_id: int, name: str, command: str, args: list[str], env: dict[str, str], cwd: str | None, enabled: bool) -> McpServerConfig
- async def update_server(session: AsyncSession, row: McpServerConfig, name: str | None = None, command: str | None = None, args: list[str] | None = None, env: dict[str, str] | None = None, cwd: str | None = None, cwd_set: bool = False, enabled: bool | None = None) -> McpServerConfig
- async def delete_server(session: AsyncSession, row: McpServerConfig) -> None

Existing analogs (read them, do not re-derive):
- shared/models.py::ChatInvariant (~line 222) — non-unique user_id FK via sa_column
- agent/invariants.py::update_global / delete_global — commit/rollback try/except shape
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add McpServerConfig model and agent/mcp_config.py CRUD service</name>
  <files>shared/models.py, agent/mcp_config.py</files>
  <read_first>
    - shared/models.py (ChatInvariant ~222-257 for the non-unique user FK; Profile ~188 for timestamps)
    - agent/invariants.py (whole file — CRUD + commit/rollback + logging pattern)
    - agent/memory.py (list_long_term_memory user_id-filtered select)
    - shared/database.py (confirm `SQLModel.metadata.create_all` in init_db and `PRAGMA foreign_keys=ON`)
  </read_first>
  <action>
    Ensure you are on branch `Day16` (if `git branch --show-current` is `main`, create/switch to `Day16`; if it exists, switch to it).

    shared/models.py: append class `McpServerConfig` at the end of the file exactly per `<interfaces>` with docstring "User-scoped MCP stdio server launch config (Day 16, D-10)." Use `sa_column=Column(Integer, ForeignKey("user.id", ondelete="CASCADE"), nullable=False, index=True)` — NEVER `Field(ondelete=...)` (CLAUDE.md). `datetime.now(timezone.utc)` for timestamps. No DB migration code needed: `init_db` runs `SQLModel.metadata.create_all`, which creates the new table on existing databases.

    agent/mcp_config.py (new): module docstring "User-scoped CRUD for MCP server configs (Day 16).", `logger = get_logger(__name__)`.
    - `load_args`: `json.loads(row.args_json)`; return `[]` on `json.JSONDecodeError` or when result is not a list; coerce items to str.
    - `load_env`: same for dict, values coerced to str.
    - `merge_env(stored, incoming)` (D-12, new logic — no existing analog): result = for each (key, value) in incoming: if value == ENV_MASK and key in stored -> keep stored[key]; if value == ENV_MASK and key not in stored -> skip the key (never persist the mask literal); else use value. Keys absent from incoming are dropped (the UI always sends the full KEY list).
    - `list_servers`: `select(McpServerConfig).where(McpServerConfig.user_id == user_id).order_by(McpServerConfig.id)`.
    - `get_server`: `session.get(McpServerConfig, server_id)`; return None if missing or `row.user_id != user_id` (caller maps to 404-never-403).
    - `create_server`: strip `name`/`command`; `cwd` stripped, empty -> None; `args_json=json.dumps(args)`, `env_json=json.dumps(env)`; `session.add` + commit/refresh inside try, `except Exception: await session.rollback(); raise`. Log `mcp_server_created` with `user_id`, `server_id`, `name` only.
    - `update_server`: apply only non-None fields; `env` goes through `merge_env(load_env(row), env)`; `cwd` applied when `cwd_set` is True (empty/None -> None) so a caller can clear it; set `updated_at = datetime.now(timezone.utc)`; commit/refresh with rollback on exception. Log `mcp_server_updated` (user_id, server_id) — never env.
    - `delete_server`: `await session.delete(row)`; commit with rollback on exception; log `mcp_server_deleted`.
    No in-memory session handling here — the REST layer (07-03) orchestrates disconnect-before-edit/delete (D-08).
  </action>
  <verify>
    <automated>python -c "import asyncio; from shared.models import McpServerConfig; from agent.mcp_config import merge_env, ENV_MASK; assert merge_env({'A':'1','B':'2'}, {'A':ENV_MASK,'C':'3'}) == {'A':'1','C':'3'}; assert merge_env({}, {'X':ENV_MASK}) == {}; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "class McpServerConfig" shared/models.py` matches
    - `grep -A6 "class McpServerConfig" shared/models.py | grep -c "ondelete=\"CASCADE\""` returns 1 and `grep -n "Field(ondelete" shared/models.py` returns no matches
    - `grep -n "McpServerConfig.user_id == user_id" agent/mcp_config.py` matches
    - `grep -c "await session.rollback()" agent/mcp_config.py` returns at least 3
    - `grep -n "shlex" agent/mcp_config.py` returns no matches
    - The verify command prints `ok`
  </acceptance_criteria>
  <done>Table defined with cascading user FK; CRUD service with merge semantics importable.</done>
</task>

<task type="auto">
  <name>Task 2: Persistence, scoping, env-merge and cascade tests</name>
  <files>tests/test_mcp_config.py</files>
  <read_first>
    - tests/conftest.py (seed_user factory fixture, clean_test_db, async_session_factory, engine)
    - tests/test_profile_api.py (style)
    - agent/mcp_config.py (Task 1 output)
  </read_first>
  <action>
    Create tests/test_mcp_config.py (module docstring; async tests; use `seed_user` fixture and `async with async_session_factory() as session`). Tests:
    - `test_create_and_list_scoped_by_user`: user A creates 2 servers, user B creates 1; `list_servers(A)` returns A's 2 in id order; `list_servers(B)` returns only B's.
    - `test_get_server_other_user_returns_none`: B calling `get_server(session, B.id, a_server.id)` returns None.
    - `test_args_roundtrip_with_spaces`: args `["C:\\Program Files\\My Dir", "--flag", ""]` round-trip exactly via `load_args`.
    - `test_update_env_merge_preserves_masked_values`: stored `{"API_KEY": "secret1", "OLD": "x"}`, update with `{"API_KEY": ENV_MASK, "NEW": "v"}` -> `load_env` == `{"API_KEY": "secret1", "NEW": "v"}`.
    - `test_update_partial_fields_and_clear_cwd`: update `name` only keeps command/args; update with `cwd=None, cwd_set=True` clears cwd; `updated_at` increases.
    - `test_update_enabled_toggle`: `enabled=False` persists.
    - `test_delete_server`: row gone after delete.
    - `test_user_delete_cascades_to_servers`: delete the `User` row (session.delete + commit) -> `list_servers(user_id)` is empty (FK pragma is ON in shared/database.py).
    - `test_config_survives_engine_restart` (success criterion 1): create, then `await engine.dispose()`, open a new session, `list_servers` still returns the row with identical command/args.
  </action>
  <verify>
    <automated>pytest tests/test_mcp_config.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_mcp_config.py -v` exits 0 with at least 9 passed tests
    - `grep -c "^async def test_\|^def test_" tests/test_mcp_config.py` returns at least 9
    - `pytest tests/ -q` exits 0
  </acceptance_criteria>
  <done>MCP-01 persistence proven: scoping, round-trip, merge semantics, cascade and restart survival.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| REST caller -> DB | Config values are user-supplied (validated at schema layer in 07-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Information Disclosure | get_server cross-user read | mitigate | get_server returns None when row.user_id != caller user_id; tested by test_get_server_other_user_returns_none |
| T-07-07 | Information Disclosure | env values at rest | accept | Local-first single-machine SQLite (CONTEXT Deferred: encryption not needed); values never logged (only user_id/server_id/name in logs) |
| T-07-08 | Tampering | env mask literal persisted as a real value | mitigate | merge_env drops ENV_MASK for unknown keys and keeps stored value for known keys |
| T-07-09 | Repudiation/Integrity | orphan configs after user delete | mitigate | FK ondelete=CASCADE via sa_column; tested by test_user_delete_cascades_to_servers |
</threat_model>

<verification>
- `pytest tests/test_mcp_config.py -v` green
- `pytest tests/ -q` green
</verification>

<success_criteria>
- McpServerConfig persisted per user with cascade; CRUD service with D-11/D-12 semantics tested
</success_criteria>

<output>
Create `.planning/phases/07-mcp-connection-day-16/07-02-SUMMARY.md` when done
</output>
