---
phase: 07-mcp-connection-day-16
plan: 05
type: execute
wave: 3
depends_on: ["07-03"]
files_modified:
  - ui/static/index.html
  - ui/static/app.js
autonomous: true
requirements: [MCP-01, MCP-02, MCP-03, MCP-04]

must_haves:
  truths:
    - "The Settings modal has an 'MCP серверы' section listing the user's servers, with an empty state when none exist"
    - "User can add, edit (env shown as KEY=•••) and delete a server; the section works independently of the modal's Save button and per-chat checkbox (D-01)"
    - "Each row shows a status badge (не подключён / подключение… / подключено / ошибка) and Подключить/Отключить, Изменить, Удалить buttons; Подключить is disabled for disabled servers (D-02)"
    - "After connect the row shows serverInfo `{name} · v{version} · MCP {protocolVersion}` and a collapsible 'Инструменты (n)' list; each tool expands to params (name, type, * required, description) and has a JSON toggle showing raw inputSchema (D-03, D-04)"
    - "On error the row shows the Russian error message and a collapsible 'Показать журнал сервера (stderr)' block (D-18)"
    - "Opening the Settings modal refreshes server status (lazy liveness)"
    - "No server-provided string is inserted as unsanitized HTML"
  artifacts:
    - path: "ui/static/index.html"
      provides: "#mcp-section markup inside #settings-modal (outside #settings-form)"
      contains: "id=\"mcp-section\""
    - path: "ui/static/app.js"
      provides: "MCP state, render, CRUD, connect/disconnect functions"
      contains: "async function loadMcpServers"
  key_links:
    - from: "ui/static/app.js::openSettingsModal"
      to: "/api/v1/mcp/servers"
      via: "loadMcpServers() via apiFetch"
      pattern: "loadMcpServers\\("
    - from: "ui/static/app.js::connectMcpServer"
      to: "/api/v1/mcp/servers/{id}/connect"
      via: "apiFetch POST"
      pattern: "/connect`"
---

<objective>
Add the "MCP серверы" section to the Settings modal (D-01..D-04, D-12, D-13, D-18) per 07-UI-SPEC.md: server list with status badges and per-row actions, inline add/edit form, connect/disconnect, serverInfo, collapsible tool rows with params and raw JSON toggle, and error + stderr display.

Purpose: The user-facing half of MCP-01..MCP-04 (success criteria 1-3 demo).
Output: markup in `ui/static/index.html`, controller code in `ui/static/app.js`.
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-mcp-connection-day-16/07-CONTEXT.md
@.planning/phases/07-mcp-connection-day-16/07-UI-SPEC.md
@.planning/phases/07-mcp-connection-day-16/07-PATTERNS.md
@.planning/phases/07-mcp-connection-day-16/07-03-SUMMARY.md
@CLAUDE.md

<interfaces>
REST (from 07-03), all via existing apiFetch(path, options) in app.js:
- GET    /api/v1/mcp/servers -> [McpServerResponse]
- POST   /api/v1/mcp/servers  {name, command, args: [str], env: {K: V}, cwd, enabled} -> McpServerResponse (201)
- PUT    /api/v1/mcp/servers/{id} (partial; env values "•••" keep stored value) -> McpServerResponse
- DELETE /api/v1/mcp/servers/{id} -> 204 (apiFetch returns null)
- POST   /api/v1/mcp/servers/{id}/connect -> McpServerResponse (HTTP 200 for both connected and error outcomes; 409 when disabled -> apiFetch throws)
- POST   /api/v1/mcp/servers/{id}/disconnect -> McpServerResponse
McpServerResponse: {id, name, command, args, env_keys, cwd, enabled, created_at, updated_at,
  connection: {status: "not_connected"|"connected"|"error", server_info: {name, version, protocol_version}|null,
               tools: [{name, description, input_schema}], error_code, error_message, detail, stderr_tail}}

Existing app.js primitives (reuse, do not modify): $(), apiFetch (~72), showToast(message, type) (~57), TASK_STATE_BADGE_CLASSES (~47) pattern, renderInvariantsPanel (~685) row-building pattern, deleteGlobalInvariant (~848) confirm pattern, setupFoldablePanels (~557; binds ONCE at startup — does not see dynamically rendered rows), openSettingsModal (~1396), bindEvents (~1479).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MCP section markup in the Settings modal</name>
  <files>ui/static/index.html</files>
  <read_first>
    - ui/static/index.html (#settings-modal ~235-305, #invariants-panel ~115-154 for fold-toggle markup, #add-user-modal for error <p> styling)
    - .planning/phases/07-mcp-connection-day-16/07-UI-SPEC.md (copy, colors, spacing, input class strings)
  </read_first>
  <action>
    In `#settings-modal`: change the inner panel classes `max-w-lg` -> `max-w-2xl` and add `max-h-[90vh] overflow-y-auto` (D-01: modal may widen/scroll). Insert a new `<section id="mcp-section" class="border-t border-slate-700 p-5 space-y-3">` AFTER the closing `</form>` of `#settings-form` (a sibling inside the panel, NOT inside the form — avoids nested forms and Enter-key submitting the settings form; keeps MCP independent of the Save button and per-chat checkbox per D-01). Contents:
    - Header row `flex items-center justify-between`: `<h3 class="text-lg font-semibold">MCP серверы</h3>` and `<button type="button" id="btn-mcp-add" class="rounded-lg bg-indigo-600 hover:bg-indigo-500 px-3 py-1.5 text-sm font-medium transition">+ Добавить сервер</button>`.
    - `<div id="mcp-server-form" class="hidden rounded-lg bg-slate-800/50 border border-slate-700 p-3 space-y-3">` (a div, not a form) with labelled fields (labels `block text-sm text-slate-400 mb-1`, exact UI-SPEC copy): `#mcp-name` input ("Название"), `#mcp-command` input ("Команда", placeholder `C:\Users\Aleksey\go\bin\filesystem.exe`), `#mcp-args` textarea rows 3 ("Аргументы (по одному в строке)"), `#mcp-env` textarea rows 3 ("Переменные окружения (KEY=VALUE, по одной в строке)"), `#mcp-cwd` input ("Рабочая директория (необязательно)"), checkbox `#mcp-enabled` (`class="rounded"`, checked by default) with label "Включён", `<p id="mcp-form-error" class="text-xs text-red-400"></p>`, and buttons `#btn-mcp-cancel` ("Отмена", `rounded-lg px-4 py-2 text-sm text-slate-400 hover:text-white transition`) and `#btn-mcp-save` ("Сохранить", indigo primary). Text inputs use exactly `w-full rounded-lg bg-slate-800 border border-slate-700 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500`; textareas add `resize-none font-mono`.
    - `<div id="mcp-empty" class="hidden text-center text-slate-500 text-sm py-4">` containing `<p class="font-semibold">Серверы не настроены</p>` and `<p>Добавьте MCP-сервер, чтобы агент мог подключаться к нему и видеть список инструментов.</p>`.
    - `<div id="mcp-server-list" class="space-y-2"></div>`.
    Do not add a new `<style>` block; utility classes only.
  </action>
  <verify>
    <automated>python -c "import re,io; h=io.open('ui/static/index.html',encoding='utf-8').read(); ids=['mcp-section','btn-mcp-add','mcp-server-form','mcp-name','mcp-command','mcp-args','mcp-env','mcp-cwd','mcp-enabled','mcp-form-error','btn-mcp-save','btn-mcp-cancel','mcp-empty','mcp-server-list']; missing=[i for i in ids if 'id=\"%s\"'%i not in h]; assert not missing, missing; f=h.index('id=\"settings-form\"'); fe=h.index('</form>', f); assert h.index('id=\"mcp-section\"') > fe; assert 'max-w-2xl' in h; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Verify command prints `ok` (all 14 ids present; `#mcp-section` is after `#settings-form`'s `</form>`)
    - `grep -c "MCP серверы" ui/static/index.html` returns 1 and `grep -c "+ Добавить сервер" ui/static/index.html` returns 1
    - `grep -c "<style" ui/static/index.html` is unchanged from before the edit (still 1)
  </acceptance_criteria>
  <done>Static MCP section exists in the Settings modal with the UI-SPEC copy and styling.</done>
</task>

<task type="auto">
  <name>Task 2: MCP controller in app.js — load/render/CRUD/connect/tools/errors</name>
  <files>ui/static/app.js</files>
  <read_first>
    - ui/static/app.js (constants ~40-70, apiFetch ~72-89, showToast ~57, renderInvariantsPanel ~663-885, renderTaskPanel ~391-521, setupFoldablePanels ~557-568, openSettingsModal ~1396, bindEvents ~1479-1575)
    - .planning/phases/07-mcp-connection-day-16/07-UI-SPEC.md (Component Inventory, copywriting, status colors)
    - ui/static/index.html (ids from Task 1)
  </read_first>
  <action>
    Add a block delimited by the comments `// ---- MCP servers (Phase 7) ----` and `// ---- end MCP servers ----` containing:
    - Constants `MCP_STATUS_BADGE_CLASSES` = {not_connected: 'text-slate-400', connecting: 'text-sky-400', connected: 'text-emerald-400', error: 'text-red-400'} and `MCP_STATUS_LABELS` = {not_connected: 'не подключён', connecting: 'подключение…', connected: 'подключено', error: 'ошибка'}; `MCP_ENV_MASK = '•••'`.
    - State: `state.mcpServers = []`, `state.mcpEditingId = null`, `state.mcpConnecting = new Set()`, `state.mcpExpanded = new Set()` (keeps fold state across re-renders; keys like `tools-{serverId}`, `tool-{serverId}-{toolName}`, `json-{serverId}-{toolName}`, `stderr-{serverId}`).
    - `async function loadMcpServers()`: GET list into state, `renderMcpServers()`.
    - `function renderMcpServers()`: toggle `#mcp-empty` when list empty; rebuild `#mcp-server-list` via `document.createElement` (clearing with `listEl.textContent = ''`); one `renderMcpServerRow(server)` card per server (`rounded-lg bg-slate-800 border border-slate-700 p-3 space-y-2`).
    - `renderMcpServerRow(server)`: header with name (`text-sm font-semibold`), status chip using the badge maps (status `connecting` when `state.mcpConnecting.has(id)`), buttons: `Подключить` (indigo, `px-2 py-1 text-xs rounded`) or `Отключить` when connected (neutral `bg-slate-800 hover:bg-slate-700 border border-slate-700`), `Изменить` (neutral), `Удалить` (`text-red-400 hover:text-red-300`). `Подключить` gets `disabled` + `opacity-50 cursor-not-allowed` when `!server.enabled` (D-13); all row buttons disabled while connecting. Connected: serverInfo line `{name} · v{version} · MCP {protocol_version}` (`text-xs text-slate-500`) and `renderMcpTools(server)`. Error: `error_message` in `text-red-400 text-sm`, and, when `stderr_tail` is non-empty, a fold toggle labelled `Показать журнал сервера (stderr)` revealing `<pre class="text-xs text-slate-400 bg-black/30 rounded-md p-2 overflow-x-auto whitespace-pre-wrap">` with `textContent = stderr_tail` (D-18).
    - `renderMcpTools(server)`: header `Инструменты ({n})` with fold toggle (collapsed by default); each tool a collapsible row: collapsed shows name (`text-sm font-semibold`) + description (`text-xs text-slate-400 truncate`); expanded shows params from `describeMcpParams(tool.input_schema)` — one line per param: name (`font-mono text-xs`), type (`text-slate-500 text-xs`), `*` in `text-red-400` when required, description (`text-xs text-slate-400`) — plus a right-aligned `JSON` button (`text-xs text-slate-400 hover:text-white`) toggling a `<pre>` (same classes as stderr) whose `textContent = JSON.stringify(tool.input_schema, null, 2)` (D-03/D-04).
    - `describeMcpParams(schema)`: returns `[{name, type, required, description}]` from `schema.properties` / `schema.required`; array types joined with `|`, missing type -> `any`.
    - Fold wiring: because `setupFoldablePanels()` binds once at startup and cannot see these dynamic rows, use a local helper `bindMcpFold(button, body, key)` called inside the render functions: toggles `hidden`, swaps `▸`/`▾` text for icon-only toggles, sets `aria-expanded`, and adds/removes `key` in `state.mcpExpanded`; initial state honours `state.mcpExpanded`.
    - Form: `openMcpForm(server)` (null = add) fills `#mcp-name`, `#mcp-command`, `#mcp-args` (args joined by `\n`), `#mcp-env` (`env_keys.map(k => k + '=' + MCP_ENV_MASK).join('\n')`, D-12), `#mcp-cwd`, `#mcp-enabled`, clears `#mcp-form-error`, shows `#mcp-server-form`, sets `state.mcpEditingId`. `closeMcpForm()` hides/clears. `parseMcpArgs(text)`: split on `\n`, strip trailing `\r`, drop fully-empty lines, NO shell parsing (D-11). `parseMcpEnv(text)`: per non-blank line split at FIRST `=`; missing `=` or empty key -> throw `Error('Строка ' + n + ': ожидается KEY=VALUE')`.
    - `saveMcpServer()`: empty name/command -> set `#mcp-form-error` to `Заполните название и команду` and stop (no path validation, D-15). POST when adding, PUT when editing; if the edited server's `connection.status === 'connected'` before the PUT, after success `showToast('Сервер «' + name + '» отключён из-за изменения настроек. Подключите заново.', 'info')` (D-08); otherwise `showToast('Сервер сохранён', 'success')`. Then `closeMcpForm()` and `loadMcpServers()`. API errors -> `#mcp-form-error.textContent = err.message`.
    - `deleteMcpServer(server)`: `confirm()` with `Сервер «{name}» подключён и будет отключён перед удалением. Продолжить?` when connected, else `Удалить сервер «{name}»? Это действие нельзя отменить.`; DELETE; toast; reload.
    - `connectMcpServer(server)`: add id to `state.mcpConnecting`, re-render, POST connect, replace the server in `state.mcpServers` with the response, remove from set (in `finally`), re-render; toast `Подключено: {n} инструментов` on connected, error toast with `error_message` on error; thrown errors (409/network) -> `showToast(err.message, 'error')`. `disconnectMcpServer(server)`: POST disconnect, replace, re-render.
    - Wire in `bindEvents()`: `#btn-mcp-add` -> `openMcpForm(null)`, `#btn-mcp-cancel` -> `closeMcpForm()`, `#btn-mcp-save` -> `saveMcpServer()` (all `.catch(err => showToast(err.message, 'error'))`).
    - At the end of `openSettingsModal()` call `loadMcpServers().catch((err) => showToast(err.message, 'error'))` (D-09 lazy liveness on modal open; must not block showing the modal).
    Sanitization rule: every server-provided string (names, descriptions, params, error_message, detail, stderr_tail, JSON) is set via `textContent`; the MCP block uses `innerHTML` nowhere except wrapped in `DOMPurify.sanitize(...)`.
  </action>
  <verify>
    <automated>python -c "import io,re; s=io.open('ui/static/app.js',encoding='utf-8').read(); a=s.index('// ---- MCP servers (Phase 7) ----'); b=s.index('// ---- end MCP servers ----'); blk=s[a:b]; fns=['loadMcpServers','renderMcpServers','renderMcpServerRow','renderMcpTools','describeMcpParams','bindMcpFold','openMcpForm','closeMcpForm','parseMcpArgs','parseMcpEnv','saveMcpServer','deleteMcpServer','connectMcpServer','disconnectMcpServer']; miss=[f for f in fns if 'function '+f not in blk]; assert not miss, miss; bad=[l for l in blk.splitlines() if 'innerHTML' in l and 'DOMPurify.sanitize' not in l]; assert not bad, bad; assert 'loadMcpServers(' in s[s.index('async function openSettingsModal'):]; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Verify command prints `ok` (all 14 functions present in the MCP block, no unsanitized innerHTML, openSettingsModal calls loadMcpServers)
    - `grep -c "Показать журнал сервера (stderr)" ui/static/app.js` returns 1
    - `grep -c "Инструменты (" ui/static/app.js` returns at least 1
    - `grep -n "shlex\|split(' ')" ui/static/app.js | grep -i mcp` returns no matches
    - `grep -c "'/api/v1/mcp/servers'\|\`/api/v1/mcp/servers" ui/static/app.js` returns at least 4
    - `pytest tests/ -q` exits 0
  </acceptance_criteria>
  <done>The Settings modal fully manages MCP servers: CRUD, connect/disconnect, status badges, serverInfo, collapsible tools with params and JSON, error + stderr display.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent API -> browser DOM | Tool descriptions, inputSchema, stderr and error details originate from an arbitrary child process |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-20 | Tampering (XSS) | renderMcpServerRow / renderMcpTools | mitigate | All server-provided strings via textContent; innerHTML only through DOMPurify.sanitize (verify script rejects any other innerHTML in the MCP block) |
| T-07-21 | Information Disclosure | env editing | mitigate | API never returns env values; UI renders KEY=••• and sends the mask back so the server keeps stored values |
| T-07-22 | Spoofing (CSRF-ish) | MCP POST/PUT/DELETE | accept | Same as the rest of the app: session cookie + existing CORS_ORIGINS allowlist; no new auth surface |
</threat_model>

<verification>
- Both verify scripts print `ok`; `pytest tests/ -q` green
- Visual/interactive confirmation happens in 07-06
</verification>

<success_criteria>
- UI covers MCP-01 (CRUD), MCP-02 (status + serverInfo), MCP-03 (tool list with params), MCP-04 (readable errors + stderr)
</success_criteria>

<output>
Create `.planning/phases/07-mcp-connection-day-16/07-05-SUMMARY.md` when done
</output>
