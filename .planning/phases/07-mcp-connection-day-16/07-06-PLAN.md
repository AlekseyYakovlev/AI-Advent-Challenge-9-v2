---
phase: 07-mcp-connection-day-16
plan: 06
type: execute
wave: 4
depends_on: ["07-04", "07-05"]
files_modified: []
autonomous: false
requirements: [MCP-01, MCP-02, MCP-03, MCP-04, MCP-05, MCP-06]

must_haves:
  truths:
    - "Full pytest suite passes including the MCP tests (success criterion 5)"
    - "CLI against the real filesystem.exe prints serverInfo filesystem-mcp-server and 17 tools (success criterion 4)"
    - "In the running app, a filesystem.exe server config survives restart, is invisible to another user, connects showing serverInfo and 17 tools with params (criteria 1-2) (D-07)"
    - "A bad command path and `filesystem.exe -list <dir>` both show readable errors in the UI while /health stays OK (criterion 3)"
    - "No filesystem.exe process remains after Disconnect or app shutdown"
  artifacts: []
  key_links: []
---

<objective>
End-to-end acceptance of Phase 7 against the real Go filesystem MCP server: automated evidence (full test suite, CLI runs, orphan-process check) followed by a human walkthrough of the Settings UI covering ROADMAP success criteria 1-5.

Purpose: Prove the phase goal in the real app before merging branch Day16.
Output: Verified phase (no code changes unless the walkthrough reveals defects, which are fixed via gap closure).
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-mcp-connection-day-16/07-CONTEXT.md
@.planning/phases/07-mcp-connection-day-16/07-RESEARCH.md
@.planning/phases/07-mcp-connection-day-16/07-03-SUMMARY.md
@.planning/phases/07-mcp-connection-day-16/07-04-SUMMARY.md
@.planning/phases/07-mcp-connection-day-16/07-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated acceptance evidence (tests, CLI against filesystem.exe, orphan check)</name>
  <files>(none — read-only verification)</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 7 success criteria 1-5)
    - .planning/phases/07-mcp-connection-day-16/07-RESEARCH.md (Pitfall 1: use `-list` for PROCESS_EXITED, not a nonexistent dir)
    - scripts/mcp_list_tools.py
  </read_first>
  <action>
    Run and record outputs (for the SUMMARY):
    1. `pytest tests/ -v` — must pass with 0 failures; note MCP test counts (test_mcp_client, test_mcp_config, test_mcp_api, test_mcp_cli) and whether the two real-binary tests passed or skipped.
    2. `python scripts/mcp_list_tools.py C:/Users/Aleksey/go/bin/filesystem.exe C:/Projects/AiAdventAgentV2` — expect exit 0, `Server: filesystem-mcp-server`, `Tools (17):`.
    3. `python scripts/mcp_list_tools.py C:/Users/Aleksey/go/bin/filesystem.exe -list C:/Projects/AiAdventAgentV2` — expect exit 1 and `ERROR PROCESS_EXITED` on stderr.
    4. `python scripts/mcp_list_tools.py C:/nope/missing.exe` — expect exit 1 and `ERROR COMMAND_NOT_FOUND`.
    5. Orphan check: `python -c "import psutil; print([p.pid for p in psutil.process_iter(['name']) if (p.info['name'] or '').lower() == 'filesystem.exe'])"` — expect `[]`.
    If filesystem.exe is missing, stop and report (the demo target is a stated prerequisite). Any failure here is a defect to fix before Task 2.
  </action>
  <verify>
    <automated>pytest tests/ -q && python scripts/mcp_list_tools.py C:/Users/Aleksey/go/bin/filesystem.exe C:/Projects/AiAdventAgentV2</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/ -q` exits 0
    - Step 2 exits 0 and stdout contains `Server: filesystem-mcp-server` and `Tools (17):`
    - Steps 3 and 4 exit 1 with `ERROR PROCESS_EXITED` / `ERROR COMMAND_NOT_FOUND` respectively
    - Step 5 prints `[]`
  </acceptance_criteria>
  <done>Automated evidence for criteria 4 and 5 captured; no orphaned filesystem.exe.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human walkthrough of the MCP Settings UI (criteria 1-3)</name>
  <files>(none)</files>
  <read_first>
    - .planning/phases/07-mcp-connection-day-16/07-UI-SPEC.md
  </read_first>
  <action>Claude starts the app (`python run.py`) in the background and presents the steps below; the user performs them in Chrome and reports.</action>
  <what-built>MCP server management in the Settings modal: CRUD, Connect/Disconnect, status badges, serverInfo, collapsible tool list with params and raw JSON, error messages with stderr tail; REST API and CLI from plans 07-01..07-05.</what-built>
  <how-to-verify>
    1. Open http://localhost:8000, log in, open Настройки (⚙️). Scroll to "MCP серверы" — empty state "Серверы не настроены" is shown.
    2. Click "+ Добавить сервер": Название `filesystem`, Команда `C:\Users\Aleksey\go\bin\filesystem.exe`, Аргументы `C:\Projects\AiAdventAgentV2` (one line), env `DEMO_TOKEN=abc123`. Сохранить. Row appears with badge "не подключён".
    3. Click Изменить: env field shows `DEMO_TOKEN=•••` (value hidden). Cancel.
    4. Click Подключить: badge goes "подключение…" then "подключено"; line shows `filesystem-mcp-server · v1.0.0 · MCP 2024-11-05`; expand "Инструменты (17)"; expand one tool (e.g. read_file) — params with type and `*` for required; click JSON — raw inputSchema appears.
    5. Close the modal, stop the app (Ctrl+C), confirm in Task Manager no filesystem.exe remains; start `python run.py` again, open Settings: the server is still listed with status "не подключён" (criterion 1, D-07).
    6. Log in as a second user (create via 👥 if needed) in a private window: their MCP list is empty (criterion 1 isolation).
    7. As the first user, add a server with Команда `C:\nope\missing.exe`, Подключить — badge "ошибка", message "Команда не найдена. Проверьте путь к исполняемому файлу." Then open http://localhost:8001/health — returns OK (criterion 3).
    8. Add a server with Команда `C:\Users\Aleksey\go\bin\filesystem.exe`, Аргументы two lines `-list` and `C:\Projects\AiAdventAgentV2`, Подключить — "Процесс сервера неожиданно завершился." shown (stderr block appears only if the server wrote stderr); /health still OK.
    9. Connect the working server, then Изменить -> change the name -> Сохранить: toast "…отключён из-за изменения настроек. Подключите заново." and badge "не подключён" (D-08). Untick "Включён" and save: Подключить is disabled (D-13).
    10. Delete a connected server: confirm text mentions it will be disconnected first; row disappears.
  </how-to-verify>
  <verify>
    <human-check>User confirms steps 1-10 behave as described</human-check>
  </verify>
  <acceptance_criteria>
    - User replies "approved", or lists the failing step numbers (each becomes a gap for /bm:plan-phase --gaps)
  </acceptance_criteria>
  <done>User approved the walkthrough; criteria 1-3 observed in the real app.</done>
  <resume-signal>Type "approved" or describe issues (with step numbers)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| none new | Verification-only plan; exercises boundaries defined in 07-01..07-05 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-23 | Denial of Service | orphaned filesystem.exe after shutdown | mitigate | Task 1 step 5 and walkthrough step 5 check that no filesystem.exe process remains |
| T-07-24 | Information Disclosure | cross-user visibility | mitigate | Walkthrough step 6 checks a second user sees an empty list (on top of automated IDOR tests in 07-03) |
</threat_model>

<verification>
- All automated checks in Task 1 pass; user approves Task 2
</verification>

<success_criteria>
- ROADMAP Phase 7 success criteria 1-5 all observed
</success_criteria>

<output>
Create `.planning/phases/07-mcp-connection-day-16/07-06-SUMMARY.md` when done
</output>
