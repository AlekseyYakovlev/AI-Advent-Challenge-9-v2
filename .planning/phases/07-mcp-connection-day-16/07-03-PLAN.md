---
phase: 07-mcp-connection-day-16
plan: 03
type: execute
wave: 2
depends_on: ["07-01", "07-02"]
files_modified:
  - agent/schemas.py
  - agent/main.py
  - tests/test_mcp_api.py
autonomous: true
requirements: [MCP-01, MCP-02, MCP-03, MCP-04, MCP-05]

must_haves:
  truths:
    - "An authenticated user can create, list, edit and delete their MCP server configs over REST; other users get 404 (never 403) and never see them"
    - "Env values are never returned by any MCP endpoint — only env key names"
    - "POST /connect returns status connected with serverInfo and tools, or status error with a fixed error_code, Russian error_message, detail and stderr_tail — always HTTP 200 for connect outcomes"
    - "After any failed connect GET /health still returns 200 (D-19)"
    - "Editing, disabling or deleting a connected server disconnects it first (D-08, D-13); connecting a disabled server returns 409"
    - "Listing servers / fetching status performs the lazy liveness check (D-09)"
    - "Agent shutdown closes all live MCP sessions (lifespan, D-06)"
  artifacts:
    - path: "agent/schemas.py"
      provides: "McpServerCreate, McpServerUpdate, McpServerResponse"
      contains: "class McpServerResponse"
    - path: "agent/main.py"
      provides: "/api/v1/mcp/servers CRUD + connect/disconnect/status + lifespan cleanup"
      contains: "/api/v1/mcp/servers"
    - path: "tests/test_mcp_api.py"
      provides: "REST CRUD, IDOR, connect success/failure, health, lifecycle tests"
      min_lines: 150
  key_links:
    - from: "agent/main.py connect endpoint"
      to: "agent/mcp_client.py::connect_server"
      via: "await mcp_client.connect_server(current_user.id, row.id, ...)"
      pattern: "mcp_client\\.connect_server"
    - from: "agent/main.py lifespan"
      to: "agent/mcp_client.py::cleanup_all_sessions"
      via: "shutdown branch before engine.dispose()"
      pattern: "cleanup_all_sessions"
    - from: "agent/main.py PUT/DELETE"
      to: "agent/mcp_client.py disconnect/cleanup"
      via: "disconnect before DB update/delete"
      pattern: "mcp_client\\.(disconnect_server|cleanup_server)"
---

<objective>
Expose MCP server management and connection over the Agent REST API: user-scoped CRUD, connect/disconnect/status endpoints backed by `agent/mcp_client.py`, lifespan cleanup, plus a full API test suite (success + failure paths, IDOR, /health survival).

Purpose: The UI (07-05) talks only to these endpoints; they enforce ownership, D-08/D-13 auto-disconnect, D-09 lazy liveness and D-19 crash isolation.
Output: new schemas, routes in `agent/main.py`, `tests/test_mcp_api.py`.
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/07-mcp-connection-day-16/07-CONTEXT.md
@.planning/phases/07-mcp-connection-day-16/07-PATTERNS.md
@.planning/phases/07-mcp-connection-day-16/07-01-SUMMARY.md
@.planning/phases/07-mcp-connection-day-16/07-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- From 07-01 (agent/mcp_client.py, agent/schemas.py) — use directly -->
- McpConnectResult(status: McpConnectionStatus, server_info, tools, error_code, error_message, detail, stderr_tail)
- McpConnectionStatus: NOT_CONNECTED="not_connected", CONNECTED="connected", ERROR="error"
- async connect_server(user_id, server_id, command, args, env, cwd) -> McpConnectResult
- async disconnect_server(user_id, server_id) -> McpConnectResult
- async get_status(user_id, server_id) -> McpConnectResult
- is_connected(user_id, server_id) -> bool
- async cleanup_server(user_id, server_id) -> None
- async cleanup_all_sessions() -> None

<!-- From 07-02 (agent/mcp_config.py, shared/models.py) -->
- McpServerConfig(id, user_id, name, command, args_json, env_json, cwd, enabled, created_at, updated_at)
- ENV_MASK = "•••"; load_args(row); load_env(row)
- list_servers(session, user_id); get_server(session, user_id, server_id) -> row | None
- create_server(session, user_id, name, command, args, env, cwd, enabled)
- update_server(session, row, name=, command=, args=, env=, cwd=, cwd_set=, enabled=)
- delete_server(session, row)

<!-- Contracts this plan CREATES (07-05 frontend consumes) -->
REST (all require session cookie via Depends(get_current_user); 401 when missing):
- GET    /api/v1/mcp/servers                      -> 200 list[McpServerResponse]
- POST   /api/v1/mcp/servers   body McpServerCreate -> 201 McpServerResponse
- PUT    /api/v1/mcp/servers/{server_id} body McpServerUpdate -> 200 McpServerResponse
- DELETE /api/v1/mcp/servers/{server_id}          -> 204
- POST   /api/v1/mcp/servers/{server_id}/connect   -> 200 McpServerResponse (connection.status connected|error); 409 if disabled
- POST   /api/v1/mcp/servers/{server_id}/disconnect -> 200 McpServerResponse
- GET    /api/v1/mcp/servers/{server_id}/status    -> 200 McpServerResponse
- Foreign/missing server_id -> 404 {"detail": "MCP server {id} not found"}

McpServerResponse JSON: {id, name, command, args: [str], env_keys: [str], cwd: str|null, enabled: bool, created_at, updated_at, connection: McpConnectResult}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Request/response schemas and MCP REST endpoints + lifespan cleanup</name>
  <files>agent/schemas.py, agent/main.py</files>
  <read_first>
    - agent/schemas.py (GlobalInvariantCreate/Update/Response ~319-345; the Mcp* result models from 07-01)
    - agent/main.py (imports ~1-70; lifespan ~318-327; _get_chat_invariant_or_404 ~128-143; profile endpoints ~811-830; global invariant endpoints ~833-893; lm-studio load-model ~914)
    - agent/mcp_client.py and agent/mcp_config.py (07-01/07-02 outputs)
    - agent/dependencies.py (get_current_user)
  </read_first>
  <action>
    agent/schemas.py — add after the Mcp result models (07-01): constants `MCP_NAME_MAX_LENGTH = 100`, `MCP_COMMAND_MAX_LENGTH = 1000`, `MCP_ARG_MAX_LENGTH = 1000`, `MCP_ARGS_MAX_ITEMS = 50`, `MCP_ENV_MAX_ITEMS = 50`, `MCP_ENV_VALUE_MAX_LENGTH = 4000`, `MCP_CWD_MAX_LENGTH = 1000`; and:
    - `McpServerCreate`: `name: str` (min 1, max 100), `command: str` (min 1, max 1000), `args: list[str] = []` (max 50 items), `env: dict[str, str] = {}` (max 50 items), `cwd: Optional[str] = None` (max 1000), `enabled: bool = True`. `field_validator`s: `name`/`command` stripped and rejected if empty after strip (D-15: no path existence check — any non-empty command is accepted); each arg length <= MCP_ARG_MAX_LENGTH; each env key matches `^[A-Za-z_][A-Za-z0-9_]*$` and each value length <= MCP_ENV_VALUE_MAX_LENGTH.
    - `McpServerUpdate`: same fields all Optional (default None) with the same validators applied when not None.
    - `McpServerResponse`: `id: int, name: str, command: str, args: list[str], env_keys: list[str], cwd: Optional[str], enabled: bool, created_at: datetime, updated_at: datetime, connection: McpConnectResult`. There is deliberately NO env-values field (D-12 masking; UI renders `KEY=•••`).

    agent/main.py:
    - Imports: `from agent import mcp_client, mcp_config` (extend the existing `from agent import invariants, memory, profile, tasks` line), new schemas, `McpServerConfig` from shared.models.
    - `_get_mcp_server_or_404(session, user_id, server_id) -> McpServerConfig`: `mcp_config.get_server`; None -> log `mcp_server_access_denied` (user_id, server_id) and raise 404 `f"MCP server {server_id} not found"` (never 403).
    - `_mcp_server_to_response(row, connection: McpConnectResult) -> McpServerResponse`: `args=mcp_config.load_args(row)`, `env_keys=sorted(mcp_config.load_env(row).keys())`.
    - Endpoints exactly per `<interfaces>`, each with `session: AsyncSession = Depends(get_session)` and `current_user: User = Depends(get_current_user)` and a one-line docstring:
      - GET list: for each row, `connection = await mcp_client.get_status(current_user.id, row.id)` (D-09 lazy liveness).
      - POST create: `mcp_config.create_server(...)` with `body.model_dump()` fields; connection = NOT_CONNECTED result.
      - PUT update: load-or-404; if `mcp_client.is_connected(current_user.id, row.id)` -> `await mcp_client.disconnect_server(...)` BEFORE the DB write (D-08 edit, D-13 disable — any saved edit disconnects); then `mcp_config.update_server(session, row, ..., cwd_set="cwd" in body.model_fields_set)` using `body.model_dump(exclude_unset=True)`; respond with `await mcp_client.get_status(...)`.
      - DELETE: load-or-404; `await mcp_client.cleanup_server(current_user.id, row.id)` first (D-08), then `mcp_config.delete_server`; 204.
      - POST connect: load-or-404; if not `row.enabled` -> 409 `"MCP server is disabled"` (D-13); `result = await mcp_client.connect_server(current_user.id, row.id, row.command, mcp_config.load_args(row), mcp_config.load_env(row) or None, row.cwd)`; return 200 response carrying the result whether connected or error (D-17 codes live in the JSON body). Wrap the call in `try/except Exception as exc` as a last-resort guard that logs `mcp_connect_unexpected_error` (error=str(exc)) and returns a PROTOCOL_ERROR result via `mcp_client.build_error_result`, so no connect path can 500 or crash the Agent (D-19).
      - POST disconnect: load-or-404; `await mcp_client.disconnect_server(...)`.
      - GET status: load-or-404; `await mcp_client.get_status(...)`.
      - Log events `mcp_connect_requested` / `mcp_disconnect_requested` with user_id and server_id only (never env).
    - lifespan: insert `await mcp_client.cleanup_all_sessions()` between `logger.info("agent_shutting_down")` and `await engine.dispose()` (D-06: no orphaned filesystem.exe on shutdown).
  </action>
  <verify>
    <automated>python -c "from agent.main import app; paths = {r.path for r in app.routes}; need = {'/api/v1/mcp/servers', '/api/v1/mcp/servers/{server_id}', '/api/v1/mcp/servers/{server_id}/connect', '/api/v1/mcp/servers/{server_id}/disconnect', '/api/v1/mcp/servers/{server_id}/status'}; assert need <= paths, need - paths; print('ok')"</automated>
  </verify>
  <acceptance_criteria>
    - Verify command prints `ok`
    - `grep -n "cleanup_all_sessions" agent/main.py` matches inside `lifespan`
    - `grep -n "HTTP_409_CONFLICT" agent/main.py` matches
    - `grep -n "HTTP_403" agent/main.py | grep -i mcp` returns no matches
    - `grep -n "env_keys" agent/schemas.py` matches and `grep -n "class McpServerResponse" -A 14 agent/schemas.py | grep -c "env:"` returns 0
    - `pytest tests/ -q` exits 0
  </acceptance_criteria>
  <done>All seven MCP endpoints registered, ownership-checked, env-masked; lifespan closes sessions.</done>
</task>

<task type="auto">
  <name>Task 2: REST API tests — CRUD, IDOR, masking, connect success/failure, /health, lifecycle</name>
  <files>tests/test_mcp_api.py</files>
  <read_first>
    - tests/conftest.py (authenticated_client, second_authenticated_client, client fixtures; cleanup_all_sessions teardown from 07-01)
    - tests/test_profile_api.py and tests/test_invariants_api.py (style, 401 and 404 checks)
    - tests/test_mcp_client.py (FIXTURE path constant and fixture modes from 07-01)
    - agent/main.py (Task 1 endpoints)
  </read_first>
  <action>
    Create tests/test_mcp_api.py (module docstring). Helper `_payload(mode: str = "ok", **overrides) -> dict` building `{"name": "fixture", "command": sys.executable, "args": [FIXTURE, mode], "env": {}, "cwd": None, "enabled": True}`; helper `_create(client, **kw) -> dict`. Tests (all async):
    - `test_mcp_endpoints_require_auth`: unauthenticated `client` GET `/api/v1/mcp/servers` and POST -> 401.
    - `test_crud_roundtrip`: create (201, connection.status "not_connected"), list contains it, PUT name -> updated, DELETE 204, list empty.
    - `test_validation_rejects_blank_command_and_bad_env_key`: command "   " -> 422; env key "1BAD" -> 422.
    - `test_env_values_never_returned`: create with env `{"SECRET_TOKEN": "supersecret123"}` -> `"supersecret123" not in resp.text`, `env_keys == ["SECRET_TOKEN"]`; same for GET list, PUT and status responses.
    - `test_update_with_masked_env_preserves_value`: PUT env `{"SECRET_TOKEN": "•••", "B": "2"}` then load the row via `async_session_factory` + `mcp_config.load_env` -> `{"SECRET_TOKEN": "supersecret123", "B": "2"}`.
    - `test_cross_user_access_returns_404`: first user creates; `second_authenticated_client` gets 404 on GET status, PUT, DELETE, POST connect, POST disconnect; its list is `[]`; first user's row still exists.
    - `test_connect_fixture_server_success` (MCP-02/03): POST connect -> 200, connection.status "connected", server_info.name "fixture-server", `{t["name"] for t in tools} == {"echo", "add"}`, each tool has `input_schema` dict; GET list shows same connected status and tools.
    - `test_connect_bad_command_reports_error_and_health_ok` (MCP-04, SC3): command `C:/definitely/not/here/nope.exe` -> 200, status "error", error_code "COMMAND_NOT_FOUND", error_message starts with "Команда не найдена"; then GET `/health` -> 200.
    - `test_connect_exiting_server_reports_process_exited_and_health_ok`: mode "garbage" -> error_code "PROCESS_EXITED"; `/health` 200; then a subsequent connect of an "ok" server by the same client succeeds (Agent unaffected).
    - `test_update_connected_server_disconnects` (D-08): connect, PUT name -> response connection.status "not_connected"; `mcp_client.is_connected(user_id, id)` False.
    - `test_disable_connected_server_disconnects_and_connect_rejected` (D-13): connect, PUT `{"enabled": false}` -> not_connected; POST connect -> 409.
    - `test_delete_connected_server_cleans_registry`: connect then DELETE -> 204 and `mcp_client.is_connected` False.
    - `test_disconnect_endpoint`: connect, POST disconnect -> "not_connected".
    - `test_status_detects_exited_server` (D-09): mode "die_after", connect -> connected; `await asyncio.sleep(2.5)`; GET status -> status "error", error_code "PROCESS_EXITED".
    - `test_status_after_failed_connect_shows_error`: failed connect then GET list -> that row's connection.status "error".
    Use `authenticated_client.seeded_user_id` for direct mcp_client assertions.
  </action>
  <verify>
    <automated>pytest tests/test_mcp_api.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_mcp_api.py -v` exits 0 with at least 14 passed tests
    - `grep -n "second_authenticated_client" tests/test_mcp_api.py` matches
    - `grep -n "/health" tests/test_mcp_api.py` returns at least 2 matches
    - `pytest tests/ -q` exits 0
  </acceptance_criteria>
  <done>REST layer proven for CRUD, scoping, masking, both connect outcomes, D-08/D-09/D-13 and /health survival.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Agent REST | Untrusted JSON bodies and path ids; session cookie auth |
| Agent -> child process | Only the owner's stored config is launched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Spoofing | all /api/v1/mcp/* | mitigate | Depends(get_current_user) on every endpoint (HTTP-only session cookie); 401 test |
| T-07-11 | Elevation of Privilege / IDOR | server_id path param | mitigate | _get_mcp_server_or_404 checks row.user_id == current_user.id, 404 never 403; registry keyed by (user_id, server_id); cross-user test covers all 5 id routes |
| T-07-12 | Elevation of Privilege | arbitrary command execution via config | accept | Every account is admin in this local-first app (CLAUDE.md); a user can only launch their own configs; spawn is argv-list (no shell); D-15 intentionally skips path validation |
| T-07-13 | Information Disclosure | env secrets in responses/logs | mitigate | McpServerResponse has env_keys only; logs carry user_id/server_id only; test_env_values_never_returned |
| T-07-14 | Denial of Service | connect failures crashing Agent | mitigate | Service returns typed results; endpoint last-resort except Exception -> PROTOCOL_ERROR result; /health tests after failures |
| T-07-15 | Denial of Service | oversized configs | mitigate | Pydantic limits: name 100, command 1000, 50 args x 1000, 50 env x 4000, cwd 1000 |
| T-07-16 | Denial of Service | leaked child processes on shutdown/edit/delete | mitigate | lifespan cleanup_all_sessions; PUT/DELETE disconnect first |
</threat_model>

<verification>
- `pytest tests/test_mcp_api.py -v` green
- `pytest tests/ -q` green
</verification>

<success_criteria>
- MCP-01..MCP-04 behavior reachable over REST with ownership enforcement and crash isolation; MCP-05 API-level tests present
</success_criteria>

<output>
Create `.planning/phases/07-mcp-connection-day-16/07-03-SUMMARY.md` when done
</output>
