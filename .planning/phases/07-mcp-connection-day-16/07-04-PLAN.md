---
phase: 07-mcp-connection-day-16
plan: 04
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - scripts/mcp_list_tools.py
  - tests/test_mcp_cli.py
autonomous: true
requirements: [MCP-06, MCP-05]

must_haves:
  truths:
    - "`python scripts/mcp_list_tools.py <command> [args…]` prints a serverInfo header and every tool with its description and parameters"
    - "On failure the script prints the error code, Russian message, detail and stderr tail to stderr and exits with code 1"
    - "The script reuses agent.mcp_client.connect_once_and_list (single implementation, D-20) — no duplicated connect logic"
    - "The script runs from the repo root without PYTHONPATH tweaks and its stdout contains no JSON log lines"
  artifacts:
    - path: "scripts/mcp_list_tools.py"
      provides: "Standalone CLI connect + list_tools"
      contains: "connect_once_and_list"
    - path: "tests/test_mcp_cli.py"
      provides: "CLI success/failure/usage tests against the fixture server"
      min_lines: 50
  key_links:
    - from: "scripts/mcp_list_tools.py"
      to: "agent/mcp_client.py::connect_once_and_list"
      via: "direct import, no HTTP"
      pattern: "from agent.mcp_client import .*connect_once_and_list"
---

<objective>
Create the standalone CLI `scripts/mcp_list_tools.py <command> [args…]` (D-20) that connects to an MCP server via the same `agent.mcp_client.connect_once_and_list` used by the REST path, prints serverInfo and the tool list, and exits 1 with code/message/stderr tail on failure. Plus tests.

Purpose: MCP-06 / success criterion 4 — console proof of connect + list_tools.
Output: `scripts/mcp_list_tools.py`, `tests/test_mcp_cli.py`.
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-mcp-connection-day-16/07-CONTEXT.md
@.planning/phases/07-mcp-connection-day-16/07-PATTERNS.md
@.planning/phases/07-mcp-connection-day-16/07-01-SUMMARY.md
@CLAUDE.md

<interfaces>
From agent/mcp_client.py (07-01):
- async def connect_once_and_list(command: str, args: list[str], env: dict[str, str] | None = None, cwd: str | None = None, timeout_seconds: float | None = None) -> McpConnectResult
From agent/schemas.py (07-01):
- McpConnectResult: status (McpConnectionStatus), server_info (name, version, protocol_version), tools [McpToolInfo(name, description, input_schema)], error_code, error_message, detail, stderr_tail
From shared/logger.py:
- def setup_logging(level: int = logging.INFO) -> None   (structlog JSON to stdout)

Contract this plan CREATES (in scripts/mcp_list_tools.py):
- def describe_params(input_schema: dict[str, Any]) -> list[tuple[str, str, bool, str]]   # (name, type, required, description)
- def format_result(result: McpConnectResult) -> str
- async def run(argv: list[str]) -> int     # 0 success, 1 connect failure, 2 usage error
- def main() -> None
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: scripts/mcp_list_tools.py CLI with tests</name>
  <files>scripts/mcp_list_tools.py, tests/test_mcp_cli.py</files>
  <read_first>
    - agent/mcp_client.py (connect_once_and_list, 07-01)
    - agent/schemas.py (McpConnectResult and friends)
    - run.py (print()-based CLI output — the CLAUDE.md-documented print exception)
    - migrate_strategies.py (one-shot asyncio.run script importing app modules)
    - shared/logger.py (setup_logging / get_logger behavior: logs go to stdout)
    - tests/test_mcp_client.py (FIXTURE constant, fixture modes)
  </read_first>
  <behavior>
    - test_run_success_prints_server_and_tools: `await run([sys.executable, FIXTURE, "ok"])` returns 0; captured stdout contains "Server: fixture-server", "Protocol: ", "Tools (2):", "- echo: Echo the given text back.", and a param line containing "text" and "string" and "*"
    - test_run_bad_command_exits_1: `await run(["C:/definitely/not/here/nope.exe"])` returns 1; captured stderr contains "COMMAND_NOT_FOUND" and "Команда не найдена"; stdout is empty
    - test_run_process_exited_shows_stderr_tail: `await run([sys.executable, FIXTURE, "stderr_exit"])` returns 1; stderr contains "PROCESS_EXITED" and "stderr line 25"
    - test_run_without_args_is_usage_error: `await run([])` returns 2 and stderr contains "Usage"
    - test_describe_params_handles_types: schema {"properties": {"a": {"type": "integer", "description": "first"}, "b": {"type": ["string", "null"]}, "c": {}}, "required": ["a"]} -> [("a","integer",True,"first"), ("b","string|null",False,""), ("c","any",False,"")]
    - test_script_runs_as_subprocess: `asyncio.create_subprocess_exec(sys.executable, "scripts/mcp_list_tools.py", sys.executable, FIXTURE, "ok", cwd=<repo root>, stdout=PIPE, stderr=PIPE)` -> returncode 0, first stdout line starts with "Server:" (no JSON log noise), output contains "echo"
  </behavior>
  <action>
    Write tests/test_mcp_cli.py first (load the script with `importlib.util.spec_from_file_location("mcp_list_tools", REPO_ROOT / "scripts" / "mcp_list_tools.py")` so importing never runs `main()`; use `capsys`), confirm RED, then implement.

    scripts/mcp_list_tools.py (new `scripts/` directory; no `__init__.py`):
    - Module docstring with usage line `python scripts/mcp_list_tools.py <command> [args...]`.
    - Before local imports: `sys.path.insert(0, str(Path(__file__).resolve().parent.parent))` so `agent`/`shared` import from the repo root (the script dir is sys.path[0] otherwise). Then `from agent.mcp_client import connect_once_and_list` (D-20: single implementation — do NOT copy any stdio_client/ClientSession code here) and `from shared.logger import setup_logging`.
    - `describe_params`: iterate `input_schema.get("properties", {})` in insertion order; type = "|".join when list, "any" when missing; required from `input_schema.get("required", [])`; description default "".
    - `format_result` (success): lines `Server: {name}`, `Version: {version}`, `Protocol: {protocol_version}`, blank, `Tools ({n}):`, then per tool a blank line, `- {name}: {description or ''}` and per param `    {name} ({type}){' *' if required else ''}: {description}`; tools with no params print `    (no parameters)`.
    - `run(argv)`: empty argv -> write `Usage: python scripts/mcp_list_tools.py <command> [args...]` to stderr, return 2. Else `result = await connect_once_and_list(argv[0], argv[1:])`. Status connected -> `print(format_result(result))`, return 0. Otherwise print to `sys.stderr`: `ERROR {error_code}: {error_message}`, `Detail: {detail}` and, when stderr_tail non-empty, `--- stderr (last 20 lines) ---` followed by the tail; return 1. `print()` is allowed here as intentional console output (same CLAUDE.md exception as run.py); no structlog for user-facing output.
    - `main()`: `sys.stdout.reconfigure(encoding="utf-8")` and same for stderr (Windows console + Russian messages); call `setup_logging(logging.CRITICAL)` and `logging.getLogger().setLevel(logging.CRITICAL)` so neither structlog JSON lines from agent.mcp_client nor the mcp SDK's stdlib `logger.exception` (RESEARCH Pitfall 3) pollute CLI output; then `sys.exit(asyncio.run(run(sys.argv[1:])))`. Guard with `if __name__ == "__main__": main()`. Logging config lives in `main()` only, so importing the module in tests does not reconfigure global logging.
  </action>
  <verify>
    <automated>pytest tests/test_mcp_cli.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_mcp_cli.py -v` exits 0 with at least 6 passed tests
    - `grep -n "from agent.mcp_client import" scripts/mcp_list_tools.py` matches and `grep -c "stdio_client\|ClientSession" scripts/mcp_list_tools.py` returns 0
    - `python scripts/mcp_list_tools.py C:/definitely/not/here/nope.exe; echo "exit=$?"` prints `exit=1` and `ERROR COMMAND_NOT_FOUND` on stderr
    - `python scripts/mcp_list_tools.py` (no args) exits with code 2
    - If `C:\Users\Aleksey\go\bin\filesystem.exe` exists: `python scripts/mcp_list_tools.py C:/Users/Aleksey/go/bin/filesystem.exe .` exits 0 and stdout contains `Server: filesystem-mcp-server` and `Tools (17):`
    - `pytest tests/ -q` exits 0
  </acceptance_criteria>
  <done>CLI proves connect + list_tools from the console via the shared implementation; failure path exits 1 with code/message/stderr tail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI user -> child process | Operator runs an arbitrary local command of their choosing |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-17 | Elevation of Privilege | CLI command argv | accept | Operator-invoked local tool running with the operator's own privileges; argv passed as list, no shell |
| T-07-18 | Denial of Service | hung server | mitigate | Inherits MCP_CONNECT_TIMEOUT and cleanup from connect_once_and_list |
| T-07-19 | Information Disclosure | env/log noise to console | mitigate | CLI sets logging to CRITICAL in main(); env not accepted by CLI |
</threat_model>

<verification>
- `pytest tests/test_mcp_cli.py -v` green; `pytest tests/ -q` green
</verification>

<success_criteria>
- MCP-06 satisfied; success criterion 4 reproducible against filesystem.exe
</success_criteria>

<output>
Create `.planning/phases/07-mcp-connection-day-16/07-04-SUMMARY.md` when done
</output>
