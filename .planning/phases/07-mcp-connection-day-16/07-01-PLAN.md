---
phase: 07-mcp-connection-day-16
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - shared/config.py
  - agent/schemas.py
  - tests/fixtures/mcp_stdio_server.py
  - agent/mcp_client.py
  - tests/test_mcp_client.py
  - tests/conftest.py
autonomous: true
requirements: [MCP-02, MCP-03, MCP-04, MCP-05]

must_haves:
  truths:
    - "Connecting to a working stdio MCP server returns status connected, serverInfo (name, version, protocolVersion) and the full tool list with name, description and inputSchema"
    - "A nonexistent command, a server that exits, a hung server and a protocol-violating server each produce a distinct fixed error code (COMMAND_NOT_FOUND, PROCESS_EXITED, HANDSHAKE_TIMEOUT, PROTOCOL_ERROR) with a Russian message, never a raw ExceptionGroup"
    - "A connected session stays alive in an in-memory registry keyed by (user_id, server_id) until disconnect, and its ClientSession is usable from other asyncio tasks"
    - "A status check on a session whose server process died flips it to error PROCESS_EXITED (lazy liveness, D-09)"
    - "No failed or closed connect leaves an orphaned child process or an unclosed stderr temp file"
    - "The default pytest suite passes without filesystem.exe installed; real-binary tests skip when it is absent"
  artifacts:
    - path: "agent/mcp_client.py"
      provides: "open_session / connect_once_and_list / connect_server / disconnect_server / get_status / cleanup_* + classify_mcp_error + owner-task registry"
      exports: ["classify_mcp_error", "open_session", "connect_once_and_list", "connect_server", "disconnect_server", "get_status", "is_connected", "get_live_session", "cleanup_server", "cleanup_user_sessions", "cleanup_all_sessions"]
    - path: "agent/schemas.py"
      provides: "McpConnectionStatus, McpErrorCode, McpServerInfo, McpToolInfo, McpConnectResult"
      contains: "class McpConnectResult"
    - path: "tests/fixtures/mcp_stdio_server.py"
      provides: "Portable Python stdio MCP fixture server with modes ok/garbage/hang/bad_protocol/die_after/stderr_exit"
    - path: "tests/test_mcp_client.py"
      provides: "connect + list_tools success and failure-path tests"
      min_lines: 120
    - path: "requirements.txt"
      contains: "mcp==1.30.0"
    - path: "shared/config.py"
      contains: "MCP_CONNECT_TIMEOUT"
  key_links:
    - from: "agent/mcp_client.py"
      to: "mcp.client.stdio.stdio_client"
      via: "owner task entering stdio_client + ClientSession in one task"
      pattern: "stdio_client\\(.*errlog="
    - from: "agent/mcp_client.py"
      to: "shared/config.py"
      via: "settings.MCP_CONNECT_TIMEOUT default timeout"
      pattern: "MCP_CONNECT_TIMEOUT"
    - from: "tests/conftest.py"
      to: "agent/mcp_client.py"
      via: "autouse teardown calls cleanup_all_sessions"
      pattern: "cleanup_all_sessions"
---

<objective>
Build the Agent-side MCP stdio client core: dependency pin, timeout config, typed result schemas, a portable fixture MCP server, and `agent/mcp_client.py` implementing the owner-task persistent session registry (D-05/D-06), single-timeout handshake (D-16), error classification into fixed codes (D-17), stderr tail capture (D-18) and lazy liveness (D-09). This is the single connect/list implementation reused by the REST layer (07-03) and the CLI (07-04, D-20).

Purpose: Every other plan in this phase calls into this module; it must be correct, tested and leak-free first.
Output: `agent/mcp_client.py`, result schemas, `tests/fixtures/mcp_stdio_server.py`, `tests/test_mcp_client.py`, pinned `mcp` dependency.
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-mcp-connection-day-16/07-CONTEXT.md
@.planning/phases/07-mcp-connection-day-16/07-RESEARCH.md
@.planning/phases/07-mcp-connection-day-16/07-PATTERNS.md
@CLAUDE.md

<interfaces>
<!-- Contracts this plan CREATES. Plans 07-03 and 07-04 code against these exactly. -->

agent/schemas.py (new, append near ModelLoadStatus/ModelLoadResult):
- class McpConnectionStatus(str, Enum): NOT_CONNECTED = "not_connected", CONNECTED = "connected", ERROR = "error"
  ("connecting" is a UI-only transient state, never returned by the API)
- class McpErrorCode(str, Enum): COMMAND_NOT_FOUND, PROCESS_EXITED, HANDSHAKE_TIMEOUT, PROTOCOL_ERROR (value == name)
- class McpServerInfo(BaseModel): name: str; version: str; protocol_version: str
- class McpToolInfo(BaseModel): name: str; description: Optional[str] = None; input_schema: dict[str, Any]
- class McpConnectResult(BaseModel): status: McpConnectionStatus; server_info: Optional[McpServerInfo] = None; tools: list[McpToolInfo] = []; error_code: Optional[McpErrorCode] = None; error_message: Optional[str] = None; detail: Optional[str] = None; stderr_tail: Optional[str] = None

agent/mcp_client.py (new):
- STDERR_TAIL_LINES = 20; LIVENESS_TIMEOUT = 2.0; CLOSE_TIMEOUT = 5.0; READY_GRACE_SECONDS = 5.0
- MCP_ERROR_MESSAGES: dict[McpErrorCode, str] (Russian copy from 07-UI-SPEC.md error table; HANDSHAKE_TIMEOUT contains "{timeout}")
- class SessionHandle: session: ClientSession | None; result: McpConnectResult | None; ready/close_requested/closed: asyncio.Event; error: BaseException | None; task: asyncio.Task | None; errfile (tempfile.TemporaryFile, text mode)
- def classify_mcp_error(exc: BaseException) -> tuple[McpErrorCode, str]
- def read_stderr_tail(errfile, max_lines: int = STDERR_TAIL_LINES) -> str
- def build_error_result(code: McpErrorCode, detail: str, stderr_tail: str, timeout_seconds: float) -> McpConnectResult
- async def open_session(command: str, args: list[str], env: dict[str, str] | None, cwd: str | None, timeout_seconds: float | None = None) -> tuple[SessionHandle | None, McpConnectResult]
- async def close_handle(handle: SessionHandle) -> None
- async def connect_once_and_list(command: str, args: list[str], env: dict[str, str] | None = None, cwd: str | None = None, timeout_seconds: float | None = None) -> McpConnectResult
- async def connect_server(user_id: int, server_id: int, command: str, args: list[str], env: dict[str, str] | None, cwd: str | None) -> McpConnectResult
- async def disconnect_server(user_id: int, server_id: int) -> McpConnectResult   (idempotent, returns NOT_CONNECTED result)
- async def get_status(user_id: int, server_id: int) -> McpConnectResult   (lazy liveness, D-09)
- def is_connected(user_id: int, server_id: int) -> bool
- def get_live_session(user_id: int, server_id: int) -> ClientSession | None   (reuse hook for MCP-F1, not used by LLM this phase)
- async def cleanup_server(user_id: int, server_id: int) -> None
- async def cleanup_user_sessions(user_id: int) -> None
- async def cleanup_all_sessions() -> None

Existing, from agent/schemas.py (pattern to mirror):
- class ModelLoadStatus(str, Enum) / class ModelLoadResult(BaseModel) at ~line 106-120 — typed result, service never raises to caller.

From mcp 1.30.0 (verified in RESEARCH.md):
- from mcp import ClientSession, StdioServerParameters
- from mcp.client.stdio import stdio_client   # stdio_client(server: StdioServerParameters, errlog=<real OS file>)
- from mcp.shared.exceptions import McpError
- StdioServerParameters(command=str, args=list[str], env=dict[str,str] | None, cwd=str | None)  # SDK merges env over its safe default env
- ClientSession.initialize() -> InitializeResult (.serverInfo.name, .serverInfo.version, .protocolVersion)
- ClientSession.list_tools() -> ListToolsResult (.tools: list[Tool] with .name, .description, .inputSchema)
- ClientSession.send_ping()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin mcp, add MCP_CONNECT_TIMEOUT, result schemas, and the fixture stdio server</name>
  <files>requirements.txt, shared/config.py, agent/schemas.py, tests/fixtures/mcp_stdio_server.py</files>
  <read_first>
    - requirements.txt
    - shared/config.py
    - agent/schemas.py (ModelLoadStatus / ModelLoadResult ~lines 100-125 are the pattern to mirror)
    - .planning/phases/07-mcp-connection-day-16/07-RESEARCH.md (Standard Stack, Pitfall 1, Code Examples)
    - .planning/phases/07-mcp-connection-day-16/07-UI-SPEC.md (error code -> Russian message table)
  </read_first>
  <action>
    Branch first: if `git branch --show-current` is `main`, create and switch to branch `Day16` (per PROJECT.md branching constraint); if `Day16` already exists, switch to it.

    1. requirements.txt: add a new section header block `# MCP INTEGRATION` (same `# ====` banner style) before the TESTING section containing the line `mcp==1.30.0               # Model Context Protocol SDK (stdio client, Day 16)`. Exact pin 1.30.0 per RESEARCH.md (do NOT use 2.x). Run `pip install mcp==1.30.0` (no-op if installed).

    2. shared/config.py: add field `MCP_CONNECT_TIMEOUT: float = 10.0` to `Settings` after `LLM_TIMEOUT` (D-16, .env-overridable, not per-server).

    3. agent/schemas.py: add `McpConnectionStatus`, `McpErrorCode`, `McpServerInfo`, `McpToolInfo`, `McpConnectResult` exactly as listed in the `<interfaces>` block, placed directly after `ModelLoadResult`, each with a one-line docstring. Add `Any` to the typing import if missing. Use `Field(default_factory=list)` for `tools`.

    4. tests/fixtures/mcp_stdio_server.py (new directory, no `__init__.py` needed — it is executed as a script, not imported): a standalone script run as `python tests/fixtures/mcp_stdio_server.py <mode>`. Module docstring. Use `sys.stdout.write`/`sys.stderr.write` (no `print()`). Modes selected by `sys.argv[1]`:
       - `ok`: write `fixture-server starting\n` to stderr and flush; build `FastMCP("fixture-server")` (from `mcp.server.fastmcp import FastMCP`) with two tools: `echo(text: str) -> str` (docstring "Echo the given text back.") and `add(a: int, b: int = 0) -> int` (docstring "Add two integers."); call `.run(transport="stdio")`.
       - `die_after`: same server as `ok` but first start `threading.Timer(1.0, os._exit, args=(3,))` (daemon) so the process dies ~1 s after start — used by liveness tests.
       - `garbage`: write `this is not json-rpc\n` to stdout, flush, write `fatal: fixture exiting\n` to stderr, exit 0 (reproduces PROCESS_EXITED like `filesystem.exe -list`, per RESEARCH Pitfall 1).
       - `stderr_exit`: write 25 lines `stderr line 1` .. `stderr line 25` to stderr, flush, exit 1 (stderr-tail test).
       - `hang`: `time.sleep(120)` without writing anything (HANDSHAKE_TIMEOUT).
       - `bad_protocol`: read one line from stdin, `json.loads` it, reply on stdout with one line `{"jsonrpc":"2.0","id":<same id>,"result":{"protocolVersion":"1999-01-01","capabilities":{},"serverInfo":{"name":"bad","version":"0"}}}` + newline, flush, then keep reading stdin until EOF (PROTOCOL_ERROR via unsupported version).
       - unknown mode: write usage to stderr, exit 2.
  </action>
  <verify>
    <automated>python -c "from agent.schemas import McpConnectResult, McpErrorCode, McpConnectionStatus; from shared.config import settings; assert settings.MCP_CONNECT_TIMEOUT == 10.0; assert McpErrorCode.PROCESS_EXITED.value == 'PROCESS_EXITED'; print('ok')" && python tests/fixtures/mcp_stdio_server.py garbage</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^mcp==1.30.0" requirements.txt` returns 1
    - `grep -n "MCP_CONNECT_TIMEOUT: float = 10.0" shared/config.py` matches
    - `grep -n "class McpConnectResult\|class McpErrorCode\|class McpConnectionStatus\|class McpToolInfo\|class McpServerInfo" agent/schemas.py` returns 5 lines
    - `python tests/fixtures/mcp_stdio_server.py garbage` prints `this is not json-rpc` and exits 0
    - `python tests/fixtures/mcp_stdio_server.py stderr_exit` exits with code 1
    - `grep -c "print(" tests/fixtures/mcp_stdio_server.py` returns 0
    - `git branch --show-current` prints `Day16`
  </acceptance_criteria>
  <done>mcp pinned, timeout configurable, result schemas importable, fixture server runnable in all six modes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement agent/mcp_client.py (owner-task registry, timeout, classification, liveness) with tests</name>
  <files>agent/mcp_client.py, tests/test_mcp_client.py, tests/conftest.py</files>
  <read_first>
    - .planning/phases/07-mcp-connection-day-16/07-RESEARCH.md (Pattern 1 owner task, Pattern 2 classification table, Pattern 3 errlog temp file, Pitfalls 1-4)
    - .planning/phases/07-mcp-connection-day-16/07-PATTERNS.md (mcp_client.py section: LMStudioClient typed-result pattern, agent/state.py registry pattern)
    - agent/llm_client.py (LMStudioClient: lock + typed-result error handling style)
    - agent/state.py (registry dict + cleanup function style)
    - agent/schemas.py (the Mcp* models added in Task 1)
    - tests/conftest.py (clean_test_db autouse fixture — you will extend its teardown)
    - tests/test_lm_studio_client.py (test structure/naming style)
  </read_first>
  <behavior>
    - test_connect_once_success_fixture: connect_once_and_list(sys.executable, [FIXTURE, "ok"]) -> status CONNECTED, server_info.name == "fixture-server", protocol_version non-empty, tool names == {"echo", "add"}, add.input_schema["required"] contains "a", echo.description == "Echo the given text back."
    - test_command_not_found: command "C:/definitely/not/here/nope.exe" -> status ERROR, error_code COMMAND_NOT_FOUND, error_message == MCP_ERROR_MESSAGES[COMMAND_NOT_FOUND]; neither error_message nor detail contains "TaskGroup" or "ExceptionGroup"
    - test_process_exited_garbage: mode "garbage" -> error_code PROCESS_EXITED
    - test_stderr_tail_last_20_lines: mode "stderr_exit" -> PROCESS_EXITED, stderr_tail.splitlines() has exactly 20 lines, last line "stderr line 25", first line "stderr line 6"
    - test_handshake_timeout: mode "hang", timeout_seconds=1.5 -> HANDSHAKE_TIMEOUT, error_message contains "1.5 с", elapsed wall-clock < 1.5 + 2.0 + 4.0
    - test_protocol_error_bad_version: mode "bad_protocol" -> PROTOCOL_ERROR
    - test_classify_mcp_error_unwraps_nested_groups: nested ExceptionGroup(ExceptionGroup(McpError(ErrorData(code=-32000, message="Connection closed")))) -> PROCESS_EXITED; FileNotFoundError -> COMMAND_NOT_FOUND; TimeoutError -> HANDSHAKE_TIMEOUT; BrokenPipeError -> PROCESS_EXITED; RuntimeError("Unsupported protocol version from the server: x") -> PROTOCOL_ERROR
    - test_connect_server_registry_and_disconnect: connect_server(1, 1, ...ok) -> CONNECTED; is_connected(1, 1) True; get_live_session(1, 1).list_tools() awaited from the test task returns 2 tools (cross-task use); disconnect_server(1, 1) -> NOT_CONNECTED; is_connected False; get_live_session None
    - test_connect_server_twice_replaces_session: second connect_server on same key leaves exactly one registry entry and the first handle's closed event is set
    - test_registry_is_user_scoped: connect_server(1, 1, ok); is_connected(2, 1) is False and get_status(2, 1).status == NOT_CONNECTED
    - test_get_status_detects_exited_process: connect_server with mode "die_after", await asyncio.sleep(2.5), get_status -> ERROR with error_code PROCESS_EXITED; is_connected False afterwards
    - test_failed_connect_status_is_error_then_disconnect_clears: failed connect_server -> get_status returns ERROR with same code; disconnect_server -> get_status NOT_CONNECTED
    - test_cleanup_all_sessions_closes_everything: two connected keys -> cleanup_all_sessions() -> both handles' closed events set, no keys connected
    - test_real_filesystem_server (skipif filesystem.exe absent; path from env MCP_FILESYSTEM_EXE, default C:\Users\Aleksey\go\bin\filesystem.exe): connect_once_and_list(exe, [str(tmp_path)]) -> server_info.name == "filesystem-mcp-server", len(tools) == 17
    - test_real_filesystem_list_flag_exits (same skipif): args ["-list", str(tmp_path)] -> PROCESS_EXITED
  </behavior>
  <action>
    Write tests/test_mcp_client.py first (RED, run and see failures), then implement agent/mcp_client.py (GREEN). Module constant in tests: `FIXTURE = str(Path(__file__).parent / "fixtures" / "mcp_stdio_server.py")`; always spawn with `sys.executable` so tests never depend on filesystem.exe.

    agent/mcp_client.py — module docstring, `logger = get_logger(__name__)`, stdlib -> third-party -> local imports, full type hints, implement exactly the `<interfaces>` contract:

    - `MCP_ERROR_MESSAGES` (Russian, from 07-UI-SPEC.md): COMMAND_NOT_FOUND "Команда не найдена. Проверьте путь к исполняемому файлу.", PROCESS_EXITED "Процесс сервера неожиданно завершился.", HANDSHAKE_TIMEOUT "Превышено время ожидания подключения ({timeout} с).", PROTOCOL_ERROR "Ошибка протокола MCP при подключении к серверу.". `build_error_result` formats `{timeout}` with the timeout as `int` when integral else as-is (10.0 -> "10", 1.5 -> "1.5").

    - `classify_mcp_error` (D-17): first flatten `BaseExceptionGroup` recursively to leaves (`_flatten_exception_group`), then check leaves in priority order: any `TimeoutError` -> HANDSHAKE_TIMEOUT; any `BrokenPipeError`/`ConnectionResetError`/`EOFError` -> PROCESS_EXITED; any `FileNotFoundError`/`PermissionError`/other `OSError` -> COMMAND_NOT_FOUND; any leaf whose `str()` contains "Connection closed" -> PROCESS_EXITED; otherwise PROTOCOL_ERROR. `detail` = "; ".join of `f"{type(leaf).__name__}: {leaf}"` for leaves, truncated to 2000 chars. Never return the ExceptionGroup's own str.

    - Owner task `_owner_task(handle, params, timeout_seconds)` per RESEARCH Pattern 1 — the ONLY place that enters the context managers (anyio cancel-scope rule): `async with asyncio.timeout(timeout_seconds) as deadline:` wrapping `async with stdio_client(params, errlog=handle.errfile) as (read, write):` and `async with ClientSession(read, write) as session:`; inside: `init = await session.initialize()`, `tools = await session.list_tools()` (both inside the timeout, D-16), build `McpConnectResult(status=CONNECTED, server_info=McpServerInfo(name=init.serverInfo.name, version=init.serverInfo.version, protocol_version=str(init.protocolVersion)), tools=[McpToolInfo(name=t.name, description=t.description, input_schema=t.inputSchema) ...])`, set `handle.session`, `handle.result`, call `deadline.reschedule(None)`, `handle.ready.set()`, then `await handle.close_requested.wait()`. `except Exception as exc: handle.error = exc` (ExceptionGroup is an Exception subclass; do not catch CancelledError). `finally: handle.session = None; handle.ready.set(); handle.closed.set()`.

    - `errfile` = `tempfile.TemporaryFile(mode="w+", encoding="utf-8", errors="replace")` created per handle (RESEARCH Pattern 3 — never io.StringIO; Windows needs a real fd). `read_stderr_tail`: `errfile.flush()`, `seek(0)`, read, `splitlines()[-max_lines:]`, join with "\n"; wrap in `except (OSError, ValueError)` returning "".

    - `open_session`: timeout defaults to `settings.MCP_CONNECT_TIMEOUT`; `StdioServerParameters(command=command, args=args, env=env or None, cwd=cwd or None)` — pass the stored env dict straight through (SDK merges it over its safe default env incl. PATH — D-12; do NOT splice os.environ). Create handle + `asyncio.create_task(_owner_task(...))`, then `await asyncio.wait_for(handle.ready.wait(), timeout_seconds + READY_GRACE_SECONDS)`; if that safety wait itself times out, cancel the task, await it suppressing `asyncio.CancelledError`, and return HANDSHAKE_TIMEOUT. If `handle.error` is set (or result missing): classify, read stderr tail, `await close_handle(handle)`, return `(None, error_result)`. On success return `(handle, handle.result)`. Log `mcp_connect_ok` (command, tool_count) / `mcp_connect_failed` (command, error_code) — NEVER log env or env values.

    - `close_handle`: set `close_requested`; `await asyncio.wait_for(handle.closed.wait(), CLOSE_TIMEOUT)`; on `TimeoutError` cancel task and await it suppressing CancelledError; finally close errfile (suppress OSError). Idempotent.

    - `connect_once_and_list` (D-20 single implementation for the CLI): `open_session` then, if handle, `close_handle`; return the result.

    - Registry (Agent-process memory only, D-05/D-07): module dicts `_sessions: dict[tuple[int, int], SessionHandle]`, `_last_results: dict[tuple[int, int], McpConnectResult]`, `_locks: dict[tuple[int, int], asyncio.Lock]` (per-key lock serializes concurrent Connect on the same server — discretion choice: serialize, not reject). `connect_server`: under the key lock, close any existing handle for the key first, then `open_session`; on success store handle in `_sessions` and pop `_last_results`; on failure store the error result in `_last_results`. `disconnect_server`: under lock, pop and close handle, pop last result, return `McpConnectResult(status=NOT_CONNECTED)`. `get_status` (D-09): no handle -> `_last_results.get(key)` or NOT_CONNECTED; handle present -> dead if `handle.task.done()` or `handle.closed.is_set()`, else `await asyncio.wait_for(handle.session.send_ping(), LIVENESS_TIMEOUT)` and treat any `Exception` (incl. TimeoutError/McpError) as dead; dead -> read stderr tail, pop + close handle, store and return `build_error_result(PROCESS_EXITED, "server exited", tail, ...)`; alive -> return `handle.result`. `cleanup_server` = disconnect + pop lock. `cleanup_user_sessions(user_id)` iterates keys with matching user_id (forward hook for a future user-delete endpoint, D-06). `cleanup_all_sessions` closes every handle (use `asyncio.gather` of close_handle calls with `return_exceptions=True` — closing is independent per server), then clears all three dicts.

    tests/conftest.py: in `clean_test_db` teardown, BEFORE `await engine.dispose()`, add `await mcp_client.cleanup_all_sessions()` (import `from agent import mcp_client` next to the existing `from agent import state as agent_state`), so no test leaks a child process or a lock bound to a closed event loop.
  </action>
  <verify>
    <automated>pytest tests/test_mcp_client.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_mcp_client.py -v` exits 0; all fixture-based tests pass; the two real-binary tests pass or are reported SKIPPED
    - `grep -n "errlog=" agent/mcp_client.py` matches and `grep -c "StringIO" agent/mcp_client.py` returns 0
    - `grep -n "tempfile.TemporaryFile" agent/mcp_client.py` matches
    - `grep -n "asyncio.timeout" agent/mcp_client.py` matches and `grep -n "reschedule(None)" agent/mcp_client.py` matches
    - `grep -En "logger\.(info|warning|error|debug)\(.*env" agent/mcp_client.py` returns no matches (env never logged)
    - `grep -En "except:" agent/mcp_client.py` returns no matches
    - `grep -n "cleanup_all_sessions" tests/conftest.py` matches
    - `pytest tests/ -q` exits 0 (no regressions in the existing 326 tests)
  </acceptance_criteria>
  <done>mcp_client.py implements the full interface; all four error codes proven by tests against the fixture server; registry, cross-task session use, lazy liveness and cleanup proven; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent process -> child MCP process | Agent spawns a user-configured executable and parses its stdout as JSON-RPC |
| child stderr/stdout -> API response | Untrusted text (tool descriptions, stderr) flows into results shown to the browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Elevation of Privilege | open_session spawning `command` | accept | Local-first single-deployment app where every account is admin (CLAUDE.md); spawn uses argv list via stdio_client (no shell, no shlex, D-11) so no shell-injection; configs are only launchable by their owner (enforced in 07-03) |
| T-07-02 | Denial of Service | hung/misbehaving server | mitigate | Single `asyncio.timeout(MCP_CONNECT_TIMEOUT)` around spawn+initialize+list_tools plus READY_GRACE_SECONDS safety wait with task cancel; failures are returned as typed results, never raised into the event loop (D-19) |
| T-07-03 | Denial of Service | orphaned child processes / temp files | mitigate | Owner task owns the context managers so SDK cleanup always runs; close_handle cancels on CLOSE_TIMEOUT; errfile closed in close_handle; cleanup_all_sessions for shutdown; per-key lock + replace-on-reconnect prevents duplicate processes per server |
| T-07-04 | Information Disclosure | env secrets in logs | mitigate | Env dict passed only to StdioServerParameters; no log call includes env; acceptance grep enforces |
| T-07-05 | Tampering | malformed JSON-RPC from server | mitigate | Parsing delegated to mcp SDK; all resulting exceptions flattened and mapped to PROTOCOL_ERROR/PROCESS_EXITED; raw ExceptionGroup text never returned |
| T-07-SC | Tampering | pip install mcp | mitigate | RESEARCH.md Package Legitimacy Audit: mcp (Anthropic official, PyPI) slopcheck [OK]; exact pin `mcp==1.30.0`, already installed and verified — no checkpoint required |
</threat_model>

<verification>
- `pytest tests/test_mcp_client.py -v` green (real-binary tests pass or skip)
- `pytest tests/ -q` green
- After the test run, no `python.exe` fixture processes remain: `tasklist | grep -i mcp_stdio` is not applicable on Windows; instead confirm `python -c "import psutil; print([p.info for p in psutil.process_iter(['cmdline']) if p.info['cmdline'] and any('mcp_stdio_server' in c for c in p.info['cmdline'])])"` prints `[]`
</verification>

<success_criteria>
- All four D-17 error codes produced and asserted by tests
- Persistent session registry keyed by (user_id, server_id) with lazy liveness and full cleanup
- `mcp==1.30.0` pinned; default suite independent of filesystem.exe
</success_criteria>

<output>
Create `.planning/phases/07-mcp-connection-day-16/07-01-SUMMARY.md` when done
</output>
