---
phase: 04-task-state-machine-day-13
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/models.py
  - agent/schemas.py
  - agent/tasks.py
  - agent/tools.py
  - agent/main.py
  - agent/ws.py
  - ui/static/index.html
  - ui/static/app.js
  - tests/test_tasks.py
  - tests/test_task_api.py
autonomous: true
requirements: [TASK-01, TASK-02, TASK-03, TASK-05]

must_haves:
  truths:
    - "An LLM `create_task` tool call during a chat turn persists a Task row in the `planning` state, scoped to that chat's `chat_id` and `user_id`"
    - "A single chat can hold several Task rows at once — there is no unique constraint and no 'current task' pointer"
    - "Every created task has exactly one TaskTransition row with `from_state=NULL` and `to_state=planning`"
    - "GET /api/v1/chats/{chat_id}/tasks returns that chat's tasks with embedded history, and returns 404 for a chat the caller does not own"
    - "The sidebar Задачи panel lists every task of the selected chat with its title, goal and a state badge, and refreshes after a turn that created a task"
  artifacts:
    - path: "shared/models.py"
      provides: "TaskState enum, Task table, TaskTransition table"
      contains: "class Task(SQLModel, table=True)"
    - path: "agent/tasks.py"
      provides: "Task CRUD layer (create_task, list_tasks_for_chat, list_transitions)"
      min_lines: 50
    - path: "agent/schemas.py"
      provides: "CreateTaskArgs, TaskResponse, TaskTransitionResponse, TASK_* length constants"
      contains: "class CreateTaskArgs(BaseModel)"
    - path: "tests/test_tasks.py"
      provides: "create_task tool-path coverage"
    - path: "tests/test_task_api.py"
      provides: "GET /api/v1/chats/{chat_id}/tasks coverage incl. cross-user 404"
    - path: "ui/static/index.html"
      provides: "#task-panel stacked sidebar section"
      contains: "id=\"task-panel\""
  key_links:
    - from: "agent/tools.py"
      to: "agent/tasks.py::create_task"
      via: "@register_tool(\"create_task\", CreateTaskArgs, ...) handler"
      pattern: "tasks\\.create_task"
    - from: "agent/main.py"
      to: "agent/tasks.py::list_tasks_for_chat"
      via: "GET /api/v1/chats/{chat_id}/tasks"
      pattern: "tasks\\.list_tasks_for_chat"
    - from: "ui/static/app.js"
      to: "/api/v1/chats/{chat_id}/tasks"
      via: "apiFetch in loadChatTasks"
      pattern: "chats/\\$\\{chatId\\}/tasks"
    - from: "agent/ws.py"
      to: "done frame"
      via: "task_writes key derived from task-tool results"
      pattern: "task_writes"
---

<objective>
Deliver the first working vertical slice of the task state machine: the LLM recognizes a unit of work, calls `create_task`, and the user immediately sees that task — title, goal, state — in a new "Задачи" sidebar panel.

Purpose: TASK-01/02/03/05 need a real, end-to-end path before any lifecycle refinement makes sense. This plan builds the tables, the CRUD layer, the first tool registration, the read endpoint and the panel in one slice so the feature is observable from turn one.
Output: `Task`/`TaskTransition` tables, `agent/tasks.py`, `create_task` tool, `GET /api/v1/chats/{chat_id}/tasks`, `#task-panel`, `task_writes` on the WS `done` frame, and two new test modules.

Branch: work happens on `Day13` (create from `main` if it does not exist yet — PROJECT.md requires one branch per phase, pushed and merged to `main`, never deleted).
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-task-state-machine-day-13/04-CONTEXT.md
@.planning/phases/04-task-state-machine-day-13/04-RESEARCH.md
@.planning/phases/04-task-state-machine-day-13/04-PATTERNS.md
@.planning/phases/04-task-state-machine-day-13/04-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. Extracted from the codebase this session. Use directly — no exploration. -->

agent/tools.py (DO NOT MODIFY the registry/dispatcher machinery, lines 16-155):
  ToolHandler = Callable[[AsyncSession, int, int, dict[str, Any]], Awaitable[dict[str, Any]]]
  def register_tool(name: str, args_model: type[BaseModel], description: str) -> Callable[[ToolHandler], ToolHandler]
  def build_tool_schemas() -> list[dict[str, Any]]
  async def dispatch_tool_calls(session, user_id: int, chat_id: int, tool_calls: list[dict]) -> list[dict]
      # each result: {"tool_call_id", "name", "ok": bool, "content": <json str of handler return>, "write": {"id","key","layer"}}
  # existing registered handler signature to copy:
  async def _save_working_memory(session: AsyncSession, user_id: int, chat_id: int, args: dict[str, Any]) -> dict[str, Any]

agent/main.py:
  async def _get_chat_or_404(session: AsyncSession, chat_id: int, user_id: int) -> Chat   # line 82, raises 404 never 403
  def _profile_to_response(row: Profile) -> ProfileResponse                               # line 140
  # existing imports to extend: `from agent import memory, profile` (line 37),
  #   `from agent.state import CORS_ORIGINS, cleanup_chat_caches` (line 35),
  #   `from agent.schemas import (...)` (line 16), `from shared.models import (...)` (line 49)

agent/memory.py:
  async def list_working_memory(session: AsyncSession, chat_id: int) -> list[WorkingMemory]
  async def save_working_memory(session, user_id: int, chat_id: int, key: str, content: str) -> WorkingMemory

shared/models.py (top-of-file imports already present — reuse, do not re-add):
  from datetime import datetime, timezone
  from enum import Enum
  from typing import Optional
  from sqlalchemy import Column, Enum as SAEnum, ForeignKey, Integer, UniqueConstraint
  from sqlmodel import Field, SQLModel
  # Settings.strategy (lines 90-98) is the exact SAEnum idiom to copy, including values_callable.
  # WorkingMemory (lines 138-163) is the exact dual-FK ondelete="CASCADE" idiom to copy.

agent/schemas.py existing constants block (lines 11-22) — extend, do not fork:
  TITLE_MAX_LENGTH = 200 ... MEMORY_VALUE_MAX_LENGTH = 50_000, PROFILE_FIELD_MAX_LENGTH = 2000

agent/ws.py (inside _handle_chat_message, per-chat lock already held):
  line 256: memory_writes: list[dict[str, Any]] = []
  line 258: tool_results = await dispatch_tool_calls(session, chat.user_id, chat_id, pending_tool_calls)
  line 309: memory_writes = [r["write"] for r in tool_results if r["ok"] and r["write"] is not None]
  line 342-349: await websocket.send_json({"type": "done", "message_id": ..., "stats": stats, "memory_writes": memory_writes})

ui/static/app.js:
  state object (lines 27-30) currently has `lastMemory: null,` and `lastProfile: null,`
  async function loadChatMemory(chatId)          # line 252 — shape to mirror
  function renderMemoryEntries(container, entries)  # line 262 — replaceChildren/createElement/textContent
  line 446:  await loadChatMemory(chatId);        # chat-select path
  line 623:  case 'done':  ... line 631: loadChatMemory(state.currentChatId);
  showToast(message, 'success' | 'error')         # line 34
  apiFetch(path, options)                         # returns parsed JSON, throws on non-2xx

tests/conftest.py fixtures available:
  authenticated_client -> AsyncClient with `.seeded_user_id`
  second_authenticated_client -> AsyncClient as a different user (for IDOR tests)
  async_session_factory (from shared.database) for direct DB setup/assertions
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing end-to-end tests for task creation and retrieval</name>
  <files>tests/test_tasks.py, tests/test_task_api.py</files>
  <read_first>
    - tests/conftest.py (fixture shapes: authenticated_client, second_authenticated_client, async_session_factory)
    - tests/test_tools.py (existing dispatch_tool_calls test harness — copy how tool calls are constructed)
    - tests/test_memory_api.py (existing REST-endpoint test shape for a chat-scoped GET)
    - .planning/phases/04-task-state-machine-day-13/04-RESEARCH.md ("Code Examples" section — exact target schema and response shapes)
  </read_first>
  <behavior>
    tests/test_tasks.py (tool path, via `agent.tools.dispatch_tool_calls`):
    - `test_create_task_tool_persists_task_and_first_transition`: dispatch one `create_task` call with
      arguments {"title": "Ship Day 13", "description": "Build the task state machine", "goal": "All five TASK reqs pass"}
      for a seeded chat/user. Assert result `ok is True`; assert exactly one Task row exists for that chat with
      `state is TaskState.PLANNING`, `is_paused is False`, `delegate_to is None`, matching title/description/goal,
      and `user_id`/`chat_id` equal to the dispatcher-supplied values (never taken from arguments).
    - `test_create_task_writes_creation_transition_row`: the created task has exactly one TaskTransition row with
      `from_state is None` and `to_state is TaskState.PLANNING`.
    - `test_chat_holds_multiple_concurrent_tasks` (TASK-02): dispatch two `create_task` calls in one
      `dispatch_tool_calls` invocation; assert two distinct Task rows exist for the same `chat_id` with different ids.
    - `test_create_task_rejects_delegate_to_argument` (D-03): dispatch `create_task` with an extra
      `"delegate_to": "subagent-x"` argument; assert the persisted row still has `delegate_to is None`.
    - `test_task_state_enum_stores_lowercase_values`: read the raw `state` column value back through a plain
      SQLAlchemy text/select and assert it is the string `"planning"`, never `"PLANNING"`.

    tests/test_task_api.py (REST path, via authenticated_client):
    - `test_get_chat_tasks_returns_tasks_with_history`: create a chat, insert a Task + its creation TaskTransition
      directly via async_session_factory, GET `/api/v1/chats/{chat_id}/tasks`, assert 200, a one-element list, and
      that element carries id/title/description/goal/state/is_paused/delegate_to/created_at/updated_at plus a
      `history` list whose single entry has `from_state is None` and `to_state == "planning"`.
    - `test_get_chat_tasks_empty_list_for_new_chat`: 200 and `[]`.
    - `test_get_chat_tasks_other_users_chat_returns_404` (IDOR): second_authenticated_client GETs the first
      user's chat tasks; assert status 404 (never 403) and that the body does not contain any task title.
    - `test_get_chat_tasks_requires_auth`: unauthenticated client gets 401.
  </behavior>
  <action>
    Create `tests/test_tasks.py` and `tests/test_task_api.py` implementing exactly the behaviors above.
    Import `Task`, `TaskState`, `TaskTransition` from `shared.models`; import `dispatch_tool_calls` from `agent.tools`;
    use `async_session_factory` from `shared.database` for direct DB setup and assertions, and the
    `authenticated_client` / `second_authenticated_client` fixtures for REST calls.
    Build tool-call payloads in the OpenAI wire shape the dispatcher expects:
    a dict with `"id"` and `"function": {"name": "create_task", "arguments": <json string>}`.
    Follow `pytest.ini`'s `asyncio_mode = auto` — no `@pytest.mark.asyncio` needed unless the neighbouring
    test files already use it (match whichever style `tests/test_tools.py` uses).
    These tests MUST fail at this point because `shared.models.Task`, `agent/tasks.py`, the `create_task`
    registration and the GET route do not exist yet. Do not create stub modules to make collection succeed.
  </action>
  <verify>
    <automated>python -m pytest tests/test_tasks.py tests/test_task_api.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_tasks.py tests/test_task_api.py -x` exits NON-ZERO (RED phase — expected import/attribute error or assertion failure).
    - `tests/test_tasks.py` contains the five test function names listed in `<behavior>`.
    - `tests/test_task_api.py` contains the four test function names listed in `<behavior>`.
    - `grep -c "second_authenticated_client" tests/test_task_api.py` returns at least 1.
    - Neither file creates or imports a placeholder/stub implementation module.
    - `python -m pytest tests/ -q --ignore=tests/test_tasks.py --ignore=tests/test_task_api.py` still passes (no existing test broken).
  </acceptance_criteria>
  <done>Both new test modules exist, express the full create-and-read contract, and fail for the right reason: the implementation does not exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Task/TaskTransition storage, the create_task tool and the tasks read endpoint</name>
  <files>shared/models.py, agent/schemas.py, agent/tasks.py, agent/tools.py, agent/main.py, agent/ws.py</files>
  <read_first>
    - shared/models.py (existing imports, `Settings.strategy` SAEnum idiom at lines 90-98, `WorkingMemory` at lines 138-163)
    - agent/schemas.py (constants block lines 11-22, `SaveWorkingMemoryArgs` lines 170-183, `ChatMemoryResponse` lines 161-168, `ProfileResponse` lines 208-215)
    - agent/memory.py (full file — the CRUD module shape `agent/tasks.py` must mirror)
    - agent/profile.py (full file — the commit/rollback/refresh + logger.info write idiom)
    - agent/tools.py (lines 1-22 imports/registry and lines 158-190 registered handlers)
    - agent/main.py (lines 1-160 imports + `_get_chat_or_404` + `_profile_to_response`, and `get_chat_memory` at lines 420-443)
    - agent/ws.py (lines 250-350 — the tool_results / memory_writes / done-frame block)
    - .planning/phases/04-task-state-machine-day-13/04-PATTERNS.md (pattern assignments for each of these files)
  </read_first>
  <action>
    **shared/models.py** — append after the existing memory/profile models, reusing the file's existing imports:
    - `class TaskState(str, Enum)` with members `PLANNING = "planning"`, `EXECUTION = "execution"`,
      `VALIDATION = "validation"`, `DONE = "done"`, `CANCELLED = "cancelled"` (D-07).
    - `class Task(SQLModel, table=True)` with: `id: Optional[int]` primary key;
      `user_id: int` via `sa_column=Column(Integer, ForeignKey("user.id", ondelete="CASCADE"), nullable=False)`;
      `chat_id: int` via `sa_column=Column(Integer, ForeignKey("chat.id", ondelete="CASCADE"), nullable=False)`;
      `title: str = Field(max_length=200)`; `description: str = Field(default="", max_length=5_000)`;
      `goal: str = Field(default="", max_length=2_000)`;
      `state: TaskState = Field(default=TaskState.PLANNING, sa_column=Column(SAEnum(TaskState, values_callable=lambda enum_cls: [member.value for member in enum_cls]), nullable=False))`;
      `is_paused: bool = Field(default=False)` (D-04, orthogonal flag — NOT a state value);
      `delegate_to: Optional[str] = Field(default=None, max_length=200)` (D-03, unused this phase);
      `created_at` and `updated_at` via `default_factory=lambda: datetime.now(timezone.utc)`.
      Do NOT add a UniqueConstraint on chat_id and do NOT add a `current_task_id` pointer to `Chat` (D-08).
    - `class TaskTransition(SQLModel, table=True)` with: `id` primary key;
      `task_id: int` via `sa_column=Column(Integer, ForeignKey("task.id", ondelete="CASCADE"), nullable=False)`;
      `from_state: Optional[TaskState]` via the same SAEnum column but `nullable=True` (creation row uses None);
      `to_state: TaskState` via the same SAEnum column, `nullable=False`;
      `note: str = Field(default="", max_length=2_000)`; `created_at` via the same default_factory.
    - Never use `Field(foreign_key=...)` or `Field(ondelete=...)` — SQLModel silently ignores the latter.
    - No migration function is needed: `shared/database.py::init_db` already calls `SQLModel.metadata.create_all`
      unconditionally, which is how `WorkingMemory`/`LongTermMemory` were added in Phase 2.

    **agent/schemas.py** — extend the existing constants block with
    `TASK_TITLE_MAX_LENGTH = 200`, `TASK_DESCRIPTION_MAX_LENGTH = 5_000`, `TASK_GOAL_MAX_LENGTH = 2_000`,
    `TASK_NOTE_MAX_LENGTH = 2_000`. Then add:
    - `class CreateTaskArgs(BaseModel)` with `title`, `description`, `goal`, each
      `Field(min_length=1, max_length=<the matching constant>, description=<short LLM-facing hint>)`.
      It must NOT contain a `delegate_to`, `chat_id`, `user_id` or `state` field (D-03 / dispatcher owns scope).
    - `class TaskTransitionResponse(BaseModel)`: `from_state: Optional[str] = None`, `to_state: str`,
      `note: str = ""`, `created_at: datetime`.
    - `class TaskResponse(BaseModel)`: `id: int`, `title: str`, `description: str`, `goal: str`, `state: str`,
      `is_paused: bool`, `delegate_to: Optional[str] = None`, `created_at: datetime`, `updated_at: datetime`,
      `history: list[TaskTransitionResponse]`.

    **agent/tasks.py** (NEW) — module docstring `"""Thin CRUD layer owning all reads and writes to the task tables."""`,
    imports in stdlib → third-party → local order, `logger = get_logger(__name__)`. No FastAPI import in this module.
    - `async def create_task(session, user_id: int, chat_id: int, title: str, description: str, goal: str) -> Task`:
      construct the Task with `state=TaskState.PLANNING`, `session.add(task)`, `await session.flush()` to obtain
      `task.id`, then `session.add(TaskTransition(task_id=task.id, from_state=None, to_state=TaskState.PLANNING, note=""))`,
      then `try: await session.commit() / except Exception: await session.rollback(); raise`, then
      `await session.refresh(task)`, then `logger.info("task_created", chat_id=chat_id, task_id=task.id)`, return task.
    - `async def list_tasks_for_chat(session, chat_id: int) -> list[Task]`: `select(Task).where(Task.chat_id == chat_id).order_by(Task.created_at, Task.id)`,
      `result = await session.exec(...)`, `return list(result.all())`. Flat list, no "current" pointer (D-08).
    - `async def list_transitions(session, task_id: int) -> list[TaskTransition]`: oldest-first
      (`order_by(TaskTransition.created_at, TaskTransition.id)`) per D-11.

    **agent/tools.py** — additions ONLY. Change `from agent import memory` to `from agent import memory, tasks`;
    add `CreateTaskArgs` to the `from agent.schemas import (...)` list. Register a handler with
    `@register_tool("create_task", CreateTaskArgs, <description>)` where the description tells the model to create a
    task when it recognizes a distinct, trackable unit of work in this chat, that the task starts in the `planning`
    state, and that it must NOT be used for state changes. The handler signature is
    `async def _create_task(session: AsyncSession, user_id: int, chat_id: int, args: dict[str, Any]) -> dict[str, Any]`,
    it calls `tasks.create_task(session, user_id, chat_id, args["title"], args["description"], args["goal"])`
    and returns `{"status": "created", "id": row.id, "title": row.title, "state": row.state.value}`.
    Do NOT touch `register_tool`, `_clean_schema`, `build_tool_schemas`, `dispatch_tool_calls` or `_SCOPE_KEYS`.

    **agent/main.py** —
    - Extend `from agent import memory, profile` to include `tasks`.
    - Add `Task`, `TaskTransition`, `TaskState` to the `from shared.models import (...)` block and
      `TaskResponse`, `TaskTransitionResponse` to the `from agent.schemas import (...)` block.
    - Add `def _task_to_response(row: Task, history: list[TaskTransition]) -> TaskResponse` directly after
      `_profile_to_response` (around line 148), mapping every field and building
      `history=[TaskTransitionResponse(from_state=(h.from_state.value if h.from_state is not None else None), to_state=h.to_state.value, note=h.note, created_at=h.created_at) for h in history]`.
    - Add `@app.get("/api/v1/chats/{chat_id}/tasks", response_model=list[TaskResponse])` named
      `get_chat_tasks(chat_id: int, session: AsyncSession = Depends(get_session), current_user: User = Depends(get_current_user)) -> list[TaskResponse]`.
      Body: `await _get_chat_or_404(session, chat_id, current_user.id)` first (IDOR gate, 404 never 403), then
      `rows = await tasks.list_tasks_for_chat(session, chat_id)` and return
      `[_task_to_response(row, await tasks.list_transitions(session, row.id)) for row in rows]`.
      Place it next to `get_chat_memory` (around line 443).

    **agent/ws.py** — keep `agent/tools.py` untouched and partition the results here instead:
    - Add a module-level constant `TASK_TOOL_NAMES = ("create_task", "transition_task", "pause_task", "resume_task")`
      near the other module constants (all four names are declared now so later plans need no further edit here).
    - Initialise `task_writes: list[dict[str, Any]] = []` alongside `memory_writes` at line 256.
    - Change the `memory_writes` comprehension (line 309) to also require `r["name"] not in TASK_TOOL_NAMES`,
      so task tool results never leak into the memory summary.
    - Immediately after it, build `task_writes` from the same `tool_results`: for each result where `r["ok"]`
      and `r["name"] in TASK_TOOL_NAMES`, `json.loads(r["content"])` and append
      `{"id": payload.get("id"), "title": payload.get("title"), "state": payload.get("state")}`.
      Guard the parse with `except json.JSONDecodeError: continue` — never a bare `except:`.
    - Add `"task_writes": task_writes` to the `done` frame dict at line 342.

    Conventions that apply to every file above: type hints on all parameters and returns; `async`/`await` for all I/O;
    `datetime.now(timezone.utc)` never `datetime.utcnow()`; `structlog` via `get_logger(__name__)` never `print()`;
    `.is_(None)` never `== None` in `where()`; `await session.commit()` after writes and `await session.rollback()`
    in exception handlers; no bare `except:`.
  </action>
  <verify>
    <automated>python -m pytest tests/test_tasks.py tests/test_task_api.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_tasks.py tests/test_task_api.py -v` passes (all nine tests GREEN).
    - `python -m pytest tests/ -q` passes with zero failures (no regression in the 33 existing test modules).
    - `grep -c "values_callable" shared/models.py` returns at least 4 (Settings.strategy plus Task.state, TaskTransition.from_state, TaskTransition.to_state).
    - `grep -n "ondelete=\"CASCADE\"" shared/models.py` shows entries for `user.id`, `chat.id` (Task) and `task.id` (TaskTransition).
    - `grep -c "Field(foreign_key=" shared/models.py` returns 0 for the new Task/TaskTransition blocks.
    - `git diff --stat agent/tools.py` shows only additions; `grep -n "def dispatch_tool_calls" agent/tools.py` is byte-identical to its pre-task definition (verify with `git diff agent/tools.py` containing no `-` line inside lines 25-155).
    - `grep -c "delegate_to" agent/schemas.py` returns 1 (only inside `TaskResponse`, never in `CreateTaskArgs`).
    - `grep -n "task_writes" agent/ws.py` shows the initialisation, the build loop, and the `done`-frame key.
    - `python -c "from agent.tools import build_tool_schemas; print([s['function']['name'] for s in build_tool_schemas()])"` prints a list containing `create_task`.
  </acceptance_criteria>
  <done>The LLM can create a task through the existing dispatcher, the row and its creation transition persist with correct ownership and cascade wiring, and the REST read endpoint returns them with 404 for non-owners.</done>
</task>

<task type="auto">
  <name>Task 3: Add the Задачи sidebar panel so the user sees created tasks</name>
  <files>ui/static/index.html, ui/static/app.js</files>
  <read_first>
    - ui/static/index.html lines 38-100 (the `<aside>` sidebar: `#memory-panel`, `#profile-panel`, `#agent-status`)
    - ui/static/app.js lines 20-50 (state object and showToast), 252-340 (loadChatMemory/renderMemoryEntries/renderMemoryPanel/loadProfile/saveProfile), 430-460 (chat-select path), 615-640 (handleWsMessage 'done' case)
    - .planning/phases/04-task-state-machine-day-13/04-UI-SPEC.md (Copywriting Contract and the per-state badge colour table — all strings are Russian)
    - .planning/phases/04-task-state-machine-day-13/04-RESEARCH.md Pitfall 6 (panels are stacked divs, NOT click-to-switch tabs)
  </read_first>
  <action>
    **ui/static/index.html** — insert a new stacked sidebar section between the closing `</div>` of `#profile-panel`
    (line 92) and the `#agent-status` div (line 93). Do NOT build tab-switching UI: there is no `.tab-button`,
    `.tab-content` or `aria-selected` machinery anywhere in this codebase, and `#memory-panel`/`#profile-panel`
    are always-visible stacked sections (RESEARCH Pitfall 6). Markup:
    - Container `<div id="task-panel" class="border-t border-slate-800 p-3 text-xs text-slate-400 overflow-y-auto max-h-72">`
      (identical container classes to `#memory-panel`, including the `overflow-y-auto max-h-72` scroll cap).
    - Heading row: `<h3 class="text-slate-300 font-semibold mb-2">Задачи <span id="task-count" class="text-white font-semibold">0</span></h3>`.
    - List container: `<div id="task-list" class="space-y-2"></div>`.

    **ui/static/app.js** —
    - Add `lastTasks: null,` to the `state` object next to `lastMemory: null,` / `lastProfile: null,` (lines 28-29).
    - Add two module-level maps near the other top-level constants:
      `TASK_STATE_LABELS = { planning: 'планирование', execution: 'выполнение', validation: 'проверка', done: 'готово', cancelled: 'отменено' }`
      and `TASK_STATE_BADGE_CLASSES = { planning: 'text-slate-400', execution: 'text-sky-400', validation: 'text-amber-400', done: 'text-emerald-400', cancelled: 'text-red-400' }`
      (exact hues from UI-SPEC §Color; indigo stays reserved and is not used for any badge).
    - `async function loadChatTasks(chatId)`: mirror `loadChatMemory` — `await apiFetch(`/api/v1/chats/${chatId}/tasks`)`,
      store in `state.lastTasks`, call `renderTaskPanel()`. On error: `console.error('Failed to load tasks:', err)`
      and `showToast('Не удалось загрузить задачи. Проверьте соединение и попробуйте снова.', 'error')`.
    - `function renderTaskPanel()`: read `state.lastTasks` (bail if null); set `$('task-count').textContent = String(tasks.length)`;
      `$('task-list').replaceChildren()`. If the list is empty, append a `text-slate-600` div with textContent `—`
      followed by a `text-slate-600` div with textContent `Задачи появятся здесь, когда агент создаст новую`.
      Otherwise append one card per task: `<div class="rounded-lg bg-slate-800 px-2 py-1">` containing
      a title line (`text-sm font-semibold text-slate-300`, `textContent = task.title`),
      a meta row (`flex items-center gap-1`) holding a state chip whose className is
      `` `rounded px-1 ${TASK_STATE_BADGE_CLASSES[task.state]}` `` and whose textContent is `TASK_STATE_LABELS[task.state]`,
      plus — only when `task.is_paused` is true — a second chip `text-slate-500` with textContent `⏸` (D-04: shown
      alongside the state badge, never replacing it),
      and a goal line (`text-slate-400 truncate`, `textContent = task.goal`, `title = task.goal`).
      Build every node with `document.createElement` and assign text via `textContent` only — never `innerHTML`,
      never template-string HTML: task titles/descriptions/goals are LLM-generated and must not be parsed as markup.
    - Call `loadChatTasks(chatId)` in the chat-select path immediately after the existing `await loadChatMemory(chatId);`
      (line 446), and inside `case 'done':` immediately after `loadChatMemory(state.currentChatId);` (line 631),
      so a turn containing a `create_task` tool call refreshes the panel exactly the way memory writes already do.
    - Use camelCase identifiers throughout, matching this file's 100% camelCase convention.
  </action>
  <verify>
    <automated>python -m pytest tests/ -q && grep -c 'id="task-panel"' ui/static/index.html && grep -c "loadChatTasks" ui/static/app.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'id="task-panel"' ui/static/index.html` returns 1, and `grep -c 'id="task-list"' ui/static/index.html` returns 1, and `grep -c 'id="task-count"' ui/static/index.html` returns 1.
    - `grep -n 'task-panel' ui/static/index.html` shows the block positioned after the line containing `btn-save-profile` and before the line containing `id="agent-status"`.
    - `grep -cE 'tab-button|aria-selected|tab-content' ui/static/index.html` returns 0 (no tab machinery introduced).
    - `grep -c "loadChatTasks" ui/static/app.js` returns at least 3 (definition + chat-select call + 'done' call).
    - `grep -c "lastTasks" ui/static/app.js` returns at least 3.
    - `grep -n "Задачи появятся здесь, когда агент создаст новую" ui/static/app.js` returns a match (exact UI-SPEC empty-state copy).
    - `grep -n "Не удалось загрузить задачи. Проверьте соединение и попробуйте снова." ui/static/app.js` returns a match (exact UI-SPEC error copy).
    - `grep -n "innerHTML" ui/static/app.js` shows no match inside `renderTaskPanel` (run `sed -n '/function renderTaskPanel/,/^}/p' ui/static/app.js | grep -c innerHTML` → 0).
    - `grep -o "text-sky-400\|text-amber-400\|text-emerald-400\|text-red-400\|text-slate-400" ui/static/app.js | sort -u` includes all five badge classes.
    - `python -m pytest tests/ -q` still passes.
  </acceptance_criteria>
  <done>Selecting a chat renders the Задачи panel with a count and one card per task showing title, Russian state label in the UI-SPEC colour, goal, and a ⏸ chip when paused; the panel refreshes at the end of every streamed turn.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Agent REST | Session-cookie-authenticated but otherwise untrusted; `chat_id` in the path is attacker-controlled |
| LLM → tool dispatcher | Tool-call arguments are model-generated (or prompt-injected by the user) and must be treated as untrusted input |
| Agent → Browser DOM | Task title/description/goal originate from the LLM and are rendered into the sidebar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | `GET /api/v1/chats/{chat_id}/tasks` | mitigate | Call `_get_chat_or_404(session, chat_id, current_user.id)` before any task query; returns 404 (never 403) so it is not an ownership oracle. Covered by `test_get_chat_tasks_other_users_chat_returns_404`. |
| T-04-02 | Spoofing | `GET /api/v1/chats/{chat_id}/tasks` | mitigate | `current_user: User = Depends(get_current_user)` — Phase 1 session-cookie gate reused unchanged. Covered by `test_get_chat_tasks_requires_auth`. |
| T-04-03 | Tampering | `create_task` tool handler | mitigate | `user_id`/`chat_id` are taken from the dispatcher's trusted WS-session values, never from `args`; `CreateTaskArgs` has no `chat_id`/`user_id`/`state`/`delegate_to` field, so extra LLM-supplied keys are dropped by Pydantic validation. Covered by `test_create_task_rejects_delegate_to_argument`. |
| T-04-04 | Elevation of Privilege | `Task.delegate_to` | mitigate | Column exists but is never LLM-settable this phase (D-03); absent from `CreateTaskArgs`, so the model cannot pre-authorize future subagent delegation. |
| T-04-05 | Denial of Service | LLM-supplied task strings | mitigate | `TASK_TITLE_MAX_LENGTH=200`, `TASK_DESCRIPTION_MAX_LENGTH=5_000`, `TASK_GOAL_MAX_LENGTH=2_000` enforced by Pydantic `Field(max_length=...)` before any DB write, bounding both row size and future prompt-injection volume. |
| T-04-06 | Tampering | Task rendering in `ui/static/app.js` | mitigate | `renderTaskPanel` builds nodes with `document.createElement` and sets text via `textContent` only — no `innerHTML`, no template-string HTML — so an LLM-authored title containing markup cannot execute. |
| T-04-07 | Repudiation | Task lifecycle audit | mitigate | Every task creation writes an append-only `TaskTransition` row (`from_state=NULL`, `to_state=planning`) in the same commit as the Task row. |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | This phase installs zero new packages (RESEARCH.md §Package Legitimacy Audit: not applicable). No `requirements.txt` change; slopcheck gate not triggered. |
</threat_model>

<verification>
- `python -m pytest tests/ -q` → zero failures.
- `python -m pytest tests/test_tasks.py tests/test_task_api.py -v` → all tests pass.
- `python -c "from agent.tools import build_tool_schemas; print([s['function']['name'] for s in build_tool_schemas()])"` → list contains `create_task` alongside `save_working_memory` and `save_long_term_memory`.
- `git diff agent/tools.py` → contains no removed line inside the registry/dispatcher block (lines 16-155).
- Manual smoke (optional this plan, formally verified in 04-04): `python run.py`, open the UI, select a chat — the Задачи panel renders with `—` and the helper line when the chat has no tasks.
</verification>

<success_criteria>
- `Task` and `TaskTransition` tables exist with `user_id`/`chat_id`/`task_id` FKs declared via `sa_column=Column(ForeignKey(..., ondelete="CASCADE"))` and enum columns using `values_callable`.
- `create_task` is registered in `agent/tools.py` without any change to `dispatch_tool_calls`, `register_tool`, `build_tool_schemas` or `_SCOPE_KEYS`.
- A chat can hold multiple tasks simultaneously (TASK-02) and each starts in `planning` (TASK-01).
- `GET /api/v1/chats/{chat_id}/tasks` returns tasks with embedded oldest-first history and 404s for non-owners.
- The `#task-panel` sidebar section shows each task's title, Russian state label and goal (TASK-05, partial — history timeline lands in 04-02).
- The WS `done` frame carries a `task_writes` array separate from `memory_writes`.
</success_criteria>

<output>
Create `.planning/phases/04-task-state-machine-day-13/04-01-SUMMARY.md` when done
</output>
