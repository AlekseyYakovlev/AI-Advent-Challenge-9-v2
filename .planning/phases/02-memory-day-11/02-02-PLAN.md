---
phase: 02-memory-day-11
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/llm_client.py
  - tests/test_lm_studio_client.py
  - tests/test_model_switch_lock.py
autonomous: true
requirements: [MEM-03]

must_haves:
  truths:
    - "Loading a model from the app's model selector actually loads it in the installed LM Studio version instead of failing with 'Unexpected endpoint or method'"
    - "Unloading a previously loaded model succeeds using the instance identifier LM Studio returned at load time"
    - "A model load timeout still triggers the existing 5-second emergency unload and returns ERROR"
    - "LM Studio being down still surfaces as UNREACHABLE / 'LM Studio is not running', not a generic HTTP error"
  artifacts:
    - path: "agent/llm_client.py"
      provides: "LM Studio v1 model load/unload control calls"
      contains: "/api/v1/models/load"
    - path: "tests/test_lm_studio_client.py"
      provides: "respx mocks pinned to the v1 endpoints"
      contains: "/api/v1/models/load"
  key_links:
    - from: "agent/llm_client.py::_load_model_locked"
      to: "LM Studio /api/v1/models/load"
      via: "httpx POST, response instance_id recorded"
      pattern: "api/v1/models/load"
    - from: "agent/llm_client.py::_unload_model_http"
      to: "LM Studio /api/v1/models/unload"
      via: "httpx POST with instance_id body"
      pattern: "instance_id"
---

<objective>
Fix the pre-existing LM Studio control-plane bug that blocks loading a tool-capable model from the app's own UI.

`LMStudioClient` calls `POST /api/v0/models/load` and `POST /api/v0/models/unload`. 02-RESEARCH.md Pitfall 5 verified live against this machine's actual LM Studio instance that both v0 endpoints return `{"error":"Unexpected endpoint or method..."}` — v0 model management is gone. The working endpoints are `POST /api/v1/models/load` (body `{"model": "<id>"}`, returns `{"type","instance_id","load_time_seconds","status":"loaded"}`) and `POST /api/v1/models/unload` (body `{"instance_id": "<id>"}` — note the field is `instance_id`, NOT `model`). The existing tests never caught this because they only assert against `respx` mocks of the v0 URLs.

This is scoped into Phase 2 deliberately: MEM-03's acceptance demo requires a `tool_use`-capable model (`qwen/qwen3.5-9b`) to be loaded, and the app's own model selector is the demo path. The change is isolated to two functions plus their mocks and runs in Wave 1 in parallel with Plan 01, before Plan 03 extends the same file.

Purpose: unblock the MEM-03 demo path.
Output: v1-correct load/unload with instance-id tracking, and tests pinned to the real endpoint contract.
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-memory-day-11/02-RESEARCH.md

<interfaces>
<!-- Current contracts in agent/llm_client.py that must keep working unchanged for callers. -->

class LMStudioClient:
    def __init__(self, base_url: str | None = None) -> None
        self._base_url, self._openai_base = f"{base_url}/v1", self._control_base = self._base_url
        self._model_switch_lock = asyncio.Lock()
        self._current_loaded_model: str | None = None
    async def list_models(self) -> list[dict[str, Any]]                  # GET {openai_base}/models - WORKS, do not change
    async def is_model_loaded(self, model_id: str) -> bool               # do not change
    async def load_model(self, model_id: str, gpu_offload: int, context_length: int | None) -> ModelLoadResult
    async def _load_model_locked(self, model_id, gpu_offload, context_length) -> ModelLoadResult
    async def unload_model(self, model_id: str) -> ModelLoadResult
    async def _unload_model_locked(self, model_id: str) -> ModelLoadResult
    async def _unload_model_http(self, model_id: str) -> None
    async def _emergency_unload(self, model_id: str) -> None

Module constants: LOAD_TIMEOUT = 120.0, UNLOAD_TIMEOUT = 10.0, EMERGENCY_UNLOAD_TIMEOUT = 5.0
ModelLoadResult / ModelLoadStatus live in agent/schemas.py: LOADED / IDLE / UNREACHABLE / ERROR.

Callers that must keep working unchanged:
  agent/main.py::load_lm_studio_model   -> lm_studio_client.load_model(body.model_id, body.gpu_offload, body.context_length)
  agent/main.py::unload_lm_studio_model -> lm_studio_client.unload_model(model_id)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate LMStudioClient model load/unload to the v1 control endpoints</name>
  <files>agent/llm_client.py</files>
  <read_first>
    - agent/llm_client.py lines 127-282 (the whole `LMStudioClient` class plus module constants)
    - .planning/phases/02-memory-day-11/02-RESEARCH.md section "Pitfall 5" and "State of the Art" (the verified v1 request/response shapes)
    - agent/schemas.py lines 96-126 (`ModelLoadStatus`, `ModelLoadResult`, `ModelLoadRequest`)
  </read_first>
  <action>
Before editing, probe the live LM Studio instance to confirm the accepted request body, since the app is being pointed at a real local server:

`curl -s -X POST http://localhost:1234/api/v1/models/load -H "Content-Type: application/json" -d '{"model":"qwen/qwen3.5-9b"}'` — expect a JSON object containing `instance_id` and `"status":"loaded"`. Then `curl -s -X POST http://localhost:1234/api/v1/models/unload -H "Content-Type: application/json" -d '{"instance_id":"<returned id>"}'`. If LM Studio is not running, skip the probe and implement the minimal verified contract below unchanged.

Changes in `agent/llm_client.py`, `LMStudioClient` only:

1. Add `self._instance_ids: dict[str, str] = {}` to `__init__`, mapping `model_id` → the `instance_id` LM Studio returned when that model was loaded. Keep `_current_loaded_model` exactly as-is — it is used for the load/switch logic and `_emergency_unload`.

2. In `_load_model_locked`: change the URL from `f"{self._control_base}/api/v0/models/load"` to `f"{self._control_base}/api/v1/models/load"`. Send body `{"model": model_id}` only — the verified v1 contract does not document `gpu_offload`/`context_length`, so do NOT forward them blindly. Keep both parameters in the signature (callers and `ModelLoadRequest` depend on them) and emit `logger.info("lm_studio_load_params_ignored", model_id=model_id, gpu_offload=gpu_offload, context_length=context_length)` when either is non-default, so the limitation is visible in logs rather than silent. If the live probe above proved the v1 endpoint accepts these fields, forward them instead and note the verified shape in the SUMMARY.

3. Still in `_load_model_locked`: on success, parse the response JSON and record `self._instance_ids[model_id] = body["instance_id"]` when present, guarded with `.get("instance_id")` so a missing field does not raise. Keep all existing `except httpx.TimeoutException` / `except httpx.ConnectError` / `except httpx.HTTPError` branches and their `ModelLoadResult` returns byte-for-byte identical, including the `_emergency_unload` call on timeout.

4. In `_unload_model_http`: change the URL to `f"{self._control_base}/api/v1/models/unload"` and the body to `{"instance_id": instance_id}` where `instance_id = self._instance_ids.get(model_id, model_id)` (falling back to the model id keeps the call well-formed when the process did not perform the load itself). Keep `UNLOAD_TIMEOUT` and `response.raise_for_status()`.

5. In `_unload_model_locked` and `_emergency_unload`: after a successful unload, `self._instance_ids.pop(model_id, None)` alongside the existing `_current_loaded_model` reset.

Do not touch `list_models` / `is_model_loaded` — they use `/v1/models` on the OpenAI-compatible base and are verified working. Do not add any new code against `/api/v0/*`. Keep type hints, structlog calls, and the no-bare-`except` rule.
  </action>
  <verify>
    <automated>pytest tests/test_lm_studio_client.py tests/test_model_switch_lock.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'api/v0/models' agent/llm_client.py` outputs `0`.
    - `grep -c 'api/v1/models/load\|api/v1/models/unload' agent/llm_client.py` outputs `2`.
    - `grep -c '"instance_id"' agent/llm_client.py` outputs at least `2` (response capture + unload body).
    - `grep -c '_instance_ids' agent/llm_client.py` outputs at least `4` (init, load capture, unload lookup, cleanup).
    - `python -c "import agent.llm_client"` exits 0.
    - Signatures unchanged: `grep -c 'async def load_model(self, model_id: str, gpu_offload: int, context_length: int | None)' agent/llm_client.py` outputs `1`.
  </acceptance_criteria>
  <done>Model load and unload target the v1 control endpoints, load captures and stores `instance_id`, unload sends `instance_id`, and all existing error/timeout/unreachable behavior is preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint the LM Studio mocks to the v1 contract</name>
  <files>tests/test_lm_studio_client.py, tests/test_model_switch_lock.py</files>
  <read_first>
    - tests/test_lm_studio_client.py (full - all respx routes at lines 25, 49, 52, 73, 88)
    - tests/test_model_switch_lock.py (full - respx routes at lines 40, 43)
    - agent/llm_client.py (as modified in Task 1)
  </read_first>
  <action>
Update every `respx.post(f"{BASE_URL}/api/v0/models/load")` to `/api/v1/models/load` and every `respx.post(f"{BASE_URL}/api/v0/models/unload")` to `/api/v1/models/unload`, in both files. There are 7 route registrations total (5 in `tests/test_lm_studio_client.py`, 2 in `tests/test_model_switch_lock.py`).

Make the load mocks return a body matching the verified v1 response so the new `instance_id` capture is exercised rather than silently skipped: `httpx.Response(200, json={"type": "llm", "instance_id": "inst-test-1", "load_time_seconds": 0.1, "status": "loaded"})`.

Add one new test to `tests/test_lm_studio_client.py` named `test_unload_sends_instance_id_from_load_response`: load a model against the mocked v1 load route, then unload it, and assert the unload request body parsed from the captured respx request equals `{"instance_id": "inst-test-1"}` — this is the regression guard that stops anyone reverting the body field to `{"model": ...}`.

Add one new test named `test_load_targets_v1_endpoint` asserting that a `respx` route registered on `/api/v0/models/load` is NOT called (`assert not v0_route.called`) while the v1 route is (`assert v1_route.called`) — this makes the v0 regression detectable, which the old mock-only tests could not do.

Do not change any assertion about `ModelLoadStatus` values, timeout behavior, or the emergency-unload path — those behaviors must remain identical.
  </action>
  <verify>
    <automated>pytest tests/test_lm_studio_client.py tests/test_model_switch_lock.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_lm_studio_client.py tests/test_model_switch_lock.py -v` exits 0, including the two new tests.
    - `grep -rc 'api/v0/models/load"' tests/ --include=*.py` reports `0` matches outside the deliberate `test_load_targets_v1_endpoint` negative-assertion route.
    - `grep -c 'instance_id' tests/test_lm_studio_client.py` outputs at least `3`.
    - `pytest tests/ -q` exits 0 (no other test regressed).
  </acceptance_criteria>
  <done>All LM Studio control-plane mocks target v1, the unload body contract is asserted, and a negative assertion proves v0 is no longer called.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Agent process → LM Studio control API (`localhost:1234`) | Unauthenticated local HTTP control plane that can load/unload models |
| browser → `POST /api/v1/lm-studio/load-model` | Authenticated user supplies a `model_id` string forwarded to the control plane |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Denial of Service | `_load_model_locked` | mitigate | Existing `LOAD_TIMEOUT`/`_emergency_unload` with `EMERGENCY_UNLOAD_TIMEOUT` preserved unchanged; `_model_switch_lock` still serializes concurrent loads |
| T-02-07 | Tampering | `model_id` forwarded to the control plane | accept | `model_id` is already gated by `get_current_user` on `/api/v1/lm-studio/load-model`; LM Studio only resolves locally-downloaded model ids, and the app is a single-machine local deployment |
| T-02-08 | Information Disclosure | LM Studio error bodies | mitigate | Errors are returned as typed `ModelLoadResult` messages and logged via structlog with `model_id`/`error` only — no request bodies or secrets logged |
| T-02-SC | Tampering | npm/pip installs | mitigate | Not applicable — zero new packages (02-RESEARCH.md Package Legitimacy Audit); no install task in this plan |
</threat_model>

<verification>
- `pytest tests/ -q` green.
- With LM Studio running: start the app, pick `qwen/qwen3.5-9b` in the model selector, and confirm the load succeeds (no "Unexpected endpoint or method" in the agent log) and the model reports loaded via `GET http://localhost:1234/v1/models`.
- `grep -rn "api/v0" agent/` returns nothing.
</verification>

<success_criteria>
- Model load/unload works against the installed LM Studio version through the app's own UI.
- `instance_id` is captured at load and used at unload.
- Every pre-existing timeout / unreachable / emergency-unload behavior is unchanged and still covered by tests.
- Tests can now detect a regression back to the v0 endpoints.
</success_criteria>

<output>
Create `.planning/phases/02-memory-day-11/02-02-SUMMARY.md` when done. Record in it whether the live probe confirmed `gpu_offload`/`context_length` support on the v1 load endpoint.
</output>
