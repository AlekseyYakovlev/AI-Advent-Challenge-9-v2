---
phase: 06-controlled-transitions-day-15
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/models.py
  - shared/database.py
  - agent/tasks.py
  - tests/test_tasks.py
  - tests/test_database.py
autonomous: true
requirements: [TRANS-01, TRANS-03]

must_haves:
  truths:
    - "A task in 'planning' cannot be moved directly to 'done' — the attempt is refused and the task stays in 'planning'"
    - "A task in 'done' or 'cancelled' cannot be moved to any other state, forward or backward"
    - "Every refused attempt — transition, cancel, pause, or resume — leaves a permanent, human-readable record of what was attempted and why it was refused"
    - "Pausing or resuming a finished task is refused; resuming a task that is not paused is refused"
    - "An existing app.db upgraded in place gains the new rejection columns without a crash"
  artifacts:
    - path: "agent/tasks.py"
      provides: "Transition-graph edge table, IllegalTransitionError, legality gates on transition_task/set_paused/cancel_task, rejected-row writes on every rejection path, build_transition_illegal_prompt"
      contains: "class IllegalTransitionError"
    - path: "shared/models.py"
      provides: "TaskTransition.rejected and TaskTransition.rejection_reason columns"
      contains: "rejection_reason"
    - path: "shared/database.py"
      provides: "Idempotent additive migration for the two new tasktransition columns"
      contains: "migrate_add_task_transition_rejection_columns"
    - path: "tests/test_tasks.py"
      provides: "Rejection tests for skip-ahead, two-steps-back, terminal exit, pause/resume/cancel guards, and the rejected history row each one writes"
      contains: "IllegalTransitionError"
  key_links:
    - from: "agent/tasks.py::transition_task"
      to: "shared/models.py::TaskTransition"
      via: "rejected-row write on the illegal branch"
      pattern: "rejected=True"
    - from: "agent/tasks.py::set_paused"
      to: "shared/models.py::TaskTransition"
      via: "self-loop rejected-row write on the terminal-state and resume-no-op branches (D-09/D-11)"
      pattern: "rejected=True"
    - from: "shared/database.py::init_db"
      to: "migrate_add_task_transition_rejection_columns"
      via: "migration call before create_all"
      pattern: "await migrate_add_task_transition_rejection_columns"
    - from: "agent/tasks.py::transition_task"
      to: "agent/tasks.py::_get_owned_task"
      via: "ownership check runs strictly before the legality check"
      pattern: "_get_owned_task"
---

## Phase Goal

**As a** user of this agent chat, **I want to** have task state changes strictly validated against an explicit transition graph, **so that** the agent cannot silently skip, reverse, or resurrect lifecycle steps, and every refused attempt leaves a clear, explainable trace instead of a silent no-op or a crash.

<objective>
Make the transition graph real in the one place it cannot be bypassed: the CRUD domain layer (`agent/tasks.py`). After this plan, an illegal transition attempted through *any* caller (LLM tool call, REST endpoint, or a future one) is refused, the task's state is untouched, and a `rejected=True` history row explains why.

Purpose: TRANS-01 (explicit allowed-transition set, illegal transitions rejected) and the graph half of TRANS-03 (pause/resume never violates the graph). Every later plan in this phase consumes the `IllegalTransitionError` and the rejection columns created here.
Output: Two new `TaskTransition` columns + idempotent migration, a directed-edge table and terminal-state set, `IllegalTransitionError`, legality gates on `transition_task`/`set_paused`/`cancel_task` that each persist a rejected history row, `build_transition_illegal_prompt`, and the tests proving all of it.
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-controlled-transitions-day-15/06-CONTEXT.md
@.planning/phases/06-controlled-transitions-day-15/06-PATTERNS.md
@.planning/phases/06-controlled-transitions-day-15/06-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Contracts this plan creates. Plans 02 and 03 import these directly — do not rename. -->

agent/tasks.py (new, module level):
- `_FORWARD_EDGES: dict[TaskState, TaskState]` — PLANNING→EXECUTION, EXECUTION→VALIDATION, VALIDATION→DONE
- `_BACKWARD_EDGES: dict[TaskState, TaskState]` — EXECUTION→PLANNING, VALIDATION→EXECUTION
- `_TERMINAL_STATES: set[TaskState]` — {DONE, CANCELLED}
- `class IllegalTransitionError(Exception)` with `__init__(self, task_id: int, from_state: TaskState, to_state: TaskState, reason: str) -> None` storing `task_id`, `from_state`, `to_state`, `reason` as attributes and passing `reason` to `super().__init__`
- `def _legal_targets(from_state: TaskState) -> list[TaskState]`
- `def _is_legal_transition(from_state: TaskState, to_state: TaskState) -> bool`
- `def build_transition_illegal_prompt(rejected: list[dict[str, Any]]) -> str`

shared/models.py (existing class `TaskTransition`, additive):
- `rejected: bool` (default False)
- `rejection_reason: Optional[str]` (default None)

Rejection-row convention consumed by Plan 03's renderer:
- `transition_task` / `cancel_task` rejections write a row whose `from_state` is the task's current state and whose `to_state` is the attempted target (a real, different state)
- `set_paused` rejections write a **self-loop** row (`from_state == to_state == task.state`), which is how a blocked pause/resume is distinguished from a blocked edge move
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add rejection columns to TaskTransition with an idempotent in-place migration</name>
  <files>shared/models.py, shared/database.py, tests/test_database.py</files>
  <read_first>
    - shared/models.py (read the whole `TaskTransition` class, lines ~336-369, and the `Task.is_paused` field style above it)
    - shared/database.py (read `migrate_add_context_length`, `migrate_add_user_id_columns`, and `init_db` — lines ~75-118 and ~177-183)
    - tests/test_database.py (read `test_init_db_creates_all_tables` and `test_init_db_migrates_branching_strategy`)
    - .planning/phases/06-controlled-transitions-day-15/06-PATTERNS.md (sections `shared/models.py` and `shared/database.py`)
  </read_first>
  <action>
Implements D-09 and D-10 (one shared chronological history table, no new table).

In `shared/models.py`, add two fields to the existing `TaskTransition` class, placed immediately after the existing `note` field and before `created_at`: `rejected: bool = Field(default=False)` and `rejection_reason: Optional[str] = Field(default=None)`. Use plain `Field(...)` with no `sa_column` — match the style of `Task.is_paused`. Do NOT put a `max_length` on `rejection_reason` at the ORM layer; length capping belongs in `agent/schemas.py` (Plan 03) per the project's ORM-vs-API-schema separation.

In `shared/database.py`, add `async def migrate_add_task_transition_rejection_columns(conn: Any) -> None:` mirroring `migrate_add_context_length` exactly: first a `SELECT name FROM sqlite_master WHERE type='table' AND name='tasktransition'` guard that returns early when the table does not exist yet; then `PRAGMA table_info(tasktransition)` to collect existing column names; then, guarded per column, `ALTER TABLE tasktransition ADD COLUMN rejected BOOLEAN DEFAULT 0` and `ALTER TABLE tasktransition ADD COLUMN rejection_reason TEXT`. Log with `logger.info("migrating_tasktransition_add_rejection_columns")` before the ALTERs. The table name is lowercase `tasktransition` (SQLModel default naming, confirmed by `test_init_db_creates_all_tables`).

Call it from `init_db()` alongside the two existing migration calls, BEFORE `await conn.run_sync(SQLModel.metadata.create_all)` — `create_all` never adds columns to a table that already exists, so an existing `app.db` would otherwise raise `sqlite3.OperationalError: no such column` at the first rejected-transition write.

In `tests/test_database.py`, add `test_migrate_add_task_transition_rejection_columns_is_idempotent`: run the migration path twice against the same engine (call `init_db()` twice, or open a connection and call `migrate_add_task_transition_rejection_columns(conn)` twice inside `engine.begin()`), then assert via `PRAGMA table_info(tasktransition)` that `rejected` and `rejection_reason` each appear exactly once and that the second run raised nothing.
  </action>
  <acceptance_criteria>
    - `grep -n "rejection_reason" shared/models.py` matches inside the `TaskTransition` class body
    - `grep -n "migrate_add_task_transition_rejection_columns" shared/database.py` returns at least 2 hits (the definition and the `init_db` call site)
    - In `shared/database.py::init_db`, the new migration call appears on a line BEFORE the `conn.run_sync(SQLModel.metadata.create_all)` line
    - `pytest tests/test_database.py -v` passes, including the new idempotency test
    - `pytest tests/test_task_api.py tests/test_tasks.py -v` still passes (no regression from the model change)
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_database.py tests/test_tasks.py tests/test_task_api.py -v</automated>
  </verify>
  <done>`TaskTransition` has `rejected`/`rejection_reason`; `init_db()` adds them in place to a pre-existing database, idempotently, proven by a passing test.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce the transition graph in transition_task and persist rejected attempts</name>
  <files>agent/tasks.py, tests/test_tasks.py</files>
  <behavior>
    - `transition_task` planning→execution succeeds, task.state becomes EXECUTION, one `rejected=False` history row appended (unchanged existing behavior)
    - `transition_task` execution→planning succeeds (one-step-back rework, D-01)
    - `transition_task` validation→execution succeeds (one-step-back rework, D-01)
    - `transition_task` planning→done raises `IllegalTransitionError`; `task.state` remains PLANNING; `task.updated_at` unchanged; exactly one extra history row with `rejected is True`, `from_state is TaskState.PLANNING`, `to_state is TaskState.DONE`, non-empty `rejection_reason`
    - `transition_task` validation→planning (two steps back) raises `IllegalTransitionError` and writes a rejected row
    - `transition_task` planning→planning (self-loop) raises `IllegalTransitionError`
    - `transition_task` done→execution and cancelled→planning raise `IllegalTransitionError` (terminal states have no exit, D-01)
    - `transition_task` with a task_id owned by another user raises `TaskNotFoundError` (NOT `IllegalTransitionError`) even when the target state would also be illegal — ownership resolves first
    - `build_transition_illegal_prompt` returns a string containing the task id, the from-state value, and the to-state value for each entry passed in
  </behavior>
  <action>
Implements D-01, D-02, D-08 (prompt builder only — wiring is Plan 02), D-09.

In `agent/tasks.py`, add at module level, below the existing `TaskNotFoundError`: `_FORWARD_EDGES` mapping PLANNING→EXECUTION, EXECUTION→VALIDATION, VALIDATION→DONE; `_BACKWARD_EDGES` mapping EXECUTION→PLANNING, VALIDATION→EXECUTION; `_TERMINAL_STATES` = {TaskState.DONE, TaskState.CANCELLED}. Note `CANCELLED` is reachable only via `cancel_task`, never via the edge table — do not add any edge whose target is CANCELLED.

Add `class IllegalTransitionError(Exception)` with the one-line docstring style used by `TaskNotFoundError`, and an `__init__(self, task_id: int, from_state: TaskState, to_state: TaskState, reason: str) -> None` that stores all four as attributes and calls `super().__init__(reason)` so `str(exc)` is the human-readable reason. Add helpers `_legal_targets(from_state)` (returns `[]` for terminal states, otherwise the forward and/or backward target) and `_is_legal_transition(from_state, to_state)`.

In `transition_task`, insert the legality gate immediately AFTER the existing `task = await _get_owned_task(...)` line and after `previous_state = task.state`, and strictly BEFORE any mutation of `task.state`/`task.updated_at`/`session.add(task)`. This ordering is security-critical (see this plan's threat model T-06-01). On an illegal edge: build a reason string naming the task id, the current state value, the attempted state value, and the legal next states (or the words "terminal state" when `_legal_targets` is empty); `session.add(TaskTransition(task_id=task.id, from_state=previous_state, to_state=new_state, note=note, rejected=True, rejection_reason=reason))`; commit inside the existing `try/except Exception: await session.rollback(); raise` scaffolding (do NOT call `session.refresh(task)` on this branch); `logger.warning("task_transition_rejected", task_id=..., chat_id=..., from_state=previous_state.value, to_state=new_state.value)`; then `raise IllegalTransitionError(task.id, previous_state, new_state, reason)`. The illegal branch must never touch `task.state` or `task.updated_at`.

Update `transition_task`'s docstring: delete the "Does not validate that previous_state -> new_state is a legal edge -- transition-graph legality enforcement is Phase 6's job (TRANS-01)." sentence and replace it with a statement that only single-edge moves along the graph are accepted.

Add `def build_transition_illegal_prompt(rejected: list[dict[str, Any]]) -> str:` mirroring the tone and shape of `agent/invariants.py::build_justify_retract_prompt` (read it first). Each entry is the tool-result dict produced in Plan 02 with keys `task_id`, `from_state`, `to_state`, `error`. Produce one line per entry stating the attempted move was not legal plus the reason, then a closing instruction telling the model to either call `transition_task` again with a legal next state or explain to the user why it attempted that move, replying briefly in the user's language without repeating its whole previous answer. This requires adding `from typing import Any` to the imports.

In `tests/test_tasks.py`, add the tests enumerated in `<behavior>`. Reuse the existing `_seed_chat`, `_call`, `_create_task_via_tool` helpers and the `authenticated_client` fixture; drive the CRUD functions either directly through `async_session_factory()` or via `dispatch_tool_calls` — at this point in the phase the dispatcher still returns `ok=True` for error results (Plan 02 changes that), so assert on database state and on the raised exception type, not on `results[0]["ok"]`.
  </action>
  <acceptance_criteria>
    - `grep -n "class IllegalTransitionError" agent/tasks.py` returns exactly 1 hit
    - `grep -v '^\s*#' agent/tasks.py | grep -c "_get_owned_task"` returns at least 4 (definition plus the three call sites)
    - In `agent/tasks.py::transition_task`, the line assigning `task.state = new_state` appears AFTER the `_is_legal_transition` check, not before
    - `grep -n "Phase 6's job" agent/tasks.py` returns no hits for `transition_task`'s docstring
    - `pytest tests/test_tasks.py -v` passes with the new rejection tests
    - A test asserts that after a rejected planning→done attempt, `session.get(Task, task_id).state is TaskState.PLANNING`
    - A test asserts the rejected `TaskTransition` row has `rejected is True` and a truthy `rejection_reason`
    - A test asserts another user's task_id raises `TaskNotFoundError`, not `IllegalTransitionError`
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_tasks.py -v</automated>
  </verify>
  <done>Illegal `transition_task` calls raise `IllegalTransitionError`, leave task state untouched, and append a `rejected=True` history row; legal one-edge moves (both directions) still work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Guard pause, resume, and cancel against terminal states and no-ops, persisting every rejection</name>
  <files>agent/tasks.py, tests/test_tasks.py</files>
  <behavior>
    - `set_paused(..., is_paused=True)` on a task in PLANNING/EXECUTION/VALIDATION succeeds and sets `is_paused` True
    - `set_paused(..., is_paused=True)` on a task in DONE raises `IllegalTransitionError`; on CANCELLED likewise; `task.is_paused` unchanged
    - `set_paused(..., is_paused=False)` on a paused, non-terminal task succeeds and clears `is_paused`, leaving `task.state` unchanged
    - `set_paused(..., is_paused=False)` on a task whose `is_paused` is already False raises `IllegalTransitionError` (resume no-op guard, D-03)
    - `set_paused` writes NO `TaskTransition` row on either success path (pause or resume) — unchanged Phase 4 D-04 behavior
    - Each of the three `set_paused` rejection paths (pause on terminal, resume on terminal, resume no-op) appends exactly one `TaskTransition` row with `rejected is True`, `from_state == to_state == task.state` (self-loop), a non-empty `rejection_reason`, and `note == ""` — per D-09's unqualified "a rejected attempt is written as a row" and D-11's single-timeline requirement
    - `cancel_task` on a task in PLANNING/EXECUTION/VALIDATION succeeds and appends an accepted history row (unchanged)
    - `cancel_task` on a task already DONE raises `IllegalTransitionError` and appends a `rejected=True` row with `from_state=DONE`, `to_state=CANCELLED`; `task.state` stays DONE
    - `cancel_task` on an already-CANCELLED task raises `IllegalTransitionError` and appends a rejected row
    - After resume, a legal `transition_task` on that task still succeeds (resume continues along the graph, TRANS-03)
  </behavior>
  <action>
Implements D-03, D-04, and D-09's persistence rule for pause/resume rejections.

In `agent/tasks.py::set_paused`, insert the guards immediately after the existing `task = await _get_owned_task(...)` line and before any mutation. Compute a single local `rejection_reason: str | None = None`:
- if `task.state in _TERMINAL_STATES`, set it to a reason naming the task id, its terminal state, and whether the blocked operation was pause or resume (branch on the `is_paused` parameter for the wording);
- otherwise, for the resume case only (`is_paused` is False), if `task.is_paused` is already False, set it to a reason saying the task is not paused so there is nothing to resume.

When `rejection_reason` is not None, persist the refusal before raising: `session.add(TaskTransition(task_id=task.id, from_state=task.state, to_state=task.state, note="", rejected=True, rejection_reason=rejection_reason))`, then commit using the same `try: await session.commit() / except Exception: await session.rollback(); raise` pattern the function already uses for its success path (a separate, earlier try block — do NOT call `session.refresh(task)` on this branch), then `logger.warning("task_pause_toggle_rejected", task_id=..., chat_id=..., state=task.state.value, is_paused=task.is_paused, requested=is_paused)`, then `raise IllegalTransitionError(task.id, task.state, task.state, rejection_reason)`. Write exactly ONE `TaskTransition(` construction in `set_paused`, shared by all three rejection branches — the branches only differ in the reason string.

Why a self-loop row: pause/resume has no target state, so `from_state == to_state == task.state` is the marker that this row is a blocked *operation* rather than a blocked *edge move*. This is the same `IllegalTransitionError` type reused per CONTEXT.md's discretion note, and it keeps the Tasks-tab history a single chronological timeline of everything that was attempted (D-10/D-11). Plan 03's renderer branches on `from_state === to_state` to word these rows correctly.

Success paths are unchanged: a successful pause or resume still writes no `TaskTransition` row (Phase 4's D-04 — `is_paused` is orthogonal to lifecycle state). Update `set_paused`'s docstring to state exactly that asymmetry: no row on success, one `rejected=True` self-loop row on refusal.

In `agent/tasks.py::cancel_task`, insert a guard immediately after `_get_owned_task` and before any mutation: if `task.state in _TERMINAL_STATES`, build a reason naming the task id and its current terminal state, append `TaskTransition(task_id=task.id, from_state=task.state, to_state=TaskState.CANCELLED, note="", rejected=True, rejection_reason=reason)`, commit inside the existing try/except rollback scaffolding (no `session.refresh(task)`), `logger.warning("task_cancel_rejected", task_id=..., chat_id=..., from_state=task.state.value)`, and raise `IllegalTransitionError(task.id, task.state, TaskState.CANCELLED, reason)`. Replace the "There is deliberately no legality check on the previous state -- transition-graph legality enforcement is Phase 6's job (TRANS-01)." docstring sentence with the terminal-state rule.

Add the `<behavior>` tests to `tests/test_tasks.py`, reusing the existing helpers. Seed terminal-state tasks by driving legal transitions through the graph (planning→execution→validation→done) rather than writing `TaskState.DONE` directly, so the tests also exercise the accepted path.
  </action>
  <acceptance_criteria>
    - `grep -n "_TERMINAL_STATES" agent/tasks.py` returns at least 4 hits (definition, `_legal_targets`, `set_paused`, `cancel_task`)
    - In `agent/tasks.py::set_paused`, `task.is_paused = is_paused` appears AFTER both new guards
    - `grep -v '^\s*#' agent/tasks.py | grep -c "TaskTransition("` returns 6 (create_task, transition_task accepted, transition_task rejected, set_paused rejected, cancel_task accepted, cancel_task rejected); exactly one of those occurrences is inside `set_paused`'s body and it carries `rejected=True`
    - `pytest tests/test_tasks.py -v` passes with all new guard tests
    - A test asserts resuming a non-paused task raises `IllegalTransitionError` AND that exactly one new `TaskTransition` row exists afterwards, with `rejected is True`, `from_state == to_state == task.state`, and a truthy `rejection_reason`
    - A test asserts pausing a DONE task raises `IllegalTransitionError` and likewise appends one self-loop `rejected=True` row
    - A test asserts a SUCCESSFUL pause and a SUCCESSFUL resume each add no `TaskTransition` row (row count before == row count after)
    - A test asserts a cancel attempt on a DONE task leaves `task.state is TaskState.DONE` and appends one `rejected=True` row
  </acceptance_criteria>
  <verify>
    <automated>pytest tests/test_tasks.py tests/test_task_api.py -v</automated>
  </verify>
  <done>Pause/resume/cancel refuse terminal-state and no-op operations with `IllegalTransitionError`, each refusal persisted as a `rejected=True` history row (self-loop for pause/resume); successful pause/resume still write no row; resume still lets a task continue along the graph.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM tool call → `agent/tasks.py` | The model chooses `task_id` and `new_state`; both are attacker-influenceable through chat content |
| Browser REST call → `agent/main.py` → `agent/tasks.py` | Authenticated user supplies an arbitrary `task_id` path parameter |
| Existing `app.db` on disk → `init_db()` | Schema drift between a deployed database and the current model definitions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Information Disclosure | `agent/tasks.py::transition_task` / `set_paused` / `cancel_task` | mitigate | Keep `_get_owned_task(session, user_id, chat_id, task_id)` as the first statement in every one of the three functions; the legality check and its rejected-row write run strictly after it, so a non-owner always receives `TaskNotFoundError`, never a persisted row, and can never distinguish "task exists in state X" from "task does not exist". Enforced by an explicit test (another user's task_id + an illegal target state must raise `TaskNotFoundError`). |
| T-06-02 | Tampering | `agent/tasks.py::transition_task` | mitigate | Server-side edge table is the only authority; no client- or LLM-supplied value can widen `_FORWARD_EDGES`/`_BACKWARD_EDGES`. Illegal branch never mutates `task.state`. |
| T-06-03 | Tampering | `agent/tasks.py::cancel_task` | mitigate | Terminal-state guard prevents overwriting a DONE task's outcome with CANCELLED after the fact. |
| T-06-04 | Denial of Service | Concurrent transitions on one task | accept | Already mitigated by the pre-existing per-chat `asyncio.Lock` in `agent/state.py::chat_locks`, acquired by both the WS handler and all three REST task endpoints; no new concurrency primitive introduced. |
| T-06-05 | Tampering | `shared/database.py` schema migration | mitigate | Additive, idempotent `ALTER TABLE ... ADD COLUMN` guarded by `PRAGMA table_info`; no data-destructive DDL, no drop/rename. Idempotency proven by test. |
| T-06-06 | Denial of Service | Rejected-row writes on every refusal path (incl. pause/resume) | accept | A repeatedly-refused operation appends one small row per attempt, bounded by the same per-chat `asyncio.Lock` serialization and by the LLM turn rate; `rejection_reason` strings are server-built and short. No unbounded client-controlled growth vector; matches Phase 5's `InvariantConflict` persistence precedent (D-13). |
| T-06-SC | Tampering | pip installs | accept | This phase installs zero new packages (RESEARCH.md § Package Legitimacy Audit: "Not applicable"); `requirements.txt` is untouched. Executor must not add a dependency — if one seems necessary, stop and report instead. |
</threat_model>

<verification>
- `pytest tests/ -v` passes in full (no regressions in `test_task_api.py`, `test_task_ws.py`, `test_tools.py`, `test_context_engine_tasks.py` from the model/CRUD changes)
- `grep -rn "Phase 6's job" agent/tasks.py` returns no hits
- Running the app once against an existing `app.db` (`python run.py`, then Ctrl-C) logs `migrating_tasktransition_add_rejection_columns` on first run and not on the second
</verification>

<success_criteria>
- Illegal single-call transitions raise `IllegalTransitionError` with a human-readable reason, and the task's state is provably unchanged
- Every refusal path — `transition_task`, `cancel_task`, and `set_paused` (pause-on-terminal, resume-on-terminal, resume-no-op) — persists a `rejected=True` `TaskTransition` row with `from_state`/`to_state`/`rejection_reason`; pause/resume rows are self-loops
- Successful pause/resume still write no history row (Phase 4 D-04 preserved)
- Ownership check precedes every legality check in all three CRUD functions
- The two new columns are added idempotently to a pre-existing database
</success_criteria>

<output>
Create `.planning/phases/06-controlled-transitions-day-15/06-01-SUMMARY.md` when done
</output>
