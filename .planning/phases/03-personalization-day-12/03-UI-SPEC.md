---
phase: 3
slug: personalization-day-12
status: draft
shadcn_initialized: false
preset: none
created: 2026-09-20
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — project hard constraint forbids npm/bundler/Node packages (`CLAUDE.md` §Hard constraints, §Frontend); frontend is vanilla JS + Tailwind CDN + Marked.js + DOMPurify only. shadcn's `npx`-based init/CLI tooling is out of scope for this repo. |
| Preset | not applicable |
| Component library | none (hand-written Tailwind utility classes, no Radix/Base UI) |
| Icon library | none — existing UI uses inline emoji glyphs (⚙️, 👥) for icon-like affordances, no icon font/SVG set. Profile tab needs no new icon (text label only, matching the Memory panel's plain `<h3>` heading). |
| Font | System default (no `font-family` override anywhere in `index.html`/CSS) — Tailwind's default sans stack |

This phase adds a second sidebar panel (`#profile-panel`) to an already-shipped design system. All tokens below are **extracted from the existing codebase** (`ui/static/index.html`, `ui/static/app.js`), not invented — the job is 100% consistency with the shipped Memory panel (`#memory-panel`), Settings modal, and toast system.

---

## Spacing Scale

Declared values, as actually used by the existing sidebar/panel/form components (Tailwind's 4px-increment scale — the project already uses 12px and 20px steps in addition to the canonical 8-point set, so both are declared as legitimate, not exceptions):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon/inline gaps (`gap-1`, gap between typing dots) |
| sm | 8px | Compact spacing (`mb-2`, `gap-2`, `space-y-1`) between panel rows/buttons |
| — | 12px | Panel padding (`p-3` on `#memory-panel`, `#agent-status`) — **use this for `#profile-panel` padding to match `#memory-panel` exactly** |
| md | 16px | Sidebar top padding (`p-4`), form field vertical rhythm (`space-y-4` in Settings form) |
| — | 20px | Modal header/body padding (`px-5 py-4`, `p-5` in Settings/Add-user modals) — not used by this phase (no modal), included for completeness |
| lg | 24px | Not currently used in sidebar/panel components |

Exceptions: none — this phase reuses `#memory-panel`'s exact padding/gap values for `#profile-panel`, per the research anti-pattern warning against introducing new spacing.

---

## Typography

Sizes/weights extracted from the existing Memory panel, Settings form, and sidebar header — the Profile panel and its edit form must reuse these exactly (no new sizes/weights introduced):

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Panel heading (`#profile-panel` title, mirrors `#memory-panel`'s `<h3>`) | 12px (`text-xs`, inherited from panel container) | 600 (`font-semibold`) | default (~1.4) |
| Field label (`<label>` above each textarea, mirrors Settings form labels) | 14px (`text-sm`) | 400 (`font-normal`, default — matches `settings-system-prompt`'s label, not bolded) | 1.5 |
| Body / textarea content (profile field text, matches message/system-prompt textareas) | 14px (`text-sm`) | 400 | 1.5 |
| Helper/hint text (below each field, matches `settings-context-length`'s hint paragraphs) | 12px (`text-xs`) | 400 | 1.4 |
| Primary action button label ("Сохранить профиль") | 14px (`text-sm`) | 500 (`font-medium` — matches every other primary button: `btn-new-chat`, `btn-send`, Settings' submit) | default |

Declared set for this phase: **3 sizes** (12px, 14px — the panel reuses only these two plus the pre-existing 18px `text-lg` on the unrelated chat-title header, not touched by this phase), **2 weights** (400 regular, 600 semibold), with the pre-existing 500 (`font-medium`) exception already established codebase-wide for button labels — do not introduce a third new weight.

---

## Color

Hex values resolved from Tailwind's default `slate`/`indigo`/`red` palettes, matching every existing surface in `index.html`:

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#020617` (`bg-slate-950`) | Page background (`<body>`), main chat area |
| Secondary (30%) | `#0f172a` (`bg-slate-900`) sidebar/header + `#1e293b` (`bg-slate-800`) cards/inputs | Sidebar (`<aside>`), header bar, `#profile-panel` textareas/inputs (`bg-slate-800 border-slate-700`, exactly matching `#memory-panel`'s entry rows and the Settings form's inputs) |
| Accent (10%) | `#4f46e5` / hover `#6366f1` (`bg-indigo-600` / `hover:bg-indigo-500`) | **Reserved exclusively for:** the "Сохранить профиль" primary button, textarea focus rings (`focus:ring-indigo-500`), and any active/selected-state indicator on the Profile tab (if a tab-active affordance is added) — never for panel chrome, labels, or body text |
| Destructive | `#f87171` (`text-red-400`) | Reserved for error copy only (e.g. save-failure inline text, mirrors `add-user-error`'s `text-red-400`) — no destructive *action* exists in this phase (no delete-profile control), so this token is copy-only, not button-only |

Accent reserved for: the Profile panel's save button, its textarea focus rings, and (if implemented) an active-tab underline/background — nothing else. All borders/dividers use neutral `border-slate-700`/`border-slate-800`, never indigo.

---

## Copywriting Contract

Russian-language copy, matching the existing UI's language and tone (`index.html` is entirely in Russian: "Новый чат", "Отправить", "Сохранить", "Отмена"):

| Element | Copy |
|---------|------|
| Primary CTA | **"Сохранить профиль"** (not the bare "Сохранить" used by Settings — the extra noun disambiguates which form was submitted, since a toast/error may fire while both panels are technically reachable) |
| Panel heading | "Профиль" (mirrors `#memory-panel`'s single-word `<h3>` "Память") |
| Field labels | "Стиль общения" (style), "Формат ответа" (format), "Ограничения" (constraints) — plain field-name labels matching Settings form's label style (`text-slate-400`, not bold) |
| Field placeholders (empty-state, since this is an edit form not a list — no fields ever render a separate "empty" banner) | style: "Например: коротко и по делу, без длинных вступлений"; format: "Например: списком, с примерами кода"; constraints: "Например: не используй жаргон, всегда указывай источники" — placeholder text disappears on input, mirrors the `message-input` textarea's `placeholder="Введите сообщение…"` pattern |
| Save success | Toast, `type: 'success'` (existing `showToast()` emerald variant): "Профиль сохранён" |
| Error state | Toast, `type: 'error'` (existing `showToast()` red variant, mirrors `apiFetch()`'s thrown-error → toast pattern already used elsewhere): "Не удалось сохранить профиль. Проверьте соединение и попробуйте снова." |
| Destructive confirmation | Not applicable — this phase has no destructive action (profile fields are overwritten via normal save, never deleted/reset). If a future phase adds a "Clear profile" control, it must follow the same confirm-before-destroy pattern as any future delete action; out of scope here. |

---

## Registry Safety

Not applicable — no shadcn registry is used anywhere in this project (`Tool: none` above). This section is a structural no-op for this phase.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable |
| third-party | none | not applicable |

---

## Component & Interaction Notes (phase-specific, non-template)

These supplement the template sections above with the concrete layout contract the executor needs, derived directly from `03-RESEARCH.md`'s Anti-Patterns and the Memory-panel precedent:

- **Layout: second stacked panel, not a tab-switcher.** `03-CONTEXT.md`'s D-04 says "sidebar tab," but `03-RESEARCH.md` confirms no tab-switching UI exists anywhere in this codebase (`#memory-panel` is a permanently-visible stacked block). Build `#profile-panel` as a second `<div>` stacked in the sidebar (below `#memory-panel`, above `#agent-status`), using identical container classes: `border-t border-slate-800 p-3 text-xs text-slate-400` (or `overflow-y-auto` only if content can overflow — Profile's 3 fixed textareas are unlikely to need it). Do not introduce a new tab-switcher JS pattern.
- **Form fields:** three `<textarea>` elements (style/format/constraints), each preceded by a `<label class="block text-sm text-slate-400 mb-1">`, each styled `w-full resize-none rounded-lg bg-slate-800 border border-slate-700 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500` — byte-for-byte matching `settings-system-prompt`'s textarea classes.
- **Save button:** single button below the three fields, styled identically to the Settings form's submit button: `rounded-lg bg-indigo-600 hover:bg-indigo-500 px-4 py-2 text-sm font-medium transition`.
- **XSS safety:** any profile value rendered back into the DOM (e.g. on load) must use `.value` (for textareas) or `.textContent`, never `.innerHTML` — mirrors `renderMemoryEntries()`'s existing correct pattern and `03-RESEARCH.md` Pitfall 3.
- **Load-on-select:** mirror `loadChatMemory()`/`renderMemoryPanel()` — a `loadProfile()` called once on initial page load (profile is user-scoped, not chat-scoped, so it does not need to reload on chat switch, unlike Memory).
- **No loading skeleton needed:** given the tiny payload (3 short text fields) and precedent (Memory panel shows no loading state either), a blank/default-empty form while the fetch resolves is acceptable — do not build a spinner/skeleton.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
