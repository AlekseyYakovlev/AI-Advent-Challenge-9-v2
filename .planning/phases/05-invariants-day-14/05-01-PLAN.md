---
phase: 05-invariants-day-14
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/models.py
  - agent/schemas.py
  - agent/invariants.py
  - agent/main.py
  - ui/static/index.html
  - ui/static/app.js
  - tests/test_invariants.py
  - tests/test_invariants_api.py
autonomous: true
requirements: [INV-01, INV-05]

must_haves:
  truths:
    - "A user can type a title and a rule text in the sidebar and a global invariant row persists in SQLite, separate from the message tree (INV-01)"
    - "The GlobalInvariant table has no user_id and no chat_id column at all — the D-02 sharing guarantee is structural, not a runtime query choice"
    - "A second, different logged-in user sees and can edit/delete the exact same global invariant list (D-02, 'every account has equal admin capability')"
    - "An unauthenticated caller gets 401 from every /api/v1/invariants route"
    - "agent/tools.py is untouched by this phase — global invariant CRUD is UI-only via REST, never an LLM tool call (D-03)"
    - "A user can edit and delete an existing global invariant via PUT/DELETE, not just create one — full CRUD, not an append-only log (D-04)"
    - "The new Инварианты sidebar tab lists global invariants with add/edit/delete controls, at the same always-reachable placement as Память/Профиль/Задачи (D-10)"
    - "Every sidebar panel except the chat list (Память, Профиль, Задачи, Инварианты) renders collapsed on page load and expands/collapses from a ▸/▾ button in its own header (D-11)"
  artifacts:
    - path: "shared/models.py"
      provides: "GlobalInvariant table — title + rule_text (D-01), no ownership columns (D-02)"
      contains: "class GlobalInvariant(SQLModel, table=True)"
    - path: "agent/invariants.py"
      provides: "CRUD layer owning all GlobalInvariant reads and writes"
      min_lines: 60
      contains: "async def create_global"
    - path: "agent/schemas.py"
      provides: "GlobalInvariantCreate/Update/Response plus INVARIANT_* length constants"
      contains: "class GlobalInvariantCreate(BaseModel)"
    - path: "ui/static/index.html"
      provides: "#invariants-panel sidebar section and data-fold-toggle headers on all four panels"
      contains: "id=\"invariants-panel\""
    - path: "tests/test_invariants.py"
      provides: "GlobalInvariant CRUD-layer coverage including the no-scope-columns schema assertion"
    - path: "tests/test_invariants_api.py"
      provides: "REST coverage incl. cross-user shared access and 401-without-auth"
  key_links:
    - from: "agent/main.py"
      to: "agent/invariants.py::list_global"
      via: "GET /api/v1/invariants with no ownership filter"
      pattern: "invariants\\.list_global"
    - from: "ui/static/app.js"
      to: "/api/v1/invariants"
      via: "apiFetch inside loadInvariants"
      pattern: "'/api/v1/invariants'"
    - from: "ui/static/app.js"
      to: "all four sidebar panel bodies"
      via: "setupFoldablePanels binding every [data-fold-toggle] button"
      pattern: "data-fold-toggle|dataset\\.foldToggle"
---

<objective>
Deliver the first working vertical slice of Day 14: a user opens the new "Инварианты" sidebar tab, types a title and a rule, and that global ground rule is stored app-wide — visible and editable by every account. The same plan installs the fold/unfold mechanism the user asked for (D-11) across all four sidebar panels.

Purpose: INV-01 needs storage that is genuinely separate from the dialog and genuinely shared (D-02), plus a place to see it (INV-05). Building the table, the CRUD layer, the four REST routes and the panel in one slice makes the feature observable from the first turn instead of landing as three invisible backend plans.
Output: `GlobalInvariant` table, `agent/invariants.py`, `GET/POST/PUT/DELETE /api/v1/invariants`, `#invariants-panel`, `setupFoldablePanels()` applied to Память/Профиль/Задачи/Инварианты, and two new test modules.

Branch: work happens on `Day14` (create from `main` if it does not exist yet — PROJECT.md requires one branch per phase, pushed and merged to `main`, never deleted).
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-invariants-day-14/05-CONTEXT.md
@.planning/phases/05-invariants-day-14/05-RESEARCH.md
@.planning/phases/05-invariants-day-14/05-PATTERNS.md
@.planning/phases/05-invariants-day-14/05-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Contracts extracted from the codebase this session. Use directly — no exploration needed. -->

shared/models.py — top-of-file imports already present, reuse without re-adding:
  from datetime import datetime, timezone
  from enum import Enum
  from typing import Optional
  from sqlalchemy import Column, Enum as SAEnum, ForeignKey, Integer, UniqueConstraint
  from sqlmodel import Field, SQLModel
  # Profile (lines 188-205) is the max_length=2000 field-size precedent.
  # No class in this file sets __tablename__ — SQLModel resolves GlobalInvariant -> "globalinvariant".

agent/schemas.py — existing constants block, lines 11-25 (extend, do not fork):
  TITLE_MAX_LENGTH = 200 ... PROFILE_FIELD_MAX_LENGTH = 2000, TASK_NOTE_MAX_LENGTH = 2_000
  # ProfileUpdate (line 204) / ProfileResponse (line 212) are the Create/Update/Response shape to mirror.

agent/main.py:
  async def _get_chat_or_404(session: AsyncSession, chat_id: int, user_id: int) -> Chat      # line 86
  def _profile_to_response(row: Profile) -> ProfileResponse                                  # line 160
  @app.get("/api/v1/profile") get_profile(...)                                               # line 622
  @app.put("/api/v1/profile") update_profile(...)                                            # line 632
  # imports to extend: `from agent import memory, profile, tasks` (line 38),
  #   `from agent.schemas import (...)` (line 16), `from shared.models import (...)` (line 51)
  # HTTPException / status / Depends / get_current_user / get_session are already imported.

agent/profile.py — the exact CRUD module shape to mirror (61 lines, read it in full):
  logger = get_logger(__name__)
  try: await session.commit(); await session.refresh(row)
  except Exception: await session.rollback(); raise

ui/static/index.html (sidebar <aside>, lines 40-105):
  line 47  <nav id="chat-list" ...>            <- stays untouched, NOT foldable
  line 48  <div id="memory-panel" ...>   h3 "Память"          (font-semibold)
  line 71  <div id="profile-panel" ...>  h3 "Профиль"         (currently font-medium)
  line 93  <div id="task-panel" ...>     h3 "Задачи <span id="task-count">"
  line 97  <div id="agent-status" ...>   <- new panel goes immediately BEFORE this

ui/static/app.js:
  const state = {...}                      # lines 9-31, currently ends with `lastTasks: null,`
  const $ = (id) => document.getElementById(id);              # line 49
  function showToast(message, type)        # line 51 — 'error' | 'success' | 'info' | 'warning'
  async function apiFetch(path, options)   # line 66 — returns null on 204, throws Error on non-2xx
  function renderTaskPanel()               # line 364 — replaceChildren/createElement/textContent shape
  async function saveProfile()             # line 515 — apiFetch PUT + try/catch + showToast shape
  function bindEvents()                    # line 1082, ends line 1169
  async function init()                    # line 1171 — calls bindEvents(), loadModels(), loadProfile(), loadChats()

tests/conftest.py fixtures:
  authenticated_client -> AsyncClient (has `.seeded_user_id`)
  second_authenticated_client -> AsyncClient logged in as a DIFFERENT user
  client -> unauthenticated AsyncClient
  async_session_factory (from shared.database) for direct DB setup/assertions
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing tests for global invariant storage and its shared REST surface</name>
  <files>tests/test_invariants.py, tests/test_invariants_api.py</files>
  <read_first>
    - tests/conftest.py (fixture shapes: client, authenticated_client, second_authenticated_client, clean_test_db)
    - tests/test_profile.py (CRUD-layer unit test shape against async_session_factory)
    - tests/test_profile_api.py (REST endpoint test shape with authenticated_client)
    - .planning/phases/05-invariants-day-14/05-CONTEXT.md (D-01, D-02, D-04 — the decisions these tests encode)
  </read_first>
  <behavior>
    tests/test_invariants.py (CRUD layer, via `agent.invariants` and `async_session_factory`):
    - `test_create_global_invariant_persists_title_and_rule`: call `invariants.create_global(session, "Без Docker", "Никогда не предлагай Docker")`;
      assert the returned row has a non-None `id`, the exact title/rule_text, and `created_at`/`updated_at` both set.
    - `test_list_global_invariants_returns_all_rows_oldest_first`: create three globals; assert `list_global` returns
      three rows whose ids match creation order.
    - `test_update_global_invariant_changes_fields_and_advances_updated_at`: create one, capture `updated_at`,
      call `update_global(session, row_id, title="X", rule_text="Y")`; assert both fields changed and the new
      `updated_at` is greater than or equal to the captured one.
    - `test_update_global_invariant_returns_none_for_unknown_id`: `update_global(session, 999_999, title="X")` returns None.
    - `test_delete_global_invariant_removes_row`: `delete_global` returns True and a follow-up `list_global` is empty;
      a second `delete_global` on the same id returns False.
    - `test_global_invariant_table_has_no_ownership_columns` (D-02, structural): assert
      `"user_id" not in GlobalInvariant.__table__.columns` and `"chat_id" not in GlobalInvariant.__table__.columns`.

    tests/test_invariants_api.py (REST, via the httpx fixtures):
    - `test_list_global_invariants_requires_auth`: unauthenticated `client.get("/api/v1/invariants")` returns 401.
    - `test_create_global_invariant_returns_201_with_body`: POST `{"title": "Без Docker", "rule_text": "..."}`
      returns 201 and a body carrying `id`, `title`, `rule_text`, `created_at`, `updated_at`.
    - `test_create_global_invariant_rejects_empty_title`: POST with `{"title": "", "rule_text": "x"}` returns 422.
    - `test_second_user_sees_and_edits_the_same_global_invariant` (D-02): user A POSTs one; `second_authenticated_client`
      GETs `/api/v1/invariants` and finds it, then PUTs a new `rule_text` on it and receives 200 (explicitly NOT 403/404) —
      this test is the executable statement of "global invariants are shared, not owned".
    - `test_update_global_invariant_unknown_id_returns_404`: PUT `/api/v1/invariants/999999` returns 404.
    - `test_delete_global_invariant_returns_204_and_removes_it`: DELETE returns 204 and a follow-up GET list is empty.
    - `test_delete_global_invariant_unknown_id_returns_404`.
  </behavior>
  <action>
    Create `tests/test_invariants.py` and `tests/test_invariants_api.py` implementing exactly the behaviours above.
    Import `GlobalInvariant` from `shared.models`, `invariants` from `agent`, `async_session_factory` from
    `shared.database`. Use the `client` / `authenticated_client` / `second_authenticated_client` fixtures from
    `tests/conftest.py` for the REST module. Follow `pytest.ini`'s `asyncio_mode = auto` and match whichever
    marker style `tests/test_profile_api.py` already uses.
    These tests MUST fail right now because `shared.models.GlobalInvariant`, `agent/invariants.py` and the four
    routes do not exist. Do not create stub modules or placeholder classes to make collection succeed.
  </action>
  <verify>
    <automated>python -m pytest tests/test_invariants.py tests/test_invariants_api.py -x</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_invariants.py tests/test_invariants_api.py -x` exits NON-ZERO (RED phase — expected ImportError/AttributeError).
    - `tests/test_invariants.py` defines the six test function names listed in `<behavior>`.
    - `tests/test_invariants_api.py` defines the seven test function names listed in `<behavior>`.
    - `grep -c "second_authenticated_client" tests/test_invariants_api.py` returns at least 1.
    - `grep -c "user_id" tests/test_invariants.py` returns at least 1 (the structural D-02 assertion).
    - Neither file imports or creates a stub/placeholder implementation module.
    - `python -m pytest tests/ -q --ignore=tests/test_invariants.py --ignore=tests/test_invariants_api.py` still passes.
  </acceptance_criteria>
  <done>Both new test modules exist, encode D-01/D-02/D-04 as executable assertions, and fail only because the implementation does not exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GlobalInvariant storage, the invariants CRUD module and the four shared REST routes</name>
  <files>shared/models.py, agent/schemas.py, agent/invariants.py, agent/main.py</files>
  <read_first>
    - shared/models.py (existing imports at lines 1-8; `Profile` at lines 188-205 for the max_length convention; `Task` at 218-256 for the FK/cascade idiom)
    - agent/profile.py (full file — the CRUD module shape, logger usage and commit/rollback/refresh idiom to mirror)
    - agent/schemas.py lines 11-25 (constants block) and lines 204-220 (ProfileUpdate/ProfileResponse)
    - agent/main.py lines 1-60 (import blocks), 86-99 (`_get_chat_or_404`), 160-170 (`_profile_to_response`), 423-441 (`delete_chat` rollback idiom), 622-643 (profile routes — the unscoped precedent)
    - .planning/phases/05-invariants-day-14/05-PATTERNS.md (§`agent/invariants.py`, §`agent/main.py` — Analog A and Analog C)
    - .planning/phases/05-invariants-day-14/05-RESEARCH.md (§Pattern 1, §Pitfall 4 — the table-name trap)
  </read_first>
  <action>
    **shared/models.py** — append a new `class GlobalInvariant(SQLModel, table=True)` after `Profile`, reusing the
    file's existing imports. Docstring: `"""App-wide ground rule shared by every account (D-02 — deliberately NOT user_id-scoped)."""`
    Fields: `id: Optional[int] = Field(default=None, primary_key=True)`;
    `title: str = Field(max_length=200)`; `rule_text: str = Field(max_length=2000)`;
    `created_at` and `updated_at`, both `datetime = Field(default_factory=lambda: datetime.now(timezone.utc))`.
    Add NO `user_id`, NO `chat_id`, NO `UniqueConstraint` — the absence of ownership columns IS the D-02 guarantee
    and `tests/test_invariants.py::test_global_invariant_table_has_no_ownership_columns` asserts it.
    Do not set `__tablename__` (no class in this file does); SQLModel will resolve the table name to `globalinvariant`,
    which plan 05-02's `overrides_id` FK string depends on.
    No migration function is needed — `shared/database.py::init_db` already calls `SQLModel.metadata.create_all`
    unconditionally, the same mechanism that shipped `WorkingMemory` and `Task`.

    **agent/schemas.py** — extend the existing constants block with
    `INVARIANT_TITLE_MAX_LENGTH = 200` and `INVARIANT_RULE_MAX_LENGTH = 2000`. Then add:
    - `class GlobalInvariantCreate(BaseModel)`: `title: str = Field(min_length=1, max_length=INVARIANT_TITLE_MAX_LENGTH)`,
      `rule_text: str = Field(min_length=1, max_length=INVARIANT_RULE_MAX_LENGTH)`.
    - `class GlobalInvariantUpdate(BaseModel)`: `title: str | None = Field(default=None, min_length=1, max_length=INVARIANT_TITLE_MAX_LENGTH)`,
      `rule_text: str | None = Field(default=None, min_length=1, max_length=INVARIANT_RULE_MAX_LENGTH)`
      (consumed with `model_dump(exclude_unset=True)`, exactly like `ProfileUpdate`).
    - `class GlobalInvariantResponse(BaseModel)`: `id: int`, `title: str`, `rule_text: str`,
      `created_at: datetime`, `updated_at: datetime`.

    **agent/invariants.py** (NEW) — module docstring
    `"""Thin CRUD layer owning all reads and writes to the invariant tables."""`,
    imports ordered stdlib -> third-party -> local, `logger = get_logger(__name__)`. No FastAPI import in this module.
    Functions (all async, all fully type-hinted):
    - `list_global(session: AsyncSession) -> list[GlobalInvariant]` — `select(GlobalInvariant).order_by(GlobalInvariant.created_at, GlobalInvariant.id)`,
      `result = await session.exec(...)`, `return list(result.all())`. **No `user_id` filter — D-02.**
    - `get_global(session, invariant_id: int) -> GlobalInvariant | None` — `await session.get(GlobalInvariant, invariant_id)`.
    - `create_global(session, title: str, rule_text: str) -> GlobalInvariant` — construct, `session.add`,
      `try: await session.commit(); await session.refresh(row) except Exception: await session.rollback(); raise`,
      then `logger.info("invariant_created", scope="global", invariant_id=row.id)`, return row.
    - `update_global(session, invariant_id: int, **updates: str) -> GlobalInvariant | None` — load via `get_global`;
      return None when missing; `setattr` only the keys present in `updates`; set
      `row.updated_at = datetime.now(timezone.utc)`; same commit/rollback/refresh block;
      `logger.info("invariant_updated", scope="global", invariant_id=invariant_id)`.
    - `delete_global(session, invariant_id: int) -> bool` — load; return False when missing; else
      `try: await session.delete(row); await session.commit() except Exception: await session.rollback(); raise`;
      `logger.info("invariant_deleted", scope="global", invariant_id=invariant_id)`; return True.

    **agent/main.py** —
    - Extend `from agent import memory, profile, tasks` to include `invariants`.
    - Add `GlobalInvariantCreate`, `GlobalInvariantUpdate`, `GlobalInvariantResponse` to the `from agent.schemas import (...)` block
      and `GlobalInvariant` to the `from shared.models import (...)` block (keep both lists alphabetised as they are today).
    - Add `def _global_invariant_to_response(row: GlobalInvariant) -> GlobalInvariantResponse` next to `_profile_to_response`.
    - Add four routes directly after the existing profile routes (after line 641). Every one of them takes
      `current_user: User = Depends(get_current_user)` for authentication but applies **NO ownership filter and no
      `_get_chat_or_404` call** — that asymmetry is D-02 and is deliberate, not an oversight. Add a short comment
      on each route saying so, so a future code review does not "fix" it:
      * `@app.get("/api/v1/invariants", response_model=list[GlobalInvariantResponse])` -> `list_global_invariants`.
      * `@app.post("/api/v1/invariants", response_model=GlobalInvariantResponse, status_code=status.HTTP_201_CREATED)`
        -> `create_global_invariant(body: GlobalInvariantCreate, ...)`.
      * `@app.put("/api/v1/invariants/{invariant_id}", response_model=GlobalInvariantResponse)` ->
        `update_global_invariant(invariant_id: int, body: GlobalInvariantUpdate, ...)`; call
        `invariants.update_global(session, invariant_id, **body.model_dump(exclude_unset=True))` and raise
        `HTTPException(status_code=status.HTTP_404_NOT_FOUND, detail=f"Invariant {invariant_id} not found")` when it returns None.
      * `@app.delete("/api/v1/invariants/{invariant_id}", status_code=status.HTTP_204_NO_CONTENT)` ->
        `delete_global_invariant(invariant_id: int, ...)`; raise the same 404 when `delete_global` returns False; return None.

    Conventions that apply to every file above: type hints on all parameters and returns; `async`/`await` for all I/O;
    `datetime.now(timezone.utc)` never `datetime.utcnow()`; `structlog` via `get_logger(__name__)` never `print()`;
    `.is_(None)` never `== None` in `where()`; `await session.commit()` after writes with `await session.rollback()`
    in the exception handler; no bare `except:`. Do NOT touch `agent/tools.py` — D-03 states this phase registers
    no LLM tools for invariants.
  </action>
  <verify>
    <automated>python -m pytest tests/test_invariants.py tests/test_invariants_api.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_invariants.py tests/test_invariants_api.py -v` passes (all thirteen tests GREEN).
    - `python -m pytest tests/ -q` passes with zero failures (no regression in the existing 36 test modules).
    - `python -c "from shared.models import GlobalInvariant; print(GlobalInvariant.__tablename__, sorted(GlobalInvariant.__table__.columns.keys()))"` prints `globalinvariant` and exactly `['created_at', 'id', 'rule_text', 'title', 'updated_at']`.
    - `grep -cE "user_id|chat_id" agent/invariants.py` returns 0 at this point in the phase.
    - `git diff --stat agent/tools.py` shows no change (D-03).
    - `grep -c "await session.rollback()" agent/invariants.py` returns at least 3 (one per write function).
    - `grep -c "Field(foreign_key=\|Field(ondelete=" shared/models.py` returns 0.
    - `python -c "from agent.main import app; print(sorted({r.path for r in app.routes if 'invariant' in r.path}))"` prints `['/api/v1/invariants', '/api/v1/invariants/{invariant_id}']`.
  </acceptance_criteria>
  <done>Global invariants persist in a table with no ownership columns, and all four REST routes work, are auth-gated, and are deliberately shared across accounts.</done>
</task>

<task type="auto">
  <name>Task 3: Add the Инварианты sidebar panel and the fold/unfold retrofit across all four panels</name>
  <files>ui/static/index.html, ui/static/app.js</files>
  <read_first>
    - ui/static/index.html lines 37-106 (the whole `<aside>`: `#memory-panel`, `#profile-panel`, `#task-panel`, `#agent-status`)
    - ui/static/app.js lines 9-31 (state), 49-83 (`$`, `showToast`, `apiFetch`), 324-463 (`loadChatTasks` / `renderTaskPanel` — the list-with-actions shape), 496-528 (`loadProfile`/`saveProfile`), 1082-1187 (`bindEvents` and `init`)
    - .planning/phases/05-invariants-day-14/05-UI-SPEC.md (Copywriting Contract, Color table, Interaction Notes — every string below is quoted from it verbatim)
    - .planning/phases/05-invariants-day-14/05-PATTERNS.md (§`ui/static/index.html` + §`ui/static/app.js`, and the "No Analog Found" note on fold/unfold)
  </read_first>
  <action>
    **ui/static/index.html — fold/unfold retrofit (D-11), applied to `#memory-panel`, `#profile-panel`, `#task-panel`:**
    For each of those three panels, replace the existing `<h3>` with a header row of the form
    `<h3 class="text-slate-300 font-semibold mb-2 flex items-center justify-between">` containing (a) a `<span>`
    holding the existing heading text and any existing count span (`#task-count` stays in the header, visible while
    collapsed) and (b) a trailing
    `<button type="button" data-fold-toggle="{panel}-panel-body" aria-expanded="false" title="Свернуть/развернуть" class="text-slate-400 hover:text-white">▸</button>`.
    Then wrap every existing body element of that panel in `<div id="{panel}-panel-body" class="hidden">…</div>`
    (`memory-panel-body` wraps the three memory sub-blocks; `profile-panel-body` wraps the three textarea blocks
    plus `#btn-save-profile`; `task-panel-body` wraps `#task-list`). Normalise `#profile-panel`'s heading from
    `font-medium` to `font-semibold` so all four match UI-SPEC's 16px/600 panel-heading rule.
    `<nav id="chat-list">` gets NO toggle and NO wrapper — the chat list is explicitly excluded by D-11.

    **ui/static/index.html — new Инварианты panel:** insert immediately after the closing `</div>` of `#task-panel`
    and before `<div id="agent-status" ...>`:
    - Container `<div id="invariants-panel" class="border-t border-slate-800 p-3 text-xs text-slate-400 overflow-y-auto max-h-72">`
      (identical container classes to `#task-panel`).
    - Header `<h3 class="text-slate-300 font-semibold mb-2 flex items-center justify-between">` with a `<span>Инварианты
      <span id="invariant-conflict-badge" class="text-amber-400 font-semibold hidden">0</span></span>` and the
      `data-fold-toggle="invariants-panel-body"` button described above. The badge stays `hidden` and empty this plan —
      it is populated in 05-03; create the element now so no later plan has to re-touch the header markup.
    - Body `<div id="invariants-panel-body" class="hidden">` containing one section:
      a label row `<div class="flex items-center justify-between mb-1"><span class="text-slate-500">Глобальные</span><span id="invariant-global-count" class="text-white font-semibold">0</span></div>`,
      a list container `<div id="invariant-global-list" class="space-y-2"></div>`,
      then the create/edit form: `<input id="invariant-global-title" type="text" maxlength="200" placeholder="Название правила"
      class="mt-2 w-full rounded-lg bg-slate-800 border border-slate-700 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500">`,
      `<textarea id="invariant-global-rule" rows="2" maxlength="2000" placeholder="Текст правила"
      class="mt-1 w-full resize-none rounded-lg bg-slate-800 border border-slate-700 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-indigo-500"></textarea>`,
      and `<button id="btn-save-global-invariant" type="button"
      class="mt-1 w-full rounded-lg bg-indigo-600 hover:bg-indigo-500 px-4 py-2 text-sm font-medium transition">Добавить инвариант</button>`.
      Leave room below this section for plan 05-02's per-chat block — do not build it here.

    **ui/static/app.js:**
    - Add `lastGlobalInvariants: null,` and `editingGlobalInvariantId: null,` to the `state` object next to `lastTasks: null,`.
    - `function setupFoldablePanels()` — the single shared implementation (no per-panel copies): iterate
      `document.querySelectorAll('[data-fold-toggle]')`, resolve `document.getElementById(btn.dataset.foldToggle)`,
      skip when absent, and on click toggle `body.classList.toggle('hidden')`, then set
      `btn.textContent = collapsed ? '▸' : '▾'` and `btn.setAttribute('aria-expanded', String(!collapsed))`.
      Call it once from `bindEvents()`. Collapsed is the page-load default because the markup ships `class="hidden"`;
      fold state is deliberately not persisted across reloads (CONTEXT.md marks this low-stakes).
    - `async function loadInvariants()` — `const data = await apiFetch('/api/v1/invariants');`
      `state.lastGlobalInvariants = data; renderInvariantsPanel();` On error:
      `console.error('Failed to load invariants:', err)` and
      `showToast('Не удалось загрузить инварианты. Проверьте соединение и попробуйте снова.', 'error')`.
    - `function renderInvariantsPanel()` — mirror `renderTaskPanel`: bail when `state.lastGlobalInvariants === null`;
      set `$('invariant-global-count').textContent = String(items.length)`; `$('invariant-global-list').replaceChildren()`.
      Empty state: two stacked `text-slate-600` divs with textContent `Глобальных инвариантов пока нет` and
      `Добавьте первое правило — оно будет действовать во всех чатах`. Otherwise one card per item:
      `<div class="rounded-lg bg-slate-800 px-2 py-1">` holding a title div (`text-sm font-semibold text-slate-300`),
      a rule div (`text-slate-400`), and an action row (`flex items-center gap-2 mt-1`) with an "Изменить" button
      (`text-slate-400 hover:text-white`) and an "Удалить" button (`text-red-400 hover:text-red-300`).
      Build every node with `document.createElement` and assign text with `textContent` only — never `innerHTML`,
      never template-string HTML: invariant text is user-authored and later reaches the LLM prompt.
    - "Изменить" sets `state.editingGlobalInvariantId = item.id`, fills the two form fields from the card's values,
      and sets `$('btn-save-global-invariant').textContent = 'Сохранить инвариант'`.
    - "Удалить" calls `confirm('Удалить этот инвариант? Это действие нельзя отменить.')` first, then
      `apiFetch('/api/v1/invariants/' + id, { method: 'DELETE' })`, `showToast('Инвариант удалён', 'success')`,
      `loadInvariants()`. On failure: `showToast('Не удалось удалить инвариант. Проверьте соединение и попробуйте снова.', 'error')`.
    - `async function saveGlobalInvariant()` — read the two fields, bail with an error toast when either is blank;
      when `state.editingGlobalInvariantId` is null POST to `/api/v1/invariants`, otherwise PUT to
      `/api/v1/invariants/{id}`; on success clear both fields, reset `state.editingGlobalInvariantId = null`,
      reset the button label to `Добавить инвариант`, `showToast('Инвариант сохранён', 'success')`, then `loadInvariants()`.
      On failure: `showToast('Не удалось сохранить инвариант. Проверьте соединение и попробуйте снова.', 'error')`.
    - In `bindEvents()`: `setupFoldablePanels();` plus a click listener on `#btn-save-global-invariant` following the
      existing `$('btn-save-profile').addEventListener(...)` shape (`.catch((err) => showToast(err.message, 'error'))`).
    - In `init()`: call `await loadInvariants();` immediately after the existing `await loadProfile();`.
      Global invariants are not chat-scoped, so they load once at startup, not per chat selection.
    - Use camelCase identifiers throughout, matching this file's 100%-camelCase convention.
  </action>
  <verify>
    <automated>python -m pytest tests/ -q && node --check ui/static/app.js</automated>
  </verify>
  <acceptance_criteria>
    - `node --check ui/static/app.js` exits 0 (no syntax error).
    - `grep -c 'data-fold-toggle' ui/static/index.html` returns 4 (memory, profile, task, invariants — and no more).
    - `grep -cE 'id="(memory|profile|task|invariants)-panel-body"' ui/static/index.html` returns 4.
    - `grep -c 'id="invariants-panel"' ui/static/index.html` returns 1 and `grep -c 'id="invariant-conflict-badge"' ui/static/index.html` returns 1.
    - `grep -n 'invariants-panel' ui/static/index.html` shows the block after the line containing `id="task-list"` and before the line containing `id="agent-status"`.
    - `grep -c 'font-medium mb-2' ui/static/index.html` returns 0 (profile heading normalised to `font-semibold`).
    - `grep -c 'data-fold-toggle' ui/static/app.js` returns exactly 1 (one shared implementation, not four).
    - `grep -c "setupFoldablePanels" ui/static/app.js` returns 2 (definition + the single call in `bindEvents`).
    - `grep -c "loadInvariants" ui/static/app.js` returns at least 4 (definition + init + post-save + post-delete).
    - Each of these exact UI-SPEC strings appears in `ui/static/app.js`: `Глобальных инвариантов пока нет`, `Добавьте первое правило — оно будет действовать во всех чатах`, `Не удалось загрузить инварианты. Проверьте соединение и попробуйте снова.`, `Не удалось сохранить инвариант. Проверьте соединение и попробуйте снова.`, `Не удалось удалить инвариант. Проверьте соединение и попробуйте снова.`, `Инвариант сохранён`, `Инвариант удалён`, `Удалить этот инвариант? Это действие нельзя отменить.`, `Добавить инвариант`, `Сохранить инвариант`.
    - `sed -n '/^function renderInvariantsPanel/,/^}/p' ui/static/app.js | grep -c innerHTML` returns 0.
    - `python -m pytest tests/ -q` still passes.
  </acceptance_criteria>
  <done>Loading the app shows four collapsed sidebar panels with ▸ toggles; expanding Инварианты lets the user add, edit and delete an app-wide rule that another account also sees.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Agent REST | Session-cookie-authenticated but otherwise untrusted; `invariant_id` in the path is attacker-controlled |
| User-authored invariant text → Agent DB → browser DOM | Title/rule text is free-form user input, re-rendered into the sidebar for every account |
| User-authored invariant text → LLM system prompt (from 05-02 on) | Invariant text becomes prompt content; length caps bound the injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | all four `/api/v1/invariants` routes | mitigate | `current_user: User = Depends(get_current_user)` on every route — the Phase 1 session-cookie gate reused unchanged. Covered by `test_list_global_invariants_requires_auth`. |
| T-05-02 | Tampering | global invariant CRUD by any account | accept | D-02 + PROJECT.md: global invariants are an app-wide shared set and every account has equal "admin" capability, so no ownership check exists by design. Scoped to a single-deployment, handful-of-graders app. `test_second_user_sees_and_edits_the_same_global_invariant` pins this as intended behaviour so a later review does not "fix" it into an authz bug. Auth-only gating (T-05-01) is still required and enforced. |
| T-05-03 | Tampering | invariant text rendered in `renderInvariantsPanel` | mitigate | Every node built with `document.createElement` and text assigned via `textContent` — no `innerHTML`, no template-string HTML — so markup in a title or rule cannot execute in another account's session. Asserted by the `renderInvariantsPanel`/`innerHTML` grep gate. |
| T-05-04 | Denial of Service | oversized invariant text | mitigate | `INVARIANT_TITLE_MAX_LENGTH = 200` / `INVARIANT_RULE_MAX_LENGTH = 2000` enforced by Pydantic `Field(max_length=...)` at the API boundary before any DB write, and mirrored as `maxlength` on the two form controls. Bounds both row size and the future prompt-injection volume added in 05-02. |
| T-05-05 | Information Disclosure | unknown `invariant_id` on PUT/DELETE | accept | Global invariants are shared by design, so a 404-vs-200 distinction reveals nothing an authenticated user could not learn from `GET /api/v1/invariants`. No existence oracle exists to protect. |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | This phase installs zero new packages (05-RESEARCH.md §Package Legitimacy Audit: not applicable). No `requirements.txt` change; the slopcheck gate is not triggered. |
</threat_model>

<verification>
- `python -m pytest tests/ -q` → zero failures.
- `python -m pytest tests/test_invariants.py tests/test_invariants_api.py -v` → all thirteen tests pass.
- `python -c "from shared.models import GlobalInvariant; print(GlobalInvariant.__tablename__)"` → `globalinvariant` (the FK target string plan 05-02 depends on).
- `git diff --stat agent/tools.py` → empty (D-03: no LLM tools registered for invariants).
- `node --check ui/static/app.js` → exits 0.
- Manual smoke (formally verified in 05-04): `python run.py`, open the UI — all four sidebar panels render collapsed with a `▸` button; expanding Инварианты and adding a rule shows it in the list with a count of 1.
</verification>

<success_criteria>
- `GlobalInvariant` exists with exactly `id`, `title`, `rule_text`, `created_at`, `updated_at` — no ownership columns (INV-01, D-01, D-02).
- `agent/invariants.py` owns all reads/writes with commit/rollback on every write, and contains no `user_id`/`chat_id` reference at this stage.
- `GET/POST/PUT/DELETE /api/v1/invariants` are authenticated, unfiltered by owner, and return 404 for unknown ids.
- `agent/tools.py` is byte-identical to its pre-phase state (D-03).
- The `#invariants-panel` sidebar section lists global invariants with working add/edit/delete (INV-05, partial — per-chat list lands in 05-02, conflicts in 05-03).
- Память, Профиль, Задачи and Инварианты all ship collapsed with a working ▸/▾ toggle driven by one shared `setupFoldablePanels()` implementation (D-11); the chat list is untouched.
</success_criteria>

<output>
Create `.planning/phases/05-invariants-day-14/05-01-SUMMARY.md` when done
</output>
