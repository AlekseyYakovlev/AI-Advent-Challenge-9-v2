---
phase: quick-260924-48z
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/mcp_client.py
  - agent/main.py
  - agent/schemas.py
  - agent/dependencies.py
  - tests/fixtures/mcp_stdio_server.py
  - tests/fixtures/mcp_orphan_helper.py
  - tests/test_mcp_registry_robustness.py
  - tests/test_mcp_client.py
  - tests/test_mcp_orphan.py
  - tests/test_mcp_origin.py
  - docs/ARCHITECTURE.md
autonomous: true
requirements: [WR-01, WR-03, WR-04, WR-05, WR-06, WR-08]

must_haves:
  truths:
    - "Cancelling a request that is inside open_session leaves no fixture child process and nothing in mcp_client._sessions (WR-03)"
    - "A concurrent disconnect or reconnect always wins over a dead-handle status check: no stale PROCESS_EXITED survives (WR-05)"
    - "cleanup_server removes the per-server lock while holding it (WR-05)"
    - "A server that answers pings late stays CONNECTED and is not killed; a server whose process died is still reported PROCESS_EXITED (WR-06)"
    - "GET /api/v1/mcp/servers pings servers concurrently: N unresponsive servers cost about one LIVENESS_TIMEOUT, and one failing status check does not fail the list (WR-06)"
    - "A bad cwd produces a cwd-specific Russian message; a non-FileNotFoundError OSError produces SPAWN_FAILED with errno/strerror; a missing command stays COMMAND_NOT_FOUND (WR-08a)"
    - "read_stderr_tail reads at most the last 64 KiB of the capture file (WR-08b)"
    - "On Windows, hard-terminating a process that holds an MCP session leaves no orphaned fixture child (WR-04), proven by a PID-checked test"
    - "Mutating MCP routes (create, update, delete, connect, disconnect) reject a foreign Origin with 403 and accept an allowed or missing Origin; create/update reject a non-JSON Content-Type with 415; GET routes are unaffected (WR-01)"
  artifacts:
    - path: "agent/mcp_client.py"
      provides: "cancel-safe open_session, locked dead-handle branch, tri-state liveness probe, SPAWN_FAILED classification, bounded stderr tail"
      contains: "STDERR_TAIL_MAX_BYTES"
    - path: "agent/dependencies.py"
      provides: "require_allowed_origin and require_json_content_type dependencies"
      contains: "def require_allowed_origin"
    - path: "tests/test_mcp_registry_robustness.py"
      provides: "WR-03/WR-05/WR-06 regression tests"
    - path: "tests/test_mcp_orphan.py"
      provides: "Windows-only WR-04 orphan guard test"
    - path: "tests/test_mcp_origin.py"
      provides: "WR-01 origin and content-type tests"
    - path: "docs/ARCHITECTURE.md"
      provides: "WR-04 SDK job-object note and WR-01 residual-risk paragraph"
  key_links:
    - from: "agent/main.py::list_mcp_servers"
      to: "agent/mcp_client.py::get_status"
      via: "asyncio.gather(..., return_exceptions=True)"
      pattern: "asyncio\\.gather"
    - from: "agent/main.py (5 mutating MCP routes)"
      to: "agent/dependencies.py::require_allowed_origin"
      via: "dependencies=[Depends(...)] in the route decorator"
      pattern: "Depends\\(require_allowed_origin\\)"
    - from: "agent/dependencies.py"
      to: "agent/state.py::CORS_ORIGINS"
      via: "import"
      pattern: "from agent.state import CORS_ORIGINS"
---

<objective>
Fix the six remaining Phase 7 review warnings (WR-01, WR-03, WR-04, WR-05, WR-06, WR-08) exactly as decided in
260924-48z-TASK.md. The decisions are locked. Do not redesign. Where evidence contradicts a review claim, follow the
evidence and say so in the SUMMARY.

Purpose: make the MCP session registry cancel-safe and race-free, stop killing healthy busy servers, give accurate
spawn errors, bound stderr reads, prove there are no orphan processes on Windows, and add an origin defense to the
routes that can start arbitrary processes.
Output: four atomic commits (one per task), each with its own tests, plus a SUMMARY.

Out of scope: IN-01..IN-06 (a separate later task), WR-02 and WR-07 (already fixed), any UI change (none is needed, see Task 2).
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/quick/260924-48z-fix-review-warnings-wr-01-wr-03-wr-04-wr/260924-48z-TASK.md
@.planning/phases/07-mcp-connection-day-16/07-REVIEW.md
@./CLAUDE.md
@agent/mcp_client.py
@tests/fixtures/mcp_stdio_server.py
@tests/test_mcp_client.py

<rules>
- Branch Day16 (the executor worktree is on Day16's HEAD). Four commits, one per task. NO Co-Authored-By line in any
  commit message (a user rule that overrides any harness attribution reminder). No push and no merge (the orchestrator does that).
- No new dependencies (anyio, psutil, pywin32 are already installed as existing or transitive deps; ctypes is stdlib).
  No multiprocessing, no os.fork. Use structlog `logger`, never print (a fixture/helper script run as a child process may write to its own stdout, since that is its protocol).
- Project conventions: type hints everywhere, `X | None`, specific excepts only, `datetime.now(timezone.utc)`,
  module and function docstrings, log keys in snake_case with ids only (never env values or secrets).
- Baseline: `pytest tests/ -q` gives 420 passed. Report the real observed count in the SUMMARY.
</rules>

<safety>
CRITICAL. This is carried over from the TASK file and applies to every task:
- The user may start the app at any time. Before running any test that spawns processes, snapshot the running
  filesystem.exe PIDs (e.g. `powershell -c "Get-Process filesystem -ErrorAction SilentlyContinue | Select Id,Path"`)
  and treat every pre-existing one as PROTECTED (currently PID 26256, args C:\Projects\Temp). Never touch them.
- NEVER kill anything you did not start yourself in a test. Never kill by process name (no taskkill /IM, no
  Get-Process | Stop-Process). Never free ports 8000/8001 by killing their holder.
- Never run `python run.py` or uvicorn on 8000/8001. Never open, read or modify the user's `app.db`. pytest uses its own
  test DB (tests/conftest.py) and free ports.
- The WR-04 test may terminate ONLY the helper subprocess it launched. It must identify the fixture child by PID, and
  confirm it by cmdline containing the fixture path, parentage under the helper PID, and create_time. Before asserting,
  it must check that this exact PID disappears. Cleanup in `finally` may kill only that verified PID (and the helper).
- Test-side process discovery uses `psutil.Process().children(recursive=True)` (children of the test process only),
  diffed against a snapshot taken just before the spawn. It is never a system-wide name search.
</safety>

<interfaces>
Current agent/mcp_client.py contracts (read in full above; key points):
- `SessionHandle`: session, result, ready, close_requested, closed (asyncio.Event), error, task, errfile (text-mode TemporaryFile "w+", utf-8, errors="replace").
- `_sessions`, `_last_results`, `_locks`: dict[SessionKey, ...], SessionKey = tuple[int, int] (user_id, server_id).
- `classify_mcp_error(exc) -> tuple[McpErrorCode, str]`: TimeoutError, then _PROCESS_GONE_ERRORS, then any OSError -> COMMAND_NOT_FOUND (the bug), then "Connection closed", else PROTOCOL_ERROR.
- `read_stderr_tail(errfile, max_lines=20) -> str`: currently seek(0) + read() of the whole file.
- `build_error_result(code, detail, stderr_tail, timeout_seconds) -> McpConnectResult`: message = MCP_ERROR_MESSAGES[code].format(timeout=...).
- `open_session(command, args, env, cwd, timeout_seconds=None) -> tuple[SessionHandle | None, McpConnectResult]`: lines 199-205 catch only TimeoutError around `asyncio.wait_for(handle.ready.wait(), timeout + READY_GRACE_SECONDS)`.
- `connect_server`, `ensure_connected`, `disconnect_server` hold `_lock_for(key)`. `cleanup_server` calls disconnect_server and then pops `_locks` outside the lock. asyncio.Lock is NOT reentrant.
- `_is_alive(handle) -> bool`: any exception or a 2 s (`LIVENESS_TIMEOUT`) timeout on `session.send_ping()` counts as dead.
- `get_status(user_id, server_id)`: no lock; on a dead handle it pops, awaits close_handle and writes `_last_results[key] = PROCESS_EXITED`.
- `ensure_connected` uses `is_connected` (registry only, no ping), so it is already consistent with the WR-06 semantics. Keep tests/test_mcp_auto_connect.py green (test_dead_session_is_not_respawned relies on the finished-owner-task path).

agent/main.py:
- `list_mcp_servers` (about line 980): sequential `for row in rows: connection = await mcp_client.get_status(current_user.id, row.id)`.
- Mutating MCP routes: `POST /api/v1/mcp/servers` (create_mcp_server, body McpServerCreate), `PUT /api/v1/mcp/servers/{server_id}` (update_mcp_server, body McpServerUpdate), `DELETE /api/v1/mcp/servers/{server_id}` (delete_mcp_server, 204), `POST .../{server_id}/connect`, `POST .../{server_id}/disconnect` (no body). connect_mcp_server already maps unexpected exceptions to `build_error_result(McpErrorCode.PROTOCOL_ERROR, ..., app_config.MCP_CONNECT_TIMEOUT)`, so reuse that pattern.
- fastapi import line: `from fastapi import Cookie, Depends, FastAPI, HTTPException, Query, Response, WebSocket, status`.

agent/dependencies.py: imports `Cookie, Depends, HTTPException, status` from fastapi; defines `get_current_user`, `get_current_user_ws`.
agent/state.py: `CORS_ORIGINS = ["http://localhost:8000", "http://127.0.0.1:8000"]`. It imports only stdlib, so importing it from dependencies.py creates no cycle.
agent/schemas.py:142: `class McpErrorCode(str, Enum)` with COMMAND_NOT_FOUND, PROCESS_EXITED, HANDSHAKE_TIMEOUT, PROTOCOL_ERROR.

UI facts, verified: `ui/static/app.js:4` `AGENT_BASE = http://${window.location.hostname}:${AGENT_PORT}`. The page at :8000 calls the agent at :8001 cross-origin, so the browser sends `Origin: http://localhost:8000` or `http://127.0.0.1:8000`, and both are in CORS_ORIGINS. `apiFetch` (app.js:74) always sets `Content-Type: application/json`, including on DELETE, connect and disconnect. `renderMcpError` (app.js:1627) renders `conn.error_message`, `conn.detail` and `conn.stderr_tail` and never branches on `error_code`. No test and no code asserts "exactly four codes" (grep of tests/ agent/ ui/static confirmed; only agent/mcp_tools.py:172 reads `error_code.value` generically).

mcp SDK facts, verified (mcp 1.30.0 at C:\Python\Python313\Lib\site-packages\mcp):
- `mcp/os/win32/utilities.py::create_windows_process` (line ~136) calls `_create_job_object()` (lines 233-246). That creates a Job Object with `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` via pywin32 `win32job`. `_maybe_assign_process_to_job` (252-278) assigns the child and stores the handle on `process._job_object`. The job handle lives only in the Agent process, so when Windows tears down the Agent (TerminateProcess), the last handle closes and the OS kills every process in the job.
- Caveat: if `win32job` cannot be imported or job creation or assignment fails, the SDK only logs a warning and runs without a job. pywin32 is currently importable.
- `mcp/client/stdio/__init__.py::_terminate_process_tree` uses `terminate_windows_process_tree` (TerminateJobObject) on normal close.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Registry robustness (WR-03 cancel-safe open_session, WR-05 locked dead-handle branch and cleanup_server, WR-06 tri-state liveness and concurrent list)</name>
  <files>agent/mcp_client.py, agent/main.py, tests/fixtures/mcp_stdio_server.py, tests/test_mcp_registry_robustness.py</files>
  <behavior>
    - WR-03: start `connect_server(1, 1, sys.executable, [FIXTURE, "hang"], None, None)` as a task. Poll up to 10 s until a new psutil child of the test process whose cmdline contains FIXTURE and "hang" appears, and record its PID. Cancel the task, then assert CancelledError is raised when it is awaited. Assert that this exact PID is gone within 5 s (psutil.pid_exists false, or create_time differs), that (1, 1) is not in `mcp_client._sessions`, and that it is not in `_last_results`.
    - WR-05 (disconnect wins): connect "ok" for (1, 1). Make the handle dead the same way test_dead_session_is_not_respawned does (set close_requested, wait for closed). Hold `mcp_client._lock_for((1, 1))` in the test and start a `get_status(1, 1)` task. After `await asyncio.sleep(0.2)`, while still holding the lock, perform the disconnect effects (pop `_sessions` and `_last_results` for the key). Release the lock and await the task. Assert that (1, 1) is not in `_last_results` and that a fresh `get_status(1, 1)` returns NOT_CONNECTED.
    - WR-05 (reconnect wins): same setup, but inside the held lock install a freshly opened live handle (`await mcp_client.open_session(...)` with "ok"), set `_sessions[key]` to it and pop `_last_results`. After the task finishes, assert that `_last_results` has no key and that get_status returns CONNECTED for the new handle, which is not closed.
    - WR-05 (cleanup_server): after connect and `cleanup_server(1, 1)`, (1, 1) is not in `_locks`. Also run `cleanup_server` concurrently with a `connect_server` task via gather. Neither raises, and the final registry is consistent (either no session, or one live session and no lock leak other than an entry for a live session).
    - WR-06 (busy server kept): monkeypatch `mcp_client.LIVENESS_TIMEOUT` to 0.5. Connect a `slow_ping` fixture server whose ping delay is 3 s. `get_status` returns CONNECTED, `is_connected` stays true, and a second `get_status` also returns CONNECTED. The handle is never closed.
    - WR-06 (dead still detected): the existing tests/test_mcp_client.py::test_get_status_detects_exited_process ("die_after") must stay green UNMODIFIED if the primary approach below works.
    - WR-06 (concurrent list): with authenticated_client, create 3 servers via POST /api/v1/mcp/servers with args [FIXTURE, "slow_ping", "3"], connect each via POST .../connect, and monkeypatch LIVENESS_TIMEOUT to 0.5. Time GET /api/v1/mcp/servers. All three are CONNECTED and the elapsed time is under 1.3 s (sequential would be at least 1.5 s). A second case monkeypatches `mcp_client.get_status` to raise RuntimeError for one server id. The list still returns 200 and that server shows an ERROR/PROTOCOL_ERROR connection.
  </behavior>
  <action>
Fixture: add a `slow_ping` mode to tests/fixtures/mcp_stdio_server.py. The TASK calls it a "slow" server; the name `slow_ping` avoids confusion with the existing `slow` tool of tools_extra. It is a raw line-delimited JSON-RPC loop over stdin/stdout, similar to `_mode_bad_protocol`:
- `initialize` replies with a result that echoes the request's `params.protocolVersion`, `capabilities: {"tools": {}}` and `serverInfo {"name": "slow-ping", "version": "0"}`.
- Messages without an `id` (notifications) are ignored.
- `tools/list` replies `{"tools": []}`.
- `ping` blocks with `time.sleep(delay)` and then replies `{}`. `delay` is float(argv[2]), default 3.0. This simulates a single-threaded server busy with long work.
- Any other method replies with a JSON-RPC error -32601.
- The loop exits on EOF.
Update the module docstring and USAGE to list the new mode.

WR-03 (per TASK decision): in `open_session`, add an `except asyncio.CancelledError:` branch before the TimeoutError branch. It cancels `handle.task`, awaits it with `contextlib.suppress(asyncio.CancelledError, Exception)`, closes `handle.errfile` with `contextlib.suppress(OSError)`, and re-raises. Nothing is registered, because `connect_server` and `ensure_connected` only register after open_session returns.

WR-05 (per TASK decision):
- Add a non-pinging helper `_registry_status(key) -> McpConnectResult`. It returns the live handle's result if `_sessions[key]` is connected and has a result, else `_last_results.get(key)`, else NOT_CONNECTED.
- In `get_status`, keep the fast path lock-free: `handle is None` returns the remembered or NOT_CONNECTED result, and a probe that says alive or unresponsive returns `handle.result` WITHOUT taking the lock, because Settings polling must not block on a connect.
- Only for a DEAD probe: `async with _lock_for(key):` re-check `_sessions.get(key) is handle`. If not, return `_registry_status(key)` and write nothing, because a concurrent disconnect or connect wins. If yes, pop it, read the stderr tail, `await close_handle(handle)`, build PROCESS_EXITED, write `_last_results[key]`, and log `mcp_session_dead` with ids only.
- Extract the body of `disconnect_server` into `async def _disconnect_locked(key) -> None`, which assumes the lock is held. `disconnect_server` becomes `async with _lock_for(key): await _disconnect_locked(key)`.
- `cleanup_server` does `lock = _lock_for(key)`, then `async with lock:` it awaits `_disconnect_locked(key)` and pops `_locks[key]` only if `_locks.get(key) is lock`, all inside the lock. Do not call disconnect_server inside the lock (non-reentrant deadlock).
- `cleanup_user_sessions` and `cleanup_all_sessions` keep their current behavior.

WR-06 (per TASK decision):
- FIRST run a throwaway empirical probe (a script in the session scratchpad, not committed; it uses only the fixture and never the user's app). Connect a `die_after` fixture session via `mcp_client.connect_server`, wait 2.5 s, call `session.send_ping()` under `asyncio.wait_for(..., 3)`, and record the exception type, how long it took, and whether `handle.task.done()` becomes true shortly after. Record the finding in the SUMMARY.
- Replace `_is_alive` with `async def _probe(handle) -> Literal["alive", "unresponsive", "dead"]`, adding `from typing import Literal`.
  - DEAD when `handle.task` is None or done, `handle.closed` is set, or `handle.session` is None.
  - DEAD when send_ping raises one of `_DEAD_STREAM_ERRORS` = (anyio.ClosedResourceError, anyio.BrokenResourceError, anyio.EndOfStream, *_PROCESS_GONE_ERRORS). anyio is already installed as an mcp dependency.
  - DEAD when send_ping raises `mcp.shared.exceptions.McpError` whose str contains "Connection closed".
  - After a TimeoutError or any other Exception, yield once (`await asyncio.sleep(0)`) and re-check the task-done and closed conditions, since a transport failure ends the owner task. If either is now true, it is DEAD. Otherwise it is "unresponsive": log `mcp_ping_unresponsive` at warning level with the exception type name only.
  - Callers pass user_id and server_id for the log, so give `_probe` keyword args `user_id: int, server_id: int`.
- In get_status, "unresponsive" returns `handle.result`, i.e. still CONNECTED, and the session is kept.
- If the probe shows a dead process gives neither a stream error nor a finished owner task (only timeouts), use this fallback. Add `consecutive_ping_failures: int` to SessionHandle and treat `MAX_PING_FAILURES = 3` consecutive timeouts as DEAD, resetting the counter on a successful ping. In that case only, the die_after test may loop get_status up to 3 times. State that in the SUMMARY.
- Do not change `ensure_connected` or `is_connected`. They never ping, so they stay consistent. Confirm with tests/test_mcp_auto_connect.py.
- In agent/main.py `list_mcp_servers`: build `statuses = await asyncio.gather(*(mcp_client.get_status(current_user.id, row.id) for row in rows), return_exceptions=True)`. For each row and status pair:
  - An `Exception` instance is logged as `mcp_status_check_failed` (user_id, server_id, error type name only), and the connection becomes `mcp_client.build_error_result(McpErrorCode.PROTOCOL_ERROR, f"status check failed: {type(exc).__name__}", "", app_config.MCP_CONNECT_TIMEOUT)`, mirroring connect_mcp_server's unexpected-error handling.
  - A non-Exception BaseException is re-raised.
  - Keep the response order equal to the rows order.

Write tests/test_mcp_registry_robustness.py (module docstring, FIXTURE path constant like tests/test_mcp_client.py) implementing the behavior list. Every test that spawns a process relies on the autouse conftest teardown (`cleanup_all_sessions`) and follows the SAFETY rules (psutil children of the test process only). Commit: `fix(quick-260924-48z): make MCP registry cancel-safe, lock dead-handle status, keep busy servers (WR-03, WR-05, WR-06)`, with no Co-Authored-By.
  </action>
  <verify>
    <automated>cd C:/Projects/AiAdventAgentV2 && pytest tests/test_mcp_registry_robustness.py tests/test_mcp_client.py tests/test_mcp_auto_connect.py tests/test_mcp_api.py -q</automated>
  </verify>
  <done>All four test files pass. The new file covers WR-03 (PID gone, not registered), the WR-05 interleavings plus cleanup_server lock removal, and WR-06 (slow_ping kept CONNECTED, die_after still PROCESS_EXITED, 3 unresponsive servers listed in under 1.3 s, one failing status does not fail the list). The probe finding is noted for the SUMMARY. One commit with no Co-Authored-By.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Accurate spawn-error classification, cwd pre-check and bounded stderr tail (WR-08)</name>
  <files>agent/mcp_client.py, agent/schemas.py, tests/test_mcp_client.py</files>
  <behavior>
    - `connect_once_and_list(sys.executable, [FIXTURE, "ok"], cwd=str(tmp_path / "missing"))` returns ERROR with error_code SPAWN_FAILED. The error_message mentions the working folder (contains "cwd" or "Рабочая папка") and the path. No process is spawned: assert psutil shows no new fixture child.
    - `cwd` pointing at a regular file (not a directory) gives the same cwd-specific result.
    - A missing command ("C:/definitely/not/here/nope.exe") with a valid or absent cwd is still COMMAND_NOT_FOUND with the unchanged message (existing test_command_not_found stays green).
    - `classify_mcp_error(PermissionError(13, "Access is denied"))` returns SPAWN_FAILED. The derived reason contains "13" and "Access is denied".
    - `classify_mcp_error(OSError(193, "%1 is not a valid Win32 application"))` returns SPAWN_FAILED. `NotADirectoryError` returns SPAWN_FAILED. `FileNotFoundError` returns COMMAND_NOT_FOUND. BrokenPipeError and ConnectionResetError are still PROCESS_EXITED, since they are OSError subclasses and are checked first.
    - The SPAWN_FAILED error_message for an OSError contains the errno/strerror reason and never contains env values. Test with env={"SECRET_TOKEN": "supersecret123"} on a bad spawn and assert "supersecret123" is not in error_message, detail or stderr_tail.
    - read_stderr_tail on a text temp file with more than 64 KiB of lines returns the last 20 lines. A monkeypatched or spied read shows no more than 64 KiB + a small margin was read (e.g. check the returned first line is complete and that the implementation reads via a bounded read of `size - start` bytes). A small file still returns all lines, and a closed file returns "".
  </behavior>
  <action>
WR-08(a), per the TASK decision. Chosen option: add a new enum member `SPAWN_FAILED = "SPAWN_FAILED"` to `McpErrorCode` in agent/schemas.py. This is safe because renderMcpError renders the server-provided message and never branches on the code, no test asserts exactly four codes, and mcp_tools only reads `.value`. State this choice and the evidence in the SUMMARY.

In agent/mcp_client.py:
- Add the MCP_ERROR_MESSAGES entry for SPAWN_FAILED: "Не удалось запустить процесс сервера: {reason}."
- Add a constant message for the cwd case, e.g. `CWD_INVALID_MESSAGE = "Рабочая папка (cwd) не найдена или не является папкой: {cwd}"`, sent under code SPAWN_FAILED.
- Extend `build_error_result` with a keyword `reason: str = ""` and format with `timeout=..., reason=...`. The existing templates ignore the extra key. Keep all current call sites working unchanged.
- In `classify_mcp_error`, keep the order TimeoutError, then _PROCESS_GONE_ERRORS, then `FileNotFoundError` (COMMAND_NOT_FOUND), then any other `OSError` (SPAWN_FAILED), then "Connection closed", then PROTOCOL_ERROR. FileNotFoundError maps to COMMAND_NOT_FOUND only because cwd is validated up front, so a remaining FileNotFoundError is the command.
- Add `def spawn_failure_reason(exc: BaseException) -> str`. It returns, for the first OSError leaf, `f"[errno {e.errno}] {e.strerror}"` (plus `winerror` when present and different), falling back to `type(e).__name__`. Never include the filename's env or any env value.
- In `open_session`, after classifying, pass `reason=spawn_failure_reason(error)` to build_error_result when code is SPAWN_FAILED.
- In `open_session`, before creating the SessionHandle (so no temp file and no task are created), `if cwd and not os.path.isdir(cwd):` return `(None, McpConnectResult(status=ERROR, error_code=SPAWN_FAILED, error_message=CWD_INVALID_MESSAGE.format(cwd=cwd), detail="cwd is not a directory", stderr_tail=""))` and log `mcp_connect_failed` with error_code only. Add `import os`.
- Keep the existing `connect_server` and `ensure_connected` flows unchanged, so the result is remembered in `_last_results` like any failure.

WR-08(b):
- Add `STDERR_TAIL_MAX_BYTES = 64 * 1024`.
- Rewrite `read_stderr_tail` to work on the binary layer: `errfile.flush()`, then `raw = errfile.buffer`, `size = raw.seek(0, os.SEEK_END)`, `start = max(0, size - STDERR_TAIL_MAX_BYTES)`, `raw.seek(start)`, `data = raw.read(size - start)`, `text = data.decode("utf-8", errors="replace")`.
- If `start > 0`, drop everything up to and including the first newline, since that line is partial.
- Return the last `max_lines` lines. Keep catching `(OSError, ValueError)` and also `AttributeError`, returning "" in all three cases.
- Do not seek the text wrapper to an arbitrary byte offset (text-mode seek cookies are opaque).
- Do NOT add a size cap on the temp file itself. The SUMMARY must state that a chatty long-lived server can still grow the temp file (only reads are bounded).

Tests go in tests/test_mcp_client.py, next to the existing classification tests. Update `test_classify_mcp_error_unwraps_nested_groups` only by adding assertions. Keep all existing assertions. Commit: `fix(quick-260924-48z): classify spawn errors accurately and bound the stderr tail read (WR-08)`, with no Co-Authored-By.
  </action>
  <verify>
    <automated>cd C:/Projects/AiAdventAgentV2 && pytest tests/test_mcp_client.py tests/test_mcp_registry_robustness.py tests/test_mcp_api.py tests/test_mcp_auto_connect.py -q</automated>
  </verify>
  <done>SPAWN_FAILED exists and is used for non-FileNotFoundError OSErrors with an errno/strerror reason. A bad cwd gives a cwd-specific Russian message without spawning. A missing command is still COMMAND_NOT_FOUND. read_stderr_tail reads at most 64 KiB. All listed tests pass. One commit with no Co-Authored-By.</done>
</task>

<task type="auto">
  <name>Task 3: Windows orphan guard test for hard-terminated Agent, plus docs note (WR-04)</name>
  <files>tests/fixtures/mcp_orphan_helper.py, tests/test_mcp_orphan.py, docs/ARCHITECTURE.md</files>
  <action>
Evidence already gathered (cite it in docs and in the SUMMARY): mcp 1.30.0 `mcp/os/win32/utilities.py::create_windows_process` puts every stdio server into a Job Object created with `JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE` (`_create_job_object`, `_maybe_assign_process_to_job`). The Agent process holds the only job handle, so a hard TerminateProcess of the Agent closes it and Windows kills the job's processes. The test must PROVE this, not assume it.

Create tests/fixtures/mcp_orphan_helper.py, a standalone script with a module docstring. It inserts the project root (two levels up from the file) at `sys.path[0]` and snapshots `psutil.Process().children(recursive=True)` PIDs. It then opens a session with `await agent.mcp_client.open_session(sys.executable, [FIXTURE, "ok"], None, None, 15)`. If the session fails, it prints `ERROR <code>` and exits 1. Otherwise it finds the NEW child (recursive, not in the snapshot) whose cmdline contains the fixture path, writes `CHILD_PID=<pid>` to stdout, flushes, and awaits `asyncio.Event().wait()` forever, so it never closes the session gracefully. This stdout line is the helper's protocol, so writing to stdout is allowed here. The helper must not touch any DB: it only imports agent.mcp_client, which imports shared.config but opens no DB. The test also sets DB_PATH to a tmp file for safety.

Create tests/test_mcp_orphan.py, a module marked `pytest.mark.skipif(sys.platform != "win32", reason="Windows job-object behavior")`.
- Launch the helper with `asyncio.create_subprocess_exec(sys.executable, HELPER, stdout=PIPE, stderr=PIPE, env={**os.environ, "DB_PATH": str(tmp_path / "orphan.db"), "PYTHONPATH": project_root})`.
- Read lines under `asyncio.wait_for(..., 30)` until `CHILD_PID=` appears (on an `ERROR` line or EOF, fail with the helper's stderr).
- Verify ownership before acting: `child = psutil.Process(pid)`. Its cmdline must contain the fixture path and "ok", and the helper's PID must appear in `[p.pid for p in child.parents()]` (this proves the test started it). Record `child.create_time()`.
- Hard-terminate ONLY the helper, exactly like ui/supervisor.py::_shutdown_agent: `proc.terminate()`, then `await asyncio.wait_for(proc.wait(), 5)`, then `proc.kill()` on timeout.
- Poll for up to 5 s until `not psutil.pid_exists(pid)` or the PID exists with a different create_time (PID reuse). Assert the child is gone.
- `finally`: if the helper is still running, kill that helper process object. If the verified child PID still exists with the SAME create_time, kill that psutil.Process only (it is ours by the checks above), so a failing run leaks nothing.
- Never search by name and never touch pre-existing filesystem.exe processes (see SAFETY).

Run the test.
- If it PASSES, the finding is mitigated by the SDK. Add a short paragraph to docs/ARCHITECTURE.md under the "Known risks" area of "## Chat tool calls (built-in + MCP)", titled e.g. "**Child processes on Agent restart:**". Say that the supervisor's terminate() is a hard kill on Windows and the lifespan cleanup does not run. Say that the mcp SDK (1.30, `mcp/os/win32/utilities.py`) starts each stdio server in a Job Object with KILL_ON_JOB_CLOSE, so Windows kills the servers when the Agent dies. Note that this relies on pywin32 (`win32job`) being importable; otherwise the SDK only logs a warning. Note that tests/test_mcp_orphan.py guards the behavior. Keep all existing text. docs/ARCHITECTURE.md uses CRLF line endings: preserve them (check afterwards that `file docs/ARCHITECTURE.md` still reports CRLF, and that the count of lines without \r is 0, e.g. `grep -c $'[^\r]$' docs/ARCHITECTURE.md` gives 0 for non-empty lines).
- If it FAILS (orphan observed), implement a real fix in agent/mcp_client.py. After the transport starts, assign the child to an Agent-owned Job Object with JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE, using ctypes and kernel32 (CreateJobObjectW, SetInformationJobObject, AssignProcessToJobObject, OpenProcess). Keep the job handle in a module-level variable for the process lifetime. This is Windows-only, uses no new dependencies and no multiprocessing or os.fork. Keep the test, document the fix instead of the SDK note, and say in the SUMMARY that the evidence contradicted the earlier experiment.

Commit: `test(quick-260924-48z): guard against orphaned MCP children on hard Agent termination (WR-04)` (or `fix(...)` if a code fix was needed), with no Co-Authored-By.
  </action>
  <verify>
    <automated>cd C:/Projects/AiAdventAgentV2 && pytest tests/test_mcp_orphan.py -v && file docs/ARCHITECTURE.md</automated>
  </verify>
  <done>tests/test_mcp_orphan.py passes on Windows (skipped elsewhere). It terminates only its own helper and verifies by PID and create_time that the fixture child it started disappears. docs/ARCHITECTURE.md has the WR-04 note (the SDK job object, or the implemented fix) with CRLF preserved. The SUMMARY states the outcome, pass or fail, with the SDK file and line citations. One commit with no Co-Authored-By.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Origin and Content-Type defense on mutating MCP routes, docs residual risk, full suite (WR-01)</name>
  <files>agent/dependencies.py, agent/main.py, tests/test_mcp_origin.py, docs/ARCHITECTURE.md</files>
  <behavior>
    - With authenticated_client and header `Origin: http://evil.example`, each mutating MCP route returns 403 with a clear detail: POST /api/v1/mcp/servers, PUT /api/v1/mcp/servers/{id}, DELETE /api/v1/mcp/servers/{id}, POST .../{id}/connect, POST .../{id}/disconnect. Side effects are absent: no server is created, the row is unchanged after the PUT, the row still exists after the DELETE, and `mcp_client.is_connected` is false after the connect.
    - `Origin: null` is rejected with 403 too.
    - `Origin: http://localhost:8000` and `Origin: http://127.0.0.1:8000` pass on all five routes (201/200/204 as today).
    - No Origin header (the default test client) passes. The existing tests/test_mcp_api.py stays green.
    - GET /api/v1/mcp/servers and GET .../{id}/status with a foreign Origin still return 200 (unaffected).
    - Create and update with `content=json.dumps(payload)` and `Content-Type: text/plain` return 415. With `content=` bytes and no Content-Type header they also return 415 (this is the "simple request" trick). With `application/json; charset=utf-8` they pass.
    - DELETE, connect and disconnect with no body and no Content-Type still work (no Content-Type check on them).
  </behavior>
  <action>
Per the TASK decision, in agent/dependencies.py:
- Import `Request` from fastapi and `CORS_ORIGINS` from agent.state.
- Add `async def require_allowed_origin(request: Request) -> None`. `origin = request.headers.get("origin")`. If origin is not None and not in CORS_ORIGINS, log `mcp_origin_rejected` (warning, origin value and path only) and raise `HTTPException(status.HTTP_403_FORBIDDEN, detail="Cross-origin request rejected")`. A missing header passes: a browser always sends Origin on cross-origin POST, PUT and DELETE, while non-browser clients, curl and pytest do not.
- Add `async def require_json_content_type(request: Request) -> None`. It takes the media type as `request.headers.get("content-type", "").split(";")[0].strip().lower()`. If it is not "application/json", raise `HTTPException(status.HTTP_415_UNSUPPORTED_MEDIA_TYPE, detail="Content-Type must be application/json")`. A missing Content-Type is rejected, which blocks the no-Content-Type simple-request trick. 415 was chosen over 422 as the most accurate status that does not collide with pydantic validation errors.

In agent/main.py:
- Import the two dependencies from agent.dependencies.
- Add `dependencies=[Depends(require_allowed_origin)]` to the decorators of create_mcp_server, update_mcp_server, delete_mcp_server, connect_mcp_server and disconnect_mcp_server.
- For create and update, use `dependencies=[Depends(require_allowed_origin), Depends(require_json_content_type)]`.
- Do NOT add either dependency to GET routes or to non-MCP routes. That is out of scope, and it is documented as residual risk.
- Confirm by reading that the UI keeps working: apiFetch always sends `Content-Type: application/json` and the page origin is one of the two CORS_ORIGINS (see interfaces). Grep tests/ for MCP POST/PUT calls that use `content=` or `data=` rather than `json=`, and fix any existing test that would now hit 415 by switching it to `json=`.

Write tests/test_mcp_origin.py (module docstring, reusing a `_payload` helper like tests/test_mcp_api.py with FIXTURE "ok") covering the behavior list. Prefer `pytest.mark.parametrize` over the five routes. For connect with an allowed origin, a real fixture connect is fine (the conftest teardown closes it).

docs/ARCHITECTURE.md: add a short "**Residual risk (REST origin check):**" paragraph after the WR-04 note, preserving CRLF. It should say:
- Any authenticated user can register and start an arbitrary executable. This is by design under the equal-admin model.
- The mutating MCP routes now reject a foreign `Origin` (403) and a non-JSON body type (415).
- A request without Origin is allowed, for non-browser clients.
- Other mutating non-MCP routes are not covered by this dependency and still rely on SameSite=Lax cookies plus CORS.
- XSS inside the app's own origin is not mitigated by an origin check.

Commit: `fix(quick-260924-48z): reject cross-origin and non-JSON requests on mutating MCP routes (WR-01)`, with no Co-Authored-By.

Then run the FULL suite and record the real pass count in the SUMMARY. The baseline is 420, plus the new tests. The SUMMARY must also list: the WR-06 probe finding, the WR-08 SPAWN_FAILED choice and why it is safe, the note that the WR-08b temp file can still grow, the WR-04 outcome with SDK citations, and any place where the evidence contradicted the review.
  </action>
  <verify>
    <automated>cd C:/Projects/AiAdventAgentV2 && pytest tests/test_mcp_origin.py tests/test_mcp_api.py -q && pytest tests/ -q</automated>
  </verify>
  <done>A foreign Origin gets 403 on all five mutating MCP routes, with no side effects. Allowed and missing Origins pass. GET is unaffected. Non-JSON or missing Content-Type on create/update gets 415. The docs residual-risk paragraph is present with CRLF preserved. Full `pytest tests/ -q` passes (420 + new tests, real count reported). One commit with no Co-Authored-By.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser (other localhost origin) -> Agent REST :8001 | cookie-bearing cross-origin requests from another local web app (same-site, so SameSite=Lax does not block them) |
| Agent -> MCP child process | the Agent spawns arbitrary user-configured executables; their lifetime must be bounded by the Agent's |
| MCP child stderr -> Agent memory | untrusted, unbounded output read back on failure |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48z-01 | Elevation / Tampering (CSRF-style) | 5 mutating MCP routes in agent/main.py | mitigate | `require_allowed_origin` (403 for an Origin not in CORS_ORIGINS) + `require_json_content_type` (415) on create/update; tests in tests/test_mcp_origin.py (Task 4) |
| T-48z-02 | Elevation | non-MCP mutating routes, same-origin XSS | accept | documented residual risk in docs/ARCHITECTURE.md; by-design command execution for authenticated equal-admin users |
| T-48z-03 | Denial of Service (orphan processes) | MCP children after a hard Agent kill (ui/supervisor.py::_shutdown_agent) | mitigate | SDK Job Object with KILL_ON_JOB_CLOSE, proven by tests/test_mcp_orphan.py; an Agent-owned job via ctypes if the test shows an orphan (Task 3) |
| T-48z-04 | Denial of Service (orphan processes) | cancelled open_session (WR-03) | mitigate | cancel and await the owner task, close the errfile, re-raise; PID-checked test (Task 1) |
| T-48z-05 | Denial of Service | healthy busy servers killed by the ping timeout; sequential pings (WR-06) | mitigate | tri-state probe (timeout = unresponsive), concurrent gather in list_mcp_servers (Task 1) |
| T-48z-06 | Denial of Service (memory) | read_stderr_tail reading a whole file (WR-08b) | mitigate | 64 KiB bounded tail read; accept unbounded temp-file growth (documented in SUMMARY) |
| T-48z-07 | Information Disclosure | spawn error messages (WR-08a) | mitigate | reason is errno/strerror only, never env values; test asserts a secret is absent |
| T-48z-08 | Tampering (test harness) | tests killing user processes | mitigate | SAFETY rules: kill only PIDs the test started, verified by parentage and create_time; never by name; protected pre-existing filesystem.exe PIDs |
</threat_model>

<verification>
- `pytest tests/ -q` passes. Record the real count (baseline 420 plus the new tests).
- `git log --format=%B -4` shows four commits with no Co-Authored-By line. The branch is still Day16. Nothing is pushed.
- Pre-existing filesystem.exe PIDs (e.g. 26256) are still running after the test runs, if they were running before.
- `file docs/ARCHITECTURE.md` still reports CRLF line terminators.
</verification>

<success_criteria>
All six warnings are addressed as decided in TASK.md, each with regression tests. The WR-04 outcome is proven by a
PID-checked test and documented with SDK citations. The WR-01 origin defense works without breaking the real UI. The
full suite is green, there are four atomic commits without attribution lines, and the SUMMARY records every
evidence-based deviation.
</success_criteria>

<output>
Create `.planning/quick/260924-48z-fix-review-warnings-wr-01-wr-03-wr-04-wr/260924-48z-SUMMARY.md` when done.
</output>
