# Quick Task 260924-4xr: fix the six INFO findings IN-01..IN-06 of the Phase 7 code review

**Branch:** Day16 (stay on it; NO push, NO merge — the orchestrator does that). Commits must NOT contain any Co-Authored-By line.
**Source:** .planning/phases/07-mcp-connection-day-16/07-REVIEW.md, section "## Info" — read all six entries first.
User instruction (Russian): "исправляй оставшиеся замечания". WR-01..WR-08 are already fixed (do not touch them). Baseline: `pytest tests/ -q` = 467 passed.

## Decisions
- IN-01 (agent/main.py update_mcp_server): today it calls `disconnect_server` before `update_server`. Reorder so the DB update is
  committed FIRST and the disconnect happens only after a successful commit (the WR-07 pre-validation of the env mask must stay
  BEFORE any change, so a rejected edit still leaves everything untouched). Keep the semantics that an edit always drops a live
  connection and a remembered connect failure. Also drop the per-server lock entry from `_locks` (agent/mcp_client.py) when the
  server has neither a session nor a remembered result after a disconnect (careful: asyncio.Lock is not reentrant and WR-05 already
  introduced `_disconnect_locked` / pops the lock while held in `cleanup_server` — extend that pattern, don't break it). Tests: a
  failing commit (monkeypatch update_server to raise) leaves a live connection connected and config unchanged; successful edit
  still disconnects; `_locks` does not grow across repeated edits of an unconnected server.
- IN-02 (ui/static/app.js saveMcpServer): add an in-flight guard: disable `#btn-mcp-save` while the request runs and re-enable it
  in a `finally`; also ignore re-entry if a save is already in flight. app.js uses CRLF — preserve line endings; textContent only.
  No server-side uniqueness constraint (out of scope). Verify in a browser-level check if ports allow (see SAFETY), else say it
  was not browser-verified.
- IN-03 (ui/static/app.js): remove the dead `.catch(... showToast ...)` at the connect call site OR make the function rethrow —
  pick the minimal one and confirm no error is now swallowed or double-toasted. The "connecting indicator is lost when the modal is
  re-opened" part is a client-only limitation: do NOT redesign; note it in the SUMMARY as intentionally left.
- IN-04: the code needs Python 3.11+ (`asyncio.timeout`, `BaseExceptionGroup`). Fix the stale docs: change the two "Python 3.8+"
  mentions in CLAUDE.md (lines ~146 and ~150, "Platform Requirements") to "Python 3.11+", add a one-line comment at the top of
  requirements.txt (`# Requires Python 3.11+ (asyncio.timeout, BaseExceptionGroup)`), and add a cheap startup guard: in run.py main()
  and agent/main.py import time, fail fast with a clear message if sys.version_info < (3, 11) — keep it tiny, no new dependency, and
  a test for the guard function (put the check in a small helper in shared/ so it is testable). CLAUDE.md is the user's project
  instruction file: change ONLY those version mentions and say so explicitly in the SUMMARY.
- IN-05: replace the developer-specific placeholder of `#mcp-command` in ui/static/index.html (CRLF) with a generic one such as
  `/path/to/mcp-server` (also check ui/static/app.js and docs for the same personal path in user-visible strings). In
  tests/test_mcp_client.py replace the fixed `sleep(2.5)` in `test_get_status_detects_exited_process` with polling for the expected
  status against a deadline. The real-binary tests keep their env-var/default path + skip behavior (they are dev tests, skipping is
  safe) — do not remove them.
- IN-06: reformat the multi-line `logger.error(...)` call in agent/main.py (one kwarg per line, trailing comma) to match the file.

## SAFETY (critical)
The user's own app may be started at any moment: snapshot running filesystem.exe PIDs before any test and treat pre-existing ones
(currently 26256, args C:\Projects\Temp) as protected. NEVER kill anything you did not start yourself in a test, never kill by name,
never free 8000/8001 by killing their holder, never run `python run.py`/uvicorn on 8000/8001, never use the user's app.db. You MAY run
scripts/e2e_mcp_chat_playwright.py ONLY if ports 8000, 8001 and 18765 are all free at that moment (exit 2 = blocked is fine; report
the exit code). Full `pytest tests/ -q` must pass; report the real observed count. Preserve CRLF in app.js/index.html/docs (write
edit scripts without backslash pitfalls: build newlines with chr(10)/chr(13)).
