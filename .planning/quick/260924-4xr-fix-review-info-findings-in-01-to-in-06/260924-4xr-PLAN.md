---
phase: quick-260924-4xr
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/main.py
  - agent/mcp_client.py
  - tests/test_mcp_api.py
  - tests/test_mcp_registry_robustness.py
  - ui/static/app.js
  - ui/static/index.html
  - tests/test_mcp_client.py
  - shared/runtime.py
  - tests/test_runtime.py
  - run.py
  - requirements.txt
  - CLAUDE.md
autonomous: true
requirements: [IN-01, IN-02, IN-03, IN-04, IN-05, IN-06]

must_haves:
  truths:
    - "A PUT whose DB commit fails leaves a live MCP connection connected and the stored config unchanged (IN-01)"
    - "A successful PUT still drops a live connection and a remembered connect failure (IN-01)"
    - "Repeated edits/disconnects of an unconnected server leave no entry in mcp_client._locks (IN-01)"
    - "A double click on the MCP save button sends exactly one request; the button is disabled while it runs and re-enabled afterwards (IN-02)"
    - "Connect errors are toasted exactly once, by connectMcpServer itself (IN-03)"
    - "Running on Python < 3.11 fails fast with a clear message from run.py and agent/main.py; docs say Python 3.11+ (IN-04)"
    - "The #mcp-command placeholder is generic; test_get_status_detects_exited_process polls against a deadline (IN-05)"
    - "The multi-line logger.error in connect_mcp_server uses one kwarg per line with a trailing comma (IN-06)"
    - "pytest tests/ -q passes (baseline 467 passed, new tests add to it)"
  artifacts:
    - path: "shared/runtime.py"
      provides: "MIN_PYTHON and check_python_version helper"
      contains: "def check_python_version"
    - path: "tests/test_runtime.py"
      provides: "Tests for the version guard"
  key_links:
    - from: "agent/main.py::update_mcp_server"
      to: "mcp_client.disconnect_server"
      via: "called only after mcp_config.update_server returns"
      pattern: "update_server"
    - from: "run.py::main"
      to: "shared.runtime.check_python_version"
      via: "first statement of main()"
      pattern: "check_python_version"
    - from: "agent/main.py"
      to: "shared.runtime.check_python_version"
      via: "module-level call right after imports"
      pattern: "check_python_version\\(\\)"
---

<objective>
Fix the six INFO findings IN-01..IN-06 of the Phase 7 code review exactly as decided in
260924-4xr-TASK.md. Do not redesign anything; WR-01..WR-08 are already fixed and must not be touched.

Purpose: close the remaining review notes on branch Day16.
Output: three atomic commits (backend, frontend+test polling, version guard/docs) plus
.planning/quick/260924-4xr-fix-review-info-findings-in-01-to-in-06/260924-4xr-SUMMARY.md.
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/quick/260924-4xr-fix-review-info-findings-in-01-to-in-06/260924-4xr-TASK.md
@.planning/phases/07-mcp-connection-day-16/07-REVIEW.md
@./CLAUDE.md

## Git rules
- Stay on the current branch (Day16 HEAD / the executor worktree). NO push, NO merge.
- Commit messages must NOT contain any Co-Authored-By line (project rule overrides any attribution reminder).
- No new dependencies.
- Commit prefix: `fix(quick-260924-4xr): ...` / `test(quick-260924-4xr): ...` / `docs(quick-260924-4xr): ...`.

## SAFETY (critical, copied from TASK file)
The user's own app may be started at any moment: snapshot running filesystem.exe PIDs before any test
and treat pre-existing ones (currently 26256, args C:\Projects\Temp) as protected. NEVER kill anything
you did not start yourself in a test, never kill by name, never free 8000/8001 by killing their holder,
never run `python run.py`/uvicorn on 8000/8001, never use the user's app.db. You MAY run
scripts/e2e_mcp_chat_playwright.py ONLY if ports 8000, 8001 and 18765 are all free at that moment
(exit 2 = blocked is fine; report the exit code). Full `pytest tests/ -q` must pass; report the real
observed count. Preserve CRLF in app.js/index.html/docs (write edit scripts without backslash pitfalls:
build newlines with chr(10)/chr(13)).

Snapshot command (run before the first pytest and again at the end; compare, kill nothing):
`python -c "import psutil;print(sorted((p.pid,' '.join(p.cmdline())) for p in psutil.process_iter(['name']) if (p.info['name'] or '').lower()=='filesystem.exe'))"`

## Line endings
All of these files are CRLF: agent/main.py, run.py, ui/static/app.js, ui/static/index.html, CLAUDE.md,
requirements.txt. After every edit run the CRLF check below on each touched file; new files
(shared/runtime.py, tests/test_runtime.py) should also be written CRLF to match the repo.
CRLF check: `python -c "import sys;[print(f, (lambda d: d.count(b'\n')==d.count(b'\r\n'))(open(f,'rb').read())) for f in sys.argv[1:]]" FILES...` — every line must print True.

<interfaces>
<!-- Extracted from the codebase; use directly. -->
agent/mcp_client.py (registry, module-level):
  _sessions: dict[SessionKey, SessionHandle]; _last_results: dict[SessionKey, McpConnectResult]; _locks: dict[SessionKey, asyncio.Lock]
  def _lock_for(key) -> asyncio.Lock            # _locks.setdefault(key, asyncio.Lock())
  async def _disconnect_locked(key) -> None      # pops _sessions (closes handle) and _last_results; caller holds lock
  async def disconnect_server(user_id, server_id) -> McpConnectResult   # async with _lock_for(key): await _disconnect_locked(key); returns NOT_CONNECTED
  async def cleanup_server(user_id, server_id) -> None                  # lock = _lock_for(key); async with lock: _disconnect_locked; if _locks.get(key) is lock: del _locks[key]
  async def get_status(user_id, server_id)       # takes no lock when handle is None
  asyncio.Lock is NOT reentrant — never call disconnect_server/cleanup_server while holding the key's lock.

agent/main.py::update_mcp_server (lines ~1053-1085), current order:
  row = _get_mcp_server_or_404 -> updates = body.model_dump(exclude_unset=True)
  -> if updates.get("env") is not None: mcp_config.merge_env(mcp_config.load_env(row), updates["env"]) (EnvMaskError -> _env_mask_error)   # WR-07 pre-validation, MUST stay first
  -> await mcp_client.disconnect_server(current_user.id, row.id)       # <-- IN-01: move after the update
  -> row = await mcp_config.update_server(session, row, name=..., command=..., args=..., env=..., cwd=..., cwd_set="cwd" in body.model_fields_set, enabled=...)  (EnvMaskError -> _env_mask_error)
  -> connection = await mcp_client.get_status(current_user.id, row.id); return _mcp_server_to_response(row, connection)
mcp_config.update_server commits, refreshes, and rolls back + re-raises on any exception.
agent/main.py::connect_mcp_server (~1131-1134): the IN-06 target is
  logger.error("mcp_connect_unexpected_error", user_id=current_user.id,
               server_id=row.id, error=str(exc))
main.py references `mcp_config.update_server` via module attribute, so monkeypatch.setattr(mcp_config, "update_server", ...) intercepts it.

tests/conftest.py: `authenticated_client` / `second_authenticated_client` use httpx ASGITransport(app=app)
with default raise_app_exceptions=True, so an unhandled exception in a route propagates into the test
(use pytest.raises). `authenticated_client.seeded_user_id` is the user id. conftest runs
mcp_client.cleanup_all_sessions() between tests. tests/test_mcp_api.py helpers: `_create(client, **kw)`,
`_payload(mode, **overrides)`, `BASE = "/api/v1/mcp/servers"`; it does NOT import pytest yet.
tests/test_mcp_registry_robustness.py uses `KEY` and `_fixture_args(mode)`; see test_cleanup_server_removes_lock.

ui/static/app.js:
  line ~1484: `state.mcpConnecting = new Set();`
  async function saveMcpServer() (~1750-1794): validates name/command/env (early returns writing errorEl.textContent), then
    try { apiFetch POST/PUT } catch (err) { errorEl.textContent = err.message; return; } then toast, closeMcpForm(), await loadMcpServers().
  call site ~2016: $('btn-mcp-save').addEventListener('click', () => { saveMcpServer().catch((err) => showToast(err.message, 'error')); });
  async function connectMcpServer(server) (~1807-1827): try { ... } catch (err) { showToast(err.message, 'error'); } finally { state.mcpConnecting.delete(...); renderMcpServers(); }
  call site ~1674: connectMcpServer(server).catch((err) => showToast(err.message, 'error'));   # dead catch (IN-03)
ui/static/index.html ~318: <input type="text" id="mcp-command" placeholder="C:\Users\Aleksey\go\bin\filesystem.exe" ...>
tests/test_mcp_client.py ~320-330: test_get_status_detects_exited_process uses `await asyncio.sleep(2.5)` then get_status(1, 1); `time` and `asyncio` are already imported.
run.py main(): first statement is cleanup_port(settings.UI_PORT).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend IN-01 (disconnect after commit, drop idle locks) + IN-06 (logger.error style)</name>
  <files>agent/main.py, agent/mcp_client.py, tests/test_mcp_api.py, tests/test_mcp_registry_robustness.py</files>
  <behavior>
    - tests/test_mcp_api.py test_update_commit_failure_keeps_connection: create + connect a fixture server; monkeypatch mcp_config.update_server with an async function raising RuntimeError("commit failed"); `with pytest.raises(RuntimeError)` around the PUT {"name": "edited"}; afterwards mcp_client.is_connected(user_id, sid) is True and the DB row (async_session_factory + session.get) still has name "fixture".
    - tests/test_mcp_api.py test_update_clears_remembered_connect_failure: create with command "C:/definitely/not/here/nope.exe", args []; connect -> error; PUT {"name": "x"} -> 200 and connection.status == "not_connected"; mcp_client.has_recorded_failure(user_id, sid) is False.
    - tests/test_mcp_api.py test_repeated_edits_do_not_grow_locks: create (never connect); record len(mcp_client._locks); PUT three times; (user_id, sid) not in mcp_client._locks and len unchanged.
    - existing test_update_connected_server_disconnects and test_update_rename_env_key_with_mask_returns_422_and_changes_nothing still pass (successful edit disconnects; rejected env edit leaves connection and config untouched).
    - tests/test_mcp_registry_robustness.py test_disconnect_server_drops_idle_lock: disconnect_server(*KEY) on an idle key leaves KEY not in _locks; connect_server(... "ok") then disconnect_server(*KEY) leaves KEY not in _locks and not in _sessions.
  </behavior>
  <action>
    Write the tests above first (add `import pytest` to tests/test_mcp_api.py) and confirm the IN-01 ones fail, then implement.

    agent/mcp_client.py (per IN-01, extending the WR-05 pattern, not replacing it): add a private sync helper `_drop_lock_if_idle(key: SessionKey, lock: asyncio.Lock) -> None` with a one-line docstring that deletes `_locks[key]` only when `key not in _sessions and key not in _last_results and _locks.get(key) is lock`; it must be called while the caller still holds `lock`. Rewrite `disconnect_server` to `lock = _lock_for(key)`, `async with lock: await _disconnect_locked(key); _drop_lock_if_idle(key, lock)`, returning NOT_CONNECTED as today. Make `cleanup_server` use the same helper (behaviour unchanged, since _disconnect_locked empties both dicts). Do NOT call disconnect_server from inside any held lock (asyncio.Lock is not reentrant). Add a short "why" comment that this mirrors cleanup_server: a waiter on the old lock still serializes with the holder, which is the same accepted trade-off as WR-05.

    agent/main.py update_mcp_server (per IN-01): keep the WR-07 env-mask pre-validation block exactly where it is (before any change). Move `await mcp_client.disconnect_server(current_user.id, row.id)` to directly after the `try: row = await mcp_config.update_server(...) except EnvMaskError` block, so it runs only when update_server returned (i.e. committed). Keep it unconditional and keep/adjust its comment ("Unconditional, after a successful commit: also drops a remembered connect error that the edit makes stale."). Update the pre-validation comment to not say "before disconnecting" inaccurately (e.g. "Reject before changing anything so a refused edit leaves the connection untouched."). Update the docstring to "Update an MCP server config; a connected server is disconnected once the edit is saved." Then `get_status` and the response as today.

    agent/main.py connect_mcp_server (per IN-06): reformat the `logger.error("mcp_connect_unexpected_error", ...)` call to one argument per line with a trailing comma, matching the other multi-line calls in the file. No other change there.

    Preserve CRLF in agent/main.py (run the CRLF check). Commit: `fix(quick-260924-4xr): disconnect MCP server only after a committed edit and drop idle locks (IN-01, IN-06)` — no Co-Authored-By line.
  </action>
  <verify>
    <automated>pytest tests/test_mcp_api.py tests/test_mcp_registry_robustness.py tests/test_mcp_client.py -q</automated>
  </verify>
  <done>New and existing MCP API/registry/client tests pass; update_mcp_server calls disconnect_server after update_server; `grep -n "server_id=row.id, error=str(exc))" agent/main.py` finds nothing; agent/main.py still CRLF-only; one commit created.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend IN-02 + IN-03 + IN-05 placeholder, and IN-05 test polling</name>
  <files>ui/static/app.js, ui/static/index.html, tests/test_mcp_client.py</files>
  <action>
    Edit app.js/index.html with a small Python script (or an editor that is verified afterwards) that reads bytes, does exact-substring replacements, and writes back with CRLF preserved; build newlines with chr(13)+chr(10), never with backslash escapes inside shell strings. Use textContent only; no innerHTML.

    IN-02 (ui/static/app.js): next to `state.mcpConnecting = new Set();` add `state.mcpSaving = false;`. In saveMcpServer: first statement `if (state.mcpSaving) return;` (ignore re-entry); then `const saveBtn = $('btn-mcp-save'); state.mcpSaving = true; saveBtn.disabled = true;` and wrap the entire rest of the existing body (validation, request, toasts, closeMcpForm, await loadMcpServers) in `try { ... } finally { state.mcpSaving = false; saveBtn.disabled = false; }`. The existing early `return`s and the inner request try/catch stay as they are inside the outer try. No server-side uniqueness constraint (out of scope per TASK).

    IN-03 (ui/static/app.js): minimal option chosen — remove the dead `.catch((err) => showToast(err.message, 'error'))` at the connectMcpServer call site in the "Подключить" button handler, leaving `connectMcpServer(server);`. connectMcpServer already catches and toasts every error from its try, so errors are toasted exactly once and none is swallowed. Do NOT touch connectMcpServer itself and do NOT redesign the client-only "connecting" indicator (it is intentionally left; note it in the SUMMARY).

    IN-05 (ui/static/index.html): replace the `#mcp-command` placeholder value `C:\Users\Aleksey\go\bin\filesystem.exe` with `/path/to/mcp-server`. Confirm with grep that no other user-visible string in ui/static/ or docs/ contains `Aleksey` (scripts/ and tests/ dev defaults stay; the real-binary tests keep their env-var/default path + skip behaviour).

    IN-05 (tests/test_mcp_client.py, test_get_status_detects_exited_process): replace `await asyncio.sleep(2.5)` + single get_status with polling: `deadline = time.monotonic() + 10.0`, loop calling `status = await get_status(1, 1)`, break when `status.status == McpConnectionStatus.ERROR`, otherwise stop when the deadline passes and `await asyncio.sleep(0.1)` between polls; then keep the three existing assertions. Keep the docstring.

    Commit: `fix(quick-260924-4xr): guard MCP save against double submit, drop dead connect catch, generic placeholder, poll exit status (IN-02, IN-03, IN-05)` — no Co-Authored-By line.
  </action>
  <verify>
    <automated>python -c "d=open('ui/static/app.js','rb').read();h=open('ui/static/index.html','rb').read();assert d.count(b'\n')==d.count(b'\r\n') and h.count(b'\n')==h.count(b'\r\n');s=d.decode('utf-8');assert 'state.mcpSaving' in s and 'saveBtn.disabled = true' in s and 'saveBtn.disabled = false' in s;assert 'connectMcpServer(server).catch' not in s;t=h.decode('utf-8');assert 'Aleksey' not in t and 'placeholder=\"/path/to/mcp-server\"' in t;print('ok')" && pytest tests/test_mcp_client.py -q -k "exited_process"</automated>
  </verify>
  <done>Static check prints ok (CRLF intact, guard present, dead catch gone, generic placeholder); the polling test passes; one commit created. Browser-level verification of the double-click guard is optional: only via scripts/e2e_mcp_chat_playwright.py under SAFETY rules in Task 3; otherwise the SUMMARY states the guard was not browser-verified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: IN-04 Python 3.11+ guard and docs, then full-suite verification</name>
  <files>shared/runtime.py, tests/test_runtime.py, run.py, agent/main.py, requirements.txt, CLAUDE.md</files>
  <behavior>
    - check_python_version((3, 10, 12)) raises SystemExit whose message mentions "3.11" and the running version "3.10".
    - check_python_version((3, 11, 0)) and check_python_version((3, 13, 1)) return None.
    - check_python_version() with no argument returns None on the current interpreter (3.13).
    - run.py main() and agent/main.py both reference check_python_version (grep-based assertion in the test is acceptable, reading the files as text).
  </behavior>
  <action>
    Per IN-04: create shared/runtime.py (module docstring, stdlib `sys` only, no new dependency) with `MIN_PYTHON: tuple[int, int] = (3, 11)` and `def check_python_version(version: tuple[int, ...] | None = None) -> None` (default resolves to `tuple(sys.version_info)` inside the function, not in the signature) that raises `SystemExit(f"AiAdventAgentV2 requires Python 3.11+ (asyncio.timeout, BaseExceptionGroup); running {major}.{minor}.")` when `version[:2] < MIN_PYTHON`. SystemExit gives a clear one-line message without a traceback. Write tests/test_runtime.py for the behaviours above first.

    run.py: import `check_python_version` from shared.runtime (local import group) and call it as the first statement of main(), before cleanup_port. agent/main.py: import it with the other `shared.*` imports and call `check_python_version()` at module level immediately after the import block (before `logger = ...`/app creation), with a one-line "why" comment (the Agent is spawned by the supervisor, so it guards itself too).

    requirements.txt: insert `# Requires Python 3.11+ (asyncio.timeout, BaseExceptionGroup)` as the new first line (CRLF). CLAUDE.md is the user's project instruction file: change ONLY the two "Python 3.8+" mentions under "Platform Requirements" (`- Python 3.8+ (with asyncio support)` and `- Python 3.8+ runtime`) to "Python 3.11+"; touch nothing else (verify with `git diff --stat CLAUDE.md` = 2 lines changed and `git diff CLAUDE.md`). Preserve CRLF in run.py, agent/main.py, requirements.txt, CLAUDE.md.

    Commit: `fix(quick-260924-4xr): require Python 3.11+ with a startup guard and updated docs (IN-04)` — no Co-Authored-By line.

    Final verification under SAFETY: take the filesystem.exe PID snapshot, run the full `pytest tests/ -q` and record the real passed count (baseline 467 + the new tests), take the snapshot again and confirm protected PIDs are still alive and nothing of yours is left (kill nothing you did not start). Then check ports 8000, 8001 and 18765 (e.g. `python -c "import socket;..."` connect attempts); only if all three are free run `python scripts/e2e_mcp_chat_playwright.py` and report its exit code (2 = blocked is fine). Never run run.py/uvicorn on 8000/8001 and never touch app.db.

    Write .planning/quick/260924-4xr-fix-review-info-findings-in-01-to-in-06/260924-4xr-SUMMARY.md covering: each IN item and its fix; IN-03 option chosen (dead catch removed) and that the "connecting" indicator loss on modal reopen was intentionally left; the IN-02 browser-verification status; explicit statement that CLAUDE.md changed only in the two Python version mentions; the observed pytest count; the e2e exit code or why it was not run; PID snapshot before/after.
  </action>
  <verify>
    <automated>pytest tests/test_runtime.py -q && python -c "import sys;[sys.exit(f+' not CRLF') for f in ['run.py','agent/main.py','requirements.txt','CLAUDE.md','ui/static/app.js','ui/static/index.html'] if (lambda d: d.count(b'\n')!=d.count(b'\r\n'))(open(f,'rb').read())];print('crlf ok')" && git diff HEAD~1 --numstat -- CLAUDE.md && pytest tests/ -q</automated>
  </verify>
  <done>tests/test_runtime.py passes; CRLF intact on all touched files; CLAUDE.md diff is exactly the two version lines; full `pytest tests/ -q` passes with the real count reported (>= 467 + new tests); protected filesystem.exe PIDs untouched; commit created; SUMMARY written.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Agent REST | PUT /api/v1/mcp/servers/{id} from the Settings UI (auth + origin + JSON guards from WR-01 unchanged) |
| Agent -> MCP child process | stdio sessions in the per-user registry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4xr-01 | Denial of service | mcp_client._locks growth | mitigate | _drop_lock_if_idle removes the lock entry when no session/result remains (Task 1) |
| T-4xr-02 | Tampering | concurrent op on a dropped lock | accept | same trade-off already accepted by WR-05 cleanup_server; waiters on the old lock still serialize with the holder |
| T-4xr-03 | Denial of service | failed commit dropping a live session | mitigate | disconnect only after update_server returns (Task 1) |
| T-4xr-04 | Information disclosure | personal path in UI placeholder | mitigate | generic /path/to/mcp-server placeholder (Task 2) |
| T-4xr-05 | Denial of service | test run killing the user's filesystem.exe | mitigate | SAFETY PID snapshot, never kill by name or foreign PIDs (Task 3) |
| T-4xr-SC | Tampering | package installs | accept | no packages installed in this plan |
</threat_model>

<verification>
- `pytest tests/ -q` passes (real count reported; baseline 467 plus new tests).
- `git log -3` shows three commits without any Co-Authored-By line; nothing pushed or merged.
- CRLF preserved in every touched CRLF file; CLAUDE.md changed in exactly two lines.
</verification>

<success_criteria>
All six INFO findings IN-01..IN-06 fixed per the TASK decisions, each covered by a test or static
check, full suite green, SAFETY rules respected, SUMMARY written.
</success_criteria>

<output>
Create `.planning/quick/260924-4xr-fix-review-info-findings-in-01-to-in-06/260924-4xr-SUMMARY.md` when done
</output>
