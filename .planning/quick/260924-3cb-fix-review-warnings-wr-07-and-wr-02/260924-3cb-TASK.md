# Quick Task 260924-3cb: fix review warnings WR-07 and WR-02 (Phase 7 code review)

**Branch:** Day16 (stay on it; NO push, NO merge to main). Commits must NOT contain any Co-Authored-By line.
**Source:** .planning/phases/07-mcp-connection-day-16/07-REVIEW.md, findings WR-07 and WR-02 (read both entries first).
The user approved fixing exactly these two. Do NOT touch the other review findings.

## WR-07 (do first — real data loss): renaming an env key while keeping the mask silently drops the variable
Today `agent/mcp_config.py::merge_env` discards an incoming `KEY=•••` whose key is not in the stored env, and the old key is
dropped too, so a (possibly secret) variable is lost while the UI says "Сервер сохранён".
Design (decided):
- `merge_env` must reject that case instead of silently continuing: when an incoming value equals `ENV_MASK` and its key is NOT
  in `stored`, raise a small dedicated exception (e.g. `EnvMaskError`, carrying the key name) defined in agent/mcp_config.py.
  Keep the behavior "mask + known key = keep stored value" unchanged.
- The REST handlers that call `update_server` (and `create_server`, if it can receive a masked value — check it; a mask on a
  brand-new server can never refer to a stored value, so reject it there too) translate the exception into an HTTP 422 whose
  `detail` is a clear Russian message naming the key, e.g. «Для переменной KEY значение скрыто (•••), но такой переменной нет в
  сохранённых. Введите значение заново.» Follow the existing HTTPException style in agent/main.py.
- No data may be modified when the request is rejected (validate before mutating/committing; keep the existing
  rollback conventions).
- UI: check how ui/static/app.js `saveMcpServer` surfaces API errors (it should show err.message in `#mcp-form-error`). If it
  already does, NO UI change is needed — state that in the SUMMARY. If a tiny change is required keep CRLF line endings and
  textContent-only.
- Tests: replace/extend the existing 07-02 test that asserted "the mask literal for an unknown key must not be persisted" (it
  now expects the exception, and still proves nothing is persisted); add an API-level test (tests/test_mcp_api.py style) for the
  422 on rename-with-mask, that the stored env is UNCHANGED afterwards, and that keeping a known key masked still works.

## WR-02: MCP sessions/child processes survive logout; `cleanup_user_sessions` is never called
Design (decided): in `agent/main.py::logout`, after the web-session row is deleted and committed, close the user's MCP sessions
via `mcp_client.cleanup_user_sessions(current_user.id)` ONLY IF the user has no other remaining web sessions (multi-tab /
multi-device use must not lose MCP sessions of a still-logged-in browser). Look up how web sessions are modeled (the SessionRow
model imported in main.py) and count remaining rows for that user_id (only non-expired ones if an expiry column exists). A
failure in MCP cleanup must NEVER make logout fail (catch Exception, log a warning with user_id and error type only, still return 204
and delete the cookie). Note in the SUMMARY that expiry of a web session (no logout call) is still not covered.
- Tests: logout of the last web session closes the user's live MCP session (use the fixture server; assert the session registry no
  longer has it and no extra process remains); logout while another web session of the same user exists leaves the MCP session
  alive; other users' MCP sessions are never touched; a failing cleanup (monkeypatch cleanup_user_sessions to raise) still yields 204.
  Never use ports 8000/8001.

## Docs
If docs/API_SPEC.md documents /auth/logout or the MCP env update, add ONE sentence for each behavior change (422 on masked new key;
logout closes MCP sessions when the last web session ends). Do not rewrite other docs.

## SAFETY (critical)
The user's own app is running RIGHT NOW on ports 8000/8001 (PIDs 16820 / 30676) with filesystem.exe children (PIDs 1996, 26256;
PIDs change when the user restarts — snapshot before any test and treat pre-existing ones as protected). NEVER kill/stop/restart
them, never kill by process name, never free 8000/8001 by killing their holder, never run `python run.py` / uvicorn on 8000/8001 or
the Playwright script, never use the user's app.db. pytest uses free ports and is safe. Full `pytest tests/ -q` must pass
(currently 412 passed); report the real observed count.
