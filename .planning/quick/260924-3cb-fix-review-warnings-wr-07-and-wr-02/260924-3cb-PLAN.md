---
phase: quick-260924-3cb
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - agent/mcp_config.py
  - agent/main.py
  - tests/test_mcp_config.py
  - tests/test_mcp_api.py
  - tests/test_mcp_logout.py
autonomous: true
requirements: [WR-07, WR-02]

must_haves:
  truths:
    - "PUT /api/v1/mcp/servers/{id} with env {NEW_KEY: '•••'} where NEW_KEY is not stored returns 422 with a Russian detail naming NEW_KEY"
    - "After that rejected PUT the stored env_json, name and live MCP connection are all unchanged"
    - "PUT with a known key kept as '•••' still keeps the stored secret (existing behavior)"
    - "POST /api/v1/mcp/servers with any env value equal to '•••' returns 422 and creates no row"
    - "Logging out the user's last live web session closes all of that user's MCP sessions and their child processes"
    - "Logging out while the same user has another non-expired web session leaves the MCP sessions alive"
    - "Logout never touches other users' MCP sessions and still returns 204 (cookie deleted) even if MCP cleanup raises"
  artifacts:
    - path: "agent/mcp_config.py"
      provides: "EnvMaskError and strict merge_env; create_server/update_server validate env before mutating"
      contains: "class EnvMaskError"
    - path: "agent/main.py"
      provides: "422 translation in create/update MCP handlers; logout calls mcp_client.cleanup_user_sessions when no live web session remains"
      contains: "cleanup_user_sessions"
    - path: "tests/test_mcp_logout.py"
      provides: "WR-02 regression tests"
  key_links:
    - from: "agent/main.py::update_mcp_server"
      to: "agent/mcp_config.py::merge_env"
      via: "pre-validation before mcp_client.disconnect_server"
      pattern: "EnvMaskError"
    - from: "agent/main.py::logout"
      to: "agent/mcp_client.py::cleanup_user_sessions"
      via: "call after session-row delete commit, guarded by remaining-live-session count"
      pattern: "cleanup_user_sessions\\(current_user.id\\)"
---

<objective>
Fix exactly two Phase 7 review warnings (design is LOCKED in 260924-3cb-TASK.md — implement, do not redesign):
- WR-07: renaming an env key while keeping the mask (`NEW_KEY=•••`) silently drops a (possibly secret) variable. Reject it with HTTP 422 instead, modifying nothing.
- WR-02: `mcp_client.cleanup_user_sessions` is dead code; MCP sessions/child processes survive logout. Call it from logout when the user's last live web session ends.

Do NOT touch any other review finding (WR-01, WR-03..WR-06, WR-08, IN-*). In particular do NOT reorder `disconnect_server` vs `update_server` in the update handler beyond the pre-validation described below (IN-01 is out of scope).

Output: two independent local commits on the executor's worktree (based on Day16 HEAD), plus SUMMARY.
</objective>

<execution_context>
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/workflows/execute-plan.md
@C:/Users/Aleksey/.claude/plugins/cache/buildomator/bm/4.7.3/templates/summary.md
</execution_context>

<context>
@.planning/quick/260924-3cb-fix-review-warnings-wr-07-and-wr-02/260924-3cb-TASK.md
@.planning/phases/07-mcp-connection-day-16/07-REVIEW.md (entries WR-07, WR-02 only)
@./CLAUDE.md
@agent/mcp_config.py
@agent/main.py (lines 404-466 login/logout; 953-1002 create/update MCP handlers)

<safety>
SAFETY (critical, carried verbatim in spirit from the TASK file):
- The user's own app is running RIGHT NOW on ports 8000/8001 (PIDs 16820 / 30676) with filesystem.exe children (PIDs 1996, 26256; PIDs change when the user restarts — snapshot before any test and treat pre-existing ones as protected).
- NEVER kill/stop/restart them, never kill by process name, never free 8000/8001 by killing their holder.
- Never run `python run.py` / uvicorn on 8000/8001, never run the Playwright script, never open or modify the user's app.db.
- pytest (in-process ASGITransport, separate test DB) is safe. Tests in this plan may only inspect processes read-only via psutil and only reason about children of the pytest process itself.
- Commits stay local: NO push, NO merge to main. Commit messages must NOT contain any Co-Authored-By line (project memory rule overrides the default attribution).
</safety>

<interfaces>
From agent/mcp_config.py (current):
- ENV_MASK: str = "•••"
- def load_env(row: McpServerConfig) -> dict[str, str]
- def merge_env(stored: dict[str, str], incoming: dict[str, str]) -> dict[str, str]
  (currently: masked value + key in stored -> keep stored value; masked value + unknown key -> silently `continue`)
- async def create_server(session, user_id, name, command, args, env, cwd, enabled) -> McpServerConfig  (stores env via json.dumps(env) as-is)
- async def update_server(session, row, name=None, command=None, args=None, env=None, cwd=None, cwd_set=False, enabled=None) -> McpServerConfig
  (mutates row.name/command/args_json BEFORE computing merged env on line 136 — must reorder so env validation happens first)

From agent/main.py:
- imports: `from agent import invariants, mcp_client, mcp_config, memory, profile, tasks`; `from shared.models import (... Session as SessionRow ...)`; `HTTPException, status` from fastapi; `datetime, timezone`.
- create_mcp_server (line ~958) calls mcp_config.create_server(...) directly.
- update_mcp_server (line ~979): `row = await _get_mcp_server_or_404(...)`, then `await mcp_client.disconnect_server(current_user.id, row.id)`, then `updates = body.model_dump(exclude_unset=True)`, then mcp_config.update_server(... env=updates.get("env") ...).
- logout (line 444-465): deletes the SessionRow matching hash_session_token(cookie), commits (rollback+raise on error), `response.delete_cookie(SESSION_COOKIE_NAME, path="/")`, `logger.info("logout", user_id=current_user.id)`.

From shared/models.py: class Session(table): id, token_hash, user_id (FK user.id CASCADE), created_at, expires_at: datetime (SQLite may return it NAIVE — normalize with `.replace(tzinfo=timezone.utc)` when tzinfo is None, same as agent/dependencies.py::_as_aware_utc; that helper is private, so write an equivalent local check rather than importing it).

From agent/mcp_client.py:
- _sessions: dict[(user_id, server_id), SessionHandle]; _last_results: dict[...]
- async def cleanup_user_sessions(user_id: int) -> None  (closes every (user_id, *) key via cleanup_server)
- def is_connected(user_id: int, server_id: int) -> bool

From tests/conftest.py: autouse `clean_test_db` (also awaits mcp_client.cleanup_all_sessions()); fixtures `client`, `seed_user`, `authenticated_client` (user "testuser", `.seeded_user_id`, one SessionRow inserted directly), `second_authenticated_client` (user "otheruser").
From tests/test_mcp_api.py: FIXTURE path to tests/fixtures/mcp_stdio_server.py, BASE="/api/v1/mcp/servers", `_payload(mode="ok", **overrides)`, `_create(client, **kwargs)`; connect via POST f"{BASE}/{id}/connect".

UI: ui/static/app.js `apiFetch` throws `new Error(detail)` for string details and `saveMcpServer` catches the POST/PUT error and sets `$('mcp-form-error').textContent = err.message` (line ~1783-1785). So NO UI change is needed for WR-07 — do not edit app.js; state this in the SUMMARY.

Docs: docs/API_SPEC.md does NOT document /auth/logout nor the MCP env update (grep for "auth/logout", "mask", "•••" found nothing). Per TASK ("If docs/API_SPEC.md documents ..."), make NO docs change; state this in the SUMMARY.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WR-07 — reject masked value for an unknown env key with HTTP 422, modify nothing</name>
  <files>agent/mcp_config.py, agent/main.py, tests/test_mcp_config.py, tests/test_mcp_api.py</files>
  <behavior>
    - merge_env({"A": "s"}, {"A": ENV_MASK, "B": "2"}) == {"A": "s", "B": "2"} (unchanged behavior)
    - merge_env({}, {"GHOST": ENV_MASK}) raises EnvMaskError with .key == "GHOST"
    - update_server(session, row, name="x", env={"GHOST": ENV_MASK}) raises EnvMaskError and the row (name, env_json) is not modified in memory nor in the DB
    - create_server(..., env={"K": ENV_MASK}) raises EnvMaskError and no McpServerConfig row exists afterwards
    - API: PUT env {"RENAMED": ENV_MASK} on a server whose stored env is {"SECRET_TOKEN": "supersecret123"} -> 422, detail is a str containing "RENAMED" and "•••"; stored env still {"SECRET_TOKEN": "supersecret123"}; name unchanged; if the server was connected beforehand, mcp_client.is_connected is still True
    - API: POST with env {"K": ENV_MASK} -> 422 and GET list is empty
    - API: keeping a known key masked still returns 200 and keeps the secret (existing test_update_with_masked_env_preserves_value stays green)
  </behavior>
  <action>
In agent/mcp_config.py: add `class EnvMaskError(ValueError)` with a one-line docstring, an `__init__(self, key: str) -> None` storing `self.key: str = key` and passing a plain English message to super (no secret values in the message — only the key name). Change `merge_env` so that a value equal to ENV_MASK whose key is NOT in `stored` raises `EnvMaskError(key)` instead of `continue`; mask + known key keeps the stored value exactly as today; update the docstring accordingly (remove the old "must never be persisted" comment). In `create_server`, before constructing the row, call `merge_env({}, env)` (a mask on a brand-new server can never refer to a stored value, so any masked value raises) and persist its return value. In `update_server`, compute the merged env FIRST (`merged_env = merge_env(load_env(row), env)` when env is not None) before assigning ANY row attribute, so a rejected request leaves the row untouched; then assign `row.env_json = json.dumps(merged_env)` in the existing position. Keep the existing commit/rollback try blocks unchanged.

In agent/main.py: add a small private helper `_env_mask_error(exc: mcp_config.EnvMaskError) -> HTTPException` returning `HTTPException(status_code=status.HTTP_422_UNPROCESSABLE_ENTITY, detail=f"Для переменной {exc.key} значение скрыто (•••), но такой переменной нет в сохранённых. Введите значение заново.")` (key is already validated by the schema's env-key regex). In `create_mcp_server`, wrap the `mcp_config.create_server(...)` call in `try/except mcp_config.EnvMaskError as exc: raise _env_mask_error(exc) from exc`. In `update_mcp_server`, compute `updates = body.model_dump(exclude_unset=True)` BEFORE the disconnect, and if `updates.get("env") is not None` pre-validate with `mcp_config.merge_env(mcp_config.load_env(row), updates["env"])` inside `try/except mcp_config.EnvMaskError` -> raise `_env_mask_error(exc)`; this must happen BEFORE `mcp_client.disconnect_server(...)` so a rejected request does not drop a live connection ("no data may be modified"). Also wrap the `update_server` call itself with the same except for defense in depth. Log the rejection with `logger.warning("mcp_env_mask_rejected", user_id=..., server_id=..., key=exc.key)` (key name only, never values). Do not change the disconnect/update ordering otherwise (IN-01 out of scope).

Tests (write/adjust first, see them fail, then implement): in tests/test_mcp_config.py import EnvMaskError and replace `test_update_env_never_persists_mask_for_unknown_key` with a test that uses `pytest.raises(EnvMaskError)` on `update_server(session, row, name="changed", env={"GHOST": ENV_MASK})`, asserts `exc.key == "GHOST"`, and then re-reads the row in a fresh session proving env is still {} and name is unchanged (still proves the mask is never persisted). Add a create_server test for the masked-value rejection (no row persisted — list_servers returns []). Add a pure merge_env unit test for both branches. In tests/test_mcp_api.py add: (a) rename-with-mask PUT -> 422, detail string names the key, stored env and name unchanged via async_session_factory + mcp_config.load_env; connect the fixture server first and assert it is still connected after the 422; (b) POST with masked env -> 422 and list empty. Keep existing test_update_with_masked_env_preserves_value untouched as the "known key masked still works" proof. No UI change (see interfaces note). Commit: `fix(quick-260924-3cb): reject masked value for unknown MCP env key (WR-07)` — no Co-Authored-By line.
  </action>
  <verify>
    <automated>pytest tests/test_mcp_config.py tests/test_mcp_api.py -q</automated>
  </verify>
  <done>Both test files pass; merge_env raises EnvMaskError for mask+unknown key; PUT/POST return 422 with the Russian detail naming the key; stored env, name and live connection unchanged after rejection; known-key mask still preserved; app.js untouched; one local commit without Co-Authored-By.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WR-02 — logout closes the user's MCP sessions when the last live web session ends; full suite</name>
  <files>agent/main.py, tests/test_mcp_logout.py</files>
  <behavior>
    - Logout of the user's only live web session (an extra EXPIRED SessionRow for the same user may exist and must not count) -> 204, mcp_client.is_connected(user_id, server_id) is False, no (user_id, *) key in mcp_client._sessions, and no new fixture child process of the pytest process remains (poll up to ~5 s)
    - Logout while another non-expired SessionRow of the same user exists -> 204 and the MCP session stays connected
    - Another user's connected MCP session is still connected after the first user's last-session logout
    - monkeypatch mcp_client.cleanup_user_sessions to an async function raising RuntimeError -> logout still 204, the Set-Cookie header clears the session cookie, and the session row is deleted
  </behavior>
  <action>
In agent/main.py::logout, per the locked design in the TASK file: after the existing delete+commit block (keep its rollback+raise as is — a DB failure there may still fail logout), and before `response.delete_cookie`, add a guarded MCP cleanup. Add a private helper `async def _has_live_web_session(session: AsyncSession, user_id: int) -> bool` that selects `SessionRow` rows `where(SessionRow.user_id == user_id)` and returns True if any row's `expires_at` (normalized to UTC-aware when naive, as in agent/dependencies.py) is >= `datetime.now(timezone.utc)`. In logout: wrap in `try:` → `if not await _has_live_web_session(session, current_user.id): await mcp_client.cleanup_user_sessions(current_user.id)`; `except Exception as exc:` → `logger.warning("logout_mcp_cleanup_failed", user_id=current_user.id, error_type=type(exc).__name__)` (error type only, per TASK — no message, no traceback). The except must also cover a failure of the session-count query so MCP cleanup can NEVER make logout fail; still delete the cookie and return 204. Only the logging-out user's id is ever passed, so other users are untouched by construction. Add a short why-comment: multi-tab/multi-device users keep MCP sessions while any browser is still logged in; web-session expiry without a logout call is still not covered.

Create tests/test_mcp_logout.py (module docstring; reuse the pattern from tests/test_mcp_api.py: FIXTURE path, `sys.executable`, create via POST /api/v1/mcp/servers then POST .../connect with authenticated_client; insert extra SessionRow rows directly via async_session_factory with hash_session_token(generate_session_token()) and expires_at in the future or past). Process check helper: snapshot `psutil.Process().children(recursive=True)` PIDs BEFORE connecting; after logout poll (asyncio.sleep 0.1, up to ~5 s) until no child whose cmdline contains the fixture path and whose PID is not in the snapshot remains; wrap cmdline reads in `except (psutil.NoSuchProcess, psutil.AccessDenied)`. Never iterate or touch processes outside the pytest process tree; never kill anything. For the failure test use `monkeypatch.setattr(mcp_client, "cleanup_user_sessions", _boom)` where `_boom` is an async function raising RuntimeError, then assert 204, that `set-cookie` contains SESSION_COOKIE_NAME, and that the user's SessionRow is gone. Other-user test uses second_authenticated_client to connect its own fixture server and asserts it stays connected. No ports are opened by these tests (ASGITransport); never use 8000/8001.

Then run the FULL suite `pytest tests/ -q` and record the real observed pass count in the SUMMARY (baseline before this plan: 412 passed; expect 412 + new tests). SUMMARY must state: no UI change needed for WR-07 (saveMcpServer already shows err.message in #mcp-form-error), no docs change (API_SPEC.md documents neither /auth/logout nor MCP env update), and that web-session expiry without a logout call still does not close MCP sessions. Commit: `fix(quick-260924-3cb): close user's MCP sessions on last-session logout (WR-02)` — no Co-Authored-By line, no push.
  </action>
  <verify>
    <automated>pytest tests/test_mcp_logout.py tests/test_auth.py -q && pytest tests/ -q</automated>
  </verify>
  <done>Logout of the last live web session closes the user's MCP sessions and fixture child processes; another live web session of the same user keeps them; other users untouched; failing cleanup still yields 204 with cookie cleared; full suite passes with real count recorded; one local commit without Co-Authored-By.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Agent REST | Authenticated user submits MCP env dicts (may contain the mask literal) and logout requests |
| Agent -> MCP child processes | Agent owns stdio child processes per (user_id, server_id) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q3cb-01 | Tampering | mcp_config.merge_env/update_server | mitigate | Raise EnvMaskError before any row mutation; handler pre-validates before disconnect; 422 returned, nothing committed |
| T-q3cb-02 | Information disclosure | 422 detail / warning log | mitigate | Message and log carry only the env key name, never stored or submitted values |
| T-q3cb-03 | Elevation / DoS | logout MCP cleanup | mitigate | Cleanup scoped to current_user.id only; guarded by remaining-live-session check; any exception logged (error type only) and swallowed so logout always succeeds |
| T-q3cb-04 | Denial of service | lingering MCP child processes after logout | mitigate | cleanup_user_sessions closes sessions when last web session ends; expiry-without-logout accepted as residual (documented in SUMMARY) |
| T-q3cb-05 | Denial of service | user's running app (PIDs on 8000/8001, filesystem.exe) | mitigate | Tests use in-process ASGITransport, inspect only pytest's own child processes read-only; no kills, no run.py/uvicorn, no app.db |
</threat_model>

<verification>
- `pytest tests/test_mcp_config.py tests/test_mcp_api.py tests/test_mcp_logout.py tests/test_auth.py -q` passes
- `pytest tests/ -q` passes; real count reported (baseline 412)
- `grep -n "cleanup_user_sessions(current_user.id)" agent/main.py` returns one match inside logout
- `grep -n "class EnvMaskError" agent/mcp_config.py` returns one match
- `git diff --stat HEAD~2` shows no change to ui/static/app.js or docs/
- `git log -2 --format=%B | grep -ci co-authored` prints 0
- The user's processes on 8000/8001 and pre-existing filesystem.exe PIDs are still alive (read-only check, e.g. psutil.pid_exists)
</verification>

<success_criteria>
- WR-07: masked value for unknown key -> 422 with Russian detail naming the key; no DB change, no dropped connection; known masked key unchanged behavior
- WR-02: last-session logout closes the user's MCP sessions/processes; multi-session and other users unaffected; cleanup failure never breaks logout
- Two atomic local commits on the worktree, no push, no Co-Authored-By
- SUMMARY records: no UI change needed, no docs change, expiry-without-logout residual, real full-suite count
</success_criteria>

<output>
Create `.planning/quick/260924-3cb-fix-review-warnings-wr-07-and-wr-02/260924-3cb-SUMMARY.md` when done
</output>
