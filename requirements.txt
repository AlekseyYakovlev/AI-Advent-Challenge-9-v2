# ==============================================================================
# CORE WEB & ASYNC
# ==============================================================================
fastapi>=0.115.0
uvicorn>=0.30.0

# ==============================================================================
# DATABASE & ORM
# ==============================================================================
sqlmodel>=0.0.22          # Включает в себя SQLAlchemy 2.x и Pydantic v2
aiosqlite>=0.20.0         # Асинхронный драйвер для SQLite (критически важен)

# ==============================================================================
# CONFIGURATION & VALIDATION
# ==============================================================================
pydantic>=2.9.0           # Валидация данных (строго v2 для SQLModel)
pydantic-settings>=2.5.0  # Загрузка настроек из .env файла

# ==============================================================================
# LLM INTEGRATION & UTILS
# ==============================================================================
httpx>=0.27.0             # Асинхронный HTTP-клиент (для DeepSeek и LM Studio API)
tiktoken>=0.7.0           # Подсчет токенов (cl100k_base для OpenAI/DeepSeek)

# ==============================================================================
# AUTHENTICATION
# ==============================================================================
pwdlib[argon2]>=0.3.1     # Хеширование паролей (argon2id) через pwdlib + argon2-cffi

# ==============================================================================
# LOGGING & PROCESS MANAGEMENT
# ==============================================================================
structlog>=24.4.0         # Структурированное JSON-логирование
psutil>=6.0.0             # Управление процессами (Orphan cleanup, healthcheck)

# ==============================================================================
# MCP INTEGRATION
# ==============================================================================
mcp==1.30.0               # Model Context Protocol SDK (stdio client, Day 16)

# ==============================================================================
# TESTING
# ==============================================================================
pytest>=8.3.0
pytest-asyncio>=0.24.0    # Асинхронная поддержка для pytest
respx>=0.21.0             # Мокирование HTTP-запросов (httpx) для тестов LM Studio/DeepSeek